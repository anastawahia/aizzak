<div dir="rtl">

# فهرس سجلّ التنفيذ (Implementation Log Index)

> المدخل الوحيد لسجلّ التنفيذ الموحّد، بعرضَين للأحداث نفسها:
> - **حسب الخطوة** (`§3.NN`) — ملفات `3.NN.md` أدناه؛ كل خطوة جديدة = ملف جديد + سطر هنا.
> - **حسب إصدار الخطة** (v1.0→v2.22) — [`CHANGELOG.md`](CHANGELOG.md)؛ كل إصدار = صفّ هناك.
>
> كتابة تزايدية بلا لمس السجلّ القديم. التسليم الحيّ في [`../implementation-status.md`](../implementation-status.md)، والخطة في [`../implementation-plan.md`](../implementation-plan.md).

- [§2](2.md) — المرحلة 1 (الأساس)
- [§3](3.md) — المرحلة 2.1 (عقود المنافذ)
- [§3.5](3.5.md) — المرحلة 0 (حصاد المراجع من `alpha`)
- [§3.6](3.6.md) — المرحلة 3 مجموعة (أ): وحدات التمكين والأمان
- [§3.7](3.7.md) — المرحلة 3 مجموعة (ب): وحدات التفاعل والمحتوى
- [§3.8](3.8.md) — المرحلة 3 مجموعة (ج): `knowledge` 3.k1 (المُحلّلات)
- [§3.9](3.9.md) — المرحلة 3 مجموعة (ج): `knowledge` 3.k2 (خوارزميتا RRF + مُقسّم BM25)
- [§3.10](3.10.md) — المرحلة 3 مجموعة (ج): `knowledge` 3.k3 (خط الفهرسة/الاسترجاع الهجين)
- [§3.11](3.11.md) — المرحلة 3 مجموعة (ج): `knowledge` 3.k4 (النطاق + المستودع + الوارد + الهجرة/RLS)
- [§3.12](3.12.md) — المرحلة 3 مجموعة (ج): وحدة `media` (شريحة نقية + هجرة/RLS)
- [§3.13](3.13.md) — المرحلة 3 مجموعة (د): وحدتا `integrations` و`usage` (شريحتان نقيتان + هجرتان/RLS)
- [§3.14](3.14.md) — شريحة إثبات محوّلات SQL على Postgres حيّ (2.2 نواة + محوّل `media` + تصليب RLS)
- [§3.15](3.15.md) — الموجة 2: محوّلات المجموعة (أ) `workspace`+`access`+`credentials` (SQL + هجرات + RLS حيّة)
- [§3.16](3.16.md) — الموجة 3: محوّلات المجموعة (ب) `conversations`+`memory`+`files`
- [§3.17](3.17.md) — الموجة 4 (الأخيرة): محوّلات `knowledge`+`integrations`+`usage` — اكتمال المرحلة 3
- [§3.18](3.18.md) — المرحلة 2.3: محوّل Redis (`CacheProvider`) على Redis حيّ
- [§3.19](3.19.md) — المرحلة 2.4: محوّل MinIO (`StorageProvider`) — ✅ مكتملة (على خطوات مُبوَّبة)
- [§3.20](3.20.md) — المرحلة 2.5: محوّل Qdrant (`VectorStore`/`HybridVectorStore`) — ✅ مكتملة (على خطوات مُبوَّبة)
- [§3.21](3.21.md) — المرحلة 2.6: محوّل Vault (`SecretsProvider` · KV v2 + Transit) — ✅ مكتملة (على خطوات مُبوَّبة)
- [§3.22](3.22.md) — المرحلة 2.7: محوّل Firebase (`AuthProvider` · تحقّق JWT محلي) — ✅ مكتملة (على خطوات مُبوَّبة)
- [§3.23](3.23.md) — المرحلة 2.8‑أ: محوّل Ollama (`LLMProvider` — أول محوّل LLM) — ✅ مكتملة (على خطوات مُبوَّبة)
- [§3.24](3.24.md) — المرحلة 2.8‑ب‑1: محوّل OpenAI (`LLMProvider`) + استخراج البدائيات المشتركة — ✅ مكتملة (على خطوات مُبوَّبة)
- [§3.25](3.25.md) — المرحلة 2.9: `ProviderResolver` من الإعداد — ✅ مكتملة (على خطوات مُبوَّبة)
- [§3.26](3.26.md) — المرحلة 4.1: `PluginLoader` + `AgentRegistry` — ✅ مكتملة (على خطوات مُبوَّبة)
- [§3.27](3.27.md) — المرحلة 4.2: مُنفِّذ دورة الحياة (`BaseAgent` lifecycle executor) — ✅ مكتملة (على خطوات مُبوَّبة)
- [§3.28](3.28.md) — المرحلة 4.3: نظام الأدوات (`ToolRegistry` + `ToolResolver` + ربط الكتالوج الديناميكي) — ✅ مكتملة (على خطوات مُبوَّبة)
- [§3.29](3.29.md) — المرحلة 4.4: أداة مثال `web_search` بعقد `BaseTool` — ✅ مكتملة (على خطوات)
- [§3.30](3.30.md) — المرحلة 4.5 (الشقّ الساكن): أنواع العقد + `WorkflowRegistry` — ✅ (خطوة 4.5‑أ؛ 4.5‑ب المحرّك تالية)
- [§3.31](3.31.md) — المرحلة 4.5‑ب: محرّك `SequentialWorkflowEngine` — ✅ (**اكتملت المرحلة 4.5**)
- [§3.32](3.32.md) — المرحلة 4.6‑أ: بذرة DIP للـ`deps` + وكيل RAG (القالب) — ✅ (4.6 على 3 خطوات)
- [§3.33](3.33.md) — المرحلة 4.6‑ب: وكيلا Data Analysis + File Editing (+ `read_text_file`) — ✅
- [§3.34](3.34.md) — المرحلة 4.6‑ج: وكيلا Image + Video (حدثيان) + قلب AC‑04 — ✅ (**اكتملت المرحلة 4.6**)
- [§3.35](3.35.md) — المرحلة 4.7‑أ: تعديل عقد `LLMProvider` (رفع حاجب المُنسِّق) — ✅
- [§3.36](3.36.md) — المرحلة 4.7‑ب‑1: المُنسِّق `AgentOrchestrator` (الشقّ المنطقي) — ✅
- [§3.37](3.37.md) — المرحلة 4.7‑ب‑2: توصيل الجذر (أوّل إقلاع حقيقي للمنصّة) — ✅
- [§3.38](3.38.md) — المرحلة 4.7‑ج‑1: وسم `estimated` عبر طبقة البيانات (هجرة حيّة) — ✅
- [§3.39](3.39.md) — المرحلة 4.7‑ج‑2: فرض الحصّة والتقاط الاستهلاك في المُنسِّق — ✅ (**اكتملت 4.7‑ج**)
- [§3.40](3.40.md) — المرحلة 4.7‑د‑1: بذرة إنتاج الـOutbox (مظروف CloudEvents + المنفذ + المحوّل) — ✅
- [§3.41](3.41.md) — المرحلة 4.7‑د‑2: منفذ `media` الوارد + توصيل الجذر — ✅ (**اكتملت 4.7‑د**؛ وكيلا Image/Video يعملان فعلياً)
- [§3.42](3.42.md) — المرحلة 4.7‑هـ‑1: شريحة الـWorkflow (محرّك لكل طلب + محادثة D‑12 + `WorkflowResult`) — ✅ (حلّ التبعيات صار لكل خطوة؛ الفوترة في هـ‑2)
- [§3.43](3.43.md) — المرحلة 4.7‑هـ‑2: الفرض والالتقاط عبر خطوات الـWorkflow — ✅ (**اكتملت 4.7 والمرحلة 4**؛ الخطوة هي وحدة الفوترة)
- [§3.44](3.44.md) — المرحلة 5.1‑أ: إكمال المنتِجين + وحدة العمل الذرّية (UoW) — ✅ (أولى خطوات م5؛ ضمان «لا فقد» مُثبَت حيًّا)
- [§3.45](3.45.md) — المرحلة 5.1‑ب: المُرحّل Outbox → Redis Streams (ناشر + مخزن + حلقة + دور `outbox_relay`) — ✅
- [§3.46](3.46.md) — المرحلة 5.1‑ج: محرّك الاستهلاك + العمّال الثلاثة (+ `IndexMemoryItem`) — ✅ (**اكتملت 5.1**؛ إثبات AC‑08 الحيّ من طرف إلى طرف)
- [§3.47](3.47.md) — المرحلة 5.2‑أ: Idempotency — دفتر `processed_events` + فصل حالات الاستخدام (إغلاق نافذة D5) — ✅ (effectively‑once حيًّا؛ خطر R3 الأعلى مُغلق)
- [§3.48](3.48.md) — المرحلة 5.2‑ب: DLQ بعد N=5 (عدّ التسليمات + `dead_letter` + سياسة D4 النهائية) — ✅ (**اكتملت 5.2 وR3 مرفوع**؛ إصلاح علّة قاتل‑العامل)
- [§3.49](3.49.md) — المرحلة 5.3‑أ: سقف المدّة الكلّي للبثّ في المُنسِّق (`stream_max_duration_s`) — ✅ (سداد دين §3.23(ز)؛ يسري على الوكيل الواحد والـWorkflow)
- [§3.50](3.50.md) — المرحلة 5.3‑ب: SSE للردّ الواحد (`03 §3.1`) + أول ملء لطبقة الـAPI (`api/errors.py`) — ✅ (علّة «النبضة ليست موعداً»؛ مُثبَت فوق ASGI)
- [§3.51](3.51.md) — المرحلة 5.3‑ج: WebSocket التفاعلي (`GET /api/v1/ws`) + `ConnectionHub` الإطاري — ✅ (`03 §3.2` كاملاً فوق ASGI؛ المصادقة قبل القبول · تشغيلات متزامنة + cancel · 1008/1009 · جسر الإشعارات جاهز لـد)
- [§3.52](3.52.md) — المرحلة 5.3‑د: جسر الإشعارات `cg.notify` ⇒ **اكتملت المرحلة 5** — ✅ (‏`make_notification_handler` الإطاري + `build_notification_consumer`؛ مُثبَت حيًّا على Redis؛ `AC‑11` بالتركيب؛ عديم المثالية بقصد)
- [§3.53](3.53.md) — المرحلة 6.1‑أ: قِشرة تطبيق الـAPI (المصنع `create_app` + دورة الحياة + `ApiServices` + seam المصادقة + معالجات RFC 9457 + الصحّة + تركيب الـWS) — ✅ (أولى خطوات م6؛ فوق ASGI؛ طفرات 12/12)
- [§3.54](3.54.md) — المرحلة 6.1‑ب: موجّه Agents `/api/v1/agents` (`GET` list مُغلَّفاً بـ`Page[AgentOut]` · `GET {key}` مجرّداً · `POST {key}/invoke` بثّاً SSE مفوّضاً لـ`orchestrator.invoke`) — ✅ (الـinvoke بثّاً فقط؛ `AgentInvokeOut` غير المتدفّق يحتاج حفظ رسالة الوكيل الواحد ⇒ 6.1‑ج؛ طفرات 7/7)
- [§3.55](3.55.md) — المرحلة 6.1‑ج‑1: قراءة رسائل المحادثة (`list_messages` منفذاً ومحوّلاً بمؤشّر `seq` + حالة الاستخدام `ListMessages`) — ✅ (أولى ثلاث خطوات لـ6.1‑ج؛ النقطة `GET {id}/messages` كانت بلا طريقة تخزين؛ طفرات 8/8)
- [§3.56](3.56.md) — المرحلة 6.1‑ج‑2/ج‑3: موجّه Conversations (خمس نقاط عبر حالات الاستخدام) + **دورة الوكيل الواحد المحفوظة** في المُنسِّق (خيط + رسالتان + إثراء إطار `final` + `invoke_once`) ⇒ `AgentInvokeOut` غير المتدفّق صار حقيقيّاً — ✅ (نُفِّذتا دفعةً بطلب المستخدم؛ طفرات 13/13)
- [§3.57](3.57.md) — المرحلة 6.1‑د‑1: أثر تشغيلة الـWorkflow (دور الافتتاح + دورٌ لكلّ خطوة مكتملة داخل خيط D‑12 + `run_id`=مُعرّف المحادثة + `status` على المقبض) — ✅ (أولى خطوتَي 6.1‑د؛ الخيط كان يُفتح ويُترك فارغاً فلا أثر تحت `GET /workflows/runs/{id}`؛ طفرات 10/10)
- [§3.58](3.58.md) — المرحلة 6.1‑د‑2: موجّه Workflows (`GET /workflows` كتالوجاً بغلاف `API‑04` · `POST {key}/run` بثّاً أو مجموعاً · `GET /runs/{id}` قراءةً للتشغيلة عبر محادثتها) ⇒ **اكتملت 6.1‑د** — ✅ (الفشل حالةٌ لا مشكلة · `_stored_status` محافظة `completed`/`unknown` بلا تخمين؛ طفرات 11/11)
- [§3.59](3.59.md) — المرحلة 6.1‑هـ‑1: توصيل MinIO غير المتزامن كطريقة على الجذر — `StorageHandle` فتحةً متأخّرة الربط + `connect_storage()` (Vault ⇒ ربط) + seam `startup` على القِشرة ⇒ **سُدّد دَين (ز) المحمول منذ 2.4** ووكيلا الملفات يشتعلان بلا إعادة بناء — ✅ (طفرات 10/10)
- [§3.60](3.60.md) — المرحلة 6.1‑هـ‑2: خدمات تطبيق Files + Media — `FileTransferService` (توقيع PUT/GET عند `ready`) + `SoftDeleteFileService` + حِزمة `FileUseCases` + `checksum` اختياريّ حتى النطاق + `submit` يعيد `MediaJob` كاملاً + حِزمة `MediaUseCases` (أوّل بناءٍ لـ`GetJobStatus`) — ✅ (ما تحت الموجّهين قبل الموجّهين؛ طفرات 13/13)
- [§3.61](3.61.md) — المرحلة 6.1‑هـ‑3: موجّها Files وMedia (‏`/files` خمس نقاط: تسجيل 201 بـPUT موقَّع · إكمالٌ بجسدٍ اختياريّ · قائمة `API‑04` · مفرد · حذف 204 · ‏`/media/jobs` بـ**202** فوق `submit`) ⇒ **اكتملت 6.1‑هـ** — ✅ (غير الجاهز جسدٌ لا مشكلة · `Idempotency-Key` دَينٌ مُعلَّم؛ طفرات 12/12)
- [§3.62](3.62.md) — المرحلة 6.1‑و‑1: موجّها Workspace وUsage (‏`/workspace` نقطتان بلا مُعرِّفٍ في المسار · `/usage` ملخّصٌ مفرد + `GET`/`PUT /limits` بغلاف `API‑04`) + `GetWorkspace`/`ListLimits` وحِزمتاهما — ✅ (عزل المستأجر بنيويّ · الحزمة تحجب الفرض/الالتقاط `INV‑U4` · `GET /limits` يقول ما أُعِدّ لا ما سيُفرَض؛ طفرات 14/14)
- [§3.63](3.63.md) — المرحلة 6.1‑و‑2: موجّه Credentials (‏`GET` بغلاف `API‑04` · `POST` 201 · `DELETE` 204 إبطالاً لا محواً) + `ListCredentials` و`list` على المنفذ/المحوَّل وحزمة `CredentialUseCases` — ✅ (⭐ «لا يُعاد السرّ أبداً» خاصيّةُ نموذج بيانات لا ترشيحاً في المُصيِّر · وجه فكّ التعمية ليس في الحزمة · `scope='platform'` 403 لا 422 · الأبوّة من السياق؛ طفرات 15/15)
- [§3.64](3.64.md) — المرحلة 6.1‑و‑3: موجّه Knowledge (‏`POST /search` بغلاف `API‑04` و**503 `knowledge.search_unavailable`** ما دام مِحوَل التضمين غائباً · `GET /documents` بالغلاف · `GET /documents/{id}` مفرداً) + `ListDocuments`/`GetDocument` و`list` على المنفذ/المحوَّل وحزمة `KnowledgeUseCases` — ✅ (⭐ الفجوة تُبنى **مرئيّة**: 503 مسمّى لا مسارٌ غائب · الاختياريّة مُصرَّحٌ بها في النوع `KnowledgeRetrieval | None` · الحزمة بلا أيّ وجه ابتلاع · القائمة تُظهر كلّ حالة و`DocumentOut` بلا `error`؛ طفرات 16/16)
- [§3.65](3.65.md) — المرحلة 6.1‑و‑4‑1: موجّه Integrations، الكتالوج والاتّصالات (‏`GET /connectors` · `GET`/`POST /connections` · `POST /connections/{id}/authorize` · `DELETE /connections/{id}`) + `ListConnectors`/`ListConnections`/`StartConnection`/`AuthorizeConnection` و`list` على المنفذ/المحوَّل وحزمة `IntegrationsUseCases` — ✅ (⭐ الكتالوج الفارغ **جوابٌ صادق** لا فتحة 503 · ⭐ عنوان العودة يُشتَقّ من الإعداد ولا يُقبَل من الطلب أبداً · ⭐ الحزمة بلا `invoke_tool` ولا تجديد ولا ردّ نداء · `ConnectionOut` بلا رمزٍ وبلا `last_error` · القائمة تُظهر الحالات الأربع؛ طفرات 14/15 + مُطفَّرٌ مكافئ مُعلَن)
- [§3.66](3.66.md) — المرحلة 6.1‑و‑4‑2: ردّ نداء OAuth **العموميّ** (`GET /integrations/connections/oauth/callback` فوق `CompleteOAuth`) — موجّهٌ ثانٍ بلا مصادقة + `complete` سادس حقول الحزمة + `RecordingSecrets` — ✅ (⭐ «غير مصادَق» خاصيّةٌ **تُرى بنظرة** فتستحقّ موجّهها، ومُختبَرةٌ لا موصوفة · ⭐ هويّة المستأجر من ربط `state` الخادميّ **حصراً** — إصلاح ثغرة alpha · ⭐ التبادل يركب عنوان العودة المخزَّن لا واحداً يقدّمه الطالب · اللافتة المزوّرة تُرفَض قبل أيّ قراءة، والمُعادة تخسر · الرموز تمرّ بـTransit ولا يظهر منها شيء · 502 يترك صفّاً مُفسِّراً وكلمات المزوّد في السجلّ؛ طفرات 14/14)
- [§3.67](3.67.md) — المرحلة 6.1‑و‑4‑3: خوادم MCP وكتالوج الأدوات (`GET`/`POST`/`DELETE /mcp-servers` · `GET /tools`) — **وبها تكتمل 6.1** — ✅ (⭐ `DELETE` يُعطِّل **وإعادة التسجيل تُحيي** — وجها دورة الحياة يُضافان معاً وإلّا حرق الحذفُ الاسمَ الفريد إلى الأبد · ⭐ الحدّ على **نوع الحقل** لا على عضويّة الحزمة: `ToolDiscovery` فيها `list_tools` وحدها فمحاولة الاستدعاء **خطأ نوع** · ⭐ `GET /tools` تستحقّ فتحة 503 بينما `GET /connectors` لا تستحقّها — قائمةٌ فارغة هنا **كذبة** لا جوابٌ صادق · `disable` يُبقي `auth_ref` عمداً · لا حدث للتعطيل والتوقيع يقوله · شرط سقفٍ ميّتٌ حُذف بدل أن يبدو حارساً؛ طفرات 14/14)
- [§3.68](3.68.md) — المرحلة 6.2‑أ: **كتالوج الأخطاء الواحد** (‏`ProblemSpec` + `ERROR_CATALOG` بـ34 مدخلاً في النواة) + التوفيق (‏`common.validation_error` · `common.internal` · `agent.unknown` · `credentials.none_available`/409 · `integrations.oauth_state_invalid` · خريطة رفضٍ مغلقة) + حارس انجرافٍ ثنائيّ الاتّجاه — ✅ (⭐ الكتالوج في **النواة** لا في الـAPI لأنّ قرّاءه هم الرافعون الممنوعون من استيراد `app.api` · ⭐ **الحالة خاصيّةُ الرمز** لا خاصيّةُ الصنف ⇒ ثلاثة `status=502` يدويّة حُذفت بلا اختراع صنف · ⭐ الكتالوج **مرآةٌ لسلوك لا قائمة أمنيات**: الاتّجاه الخلفيّ حذف مدخلين بلا موضع ومنح مدخلين موضعيهما · ⭐ ثغرة `code=` غير الحرفيّ أُغلقت **بتعدادها** — ثلاثة مواضع، والرابع يُفشل الاختبار · حمولة B1 المجرّدة كانت تُخفي `common.error` عن كلّ صنف استثناء؛ طفرات 16/16، ودرس M03: الناجية اتّهمت مستوى الاختبار)
- [§3.69](3.69.md) — المرحلة 6.2‑ب: **اكتمال العرض؛ وبها تكتمل 6.2** (معالج `StarletteHTTPException` + `common.method_not_allowed` · `ProblemDetails` وإعلان `default` على كلّ عمليّة بنوع الوسيط الصحيح · `correlation_id` على مشكلات الـWS البروتوكوليّة) — ✅ (⭐ **أوّل ردٍّ يراه عميلٌ من عنوانٍ مطبوعٍ خطأً كان الردَّ الوحيد الذي ليس مشكلة** — ولا اختبار موجّهٍ كان يمسكه لأنّ لا موجّه يشارك فيه · ⭐ الحالة غير المخرَّطة تتدهور إلى 500 و**تفقد حالتها الأصليّة عمداً** لئلّا يُكسَر عمود الحالة في الكتالوج · ⭐ **`Retry-After` لا يُصدَر**: لا مُنتِج له و`LimitDecision` بلا وقت إعادة تعيين ⇒ أيّ قيمةٍ مخترَعة، وهي القاعدة نفسها التي حذفت `files.not_ready` · ⭐ العقد المولَّد كان **صامتاً عن الأخطاء** فأُعلن على مصنع `FastAPI` لا على كلّ موجّه، ونوع الوسيط يُصحَّح بعد التوليد لأنّ FastAPI لا يملك مكاناً لقوله · ⭐ **لا ذاكرة تخزينٍ ثانية** — الطفرة أثبتت أنّ حارسنا كان ميّتاً لأنّ `FastAPI.openapi` يخزّن بنفسه، فحُذفت الطبقة وبقيت المثاليّة مُثبَّتة؛ طفرات 11/11)

- [§3.70](3.70.md) — المرحلة 6.3‑أ: **المؤشّر المعتِم يصير كلّيّاً** (‏مِظروف base64url صارم + زوجان مطبوعان بالمفتاح `encode_id_cursor`/`decode_id_cursor` و`encode_seq_cursor`/`decode_seq_cursor` + `Limit`/`Cursor`/`DEFAULT_LIMIT` في مكانٍ واحد + مُزيَّفات ترقّم حقّاً) — ✅ (⭐ `common.invalid_cursor` كان في الكتالوج وبابه مُغلق: الفكّ المتساهل **يُسقِط** ما خرج عن الأبجديّة فيصير `"!!!!"` سلسلةً فارغة و`"aGVsbG8"` نصّاً — كلاهما **500** على عمود `uuid` — و`"!!!!NDI"` تصير **42 صامتاً**، أي موضعاً لم يطلبه أحد ولا طبقة تُبلّغ · ⭐ `Uuid = str` أخفت كذبة النوع: مؤشّر `seq` كان يعبر دالّةً موقّعة `-> Uuid` ولا يمكن لـmypy الاعتراض · ⭐ **النوع هو الوسم** — لا مُميِّز في المِظروف لأنّ الزوجين المطبوعين يُغلقان حالة المؤشّر الأجنبيّ في الاتّجاهين · التحقّق يبقى مع الـcodec لا على الحدّ لأنّ المؤشّر معتِم · ⭐ حارس الحمولة الفارغة **حُذف** بعدما نجت طفرته — درس §3.69‑(و) نفسه · المُزيَّفة التي تجيب `next_cursor=None` دائماً كانت تُخفي سطر كلّ موجّه؛ طفرات 12/12، والناجيات الثلاث كتبن القرارات)
- [§3.71](3.71.md) — المرحلة 6.3‑ب: **المؤشّر يبلغ كلّ مجموعةٍ يعلنها العقد، والاتّجاه يصير واحداً** (‏`listDocuments` و`listConnections` تُرقَّمان · `files`/`conversations` تنقلبان إلى الأحدث أوّلاً · `DEFAULT_LIMIT` يصير **20**) — ✅ (⭐ **`openapi.yaml` كان يقول منذ البداية أيَّ خمسٍ ترقّم، والتنفيذ عرف ثلاثاً** — والاثنتان الناقصتان موثَّقتان في الكود وكأنّ السؤال مفتوح («مؤشّرٌ حقيقيّ يصل مع 6.3 **إن استحقّ الكوربوس يوماً**») بينما العقد الملزِم كان قد حسم · ⭐ **السقف الذي استندت إليه قائمة الاتّصالات غير موجود**: `Limits.max_connectors` يعدّ الموصولة وحدها («صفوف `pending` حُطام مصافحة لا سعة») والقائمة تُعيد الحالات الأربع ⇒ حُطام المصافحات يتراكم بلا سقف · ⭐ **اتّجاهان في واجهةٍ واحدة**: المرقّمة تصاعديّة وغير المرقّمة تنازليّة ⇒ عميلٌ يفتح ملفّاته على **أقدم** ما رفعه ⇒ الأحدث أوّلاً في الكلّ، والاستثناء الوحيد نصّ المحادثة (سردٌ لا صندوق وارد) وهو **مُثبَّتٌ بطفرة** · ⭐ **الافتراضيّ كان 50 في الخادم و20 في العقد** ⇒ عميلٌ مولَّدٌ يرقّم بعشرين ويستقبل خمسين · الباقيات غير المرقّمة اختُبرت واحدةً واحدة بسببٍ بنيويّ لا بالتصنيف؛ طفرات 13/13، و+3 اختبارات ترقيمٍ حيّة على PG)
- [§3.72](3.72.md) — المرحلة 6.3‑ج: **حارس انجراف الاصطلاحات؛ وبها تكتمل 6.3** (‏`test_api_conventions.py` — 14 اختباراً يقرأون **الوصفين**، `openapi.yaml` الملزِم والمستند الذي يولّده التطبيق، ويقارنونهما) — ✅ (⭐ مبرّره ليس مبدأً بل **ما دُفع ثمناً في §3.71**: العقد أعلن خمساً والتنفيذ حمل ثلاثاً والافتراضيّ انجرف — أخطاءٌ يمسكها **فرقٌ** لا عين، فلا اختبار موجّهٍ يفحص مساراً غير موجود ولا مراجعٌ يقرأ عمود المُعاملات صفّاً صفّاً · المقارنة **ثنائيّة الاتّجاه** في المواضع الثلاثة التي تنجرف — العمليّات نفسها والمرقّمة والمُغلَّفة — والاتّجاه الخلفيّ هو الأثمن ولا وجود له بلا هذا الملفّ · المطابقة على `(المسار، الفعل)` بمحو مُعاملات المسار (‏`{id}` مقابل `{file_id}` عنوانٌ واحد عند العميل، و`operationId` لا يتقابل أصلاً) · ⭐ الحارس يحترم تجاوز `servers` لكلّ مسار لأنّ **الصحّة يجب ألّا تُنسَخ** — مِسبارُ موازِن حملٍ يلاحق `/api/v2` خطرُ نشرٍ لا لطفُ إصدار، فكان التصحيح في الحارس لا في العقد · ⭐ الوقت يُفحَص **مُسلسَلاً لا مُعلَناً**: `datetime` واعٍ يُسلسَل `+00:00` بصحّةٍ تامّة والنمط `Z$` يقبل واحداً فقط ⇒ يُقرأ جسد ردٍّ حقيقيّ · `pyyaml` تُعلَن لأنّ حارساً يتوقّف عن الاستيراد بصمتٍ يتوقّف عن الحراسة بصمت؛ طفرات 11/11، واثنتان منها إعادةُ إنتاجٍ لعلّتين شحنتا فعلاً)
- [§3.73](3.73.md) — المرحلة 6.4‑أ: **المصادقة الحقيقيّة** (‏Firebase JWT ⇒ `Principal`: تحقّقٌ بالمنفذ + بذر JIT + رفض الحساب المعطَّل + قراءة الأدوار طازجةً + بذر `owner` لأوّل دخول) — ✅ (⭐ **بذر `owner` كان مفقوداً**: `05 §1.5` يقول «يُنشأ Workspace ويُسنَد له `owner`» و`ProvisionUserOnFirstLogin` تتوقّف عند نصف الجملة — و**لا يمكنها غير ذلك** لأنّ `workspace` ممنوعة من استيراد `access`؛ فلولا هذا السطر لأنتجت 6.4‑ب نظاماً يُصادِق كلّ أحدٍ ويأذن للا أحد · ⭐ **ما يبلغه مسار المصادقة مبلوغٌ بلا حارس** فبابان بدالّةٍ واحدة: `seed_owner` لا `assign(role)` — حاملُه يسكّ أوّل مالكٍ لمساحةٍ جديدة ولا يستطيع أن يجعل نفسه admin في مساحةٍ قائمة · ⭐ **الحساب المعطَّل يُقرأ طازجاً كلّ طلب بلا أيّ كاش** — الضابط التعويضيّ الذي اشترته `D7` في 2.7 حين رفضت فحص الإبطال، ونقيضُ كاش‑النتيجة الذي صنّفه مرجع alpha «لا يُنقل» · ⭐ **ثلاثة أجوبةٍ لا جوابان**: 401 توكن · 403 حساب · **500 بنية تحتية** — ولا شيء هنا يلتقط بعرض، وهي علّة alpha §7 بعينها · ⭐ **مخطّط الأمان يُنشَر أخيراً** (`firebaseBearer` بـ`auto_error=False` لأنّ خطأ FastAPI حالةٌ ونوعُ وسيطٍ خاطئان) — الدَّين الوحيد الذي سمّاه مرجع alpha بوصفه «شأن م6.4» · ⭐ **ردّ نداء OAuth كان يرث الاشتراط في العقد وهو لا يستطيع أن يكون مصادَقاً** ⇒ صُحِّح العقد وكسب حارس §3.72 بُعداً رابعاً يقارن **الأمان الفعّال** في الاتّجاهين · `WsPrincipal = Principal` ومُصادِقٌ **واحد** لا صنفٌ واحد لأنّ كاش الـJWKS على المثيل · حارسٌ ميّت حُذف بعدما نجت طفرته [الثالثة]؛ junit **2060** · طفرات **15/15**)
- [§3.74](3.74.md) — المرحلة 6.4‑ب: **حرّاس RBAC؛ وبها تكتمل المرحلة 6** (‏`require(Permission.X)` على **39 عمليّة** وفق `05 §4` + فرض صلاحيّات المانيفست في المُنسِّق + فحص `agents:invoke` على السوكيت) — ✅ (⭐ **نقطة الـWebSocket كانت بلا تفويضٍ إطلاقاً**: الحارس تبعيّةُ FastAPI والسوكيت بلا مسار، فحرفيّةُ `05 §1.4` وحدها كانت ستحرس **باباً واحداً** إلى الفعل لا الفعل ⇒ طبقتان متطابقتان على النقلَين · ⭐ **`required_permissions` لم يقرأه أحد منذ 4.1** ووكيلان يعلنان `media:generate` **غير الموجودة في `05 §1.2`** — و`is_allowed` يرفض المجهول بصمت، فكان الوكيلان سيصيران غير قابلَين للاستدعاء لكلّ دورٍ بما فيه `owner`؛ الحقل الميّت لا يبقى صحيحاً لأنّه مكتوب بل لأنّ شيئاً يقرؤه ⇒ حارسان يقرآن **شجرة الإضافات الحقيقيّة** · ⭐ **الصلاحية تعدادٌ لا نصّ** لأنّ `require("files:wrtie")` يُستورَد ثمّ يرفض الجميع إلى الأبد · ⭐ **سَيم التفويض هو الوحيد الذي يفشل مغلقاً** — «لم نستطع أن نقرّر» يجب ألّا يصير «اسمح» · ⭐ **التفويض أوّلاً** قبل حلّ الاعتماد وقبل الحصّة: طلبٌ ممنوع لا يقرأ Vault ولا يظهر في دفتر استهلاكٍ لم يُسمَح له بالإنفاق منه · ⭐ الخريطة **كاملةً** مُثبَّتة (‏`05 §4` ينشر 12 والتنفيذ 39) وتُقارَن بالتطبيق المُجمَّع في الاتّجاهين، و`PermissionGuard` كائنٌ قابلٌ للنداء كي تبقى الصلاحية **مقروءةً** بعد التركيب · ⭐ اكتشافٌ عرضيّ: `include_router` **لا يُسطّح** فمشيُ `app.routes` وحده يجد صفر مسارات · الرفض **يسمّي** الصلاحية عكس الـ401 · `platform_admin` بلا محتوى مستأجرٍ مُثبَتاً على السلك · ثلاث سويتات صار مبدؤها `owner` والحساسيّة الدوريّة في موضعٍ واحد؛ junit **2086** · طفرات **12/12**)
- [§3.75](3.75.md) — المرحلة 7.1: **طوبولوجيا Compose — أوّل إقلاعٍ حاويٍّ كامل** (‏`Dockerfile` و`docker-compose.yml` و`nginx.conf` ثلاثتها كانت **صفر بايت** · أدوارٌ عند تهيئة العنقود · تهيئة Vault/MinIO · **`app/ops/provision.py`** · عنوانٌ عامّ لتوقيع MinIO) — ✅ (**بالتوازي لا بدلاً**: الخدمات الأصليّة تحتفظ بمنافذها وحزامُ التكامل قائمٌ عليها، وداخل الشبكة تُحفَظ أسماء `08 §2` حرفيّاً · ⭐ **`alembic upgrade head` ليس أمراً يملكه المستودع** — إحدى عشرة سلسلةً ⇒ أحد عشر رأساً، والتسلسل الحقيقيّ كان في `conftest.py` وحده فالحاوية تفتقر إلى **الأمر** قبل المنح · ⭐ المنح تفوّت `platform.stream_offsets` — والدرس ليس الجدول بل **وثيقتان تنجرفان ولا شيء يقارنهما** ⇒ حارسٌ ثنائيّ الاتّجاه · ⭐ conftest **يستورد** لا ينسخ وإلّا أنتج الإصلاحُ الازدواجَ الذي جاء ليزيله (143 `live_db` تمرّ) · ⭐ **السلسلة الأساس تكتب في `public`** فاصطدمت بـ`permission denied` قبل أيّ هجرة · ⭐ **الرابط المُوقَّع يُوقَّع ولا يُعاد توجيهه** — إعادة الكتابة في Nginx مستحيلةٌ لسببين مقيسين، والعنوان أصلاً في **جسد JSON** · ⭐ **`region` تُثبَّت** وإلّا حلّ minio-py الإقليم بنداءٍ على نقطة نهايةٍ لا يبلغها الخادم فمات التوقيع — علّةٌ لا يظهرها إلّا الانقسام · ⭐ **العمّال الثلاثة لا يقلع أيٌّ منهم** (‏2.10 + `MediaGenerator`) ⇒ نصف المنصّة المستهلِك غير قابلٍ للنشر و**7.2 محجوزة على محوّلين لا على عمل نشر** · ⭐ **كتلة `upstream` تحلّ الاسم مرّةً واحدة** فأوّل إعادة نشرٍ للتطبيق تُسقِط الحافّة إلى 502 بينما `ps` يقول `healthy` (فحص الصحّة يسبر داخل الحاوية) ⇒ وجهةٌ في متغيّر تُحلّ لكلّ طلب · إثباتات حيّة: WS **101 ثمّ 1008** عبر Nginx · `OPS‑02` وRLS داخل الحاوية · رابطٌ وُقِّع داخلاً وجُلب من المضيف 200؛ pytest **2096** · طفرات **15/15**)

- [§3.76](3.76.md) — المرحلة 7.3: **AppRole ودليل التشغيل — وبها تُغلق المرحلة 7** (سياسة Vault مشتقّةٌ من مواضع النداء · كتلة `server 443` وTLS · **فحص صحّةٍ من الحافّة** · قصّ المجاري · الدليل كاملاً) — ✅ (**ما لم يُشغَّل لم يُنجَز** — لا عبارة عن سلوكٍ غير مقيس · ⭐ **المكدّس كان ميّتاً عند البدء**: `vault -dev` في الذاكرة، والفخّ ليس الفقدان بل **لاتماثل التعافي** — `app` يعيده الـdaemon و`vault-bootstrap` لا، و`depends_on` ترتيبُ `up` لا سياسةَ daemon · ⭐ السياسة **مشتقّةٌ من مواضع النداء** وممنوعاتها **مُثبَتة بالتشغيل** (قراءة السياسة تثبت أنّ النصّ خُزِّن لا أنّ التوكن محكوم)، و`transit/rewrap` **لا يُمنَح** لأنّ لا كود ينادي به (سابقة §3.75‑ب) · ⭐ **`is_authenticated()` يعيد False لتوكنٍ سليم** (‏`lookup-self` ممنوع) — ورفضُ 2.6 له **للأداء** صار حاملاً **للصحّة** · ⭐ **AppRole تموت بعد ساعة**: `token_ttl=1h` بلا تجديد، وVault على **مسار التشغيل** لا الإقلاع ⇒ تطبيقٌ يخدم ساعةً ثمّ يعجز عن كلّ سرّ مستأجِر و`/health/ready` **أخضر طوال الوقت** ⇒ إعادة مصادقةٍ **واحدة** لا حلقة · ⭐ **فحص الصحّة يجب أن يعبر الحافّة** وإلّا لم يفحص شيئاً (علّة #10: 502 للعالم و`healthy` للجميع) — مُثبَتٌ **بالنقيض** · ⭐ `:80` و`:443` **يتضمّنان** ملفّاً واحداً لا ينسخانه · ⭐ **لا HSTS محليّاً** (يطبَّق لكلّ **مضيف** فيسمّم localhost لكلّ مشروعٍ آخر) · ⭐ **المجاري تنمو حتّى مع مستهلكين أصحّاء** — `XACK` لا يحذف، والقياس أوسع ممّا وُصف · إثباتات حيّة: إقلاعٌ بـ`VAULT_TOKEN=[]` · ستّة فحوصٍ للسياسة اتّجاهين · قبل/بعد على عمر التوكن · **wss 101 ⇒ 1008** فوق TLSv1.3 · الحافّة `unhealthy` عند 504 · `XLEN 3000 ⇒ 181`؛ pytest **2118** (+22) · طفرات **15/15**)

- [§3.77](3.77.md) — دَين المرحلة 2 / 2.10: **محوّل `EmbeddingProvider` — نموذجٌ محلّيّ خلف خدمة تضمين مركزيّة** (المحوّل كان **صفر بايت** · خدمة `services/embedding` **deployable منفصل** يعزل `torch` · توصيل `composition_root`/عامل الذاكرة/عامل المعرفة) — ✅ (⭐ **الطوبولوجيا مركزيّة** بقرار المستخدم: تحميلٌ واحدٌ خلف المنفذ لا لكلّ عامل · ⭐ **خبزُ الأوزان وقت البناء** (‏Apache‑2.0 · `HF_HUB_OFFLINE`) والوسم مثبَّت · ⭐ **`api_key` السحابيّ يُصالَح بالتسجيل لا بتغيير المنفذ**: `embedding-local` في `keyless_providers` ⇒ لا مفتاح يُبذَر ولا يُطالَب، والمحوّل يقبله ويتجاهله · ⭐ **خريطة الأخطاء `common.internal`/500** (سابقة `qdrant_store` لا `agent.failed`/502 — تبعيّةٌ داخليّة)، وخطأ المستدعي 422 قبل أيّ I/O · ⭐ **`dim=384` من `dimensions()` وحدها** والمخزن لا يُمَسّ (جيب التمام يبقى) · ⭐ **الجهاز تلقائيّ** cuda+fp16 وإلّا cpu+fp32 وتطبيع L2 دائماً · ⭐ **اكتشاف: `pydantic-settings` يدمج حقول القاموس البيئيّة مع `.env` لا يستبدلها** ⇒ تجاوز `PROVIDER_ROUTING` جزئيّاً يرث نطاقاً بصمت · ⭐ **فُكّ الآن**: `/search` 200 تركيبيّاً + `deps.knowledge` + عامل الذاكرة كاملاً؛ **وبقي دَيناً مستقلّاً** حاجزُ عامل المعرفة الثاني `DocumentContentResolver` (لا نصفُ 2.10) · **لا Docker هنا** ⇒ برهان الحاوية الحيّ (‏SLO + جولة `/search`) **مؤجَّلٌ بصدق** لا مُزوَّر، والوحدة/العقد/التركيب مُثبَتة؛ بوابات: ruff **471** · mypy **334** · **8/0** · pytest **2178** (+61) · طفرات المحوّل 4/4 بعد تصليب ناجٍ)

- [§3.78](3.78.md) — المرحلة 8: **القبول النهائي (Definition of Done)** — ✅ (تحقّقٌ وتوثيقٌ لا كتابة كود · تقرير [`acceptance-report.md`](../acceptance-report.md): **مصفوفة `AC‑01…16` كلّها مُحقَّقة** بدليلٍ مسمًّى قابلٍ للتشغيل · البوابات الخمس خضراء 2026‑07‑25: ruff **471** · mypy **334** · import‑linter **8/0** · pytest **2178 ناجح · 0 فشل · 10 متخطًّى مُبرَّر** منها **212 تكامل حيّ** و**121 عزل مستأجر RLS** · إثباتات 8.2 موجَّهة: Plugin+دورة الحياة 65 · RLS 121 · لا FK عابر schema · مخرجات المورّد **13/13** مُسلَّمة · الفجوات العشر مُعلَنةٌ بصدق [مفاتيح/بيئة حاويّة] وخارج نطاق `AC` ⇒ **الروزنامة 12/12**)

- [§3.79](3.79.md) — **تصفية الدَّين المُسمّى بعد القبول** (ستّة بنود: إغلاق `lifespan` · TTL التوقيع المسبق إعداداً · `Retry-After` صار له مُنتِج · `provider.rate_limited` **مرفوض** · مخزن `Idempotency-Key` على PostgreSQL · قائمة منع `auth:revoked:<sub>`) — ✅ (**ميزةٌ مُنفَّذة بجوار فقرةٍ تسمّيها دَيناً أسوأ من أيٍّ من الحالتين** ⇒ كلّ فقرةٍ وثّقت دَيناً أُغلق حُذفت أو أُعيدت كتابتها · ⭐ عطبان في العمل نصف المُنجَز: `api/main.py` كان يستعمل `Disposable`/`dispose_all` **بلا استيراد** و`create_production_app` بلا `shutdown=` ⇒ تسريبُ كلّ عميلٍ في الإنتاج · ⭐ حدّ TTL هو **حدّ المُوقِّع نفسه** (‏SigV4 ‏1ث..7ي) ⇒ قيمةٌ خارجه غير قابلة للتوقيع فتصير فشلَ إقلاع · ⭐ `Retry-After` نُفِّذ **بالامتداد الذي سمّاه `03 §4` بنفسه**، والحساب في مِحوَل الفرض لأنّ المُنسِّق لا يعرف أيّ فترةٍ ألزمت القرار · ⭐ `provider.rate_limited` **رُفض** لا أُجّل: صفٌّ في الكتالوج وعدٌ بالإصدار و`test_error_catalog` يفرضه في الاتّجاهين · ⭐ المثاليّة على PostgreSQL **لا Redis** (الإخلاء يعيد فتح نافذة التكرار صامتاً على مسارٍ يُحاسَب) بإدراجٍ‑أوّلاً تحت SAVEPOINT ⇒ **8 مطالباتٍ متزامنة = `CLAIMED` واحدة** حيّاً · أوّل جدولٍ في `platform` بسياسة RLS · حدّ SSE مُعلَنٌ لا مموَّه · ⭐ قائمة المنع **يفحصها الحارس لا المِحوَل** فيبقى `verify_token` بصفر I/O، وكاتبها `python -m app.ops.revoke` لأنّ v1 لا يعلن تسجيل خروج ⇒ لا سطحَ غير قابلٍ للبلوغ. البوابات: ruff **487** · mypy **341** · **8/0** · pytest **2262 ناجح · 0 فشل · 10 متخطًّى** (‏+84 عن الأساس 2178) · **221 تكاملاً حيّاً** منها **122 RLS**)

- [§3.80](3.80.md) — `release-blockers-plan.md` الخطوة 1: **Vault دائم** — نهاية وضع `-dev` في المسارَين (Compose وRunPod): `storage "file"` على حجمين (`vault-data`/`vault-init` منفصلَين عمداً) + `deploy/vault/start.sh` جديدٌ يستبدل `docker-entrypoint.sh` الأصليّ ويقود `operator init`/`unseal` قبل أن يُبلّغ فحص الصحّة بالنجاح — ✅ (⭐ **مفتاح Transit لم يكن خطر `bootstrap.sh` قطّ**: مُثبَتٌ حيّاً أنّ `vault write -f transit/keys/<اسم>` على مفتاحٍ موجودٍ عمليّةٌ بلا أثر (نفس `latest_version`، نفس فكّ التعمية) — الخطر كان فقدان **تخزين Vault نفسه** في وضع dev، وهو ما عولج بنقل التخزين لا بإعادة كتابة ذلك السطر · ⭐ تمرير `-config=` عبر `command:` مع إبقاء `entrypoint` الأصليّ يفشل بـ«`address already in use`» لأنّ `docker-entrypoint.sh` يُلحق `-config="$VAULT_CONFIG_DIR"` بنفسه فيُقرأ الملفّ مرّتين؛ الحلّ استبدال `entrypoint` كاملاً — وهو ما يفتح أيضاً المجال لتشغيل `operator init`/`unseal` بعد `vault server` في نفس شجرة العمليّات، وهو ما لا يسمح به `exec` الأصليّ · ⭐ `vault status` (‏1.18.5) مقيسٌ حيّاً: `1`=غير مستمع، `2`=مختوم، `0`=مفكوك الختم فقط — فحص الصحّة القائم في المستودع كان صحيحاً منذ البداية، وما كان ناقصاً هو ما خلفه · ⭐ عنوان الاستماع templated (‏`0.0.0.0` في Compose حيث الحاوية حدّ العزل، `127.0.0.1` في RunPod حيث الـPod مساحة أسماءٍ واحدة) · `.env`/`.env.example`: `VAULT_DEV_ROOT_TOKEN` محذوفٌ كليّاً (لا متغيّرٌ ميّتٌ يتظاهر بالحياة)، `VAULT_TOKEN` فارغٌ افتراضيّاً. **مُثبَتٌ حيّاً**: `docker compose restart vault` **و**`--force-recreate vault` كلاهما يُبقيان نفس معرّفات المحرّكات ونفس نسخة مفتاح Transit (`latest_version: 1`)، ونصٌّ عُمِّي قبل إعادة التشغيل يُفكّ بنجاحٍ بعدها — **بلا** إعادة تشغيل `vault-bootstrap` (‏`StartedAt` ثابت). لم تُلمَس `app` ولا بقيّة المكدّس (خطوة 4 لاحقة، تحتاج إذناً صريحاً)؛ بوابات مطابقةٌ حرفيّاً لأساس الخطة: ruff **487** · mypy **341** · **8/0** · pytest **2253 ناجح · 0 فشل · 19 متخطًّى** — لا لمس لـ`src/`)

- [§3.81](3.81.md) — `release-blockers-plan.md` الخطوة 2: **جسر الإشعارات — مجموعةٌ لكلّ عمليّة** — `_CG_NOTIFY` الثابت المشترك صار `_CG_NOTIFY_PREFIX` وبات `group` معاملاً صريحاً في `build_notification_consumer`، فتبني `from_env` مجموعةً فريدة `cg.notify.<hostname>.<pid>` لكلّ عمليّة gunicorn شقيقة بدل مجموعةٍ واحدة تتقاسمها كلّ العمليّات — ✅ (⭐ **العلّة كانت حاضرةً حتّى بنسخة API واحدة**: `WEB_CONCURRENCY=2` الافتراضيّ في كلا مساري النشر يجعل «النسخة الواحدة» عمليّتَين شقيقتَين، وRedis Streams يسلّم كلّ مدخلة لمستهلكٍ واحدٍ داخل المجموعة ⇒ نصف الإشعارات كانت تصل عمليّةً لا تحمل الجلسة المستهدَفة، بلا أثرٍ في أيّ سجلّ (`XACK` عاديّ) · ⭐ **تصحيح نصف‑التشخيص**: فقرة `notifications.py`/`build_notification_consumer` القديمة علّلت العيش في عمليّة الـAPI بأنّ عمليّةً منفصلة تحمل Hub فارغاً — صحيحٌ ولكنّه يتجاهل الأشقّاء **داخل نفس الـdeployable**؛ أُعيدت الصياغة لتقول: العيش في عمليّة الـAPI يُجيب «أيّ deployable» لا «أيّ عمليّةٍ من عدّة» — وهذا ما يُجيبه `group` لا موضع الكود · ⭐ `RedisStreamsConsumer.destroy_group`/`list_groups` (جديدتان، `XGROUP DESTROY`/`XINFO GROUPS` مُثبَتتان حيّاً على حالات المجرى/المجموعة الغائبة) + `StreamConsumer.teardown` (نظير `setup`) ⇒ إغلاقٌ نظيف يُدمِّر مجموعة العمليّة نفسها **قبل** إغلاق `redis_client` (`shutdown=(root.teardown_notify_bridge, *root.disposables())`) · ⭐ **كنسٌ آمنٌ للمجموعات اليتيمة بعد SIGKILL**: `_sweep_stale_notify_groups`/`_pid_is_alive` (`os.kill(pid, 0)`) يُدمِّران فقط مجموعةً بادئتها اسم مضيف *هذه* العمليّة **و**pid ميّتٌ فعلاً؛ برهان السلامة ضدّ عمليّةٍ شقيقةٍ تُقلِع في نفس اللحظة: يعمل هذا الخطّاف قبل أن تبني أيّ عمليّةٍ مجموعتها الخاصّة (ترتيب `startup=` قبل `background=`)، فلا حالة ثالثة بين «لم تُنشأ بعد» و«حيّةٌ بالتعريف» — خطرٌ متبقٍّ واحدٌ مُسجَّلٌ بصدق (سباق إعادة تدوير pid نادرٌ جدّاً وذاتيّ الشفاء) · ⭐ **04 §2 والكود متّفقان الآن**: عمود Consumer Group صار `cg.notify.<host>.<pid>` + فقرة ⚠️ تشرح السياسة · ⭐ **تصحيحٌ لا حذف** في `docs/log/3.52.md`: فقرة «v1 أحاديّ‑العمليّة ⇒ صحيح» كانت خلطاً بين عدد النسخ وعدد العمليّات، مُصحَّحةٌ صراحةً. **معيار الخروج**: اختبارٌ حيٌّ جديد يبني عمليّتَي جسرٍ مستقلّتين (Hub منفصل لكلٍّ) على مجرًى واحد ويثبت استقبال **كلتيهما** للإشعار؛ **مُتحقَّقٌ صراحةً أنّه يفشل على الكود القديم** (`git stash` على ملفّات المصدر ⇒ `ImportError` عند الجمع، لأنّ الشكل القديم لا يملك معاملاً `group` أصلاً) ويمرّ بعد الإصلاح (‏4/4)، مع بقاء الاختبارات الحيّة القديمة للجسر بلا مساس (‏3+7). البوابات: ruff **488** · mypy **341** · **8/0** · pytest **2274 ناجح · 0 فشل · 19 متخطًّى** (‏+21 عن أساس الخطوة: 17 hermetic + 4 حيّة))

- [§3.82](3.82.md) — `release-blockers-plan.md` الخطوة 3: **افتراضيّ العامل `WORKER` يصير `memory` لا `knowledge`** — `docker-compose.yml` كان يحمل تعليقاً صادقاً من 18 سطراً يقول إنّ `memory` وحده يقلع اليوم، ثمّ يضع تحته `WORKER: ${WORKER:-knowledge}` — الافتراضيّ يختار العامل المنهار، بينما `deploy/runpod/entrypoint.sh` يفعل الصواب — ✅ (⭐ **الإصلاح المضمَّن في `docker-compose.yml` وحده غير كافٍ**: `docker compose --profile workers config` (فحصٌ للقراءة فقط) بعد إصلاح السطر أظهر أنّ القيمة المُحلولة فعليّاً ما زالت `knowledge` — Compose يحمّل `.env` تلقائيّاً، و`.env.example` المتعقَّب (القالب الذي توجّه كلّ أدلّة النشر الثلاثة لنسخه كخطوةٍ أولى) كان يحمل `WORKER=knowledge` الخاصّ به، وهو الفائز الفعليّ في كلّ نشرٍ موثَّق لا الافتراضيّ المضمَّن؛ أُصلح `.env.example` بنفس القيمة `memory` (و`.env` المحلّي للاتّساق) وأُعيد الفحص: `WORKER: memory` · ⭐ **القرار الحامل: افتراضيٌّ آمن (`memory`) لا انعدام افتراضيّ**، لأنّ معيار الخروج نفسه «المساران متّفقان» — و`entrypoint.sh` (خارج النطاق، صحيحٌ أصلاً) يفترض `memory` بلا محاورة لأنّ RunPod حاويةٌ واحدة بلا مشغِّلٍ تفاعليّ؛ إجبار قيمةٍ صريحة في Compose وحده كان يعيد نفس صنف التناقض الذي أُطلقت الخطوة لإصلاحه. قيمةٌ خاطئة صريحة (`WORKER=bogus`) تفشل بصوتٍ لا بصمت: `app/workers/main.py::_select_worker`/`run` (قائمتان مسبقاً) ترفعان `SystemExit` تسمّي كلّ خيارٍ صالح · تصحيح `docs/design/08-local-runbook.md §4` (كانت تقول «لا عامل يقلع اليوم» — بائدة منذ 2.10 §3.77) + سطرٌ في `docs/quickstart.md` + سطرٌ يربط `deploy/runpod/supervisord.conf` بالإصلاح؛ `deploy-linux-server.md`/`deploy-runpod.md` فُحصا ولم يحتاجا لمساً (يصرّحان `WORKER=memory` بالفعل في كلّ موضع). **حارس انجراف جديد** `tests/unit/test_deploy_worker_default.py` (4 اختبارات hermetic) يقارن **ثلاثة** مصادر نصّيّة (‏`docker-compose.yml`، `entrypoint.sh`، `.env.example`) لا اثنين، ويفحص الاتّفاق **والقيمة الصحيحة** معاً (اختبارٌ منفصل يفحص أنّ الثلاثة تساوي `memory` تحديداً — الاتّفاق وحده غير كافٍ إذا اتّفقوا جميعاً على اسمٍ ينهار)؛ **مُتحقَّقٌ صراحةً أنّه يفشل بآليّة العطب نفسها على الكود القديم** (`git stash` على `docker-compose.yml`/`.env.example` ⇒ `2 failed, 2 passed` بنفس رسالة `'knowledge' == 'memory'` لا خطأ جمعٍ، الدرس الذي صحّحته مراجعة الخطوة 2)، ويمرّ 4/4 بعد الإصلاح. البوابات: ruff **489** · mypy **341** · **8/0** · pytest **2278 ناجح · 0 فشل · 19 متخطًّى** (‏+4 عن أساس الخطوة 2274). لم تُقلَع `app` ولا بقيّة المكدّس — `docker compose config` فحصٌ للقراءة فقط)

- [§3.83](3.83.md) — `release-blockers-plan.md` الخطوة 4: **صورة التضمين + برهان 2.10 — أوّل خطوةٍ تُغيّر حالة النظام لا الملفّات** (`aizzak/embedding:dev` بُنيت أخيراً — أوزان `requirements.txt` ثبّتت **بلا تعديل تثبيتٍ واحد** — والمكدّس أُقلع كاملاً فولَّد مفتاح Transit مرّةً أخيرة على Vault الدائم من الخطوة 1) — 🔵 بانتظار مراجعة المستخدم (⭐ **`aizzak/app:dev` القائمة كانت بائتة عن `src/` الحاليّ** — `docker compose up -d` لا يعيد بناء صورةٍ موجودة حتّى لو تغيّر المصدر تحتها؛ اكتُشف حين سقط سكربت الإثبات بـ`AttributeError` على حقلٍ أضافته §3.79، فأُعيد البناء · SLO مقيسٌ حقيقةً على **مسار الإنتاج نفسه** (`ExternalEmbeddingProvider.embed()` عبر مصنعه الحقيقيّ داخل حاوية `app`، 200 عيّنة بعد 10 إحماء): p50 **11.54ms**/p95 **13.25ms**/p99 **18.66ms** مقابل حدود `07 §2` 120/400/800 — **PASS** بهامشٍ واسع · `POST /api/v1/knowledge/search` أجاب 200 عبر التطبيق ASGI الحقيقيّ (`create_app` نفسها التي تبنيها `create_production_app`) مع استبدالٍ واحدٍ فقط (موثِّق HTTP، إذ لا توكن Firebase حقيقيّ متاحٌ في هذه البيئة — سَيمٌ مُعَدٌّ لهذا الغرض أصلاً) وكلّ شيءٍ آخر حقيقيّ: استدعاء تضمينٍ حقيقيّ + بحثا Qdrant دنسّ/سبارس حقيقيّان · جولة ذاكرةٍ كاملة (تذكّر←فهرسة←استرجاع) عبر نفس لَبِنات `build_memory_worker_from_env` الحقيقيّة نجحت (استرجاعٌ بنقاطٍ 0.6811) · ⭐ **اكتشافٌ منهجيّ لا عطبٌ بالكود**: `TestClient` يقود ASGI على حلقة أحداثٍ منفصلة فتنكسر عملاؤنا الحقيقيّون المربوطون بحلقة السكربت — عولج بـ`ASGITransport` + إدارة دورة حياةٍ يدويّة على نفس الحلقة · اختبار `live_embedding` (`test_orchestrator_live.py`) يمرّ **حقيقةً لا يتخطّى** (3/3) عبر نشرٍ مؤقّتٍ لمنفذ `embedding` (ملفّ تجاوزٍ خارج المستودع، أُزيل فوراً بعد التشغيل ومُثبَتٌ حيّاً أنّ المنفذ عاد داخليّاً فقط) · ⭐ **اكتشافٌ جانبيّ (خطوة 6، لم يُصلَح)**: `llama3.2:1b` تدعم استدعاء الأدوات خلافاً لـ`gemma3:1b` فتُسقط `test_tools_on_a_non_tool_calling_model_is_agent_failed` — لذا شُغِّلت البوابة الخامسة الرسميّة بالبيئة الافتراضيّة (بلا `TEST_OLLAMA_MODEL`) فبقي عدد التخطّي **19 كما كان تماماً** (`live_ollama` يتخطّى قبل الوصول لـ`live_embedding` بترتيب معاملات الدالّة) — والبرهان الحقيقيّ لـ`live_embedding` مُسجَّلٌ منفصلاً أعلاه). البوابات (بيئةٌ افتراضيّة، بلا لمس ملفّ): ruff **489** · mypy **341** · **8/0** · pytest **2278 ناجح · 0 فشل · 19 متخطًّى** — مطابقةٌ حرفيّةٌ للأساس، لا انحدار. `git status` نظيفٌ طوال الخطوة
- [§3.84](3.84.md) — `release-blockers-plan.md` الخطوة 5: **CI يقول الصدق — الفرع الصحيح، والوظيفة الخضراء التي تجمع صفراً تُحذف** — 🔵 بانتظار مراجعة المستخدم (علّتان من عائلةٍ واحدة: `on: push: branches: [main]` في مستودعٍ فرعُه `master` ⇒ **مُشغِّلُ الدفع لا يُطلَق أبداً** ولا شيء يُبلِّغ — قائمة الالتزامات تُظهر «لا فحص» وهو ما يشبه فحصاً لم ينتهِ بعد؛ ووظيفةٌ تشغّل `pytest -m integration` والوسم مُعلَنٌ ومُطبَّقٌ على **صفر** اختبار ⇒ `2272 deselected` وخضرةٌ دائمة بلا تشغيل اختبارٍ واحد — ⭐ **و`--strict-markers` لم يمسكها ولا كان ليمسكها**: هو يرفض وسماً **مُطبَّقاً** لم يُعلَن، ولا يقول شيئاً عن وسمٍ **مُعلَنٍ** لا يطبّقه أحد، وهذا اللاتماثل هو العطب نفسه · **القرار (ب): حذفٌ صريح** للوظيفة وللوسم معاً، والحزمة **كاملةً** بـ`pytest -rs` (بلا `-m` إطلاقاً) في الوظيفة الباقية — الثلاثون وحدةً في `tests/integration/` كلّها محكومةٌ بوسم `live_*` يتخطّى نظيفاً، و`-rs` يطبع اسم وسبب كلّ تخطٍّ فتقول الوظيفة بصوتٍ عالٍ ما **لم** تشغّله · البديل (أ) — خدماتٌ حقيقيّة في CI — موصوفٌ ومرفوضٌ بثلاثة أسباب: **شطرُ تهيئةِ الأدوار/الدلاء/Vault إلى مصدرٍ ثانٍ ينجرف عن الأوّل** وهو صنف العطب الذي أُنشئت الخطة لرفعه · بناءُ بنية CI ميزةٌ لا رفعُ حاجب · **لا أستطيع إثباته من هنا** · حارس انجرافٍ من 5 اختبارات **مُثبَتٌ أنّه يفشل على الحالة القديمة بآليّة العطب نفسها** (`['main']` مقابل `['master']` و`['integration']` مرّتين — لا `ImportError` ولا خطأ جمع) · ⭐ ثلاثة مزالق داخل الحارس نفسه كان كلٌّ منها سيُفرغه من معناه: `yaml.safe_load` يقرأ المفتاح `on` بوصفه `True` (YAML 1.1) فيقرأ الحارس لا شيء ويمرّ للأبد · مسحُ الوسوم كان سيعدّ هذا الملفّ نفسه فيُرضي تأكيده بنفسه · استخراج `-m` كان سيبتلع `python -m pip install` · الحدّ المُعلَن بلا تلميع: **لم يُشغَّل سير العمل على عدّاءٍ حقيقيّ ولا مرّةً واحدة** — لا سبيل لتشغيل GitHub Actions من هنا، فأوّل دفعةٍ إلى `master` هي المشاهدة الأولى · البوابات: ruff **490** · mypy **341** · **8/0** · pytest **2292 ناجح · 0 فشل · 10 متخطًّى** — والفرق عن الأساس مُفسَّرٌ بالرقم: ‏+5 حالاتٍ هي الحارس، و9 تخطّيات `live_ollama` صارت تنجح لأنّ `gemma3:1b` صار **مسحوباً** على هذه الآلة — **تغيّرٌ بيئيٌّ لا أثرٌ لهذا التعديل**، ومقدّمةٌ من مقدّمات الخطوة 6 لم تعد مطابقةً للواقع)

- [§3.85](3.85.md) — `release-blockers-plan.md` الخطوة 6 **والأخيرة**: **نظافة المستودع — ملفّان مولَّدان يخرجان من التتبّع، وخارطة الطريق تُزامَن مع الواقع** — 🔵 بانتظار مراجعة المستخدم (⛔ **البند الأوّل من نطاق الخطوة أُغلق بالقياس لا بالتعديل**: قِستُ `GET /api/tags` بنفسي فأعاد `gemma3:1b` **وحده** — وهو بالضبط ما يضبطه `.env:140` — و`llama3.2:1b` غير مثبَّتٍ إطلاقاً ⇒ لا عدم توافقٍ ولا 404؛ فلم يُلمَس `.env` ولا `PROVIDER_ROUTING` ولا سُحب نموذجٌ ولا حُذف اختبار، وأُسقطت §5‑ب **ن‑2** رسميّاً مع إبقاء درسها العامّ: بعض الاختبارات تفترض **سلوك النموذج** لا مجرّد توفّره · `.coverage` (‏69ك.ب، قاعدة SQLite تُعاد كتابتها كلّ `pytest --cov`) و`docs/ROADMAP.md:Zone.Identifier` (‏25 بايت، **تدفّق NTFS بديل** يخلّفه متصفّح ويندوز ويلتقطه `git add .` بصمت) كانا **متعقَّبَين** و`.gitignore` بلا قاعدةٍ لأيٍّ منهما ⇒ `git rm --cached` للاثنين (`.coverage` **يبقى على القرص** فهو أداةُ عملٍ لا قمامة؛ ملفّ `Zone.Identifier` حُذف نهائيّاً) + قواعد تمنع **الصنف كلّه** لا الملفّين (`*:Zone.Identifier` · `.coverage*` · `coverage.xml` · `htmlcov/` · `Thumbs.db`/`desktop.ini`) · ⭐ **التحقّق من `git` نفسه لا من نصّ `.gitignore`**: `git check-ignore -v --no-index` على ممثّلٍ لكلّ صنف، **والمرآة أيضاً** — `.env.example`/`docs/ROADMAP.md` يجب ألّا تُتجاهَل، فقاعدةٌ واسعةٌ أكثر ممّا يجب تُسقط ملفّاً حقيقيّاً من `git add` بلا أن يبلّغ أحد، وهو أسوأ من القمامة التي تزيلها · **حارس انجراف جديد** `tests/unit/test_repo_hygiene.py` (‏18 حالة) يجيب السؤالين المتقابلين، والتصنيف بدالّةٍ نقيّة `junk_reason()` تُعيد **سبباً نصّيّاً** لا `True` فتسمّي رسالةُ الفشل الملفّ **وتقول لماذا**؛ **مُثبَتٌ أنّه يفشل على الحالة القديمة بآليّة العطب نفسها** ببرهانَين: (1) `git add -f` للملفّين ⇒ `AssertionError: tracked files that must never be committed:` تسمّيهما بسببَيهما — لا خطأ جمعٍ ولا استيراد، (2) إعادة `.gitignore` ما قبل الخطوة ⇒ **8 من 9** أصنافٍ تفشل (التاسع `.DS_Store` كان مُغطًّى فعلاً، والحارس يقول ذلك بصدق) · ⭐ **رفضٌ مُعلَّل لحارس انجراف `ROADMAP.md`**: ترقيم الخارطة (12) يخالف ترقيم `implementation-status` (8) ومواءمتهما نصٌّ في الرأس ⇒ ترميزها في اختبارٍ يخلق **مصدر حقيقةٍ ثالثاً** ينجرف (نفس سبب رفض البديل (أ) في الخطوة 5)، والطرفان نثرٌ بلا حقلٍ مُهيكَل ⇒ حارسٌ إمّا هشٌّ أو مسرحيّ — فعولج **بنيويّاً** بدل الرقابة: قُلِّصت الحالة التفصيليّة من الخارطة وأُحيلت بالروابط، وأُعلن في رأسها أنّ مصدر الحقيقة الحيّة هو `implementation-status.md`/`log/` وأنّها هي البائدة عند الاختلاف · `ROADMAP.md` أُزيل تناقضه الداخليّ (الرأس يقول 12/12 بينما الجدول يضع المرحلة 6 على 🔵 والمرحلة 11 على 🟢) برمزٍ جديد **`✅ + حاشية`** يعني «مكتملةٌ لنطاق v1 وبجوارها دَينٌ مُعلَنٌ خارج نطاق `AC`» — لا إخفاء للدَّين تحت ✅؛ وحُذف ⚠️ «دليل التشغيل يحتاج تصحيح `alembic upgrade head`» بعد التحقّق أنّ `08-local-runbook.md:94` يقول عكسه صراحةً منذ §3.76؛ وأُضيف قسم **«ما بعد القبول»** بجرد ما بقي مفتوحاً مُسمًّى. البوابات: ruff **491** · mypy **341** · **8/0** · pytest **2311 ناجح · 0 فشل · 9 متخطًّى** — الفرق مُفسَّرٌ بالرقم: 2293 + **18** = 2311، والثمانية عشر هي حالات الحارس بالضبط، والتخطّيات التسع لم تتغيّر. لم تُقلَع ولم تُوقَف أيّ حاوية ولم يُمسّ أيّ حجم)

- [§3.86](3.86.md) — `p1-hardening-plan.md` الخطوة 7: **أداة تصريف/إعادة تشغيل DLQ — `python -m app.ops.dlq`** — 🔵 بانتظار مراجعة المستخدم (كانت مدخلة `<stream>.dlq` تدخل ولا تخرج إلّا بـ`redis-cli` يدويّاً؛ `src/app/ops/dlq.py` يسدّ الفجوة على نمط `provision.py`/`revoke.py` بثلاثة أفعالٍ فقط لا أكثر: `peek` (‏`XRANGE` بلا استهلاك) · `requeue` (نسخ حقل `ce` حرفيّاً إلى المجرى الأصليّ) · `purge` (‏`XDEL` يرفض بلا `--yes` صريحة، ولا سويتش «تفريغ الكلّ» عمداً) · ⭐ **المزلق الأوّل**: مدخلةٌ بلا `ce` (‏`malformed_envelope` حين لا حقل أصلاً) لا بايتات لديها لتُحفَظ — `DlqEntry.reinjectable` يفرزها في `peek` و`requeue` يرفضها بخطأٍ يسمّي المدخلة وسببها بدل XADD فارغة أو `KeyError` مبهم · ⭐ **المزلق الثاني** (‏DD‑09/`platform.processed_events`): فحص `workers/bootstrap.py` وTenantSessionFactory.begin أثبت أنّ المطالبة والأثر يلتزمان **معاً** داخل معاملةٍ واحدة، فأيّ مدخلةٍ تصل DLQ بسبب `handler_failed` لم تلتزم مطالبتها قطّ ⇒ إعادة حقنها آمنةٌ بنيويّاً؛ أمّا إعادة حقن نفس المدخلة مرّتين دون `purge` بينهما فتجد المطالبة ملتزمةً فتُبتلَع كتكرارٍ نظيف — **مُسمّاةٌ في التوثيق بصراحة، لا محلولةٌ ولا مُخترَع لها فحصٌ** (الأداة بلا منفذ Postgres) · اختباران حيّان (`tests/integration/test_dlq_ops_live.py`، مجاريً فريدةٌ منظَّفةٌ في `finally`، لا لمس لـ`stream.knowledge`/`stream.media`) يثبتان المسار كاملاً بشيفرة `RedisStreamsConsumer.dead_letter` الحقيقيّة ووصول المدخلة المُعاد حقنها فعليّاً لقراءةٍ `>` جديدة (‏`delivery_count=1`، معرّفٌ جديدٌ لا مُعاد)، و9 اختبارات حتميّة على `_StubRedis` (نمط `test_ops_revoke.py`) · فقرةٌ جديدة `08-local-runbook.md` §4.2 · **لا حالة قديمة تُثبَت الفشل عليها** — الأداة لم تكن موجودةً أصلاً، فالإثبات البديل هو مرور كلّ اختبارٍ حيٍّ عبر شيفرة الإنتاج نفسها لا محاكاة. البوابات: ruff **494** · mypy **342** · **8/0** · pytest **2331 إجماليّاً** (‏**2321 ناجح · 1 فشل · 9 متخطًّى**) — الفرق مُفسَّرٌ بالرقم: 2320 + **11** جديدة (‏9 حتميّة + 2 حيّة) = 2331؛ ⚠️ **الفشل الوحيد (`test_repo_hygiene.py`) مسبقٌ تماماً على هذه الخطوة ولم يُلمَس**: `.coverage` وُجد متعقَّباً في شجرة `0308092` نفسها (`git diff --stat f26c514 0308092 -- .coverage` بلا مخرَج) رغم أنّ §3.85 يوثّق إزالته — الالتزام الموثِّق لم يُنجز فعليّاً على الشجرة الملتزَمة؛ سُجِّل **ن‑6** جديدةً في §5‑ب من الخطة لقرارٍ منفصل. لم يُقلَع ولم يُوقَف ولم يُعَد إنشاء أيّ حاوية، ولم يُمسّ أيّ حجم)

- [§3.87](3.87.md) — `p1-hardening-plan.md` الخطوة 8: **سياسة احتفاظ للجداول الثلاثة — `python -m app.ops.retention`** — 🔵 بانتظار مراجعة المستخدم (‏`platform.processed_events`/`platform.idempotency_keys`/صفوف `platform.outbox` المنشورة تنمو بلا حدٍّ منذ إنشائها بلا مكنسةٍ ولا جدولةٍ ولا فقرة دليل تشغيل · ⭐ **التوتّر المعماريّ**: `app_rw` مُنِح `INSERT` وحدها عمداً على `outbox`/`processed_events` («دورٌ لا يملك إلّا `INSERT` لا يستطيع سرد السجلّ ولا إلغاء معالجة حدثٍ لفرض إعادة تشغيل») — فمنحُه `DELETE` كان سيهدم ذلك الضمان؛ **الحسم**: دورٌ رابعٌ منفصل `retention_sweeper` (نظير `outbox_relay` بمنحه الخاصّة، السابقة المطلوبة صراحةً) يحمل `SELECT, DELETE` فقط على الثلاثة، و`platform.idempotency_keys` (الجدول الوحيد بسياسة RLS في `platform`) يحصل على سياستَين إضافيّتَين مقصورتَين على هذا الدور وحده (`retention_sweeper_select`/`retention_sweeper_delete`، `USING (true)`، تتّحدان بمنطق OR مع `tenant_isolation` القائمة — نظير `platform_credentials_read`، 01 §3.1) فتصل المكنسة لكلّ مستأجرٍ في نداءٍ واحد دون المساس بحصر `app_rw` · هجرة `0003_retention_sweep.py` (لا `GRANT` فيها — فهرسان جديدان + السياستان، و`downgrade` منفَّذٌ فعليّاً: 14/14 الآن) · ⭐ **اكتشاف**: `CREATE POLICY ... TO retention_sweeper` يتحقّق من وجود الدور عند تطبيق DDL — أوّل هجرةٍ في هذا المستودع تحمل تبعيّةً حقيقيّةً كهذه؛ `_require_roles` في `provision.py` أُضيف له `RETENTION_ROLE` وهو يُفحَص **قبل** الهجرات أصلاً · حدودٌ ثلاثةٌ مختلفة مُعلَّلة (‏يومان لـ`idempotency_keys` — بيانات ردٍّ فعليّة لمستأجر ونافذة عميلٍ قصيرة؛ 30 يوماً لـ`processed_events` — هامش عمليّاتيّ لإعادة حقنٍ متأخّرة عبر أداة DLQ؛ 90 يوماً لصفوف `outbox` المنشورة — قيمةٌ تحقيقيّةٌ للحادثة) · `--older-than-days` يتطلّب `--table` دائماً، `--dry-run` يُحصي ولا يحذف، وصفّ `outbox` غير المنشور ليس مرشّحاً بتاتاً مهما بلغ عمره · اختبارٌ حيٌّ (‏5 حالات) يثبت الحذف **والامتناع معاً** في كلّ جدول (زوجُ صفوفٍ قديم/جديد، وثالثٌ غير منشورٍ لـ`outbox`) والعبور عبر المستأجرين لـ`idempotency_keys` ورفض `retention_sweeper` على `INSERT`/`UPDATE` بـ`42501`؛ 11 اختباراً حتميّاً (`_FakeConnection`/`_FakeEngine`، نمط `_StubRedis`) + اختبارا منحٍ في `test_ops_provision.py` · فقرة `08-local-runbook.md` §4.3 أعلنت أوّلاً أنّ `deploy/postgres/initdb/10-roles.sh`/`docker-compose.yml`/`.env.example`/`deploy/runpod/*` لم تُحدَّث، وصنّفته «مقصوداً» ضمن حدّ النطاق. ⚠️ **تصحيحٌ لاحق (مراجعة المُنسِّق، §3.87 §7)**: التصنيف كان خطأً — `_require_roles` يعمل **قبل الهجرات**، فإضافة `RETENTION_ROLE` حوّلت `provision()` من عاملٍ على عنقودٍ جديد إلى فاشلٍ عليه: **انحدارٌ أحدثته الخطوة نفسها لا فجوةً سابقة**. أُصلح فوراً على نمط `outbox_relay` حرفيّاً في `10-roles.sh`/`docker-compose.yml`/`.env.example`/`.env`/`deploy/runpod/entrypoint.sh`+`bootstrap.sh` + أدلّة النشر الثلاثة، مع حارس انجرافٍ جديد `tests/unit/test_role_provisioning_wiring.py` (4 اختبارات) يقارن الأدوار الثلاثة عبر أربعة مصادر (سابقة `test_deploy_worker_default.py`، الخطوة 3). `docker compose config` (قراءةٌ فقط) أثبت الحلّ الفعليّ؛ المكدّس بقي `healthy` بلا لمس. البوابات **بعد الإصلاح**: ruff **499** · mypy **343** · **8/0** · pytest **2353 إجماليّاً** (‏**2344 ناجح · 0 فشل · 9 متخطًّى**) — الفرق مُفسَّرٌ بالرقم: 2331 + **22** = 2353 (‏18 من التسليم الأوّل + 4 من حارس التوصيل)، والتخطّيات التسع لم تتغيّر. لم تُقلَع ولم تُوقَف ولم يُعَد إنشاء أيّ حاوية، ولم يُمسّ أيّ حجم)

- [§3.88](3.88.md) — `p1-hardening-plan.md` الخطوة 9: **الحافّة: حدّ معدّل + ترويسات أمان + قرار CORS مكتوب** — 🔵 بانتظار مراجعة المستخدم (الحافّة كانت تحمل `server_tokens off` وحدها — لا `limit_req`/`limit_conn` إطلاقاً (فلا شيء بين الإنترنت وحلقة الوكلاء غير حصّة `usage` التي تعمل **بعد** المصادقة والتفويض)، لا ترويسات أمانٍ، وسطر HSTS كان نثراً يشرح غيابه لا إعداداً. ⭐ **ثلاثة مواقعٍ، ثلاث معالجاتٍ مختلفة، كلٌّ مُعلَّل**: `/health` (فحصٌ كلّ 10 ثوانٍ) **مُستبعدةٌ كليّاً** من `limit_req`/`limit_conn` معاً (حدٌّ عليها يُسقط الفحص نفسه، وقع فعلاً من قبل — §3.76) · `/api/v1/ws` (WebSocket) **بلا `limit_req`** (اتّصالٌ طويلٌ ليس طلباً متكرّراً) **ومع `limit_conn`** (100 اتّصالٍ متزامن/عنوان — يحدّ الفجوة الحقيقيّة اليوم: انعدام سقفٍ على عدد المقابس التي يفتحها عنوانٌ واحد؛ المكافئ لكلّ‑مستخدمٍ بعد المصادقة خطوةٌ 11 منفصلة) · `/` (يخدم SSE أيضاً) **بـ`limit_req`** (20 طلب/ث/عنوان، دفعة 40 — يُفرَض عند **قبول** الطلب لا أثناء **بثّه**، فلا يقطع تدفّقاً جارياً). ⭐ **مفتاح `$binary_remote_addr` لم يُفترَض**: تحقّقٌ حيٌّ على المكدّس القائم (`docker network inspect`/`docker logs aizzak-nginx-1`) أثبت أنّ طلباً خارجيّاً حقيقيّاً يحتفظ بعنوانه عبر DNAT نشر منفذ Docker، بينما اتّصال **نفس المضيف فقط** يُصاب بـhairpin NAT إلى عنوان بوّابة الشبكة الداخليّة (`172.18.0.1`) — حالةٌ لا تقع لعميلٍ حقيقيّ ولا تمسّ `/health`. الرفض عند الحافّة **429** (`limit_req_status`/`limit_conn_status`) لا 503 الافتراضيّة — الجسم صفحة nginx الافتراضيّة لا `problem+json` (فجوةٌ مُعلَنة). ترويسات الأمان الثلاث (`X-Content-Type-Options`/`Referrer-Policy`/`X-Frame-Options`) على مستوى `http`. ⭐ **HSTS صار إعداداً حقيقيّاً لا تعليقاً، مع بقاء الاستبعاد المحلّي بلا تفريعٍ في الملفّ**: `map $host $hsts_header { default ""; }` + `add_header ... $hsts_header always;` (‏:443 فقط) — ومُتحقَّقٌ حيّاً على حاوية `nginx:1.27 --rm` منفصلة أنّ `add_header` **يُسقط الترويسة كليّاً** حين قيمتها فارغة (غيابٌ حقيقيّ لا قيمةٌ يتجاهلها المتصفّح)؛ نشرٌ حقيقيٌّ يضيف سطراً واحداً (`deploy-linux-server.md §10.3` يعطي السطر الحرفيّ). **CORS**: لا كود — القرار (من المستخدم، محسوم) أنّ الواجهة v1 على نفس الأصل خلف نفس الحافّة، مكتوبٌ صراحةً في `03-api-spec.md §5` (قسمٌ جديد) + `Requirements-v1.md:SEC-08`، مع ما يجب أن يحدث إن تغيّر الافتراض. **المساران متّفقان**: `deploy/runpod/nginx.conf` يحمل نفس المناطق/الترويسات/الخريطة حرفيّاً (فرقٌ واحدٌ مُسمّى: تحذيرٌ إضافيّ بألّا يُسمّى مضيف RunPod العابر `<POD_ID>-80.proxy.runpod.net` في خريطة HSTS). حارس انجرافٍ جديد `tests/unit/test_edge_hardening.py` (8 اختبارات) يقارن الملفّين نصّيّاً على كلّ ما سبق؛ **مُثبَتٌ أنّه يفشل على الحالة القديمة بآليّة العطب نفسها** (`git stash` على الملفّات الخمسة المعدَّلة ⇒ 6 فشلٍ يسمّي الفجوة، لا `ImportError`؛ `stash pop` وتحقّقٌ من عودة الحالة). `nginx -t` نجح في ثلاث بيئات (حاويتان `--rm` منفصلتان + الحاوية الحيّة نفسها عبر `docker compose exec`، قراءةٌ فقط بلا إعادة تحميل). المكدّس الحيّ (10 خدمات) بقي بلا تغيير طوال الجلسة، `nginx` نفسها `healthy` باستمرار. البوابات: ruff **500** · mypy **343** · **8/0** · pytest **2361 إجماليّاً** (‏**2352 ناجح · 0 فشل · 9 متخطًّى**) — الفرق مُفسَّرٌ بالرقم: 2353 + **8** = 2361 (حالات الحارس الثماني بالضبط)، والتخطّيات لم تتغيّر. تعديل ملفّاتٍ فقط — لم تُقلَع ولم تُوقَف ولم يُعَد إنشاء أيّ حاوية، ولم يُمسّ أيّ حجم)

- [§3.89](3.89.md) — `p1-hardening-plan.md` الخطوة 10: **مقياسا الـOutbox والـDLQ + إنذار — `GET /metrics`** — 🔵 بانتظار مراجعة المستخدم (‏`07-nfr-slo §7` يسمّي زمن دورة الـOutbox وامتلاء DLQ «مؤشّرَي صحّةٍ رئيسيّين» ولا شيء كان يقيسهما؛ التسجيل JSON المُهيكَل أساسٌ جيّد وليس مراقبة. ⭐ **مزلق تعدّد العمليّات (الأخطر) مُثبَتٌ حيّاً لا مُصمَّمٌ فقط**: `app` يعمل بعدّة عمّال gunicorn (‏`WEB_CONCURRENCY=2`)، فحُسم بلا `PROMETHEUS_MULTIPROC_DIR` إطلاقاً — كلّ كشطةٍ تبني `CollectorRegistry` **جديدةً** وتقرأ Postgres/Redis **مباشرةً** لحظة الطلب (لا ذاكرة عمليّةٍ بينهما)، فأيّ عمليّةٍ تُجيب تُعيد نفس الرقم بالبناء؛ مُثبَتٌ بمحرّكَين وعميلَي Redis منفصلين كليّاً يتّفقان على نفس الحالة الحقيقيّة (`tests/integration/test_metrics_source_live.py::test_two_independent_instances_report_the_identical_value`). ⭐ **دورٌ خامسٌ للقاعدة `metrics_reader`**: `app_rw` مُنِح `INSERT` وحدها على `platform.outbox` عمداً فلا يستطيع قراءة عمر أقدم صفٍّ؛ دورٌ خامسٌ (`SELECT` وحيدة، لا هجرة RLS مطلوبة — `platform.outbox` بلا سياسة RLS أصلاً) نظير `outbox_relay`/`retention_sweeper` بالضبط، موصولٌ عبر خمسة ملفّاتٍ **من أوّل تسليم** (`provision.py`/`10-roles.sh`/`docker-compose.yml`/`.env.example`/`deploy/runpod/entrypoint.sh`) — درس مراجعة الخطوة 8 مُطبَّقٌ سلفاً لا مُكتشَفٌ لاحقاً؛ حارس `test_role_provisioning_wiring.py` **مُوسَّعٌ** لأربعة أدوار. الاستعلام يستفيد من الفهرس الجزئيّ القائم `ix_outbox_unpublished` بلا فهرسٍ جديد. ⭐ **حدّ الثقة**: `location /metrics { return 404; }` محليّاً قبل `location /` على المسارين معاً — **مُثبَتٌ بحاويتَي `--rm`** (شبكةٌ معزولة: `/metrics`→404 محليّاً بلا محاولة وكالة، `/health`→502 كان سيتّصل لكنّه فشل — التفريق نفسه هو البرهان؛ شبكة `aizzak_default` الحقيقيّة: `/metrics`→404، `/health`→200 وكالةٌ حقيقيّة ناجحة، `nginx -t` نجح على المسارين معاً). سكرابٌ HTTP حقيقيّ (`httpx.ASGITransport`) يُثبت أنّ صفّاً غير منشورٍ يشيخ فيرتفع الرقم بين كشطتين متتاليتين، وأنّ صفّاً منشوراً قديماً لا يُخطَأ بصفٍّ عالق، وأنّ `XADD`/`XDEL` حقيقيَّين يرفعان عمق DLQ بمقدار واحد ويعيدانه (دلتا لا مطلق). **الإنذاران** (`deploy/prometheus/alerts.yml` — أداةٌ لكاشطٍ لاحق، لا خدمة Prometheus قائمة): زمن الدورة > 3ث (ميزانيّة p99 نفسها `07-nfr-slo §2`) لمدّة دقيقتين؛ عمق DLQ > 0 لمدّة 5 دقائق (لا عمق طبيعيّاً غير صفريّ) — **والاستجابة مكتوبةٌ في الإنذار نفسه**: `python -m app.ops.dlq peek/requeue/purge` (الخطوة 7). حارسٌ يستورد اسمَي المقياسين من `api/metrics.py` نفسه فيمسك أيّ انجرافٍ. فقرة `08-local-runbook.md §4.4` جديدة. البوابات: ruff **507 ملفّاً مُنسَّق** · mypy **347 ملفّاً بلا مشاكل** · **8/0** · pytest **2377 إجماليّاً** (‏**2368 ناجح · 0 فشل · 9 متخطًّى**) — الفرق عن أساس الخطوة 9 مُفسَّرٌ بالرقم: 2362 + **15** = 2377 (‏3 حتميّة للموجّه + 4 لملفّ الإنذارات + 1 لحارس الحافّة + 5 حيّة + 2 لمنح `metrics_reader`)، والتخطّيات التسع لم تتغيّر. ⚠️ **درسٌ بيئيّ مُكرَّر**: تشغيل `pytest` بلا تفعيل البيئة الافتراضيّة يُسقط `test_import_contracts` إلى تخطٍّ صامت (نفس درس §3.86/§3.87)؛ البوابات أعلاه من تشغيلٍ مُفعَّلٍ فعلاً. تعديل ملفّاتٍ فقط — لم تُقلَع ولم تُوقَف ولم يُعَد إنشاء أيّ حاوية، ولم يُمسّ أيّ حجم — حاويات `--rm` منفصلة للإثبات أُزيلت فوراً وتحقّقاً)

- [§3.90](3.90.md) — `p1-hardening-plan.md` الخطوة 11: **سقف WS عالميٌّ لا لكلّ عمليّة — `WsConnectionRegistry` فوق Redis** — 🔵 بانتظار مراجعة المستخدم (‏`hub.py:63` كان يحمل `_user_counts` قاموساً في ذاكرة العمليّة، و`ConnectionHub` يُبنى مرّةً لكلّ عمليّة تحت `WEB_CONCURRENCY=2` ⇒ السقف المُعلَن `ws_connections_per_user = 5` كان فعليّاً **5 × عمّال × نسخ**: خمسة مقابس لكلّ عاملٍ بلا رفضٍ واحد — **نفس جذر P0‑2** الذي عالج §3.81 نصفَه (التوجيه) وترك نصفه (القبول). العدّاد انتقل خلف منفذٍ على نمط الخطوة 10 حرفيّاً: `framework/ports/ws_connection_registry.py` + `infrastructure/streaming/redis_connection_registry.py` + توصيلٌ في `composition_root.py` فوق `redis_client` المشترَك نفسه. ⭐ **الذرّيّة استُعيدت على جانب Redis لا افتُرضت**: `try_register` كانت متزامنةً عمداً («لا `await` بين الفحص والتغيير»)، فاشتُري الضمان من جديد داخل **سكربت Lua واحد** يجري `ZREMRANGEBYSCORE` ثمّ `ZCARD` ثمّ `ZADD` كخطوةٍ لا تتجزّأ — و`hub.try_register` **لا تقرأ عدداً ثمّ تقرّر عليه** إطلاقاً؛ البرهان تزامنيٌّ حقيقيّ: 12 تسجيلاً عبر `asyncio.gather` على هبَّين شقيقَين بسقف 4 ⇒ 4 بالضبط و`ZCARD == 4`. الوقت من `redis.call('TIME')` لا من العمليّة (‏مُتحقَّقٌ حيّاً على Redis **7.0.15** المنشور). ⭐ **التسرّب**: **المُدخَلة الفرديّة** هي ما يشيخ (درجتها = آخر تجديد)، لا المفتاح — مهلةٌ على المفتاح وحده لا تنتهي أبداً ما دام المستخدم نشطاً فتبقى مُدخَلة العمليّة الميّتة محسوبةً للأبد؛ و**مَن يجدّد**: مهمّةٌ خلفيّةٌ واحدةٌ لكلّ عمليّة (`hub.start_renewal`/`stop_renewal`، موصولةٌ في `startup=`/`shutdown=` بـ`api/main.py`، والإيقاف **قبل** `disposables()` لأنّها قد تكون في منتصف نداءٍ على نفس `redis_client`)، و`renew_interval_s >= entry_ttl_s` **يرمي `ValueError` عند البناء** لأنّ بديله سقفٌ يترخّى صامتاً؛ والتجديد نفسه **يكنس** (`ZREMRANGEBYSCORE` قبل `ZADD XX`) فمُدخَلة شقيقٍ منهارٍ تُستردّ في أوّل نبضةٍ لأيّ حلقةٍ حيّة. **نمط `sweep_stale_notify_groups` قُرئ ولم يُختَر عمداً ومُعلَّلاً**: مقصورٌ على المضيف بالبناء، واسم مضيف الحاوية هو معرّفها فيتغيّر مع كلّ إعادة إنشاء ⇒ مُدخَلاتُ حاويةٍ لم تُعَد بنفس الاسم لا يكنسها أحدٌ أبداً. ⭐ **قرار عطب Redis صريحٌ ومكتوب**: **يُغلق عند القبول** (الفشل المفتوح = حذف السقف كلّيّاً تحت الطلب لأيّ من يُربك Redis، ومقبسٌ يُقبَل وRedis ساقطة لا يصله شيءٌ أصلاً لأنّ جسر 5.3‑د فوق Streams — والرفض يُترجَم إلى **1008** نفسه) و**يُبتلَع عند الإطلاق** (‏`unregister` من `finally`، والرمي يُجهض إلغاء المهامّ الذي يُحاسِب تشغيلاً مهجوراً؛ والتسرّب محدودٌ سلفاً بالتشييخ). **معيار الخروج مُثبَتٌ بالفشل أوّلاً**: `git stash -u` ⇒ مسبارٌ بالتوقيع القديم يفشل بـ`AssertionError: assert not True` على `try_register` نفسها — **بآليّة العطب لا بخطأ جمع**؛ وبقيت الآليّة مسجّلةً دائماً في الحزمة (`test_two_private_registries_do_not_enforce_one_cap`: سجلٌّ خاصٌّ لكلّ هبٍّ ⇒ أربعة مقابس على سقفٍ من اثنين). و**التسرّب مُثبَتٌ في الاتّجاهين**: مُدخَلة «العمليّة المنهارة» تختفي بعد `1.5 × ttl` **بينما** مُدخَلة الحيّ المُجدَّدة تبقى **بعينها** (المعرّفان يُقرآن من Redis بـ`ZRANGE` ويُقارَنان بالاسم لا بالعدّ). كلّ مفتاحٍ حيٍّ بادئته `ws:test:<uuid7>` ويُحذف بـ`SCAN`+`DEL` عليها وحدها — لا `FLUSHDB`، ولا لمس `ws:conn:*` ولا `stream.knowledge`/`stream.media` (مُتحقَّقٌ: صفر مفتاح `ws:*` بعد التشغيل). حارسٌ **بنيويّ** على التوصيل يستدعي `create_production_app` الحقيقيّة ويفحص صفَّي الخطّافات فعليّاً (لا نصّ الملفّ). البوابات: ruff **512 ملفّاً مُنسَّق** · mypy **350 ملفّاً بلا مشاكل** · **8/0** · pytest **2391 إجماليّاً** (‏**2382 ناجح · 0 فشل · 9 متخطًّى**) — الفرق عن أساس الخطوة 10 مُفسَّرٌ بالرقم: 2368 + **14** = 2382 (‏5 حيّة + 8 حتميّة للهب + 1 حارس التوصيل)، والتخطّيات التسع لم تتغيّر. تعديل ملفّاتٍ فقط — لا حاوية أُنشئت أو أُوقفت أو أُعيد إنشاؤها، ولا حجمٌ مُسّ، ولا ترحيلٌ شُغِّل؛ `git diff --summary` فارغ)

- [§3.91](3.91.md) — `p1-hardening-plan.md` الخطوة 12: **مسار تدوير مفتاح Transit — منحة `rewrap` + `app.ops.rotate_transit` + إجراء التشغيل** — 🔵 بانتظار مراجعة المستخدم (‏`transit/rewrap/tenant-secrets` مُمنوحةٌ الآن في `app-policy.hcl` مع أوّل مستدعٍ لها؛ `VaultSecrets.rewrap` على المحوّل وحده — **لا** على منفذ `SecretsProvider` (لا حالة استخدامٍ تطبيقيّة تحتاج تدويراً، ووضعها على المنفذ كان سيمنح كلّ مستهلكٍ قدرة تدويرٍ لا يحتاجها). ⭐ **اكتشافٌ حيٌّ صحّح تصميماً كان سيشحن مكسوراً**: الافتراض «إعادة تعمية نصٍّ عند نسخة المفتاح الحاليّة تعيد نفس البايتات» **خاطئٌ**، مقيسٌ حيّاً — Vault يولّد nonce عشوائيّاً في كلّ `rewrap` بصرف النظر عن تغيّر النسخة؛ الإصلاح: مقارنة **رقم نسخة المفتاح** المُضمَّن في بادئة Vault (`vault:v<n>:...`) لا البايتات. **جدار RLS** (نفس مشكلة الخطوتين 8 و10) حُسم بدورٍ سادسٍ منفصل `transit_rotator` — `SELECT` + `UPDATE` **مقصورٌ على عمود التعمية وحده** (لا الجدول كلّه) على الجداول الثلاثة، وهجرتان (`credentials/0002_rotator_credentials`، `integrations/0002_rotator_integrations`) تضيفان سياسات RLS مقصورةً على الدور، مُثبَتٌ حيّاً بالرفض (‏`42501`) على INSERT/DELETE/تعديل أعمدةٍ أخرى. `app.ops.rotate_transit`: معاملةٌ قصيرةٌ لكلّ صفٍّ (لا للجدول كلّه — كلّ صفٍّ يستدعي Vault فعليّاً) فآمنٌ لإعادة التشغيل وللمقاطعة في أيّ لحظة، وحرس تزامنٍ صريح (`WHERE ... AND col = :old`). **معيار الخروج الصادق** (تعمية v1 ← تدوير ← إعادة تعمية واحدٍ من نصّين ← رفع `min_decryption_version` ← الاتّجاهان معاً) مُنفَّذٌ حيّاً على مفتاحٍ اختباريٍّ فريد — لم يُلمَس `tenant-secrets` المشترك قطّ. `approle_smoke.py` امتدّ بـ[3b]/[7] وشُغِّل فعليّاً على تهيئة AppRole عابرة على Vault التطوير (لا مكدّس Compose)، أُزيلت كاملةً بعد التشغيل ومُتحقَّقٌ من عودة الحالة. فقرة `08-local-runbook.md §4.5` توثّق التسلسل الثلاثيّ الصلاحيّات كاملاً. البوابات: ruff **517** · mypy **351** · **8/0** · pytest **2413 إجماليّاً** (‏**2404 ناجح · 0 فشل · 9 متخطًّى**) — الفرق: 2382 + **22** = 2404. 🐛 **عطبٌ أمسكه المُنفِّذ نفسه**: بتّ تنفيذ `deploy/runpod/{entrypoint,bootstrap}.sh` سقط أثناء التحرير (تكرار عطب مراجعتَي الخطوتين 1 و8) — أُعيد فوراً، `git diff --summary` عاد فارغاً)

- [§3.92](3.92.md) — `p1-hardening-plan.md` الخطوة 13: **اختبار حِملٍ واحد يصدّق رقم SLO (P1-6) — `GET /api/v1/conversations/{id}`** — 🔵 بانتظار مراجعة المستخدم (الرقم المُختار سلفاً في الخطّة: «قراءة متزامنة» — p50 40ms/p95 150ms/p99 300ms. **المقاس**: 1000 طلبٍ بتزامن 16 بعد 50 إحماء عبر حاويةٍ عابرة من صورة الإنتاج نفسها على شبكة `aizzak_default`، تبني `CompositionRoot.from_env()` و`create_app` الحقيقيّين وتقودهما بـ`httpx.ASGITransport` على نفس حلقة الحدث (منهج §3.83) — p50 **41.95ms** (❌ **FAIL** بهامش 1.95ms) · p95 **84.92ms** (✅ PASS) · p99 **261.24ms** (✅ PASS بهامشٍ أضيق ممّا توحي به p95). **حُفظ الفشل كما قِيس — لم يُعَد التشغيل لتحسينه**. قفزة `nginx→app` قِيست منفصلةً (‏200 عيّنة، p50 ≈1.6-2ms) وأُعلنت إضافيّةً لا مطويّةً ولا صفراً. ⭐ **اكتشافٌ أخطر من الخطوة نفسها**: AppRole التطبيق **لا تستطيع المصادقة على Vault الحيّ إطلاقاً اليوم** (‏`hvac` الحقيقيّ ⇒ `InternalServerError: failed to determine alias name from login request`، 3/3 مُثبَتٌ حتميّاً، ومختلفٌ عن خطأ اعتمادٍ خاطئ عمداً الذي يُعطي 400 مختلفاً) — وتوكن الجذر في `vault-init` نفسه **غير صالح** (‏403)، فلا مسار إصلاحٍ إداريّ متاحٍ بلا لمس Vault الحيّ (ممنوعٌ هنا)؛ العلاج لهذه الخطوة وحدها: `VAULT_SECRET_ID` فارغٌ عمداً (المسار الموثَّق «ليس AppRole» أصلاً) + حذف `connect_storage`/جسر الإشعارات من `startup=`/`background=` (الثاني قرار سلامةٍ مستقلّ: لا ينضمّ لمجموعة مستهلكٍ حقيقيّة فوق `stream.knowledge`/`stream.media` الإنتاجيّين). **مُقترَحٌ تسجيله ملاحظةً جديدةً في §5-ب من الخطّة** (خارج نطاق هذه الخطوة). عطبان أوقفا التشغيل الحيّ وأُصلحا **قبل** القياس النهائيّ: الصورة القائمة بائتةٌ عن `prometheus-client` (أُضيفت بعد بنائها — «لا انجراف اعتماديّات» في التكليف تحقّق من `requirements.txt` غير الموجود أصلاً في هذا المستودع) وبناء SQL (`:content::jsonb` لم يُميَّز، أُصلح إلى `CAST(... AS jsonb)`). ⚠️ **حادثةٌ ذاتيّة مُسجَّلةٌ بصدق**: أثناء التصحيح، أمرا `env`/`declare -p` خامان طبعا قيمتَي `APP_RW_PASSWORD`/`AIZZAK_OWNER_PASSWORD` الحقيقيّتين في مخرجات أداةٍ عابرة للوكيل (لا في أيّ ملفٍّ أو سجلٍّ دائم) — خرقٌ لقاعدة عدم طباعة الأسرار، أُصلح إجرائيّاً فوراً (فحوصٌ لاحقة بالطول لا بالقيمة)، ويُوصى بتدوير الاعتمادين عبر مسارٍ رسميّ إن أُريد إزالة الشكّ. البذر (مساحة عمل + مستخدم + محادثة + 5 رسائل، موسومةٌ `p1-step13-<uuid7>`) عبر `aizzak_owner`+`SET LOCAL app.workspace_id`؛ **صفٌّ يتيمٌ من محاولةٍ فاشلة اكتُشف وحُذف أثناء الجلسة نفسها** قبل التقرير النهائيّ؛ التنظيف مُثبَتٌ بعدَّين مستقلَّين (‏الاختبار نفسه + `psql` بعد الجلسة) — الأربعة أصفار. الأداة الدائمة (‏`test_conversation_get_slo_live.py`/`test_nginx_edge_hop_slo_live.py`) بعلامةٍ جديدة `live_stack_slo` وفحصٍ `autouse` (لا `skipif` — سطرٌ واحدٌ لِـ`pytestmark` يشترطه حارس `test_ci_workflow_truthfulness.py`)، مُثبَتٌ تخطّيهما تحت البوابة الافتراضيّة. البوابات: ruff **519 ملفّاً منسَّق** · ruff check نظيف · mypy **351** · **8/0** · pytest **2415 إجماليّاً** (‏**2404 ناجح · 0 فشل · 11 متخطًّى**) — الفرق عن الأساس: +2 متخطًّى (ملفّا هذه الخطوة) فقط، **2404 ناجحٍ مطابقةٌ حرفيّة**، لا انحدار (تشغيلةٌ عابرةٌ منتصف الجلسة أظهرت 42 فشلاً عابراً في `live_db` أُعيد التحقّق منه بتشغيلتين نظيفتين متطابقتين لاحقاً). المكدّس العشرة `Up 35 hours` طوال الجلسة، كلّ ذي فحصٍ منها `healthy`، لا حاوية `--rm` باقية، `git diff --summary` فارغ)

- [§3.93](3.93.md) — `p1-hardening-plan.md` الخطوة 14: **تصفية الملاحظات المؤجَّلة (ن‑1 · ن‑4 · ن‑5 · ن‑8 · ن‑9 · ن‑10 · ن‑11)** — 🔵 بانتظار مراجعة المستخدم (خطوةٌ ملفّاتٍ فقط، لا لمس للمكدّس الحيّ ولا لأيّ قاعدة بيانات. **ن‑1**: وُثِّق موضع فشل الإقلاع بالكود لا افتُرض — `_make_lifespan` (`api/main.py`) توصل `sweep_stale_notify_groups` أوّل `startup=`، وهي تستدعي `RedisStreamsConsumer.list_groups` الذي لا يلتقط سوى `ResponseError` الخاصّ بـ«‏no such key»، فأيّ `ConnectionError` حقيقيّ من Redis غير متاحٍ يُجهض `lifespan` كلّه قبل `app.state.ready`؛ التوثيق وحده في `08-local-runbook.md §3.1‑ب` (نظير §3.4 Vault)، لا تعديل كود. **ن‑4**: سطرٌ تحذيريّ في `§3.0` يشرح أنّ `docker compose up -d` لا يعيد بناء صورةٍ قائمة. **ن‑5**: أُعيد التحقّق مستقلّاً (`grep` صفريّ في `src`/`tests`)، فحُذفت `testcontainers` من `pyproject.toml` (`dev` + تجاوز mypy) **بلا لمس `.venv`** (`pip show` أثبت بقاءها مثبَّتة)، وسُوِّيت `docs/design/09-testing-strategy.md` (§1/§3/§6) مع المرفق الحيّ (PG16 أصليّ/WSL، وسوم `live_*`، منحٌ في `conftest`). **ن‑9**: فقرةٌ جديدة `§3.3‑ب` تعطي أوامر `CREATE ROLE`/`ALTER ROLE`/`GRANT CONNECT` للأدوار الثلاثة الغائبة مُشتقّةً حرفيّاً من `10-roles.sh` ثمّ `docker compose up migrate` — ⛔ كتابةٌ فقط، لم يُشغَّل شيءٌ على أيّ قاعدة. **ن‑10**: الحدّ الأدنى موثَّقٌ في `§3.1` (العرَض + سكّ `secret_id` جديد)، والمقايضات الثلاث معروضةٌ على المستخدم لا محسومة — ⛔ لم يُعدَّل `bootstrap.sh` ولا أيّ TTL ولا فحص صحّةٍ جديد. **ن‑11**: قاعدةٌ دائمة جديدة في `§0` (البند 6): حظر `env`/`declare -p`/`set` على متغيّرٍ حسّاس. **ن‑8**: لم يُنشأ `.gitattributes` ولم يُلتزَم — أُعيد قياس نطاق المشكلة (56 من 701 ملفّ متعقَّب تحمل CR اليوم، نموٌّ عن 51/507 بسبب ملفّات الخطوات 11‑13 غير المُلتزَمة بعد) وكُتب الإجراء الدقيق كاملاً (محتوى الملفّ المقترَح + تسلسل أوامرٍ لالتزامٍ منفرد) بانتظار نافذة التزامٍ مخصَّصة. بوابات: ruff **519 ملفّاً منسَّق** (بلا تغيير) · ruff check نظيف · mypy **351 ملفّاً بلا مشاكل** · lint-imports **8/0** · pytest **2404 ناجح · 0 فشل · 11 متخطًّى** — مطابقةٌ حرفيّة لخطّ أساس الخطوة 13، لا انحدار؛ `git diff --summary` فارغ)
- [§3.94](3.94.md) — **إغلاق ن‑10 و ن‑11 بعد عُطلٍ حيّ.** بدأت تدويراً مُخطَّطاً لاعتمادَي القاعدة (بإذن المستخدم) فتبيّن أنّ المكدّس أُعيد تشغيله و`app` **لا يُقلع** — ن‑10 وقعت حرفيّاً. **ن‑11**: التدوير نُفِّذ في المواضع الثلاثة (Postgres · `.env` · Vault KV `secret/db`)، بلا طباعة قيمةٍ واحدة (القيم عبر stdin؛ `log_statement=none` مقيسٌ قبلها فلا يُسجَّل `ALTER ROLE`؛ `password_encryption=scram-sha-256`؛ النسخة الاحتياطيّة `shred`ت). PgBouncer لم يحتج شيئاً (`AUTH_QUERY` على `pg_shadow`). **ن‑10** حُسمت بـ(ب): `secret_id_ttl` `24h`→`720h` في `deploy/vault/bootstrap.sh` (لا في الدور الحيّ وحده) + تثبيت `VAULT_ROLE_ID`/`VAULT_SECRET_ID` في `.env` فلا تقتل إعادةُ تشغيلٍ عاديّة التطبيق. 🔴 **طبقةٌ جديدة لم تكن في التحليل**: حلقة انهيار `gunicorn` بالسرّ المنقضي تُشعل **قفل مستخدم Vault** (`login attempts exceeded`) فيُرفض السرّ الجديد الصحيح بـ`403 permission denied` **فارغة** لا `400`، ويبدو العلاج الصحيح فاشلاً؛ الترتيب المُلزِم `stop app` → فكّ القفل → سكّ → `up -d --force-recreate`. وفخٌّ ثانٍ: `docker compose start` جرّ `vault-bootstrap` بائتاً فأعاد بذر `secret/db` بالسرّ القديم و**محا التدوير بلا إنذار**. تحقّق: `app` `healthy` في 3 ثوانٍ · صفر خطأ Vault · `/health`+`/health/ready`+HTTPS 200 · `app_rw` يُصادَق بالكلمة الجديدة عبر PgBouncer · `migrate` خرج بـ0. سُجِّل خارج النطاق: صورة `migrate` بائتة (ن‑4 حيّة) والأدوار الثلاثة ما تزال غائبة عن العنقود (ن‑9 حيّة)
- [§3.95](3.95.md) — **ن‑10، النصف الثاني: الكشف — مقياسٌ ثالثٌ على `/metrics` وإنذارٌ ثالث** — 🔵 بانتظار مراجعة المستخدم (‏[§3.94](3.94.md) أغلق **التخفيف**؛ هذه تُغلق **الكشف**. `aizzak_vault_authenticated` (`1`/`0`) + `AizzakVaultAuthFailing` (`< 1` · `5m` · **`critical`**). منفذ `VaultHealth` **كُلّيّ لا يرفع أبداً** + `VaultProbe` فوق `SecretsProvider` نفسه. السبر قراءةُ KV لمسارٍ لا يُكتب فيه ⇒ `NotFoundError` = صحّة ⇒ **صفر تغييرٍ في Vault**. `is_authenticated()` ممنوع، وعمرُ `secret_id` مرفوضٌ بديلاً (`auth/*` + السرّ على السلك). ⚠️ ترتيب `except` حرجٌ لأنّ `NotFoundError` ابنةُ `AppError`. ⚠️ الكشطة نفسها تُشعل قفل المستخدم حين يموت السرّ — مقبولٌ وموثَّقٌ في ثلاثة مواضع: أوقف الكاشط مع `app`. `health.py` لم يُلمَس. بوابات: ruff 522 · mypy 353 · 8/0 · pytest 2416/0/11)
- [§3.96](3.96.md) — **أوّل تشغيلٍ أخضرَ لـCI في تاريخ المستودع: حُوكِيَ قبل أن يُدفَع، فكان أحمر** — 🔵 بانتظار مراجعة المستخدم (فحصٌ سبق الدفع لا خطوةَ خطّة. ⚠️ **تصحيحٌ مُسجَّل (§0)**: كُتبت المدخلة ورسالةُ `806a92c` على مقدّمةٍ موروثةٍ عن الخطوة 5 لم يجرِ التحقّق منها — «`gh run list` يبلّغ صفرَ تشغيلات». **خاطئة**: خمسةُ تشغيلاتٍ (07‑27 ×1 · 07‑28 ×4) **كلّها فاشلة**؛ فالجملة كانت صادقةً يوم كُتبت وكفّت عن الصدق ساعةَ نجح إصلاحُ `branches: [master]` نفسه. وآخرها (`06e40e4`) مات عند **البوّابة الأولى** `ruff format --check`، فلم تعمل `ruff check` ولا mypy ولا import-linter ولا pytest ولا مرّةً — أي أنّ انجراف `ruff` **كان يُسقط CI فعليّاً منذ ثلاثة أيّام** لا خطراً مستقبليّاً، وأنّ عطب `fitz` أدناه كان مختبئاً خلفه: **ثالثٌ خلف ثانٍ خلف أوّل**. صُحِّحت المواضع الخمسة والنصّ الأصليّ محفوظٌ سجلّاً. فبقي إصلاحُ الخطوة 5 فرضيّةً عمليّاً؛ وحوكي التشغيل بـ`git archive HEAD` + بيئةٍ جديدة + سطر التثبيت نفسه ⇒ **ثلاث بوّاباتٍ من خمسٍ حمراء**. **(1) الأخطر، ولا علاقة له بالإصدارات**: `ci.yml` يثبّت `.[dev]` بينما `test_knowledge_parsers.py` يستورد `fitz` و`pymupdf` في مجموعة `parsers` ⇒ `ModuleNotFoundError` في **الجمع** لا فشلَ اختبار: `exit 2`، صفر اختبارٍ يُنفَّذ — عطبٌ قائمٌ منذ يوم كتابة الملفّ، ستره عطبُ `branches: [main]` نفسه؛ **ثالثُ عطبٍ في العائلة ذاتها**. **(2)+(3) انجرافُ أدواتٍ لا قفلَ يمنعه** (‏`git ls-files` لا يجد lock ولا constraints): `ruff 0.16.1` في CI مقابل `0.15.21` محلّيّاً ⇒ `PLR0917` خرجت من الوضع التجريبيّ فالتقطها `select=["PL"]` بلا قرار (**7 أخطاء**، كلّها من نوع ما يُعفيه `PLR0913` عمداً)، و`ruff format` صار ينسّق كتل بايثون **داخل Markdown** (**6 ملفّات، كلّها `.md`** ولا ملفّ بايثون واحد، ثلاثةٌ منها عقودٌ مُلزِمة تحت `docs/design/`). `mypy` نجا بالصدفة، و`pytest 9.1.1` ليس انجرافاً (البيئة المحلّيّة عليه). **الحزمة نفسها سليمة**: بـ`.[dev,parsers]` ⇒ 2398/0/29، مجموعها 2427 = 2416+11 بالتطابق. **العلاجات**: `.[dev,parsers]` في `ci.yml` (تثبيتٌ لا `importorskip` — اختبارُ وحدةٍ يتخطّى نفسه إلى الأخضر هو النمط الذي أُعيدت كتابة الانسياب لإيقافه) · `PLR0917` إلى `ignore` بجانب `PLR0913` · `extend-exclude = ["*.md"]` ⚠️ **لا `exclude`** التي تستبدل الاستثناءات الافتراضيّة فيزحف المنسّق إلى `.venv` (**3471 ملفّاً**، مقيس) · حدودٌ عليا لأدوات البوّابات الخمس وحدها مع رفع الحدود الدنيا إلى ما جُرِّب فعلاً — لا يُلغي الفجوة بل **يحصرها في مدًى قِيس طرفاه** · وتصحيح نصّ `pyproject.toml` «resolved in the lockfile» — **ملفّ قفلٍ لا وجود له**، وهو جذر المفاجأة كلّها. **حارسٌ جديد** `test_the_workflow_installs_every_extra_the_tests_import_from` يستخرج الإضافات من `pip install` ويمسح استيرادات `tests/` بـ`ast` ويربطها بتوزيعاتها عبر `importlib.metadata`، مع استثناءٍ **متعمَّد** للتوزيعات العابرة (`starlette`/`cryptography`) وحدٍّ صادقٍ مكتوبٍ في نصّه. **التحقّق**: أُعيدت المحاكاة على الشجرة المُصلَحة — سطرُ التثبيت **يُقرأ من `ci.yml`** لا يُعاد كتابته، والأرشيف من `git stash create` لا `HEAD` لأنّ الإصلاح غير مُلتزَم (وإلّا «أكّدت» المحاكاة احمراراً زائفاً) ⇒ الخمس خضراء: 522 · نظيف · 353 · 8/0 · **2399/0/29 `exit=0`**؛ ومحلّيّاً **2417/0/11** = 2416+1 (الحارس الجديد)، والمجموعان متطابقان **2428**. أُصلح في الطريق ركامُ §3.95: «مقياسان» في `pyproject.toml` و`07-nfr-slo.md §7` صارا **ثلاثة**. ✅ **والحكم من العدّاء نفسه**: دُفع `806a92c` وشُغِّل — **خمسُ بوّاباتٍ خضراء في 1m59s، أوّل نجاحٍ لهذا الانسياب على الإطلاق**: 522 · نظيف · 353 · 8/0 · **2166 ناجح · 262 متخطًّى** ⇒ **2166 + 262 = 2428**، الرقم نفسه في البيئات الثلاث؛ والـ262 هي حزم `live_*` تتخطّى بصدقٍ بلا خدمات — أوّل قياسٍ لوعدٍ كان يُقرأ فقط. ولم تُلمَس حاويةٌ ولا قاعدةٌ ولا Vault، ولم تُحدَّث `.venv`. **ن‑9** تبقى الملاحظةَ الوحيدةَ المفتوحة)
- [§3.97](3.97.md) — **ن‑9: أوّل ترقيةٍ لعنقودٍ قائم، والإجراءُ المكتوب لها كان فيه ستّةُ أخطاء** — 🔵 بانتظار مراجعة المستخدم (نافذةُ نشرٍ حقيقيّةٌ بإذنٍ صريح، لا خطوةَ خطّة. **أُغلقت آخرُ ملاحظةٍ مفتوحة في `p1-hardening-plan.md §5‑ب` ⇒ 11/11**. `10-roles.sh` يعمل مرّةً واحدةً عند تهيئة الحجم الأوّل، فعنقودٌ أُنشئ قبل الخطوات 8/10/12 يبقى بلا `retention_sweeper`/`metrics_reader`/`transit_rotator` إلى الأبد. **ما نُفِّذ:** سرٌّ حقيقيٌّ (32) لـ`TRANSIT_ROTATOR_PASSWORD` في `.env` · الأدوار الثلاثة بـSQL **مشتقٍّ من `10-roles.sh` لا من الدليل**، بصلاحيّة المستخدم الخارق (‏`aizzak_owner` بلا `CREATEROLE` عمداً) والقيمُ عبر **stdin** لا `argv` و`log_statement=none` مُعادُ فحصُه · `docker compose build migrate` · `docker compose up --no-deps migrate` ⇒ **`exit 0`**. الأدوار **4 → 7** · الرؤوس `public 0002→0003_retention_sweep`، `credentials 0001→0002_rotator_credentials`، `integrations 0001→0002_rotator_integrations` · **8** سياسات RLS · فهرسان · الصورة `ac0abe6e → 09d761ea`. ⭐ **لكنّ قيمة المدخلة في الأخطاء الستّة التي كشفها التنفيذ في الإجراء المكتوب — المراجَعِ، المشتقِّ من الملفّ الصحيح:** **(أ)** تحذيرُ Vault **بائت** — منحة `transit/rewrap` وصلت السياسة الحيّة سلفاً عبر علاج ن‑11، فاتّباعُه كان سيُدخل إعادةَ تشغيل `vault-bootstrap` (‏وهي نفسها ما محا تدوير ن‑11 بصمتٍ من قبل) **بلا حاجة**؛ **(ب)** «يفحص ستّة أدوار» — يفحص **خمسة**؛ **(ج)** `up migrate` بصورةٍ متأخّرةٍ ثمانيةَ التزامات يشغّل `provision` البائت فيخرج بنجاحٍ وقد فعل لا شيء — **ن‑4 مُجسَّدةً**؛ **(د)** 🔴 والأخطر: `TRANSIT_ROTATOR_PASSWORD` عنصرٌ في بيئة خدمة `postgres`، فتعديلُه في `.env` — وهو الخطوةُ الأولى في الإجراء نفسه — يغيّر بصمتها و`up migrate` يبدأ التبعيّات ⇒ **يُعاد إنشاء قاعدة البيانات** بينما يظنّ المشغّل أنّه يشغّل هجرة (`--no-deps`)؛ **(هـ)** السرُّ كان `change-me-*` في `.env` الحيّة رغم توصيل الخطوة 12 للمتغيّر في مواضعه الخمسة — **ولا حارسَ يكشفه** لأنّ `.env` غير متعقَّبة؛ **(و)** وخطأٌ للمُنسِّق: «ستّ سياسات» والصحيح **ثمانٍ**. **براهين تتجاوز العدّ:** الحصرُ بالعمود يعمل حيّاً (`transit_rotator` يُحدِّث `ciphertext_ref` **ولا** يُحدِّث `status`) · `metrics_reader` يسجّل دخولاً **عبر PgBouncer** · `app_rw` لم يُمَسّ (16 سياسة `tenant_isolation`، و`INSERT` وحده على `platform.outbox`) · الحاويات لم تُعَد و`/health` = 200. ⚠️ **وما لم يُثبَت مسجَّل**: الجداول فارغة، فصفرُ الصفوف لا يُثبت حصر RLS — يُثبته فحصُ المنح، والبرهانُ الحقيقيّ في اختبارات `aizzak_test`. **باقٍ نافذةً ثانية**: إعادةُ إنشاء `app`/`outbox-relay` على الصورة الجديدة. **الدرس:** ما لم يُنفَّذ على شيءٍ حيٍّ لم يُتحقَّق منه — ثالثةُ «ما لم يُشغَّل لم يُنجَز» (§3.76) و«ما لم يدخل الالتزام لم يُنجَز» (ن‑6).)
- [§3.98](3.98.md) — `deferred-adapters-plan.md` الخطوة 15: **ربط MinIO مشترَك + مصنعٌ لا‑متزامن لعامل `knowledge`** — 🔵 بانتظار مراجعة المستخدم (خطوةٌ ملفّاتٍ فقط. مفاتيح MinIO قراءةُ سرٍّ **لا‑متزامنة** من Vault، و`build_knowledge_worker_from_env` كانت مصنعاً **متزامناً** ⇒ ترفع `AppError` بلا لمس التخزين؛ الحلّ قائمٌ سلفاً في طرف الـAPI منذ 6.1‑هـ‑1 ولم يُنقَل. **وحدتان مشترَكتان جديدتان** تحت `framework/di/`: `vault_binding.py::build_vault` (منطق `_build_vault` القديم **حرفيّاً**، خارج `composition_root.py` لأنّ استيراده من عاملٍ يجرّ رسم الوحدات/الوكلاء بالكامل) و`storage_binding.py::bind_minio` (جسم `connect_storage` القديم، حارس `is_bound` **داخل** الدالّة فيرثه كلا المستدعيَين ⇒ موضعٌ واحدٌ للتحقّق من شكل السرّ بدل نسختين حرّتين). `CompositionRoot.connect_storage` صار مناديّاً من سطرين. `.importlinter` عقدٌ 6: سطران موازيان لاستثناء `composition_root` القائم، بتعليقٍ يوضّح أنّهما **استخراجٌ لا ثغرة** (العقد نفسه يسمّي عمّال الوحدة جذورَ تركيبٍ لعمليّتها). حدثُ السجلّ `composition_root.storage_connected` **تقاعد** لصالح `storage_binding.minio_bound` — مُتحقَّقٌ بـ`grep` أنّه لا مرجع حيّاً آخر له. `build_knowledge_worker_from_env` صارت `async def`: تبني الآن `vault_client, secrets, _ = build_vault(settings)` ثمّ `storage = StorageHandle()` ثمّ `await bind_minio(...)` — نفس مسار الـAPI حرفيّاً. **البديل المرفوض، مسجَّلٌ في الـdocstring**: قائمة `startup=` على نمط `create_production_app` — عاملٌ واحدٌ لا يملك خطّافاً ثانياً يُرتَّب معه، فالقائمة مراسمُ بلا ثمن. `_close_vault` أُضيفت على سابقة `CompositionRoot.disposables()` — **غيرُ قابلةٍ للبلوغ بعد** لأنّ الرفع في نهاية الدالّة لا يزال غيرَ مشروط حتّى الخطوة 16، وكُتبت الآن بتعليقٍ صريح يشرح ذلك. **الرفع الصادق بقي**، يسمّي `DocumentContentResolver` **وحدها**، ولم تعد الرسالة (ولا docstring البروتوكول نفسه) تدّعي أنّ MinIO «‏needs an async secret read this synchronous factory cannot do» — ادّعاءٌ صار **كاذباً**؛ `extra` سجلّ `content_resolver_wiring_blocked` صار يذكر `storage` ضمن ما هو حقيقيّ. اختبارات: القائم صار `async` ويُموِّه `build_vault`/`bind_minio` بمزيَّفَين (بلا شبكة) ويثبت أنّ `bind_minio` استُدعيت فعلاً بـ`StorageHandle` الدالّة بنته؛ `tests/unit/test_storage_binding.py` **جديد** يثبت `bind_minio` مباشرةً (ربطٌ ناجح · `access_key`/`secret_key` فارغان ⇒ `ValidationError` · نداءٌ ثانٍ لا يقرأ السرّ مجدَّداً)؛ `test_composition_root.py` بلا تعديل. **برهان الفشل على القديم بآليّة العطب نفسها**: الدالّة القديمة متزامنة فـ`await` عليها يفشل بـ`TypeError` لا استيراد، و`bind_minio` غير موجودةٍ إطلاقاً فـ`monkeypatch.setattr` عليها يفشل بـ`AttributeError`. البوابات: ruff **525 ملفّاً منسَّق** · ruff check نظيف · mypy **355 ملفّاً بلا مشاكل** · lint-imports **8/0** · pytest **2445 عنصراً** (2412 ناجح · 10 فاشل · 11 عطب تجهيز/تفكيك · 12 متخطًّى) — الفشل كلّه في `tests/integration/test_minio_storage.py` (‏`InvalidAccessKeyId`/`403`، أساسٌ معروفٌ مسبقاً يخصّ مكدّس MinIO المحلّي لا هذه الخطوة، **مطابقٌ حرفيّاً** لما ورد في التكليف)؛ تشغيلةٌ منفصلة بلا ذلك الملفّ أعطت 2422 عنصراً كلّها ناجحٌ/متخطًّى ورمز خروجٍ `0`. ⚠️ ملاحظةٌ بيئيّةٌ صادقة: بيئة القياس تُسقط سطر الملخّص النهائيّ الواحد من كلّ تشغيلة عبر `wsl.exe`، فالعدد أعلاه مُحتسَبٌ من رموز شريط التقدّم لا مقروءٌ من سطرٍ لم يُطبَع. §6‑ج بقيت فارغة — لا ملاحظةَ خارج نطاقٍ ظهرت)
- [§3.99](3.99.md) — `containerised-test-harness-plan.md` الخطوة **أ‑2** + شريحةُ MinIO من **أ‑5**: **إصلاح حارس MinIO الحيّ — الحساب المقيَّد صار في المستودع لا في ذاكرة الجهاز** — 🔵 بانتظار مراجعة المستخدم (⚠️ **لمست حالةً حيّة**، بطلبٍ صريح: أُوقفت أربع حاويات وأُنشئ دلوٌ وحسابُ خدمة. أُنجزت خارج ترتيب خطّتها لأنّها كانت تحجب البوّابة الخامسة لـ`deferred-adapters-plan.md`. **ثلاث طبقاتٍ متراكبة، لا واحدة**: `minio.service` الأصليّة التي زُوِّد عليها `aizzak_test` **يدويّاً** في [§3.19](3.19.md) صارت `inactive` · وحاوية `alpha-infra-minio-1` من مكدَّسٍ قديم ورثت 9000 فردّت `InvalidAccessKeyId` · **والمجسّ كذب** — `_MINIO_PROBE_PORT` ثابتٌ **منفصلٌ** عن `_MINIO_ENDPOINT_DEFAULT`، فوجد 9000 مفتوحاً ولم يتخطَّ، ومضى بالحزمة كاملةً على **الخادم الخطأ**؛ ما دام الثابتان متطابقين لا يظهر شيء، ويوم انفصلا صار وعدُ «يتخطّى بأمانٍ حين لا يكون MinIO متاحاً» **غيرَ قابلٍ للوفاء بنيويّاً** لأنّ المجسّ يجيب عن سؤالٍ غير الذي تسأله الاختبارات. الجذر واحد: **حالةٌ زُوِّدت بيدٍ خارج المستودع** فكلّ إعادة إنشاءٍ للحاوية تُقلع بلا حساب. **العلاج بُني ليمنع التكرار لا ليُطفئ العَرَض**: نصفٌ ثانٍ في `deploy/minio/bootstrap.sh` ينشئ دلو `aizzak-test` + حساب خدمةٍ بسياسةٍ مقيَّدةٍ بالدلو **بالـARN** (‏`ListBucket`/`GetBucketLocation` على الدلو وأفعالُ الكائنات تحته، و**لا شيءَ على مستوى الخادم**)، **مطفأٌ افتراضيّاً** خلف `MINIO_TEST_ACCOUNT_ENABLED` لأنّ نشراً حقيقيّاً لا شأن له بسكّ اعتمادات اختبار، و**عديمُ الأثر عند التكرار بحقّ** (‏`svcacct add` يفشل على مفتاحٍ قائم ⇒ يُفحَص `svcacct info` أوّلاً ويُعدَّل بدل أن يُضاف، فتُقارِب إعادةُ التشغيل الحالةَ المقصودة بدل أن تُجهض الإقلاع) — و**شُغِّل السكربتُ نفسه** على الحاوية الحيّة عبر `docker compose run --rm --no-deps`، لا أوامرُ `mc` مكتوبةٌ لهذه الجلسة. و`conftest` صار **يشتقّ** المضيفَ والمنفذ من العنوان المستعمَل فعلاً (‏`_MINIO_PROBE_PORT` **حُذف**، و`rpartition(":")` على `TEST_MINIO_ENDPOINT`) فلا يمكن للطبقة 3 أن تعود **بالبناء لا بالانضباط**، والافتراضيّ صار `127.0.0.1:19000` حيث تنشر compose (‏9000 متروكٌ لخادمٍ أصليّ بقاعدة الإزاحة في رأس `docker-compose.yml`). بقيّةُ الخدمات في أ‑5 **لم تُمَسّ** وثوابتُها ما تزال مطابقةً لعناوينها. **إسقاط `alpha` بقرار المستخدم**: فُحص المُحتَلّ مقابل المُستعمَل أوّلاً — 9000/9090 هي العطب، و6432 **غير مذكورٍ في `conftest` إطلاقاً**، وpostgres/redis من ذلك المكدَّس بلا نشرٍ على المضيف أصلاً؛ وما يستعمله الحارس فعلاً (‏PG 5432 أصليّة · Redis 6379 عمليّةٌ مستقلّة · Qdrant وVault حاويتان منفصلتان) لم يُمَسّ. `stop` لا `rm` ⇒ العودة بأمرٍ واحد. **تحقُّقُ نصف قطر الانفجار حيًّا** بأليَس مؤقّتٍ أُزيل فوراً (سابقة §3.19): الحساب يرى `aizzak-test/` **وحده** وقراءةُ `workspace-files` تُردّ `Access Denied` — وهو ما يؤكّده `test_out_of_scope_bucket_is_denied_not_leaked` نفسه. السرّ `aizzak-test-secret`، الافتراض المودَع منذ §3.19 (قرار المستخدم) ⇒ صفرُ مادّةٍ سرّيّةٍ جديدة. البوّابات: ruff **525 ملفّاً منسَّق** · ruff check نظيف · mypy **355 ملفّاً بلا مشاكل** · lint-imports **8/0** · pytest **2432 ناجح · 0 فاشل · 11 متخطًّى** في 150.48ث `exit 0` — وحزمة MinIO **12/12**، العدد نفسه الذي سجّلته §3.19 يوم كُتبت؛ والمتخطّياتُ الإحدى عشرة كلّها صادقة (‏7 `live_openai` · 1 `live_web_search` · 2 اختبارَي حملٍ خلف `RUN_P1_6_LOAD_TEST` · 1 خدمةُ تضمينٍ غير منشورةٍ على 8080 = الخطوة أ‑3). ⚠️ **تصحيحٌ لِما سُجِّل في [§3.98](3.98.md)**: ادّعاءُ «بيئة القياس تُسقط سطر ملخّص pytest من كلّ تشغيلة» **تعميمٌ غير دقيق** — السطر يُطبع كاملاً بلا `-q`؛ فأرقام §3.98 تقديريّةٌ من رموز شريط التقدّم، وأرقامُ هذه المدخلة **مقروءةٌ من مخرَج pytest نفسه**، ونتيجةُ بوّابة §3.98 الخامسة صارت متجاوَزة إذ أُغلق أساسُها الأحمر هنا. **ما بقي:** الخطّة **2/16** (‏أ‑0/أ‑1/أ‑3/أ‑4 وبقيّة أ‑5 والمراحل ب/ج/د/هـ لم تبدأ) · حاويات `alpha-infra-*` موقوفةٌ **لا محذوفة** · و`.env` المحلّيّة **لم تُعدَّل** فإعادةُ إنشاء المكدَّس لن تُعيد سكّ حساب الاختبار تلقائيّاً حتّى تُضاف `MINIO_TEST_ACCOUNT_ENABLED=true` — تُركت للمستخدم لأنّها ملفُّه غيرُ المتعقَّب)
- [§3.100](3.100.md) — `deferred-adapters-plan.md` الخطوة 16: **`WorkerDocumentContentResolver` + ثغرة الملفّ المسموم** — 🔵 بانتظار مراجعة المستخدم (خطوةُ ملفّاتٍ فقط؛ الحالة الحيّة الملموسة هي **قواعد الاختبار وحدها**. **نصفان**: الدرزُ الفارغ في `build_knowledge_index_handler` — كلّ القطع موجودةٌ منذ 3.16/2.4/3.k1/2.10 ولم يكن ينقص إلّا **من يجمعها** — والفجوةُ بين `Limits.allowed_mime` (يسمح بـDOCX، يمنع `.xlsx`/`.json`/`.csv`) و`_ROUTES` (لا يوجّه `.docx`، يوجّه الثلاثة). **`workers/content_resolver.py` جديد**: ملفّ ← بايتات ← تحليل ← مسار تضمين، والعنوانُ من `storage_key` نفسه (INV‑F1) لا مُعادَ تركيبِه. **`asyncio.to_thread` إلزاميّة** (المنفذ متزامنٌ بالتصميم ويُلزم مستدعيه صراحةً؛ بدونها يتجمّد حلقةُ حدث العامل بأكملها على أوّل PDF) — ولها اختبارٌ يقارن **مُعرِّف الخيط** لا القيمة الراجعة، إذ اختبارُ المخرَج ينجح في الحالتين. الموضع `app.workers` مُتحقَّقٌ بـ**8/0 بلا تعديل عقد**. **`IndexRegisteredDocument.fail`** مدخلٌ ثالثٌ إلى `finalize` نفسها ⇒ **مسارٌ واحدٌ إلى الحالة النهائيّة لا اثنان**؛ ولا يكتب `indexing` إلى القاعدة (لا I/O باقٍ بعده، بخلاف `run`) بل ينقلها في الذاكرة لتلبية حافّة INV‑K2. ⭐ **النوعان اثنان لا التقاطٌ واسع**: `UnsupportedTypeError`/`ValidationError` حقيقتان نهائيّتان عن الملفّ ⇒ `failed` + حدث؛ وكلُّ ما عداهما (انقطاع MinIO/Vault/PG، و`NotFoundError` لصفٍّ اختفى) **يبقى يخرج** لإعادة التسليم، لأنّ وسمَه نهائيّاً يحرق وثيقةً على انقطاعٍ مدّتُه ثلاثون ثانية. `allowed_mime` صارت مرآةَ `_ROUTES`: DOCX أُسقط و`text/csv`/`application/json`/`spreadsheetml.sheet` أُضيفت. **`build_knowledge_worker_from_env` لم تعد ترفع** وتعيد خمسة `disposables` — الخامسُ `_close_vault` الذي كُتب في [§3.98](3.98.md) على دالّةٍ لم تكن تبلغه، وبلغه الآن **بلا إعادة عملٍ واحدة** كما قُصد. ⭐ **القرار الحامل**: العامل يبني **نفس** `SettingsProviderResolver` + `ResolveCredential` فوق **نفس** مدخلة `PROVIDER_ROUTING` بدل قراءة نموذجٍ من الإعدادات كما يفعل `memory` — لأنّ `memory` يكتب ويقرأ بإعدادات عمليّةٍ واحدة بينما `knowledge` يُفهرَس في العامل ويُستعلَم من `/search`، فطريقتان للحلّ تبعدان تعديلَ إعدادٍ واحدٍ عن الفهرسة في فضاءٍ متجهيٍّ والبحث في آخر **بلا خطأٍ واحد في سجلٍّ أو مقياس**. وفضاء `llm` وحده يُحذَف من الجدول المُمرَّر (العمليّة لا توصل محوّل LLM عمداً، و`_parse_routing` كان سيرفض البناء) — و`_embedding_routing` كُتبت **«احذف `llm`» لا «أبقِ `embedding`»** فيصل الفضاءُ المُصحَّف إلى التحليل الصارم فيُرفَض باسمه بدل أن تبتلعه قائمةُ إبقاء. **اختبارات**: `tests/unit/test_content_resolver.py` جديد (5، بالمحوّل الحقيقيّ لا مزيَّف) · +5 في `test_workers_bootstrap.py` (ثلاثةٌ للثغرة بالفرعين، وواحدٌ لـ`_embedding_routing`، واختبارُ المصنع أُعيد من «يرفع» إلى «**يبني**») · **+1 حيّ** `file.uploaded → registered → indexed` لملفّ `.txt` حقيقيّ فوق **PG + MinIO + Qdrant + Redis الحيّة** بالمُرحّل ومحرّك الاستهلاك والمحوّل الحقيقيّين (مقاطعٌ في `knowledge.chunks` تحمل النصّ المحلَّل فعلاً · `points_count` = عدد المقاطع · حدث `indexed.v1` · صفرٌ معلَّق)؛ المُزيَّف الوحيد `EmbeddingProvider` لأنّ خدمة التضمين deployable منفصلٌ لا يشغّله الحارس وليست ما بنته الخطوة، والخطّة سمّت PG+MinIO+Qdrant بالاسم. ⚠️ وفي القفزة الثانية يُعاد توجيه صفّ Outbox إلى مجرى الاختبار الفريد **قبل** أن يقرأه المُرحّل — فيبقى المُرحّل حقيقيّاً ولا يهبط شيءٌ على `stream.knowledge` الإنتاجيّ (R6). **برهانُ الفشل على القديم بآليّة العطب نفسها**: الثلاثةُ الخاصّة بالثغرة تفشل بالاستثناء نفسه **عابراً `await handler(...)`**، واختبارُ المصنع بـ`AppError` الرفعِ الملغى. ⚠️ **وما لا يمكن أن يُبرهَن هكذا مسجَّلٌ صراحةً**: `test_content_resolver.py` يفشل بـ`ModuleNotFoundError` — صنفٌ جديدٌ كلّيّاً (سابقة `bind_minio` حرفيّاً). البوّابات: ruff **527 ملفّاً منسَّق** · نظيف · mypy **356** · **8/0** · pytest **2442 ناجح · 0 فاشل · 12 متخطًّى** في 158.18ث (الأساس §3.99: 2432). ⚠️ **`bootstrap.py:79` و`:299` ما زالا يقولان إنّ المحوّل غيرُ موجود — مقصودٌ لا سهو**: الخطوة 17 هي كنسُ الادّعاءات الستّة في الكود والأربعة في الوثائق دفعةً واحدة، وقد سمّت الموضعين بالسطر. §6‑ج: **ثلاث ملاحظات** سُجّلت ولم تُنفَّذ — `NotFoundError` تبقى مسارَ إعادة تسليم · `_KEYLESS_PROVIDERS` صارت نسختين (انجرافُها يفشل بصوتٍ عالٍ لا صامتاً) · حالةُ الملفّ (`quarantined`) لا تُفحَص قبل الفهرسة، وأثرُها اليوم صفرٌ إذ لا مسارَ حيّاً يضعها)
- [§3.101](3.101.md) — `deferred-adapters-plan.md` الخطوة 17: **توثيق — عامل `knowledge` صار يُقلع** — 🔵 بانتظار مراجعة المستخدم (**تعديلُ نصٍّ فقط: ولا سطرَ منطقٍ واحد في `src/`**، ولا حالةَ حيّةً من أيّ نوع — ولا حتّى قواعد الاختبار، بخلاف [§3.100](3.100.md). المشكلة أنّ الادّعاء بالغياب **يُقرَأ كقرار**: مشغّلٌ يقرأ «‏`knowledge` يدخل حلقةَ انهيار» لن يجرّبه أبداً، ومطوّرٌ يقرأ «لا تركيبَ إرسالٍ بعد» سيبني ثانياً. **المسحُ وجد أكثر ممّا قدّرت الخطّة**: تسعةُ مواضعَ في الكود والنشر لا ستّة، و**إحدى عشرة وثيقةً لا أربع** — والمعيار `grep` لا عدّاد، فلم يكن الرقمُ المكتوب مسبقاً حاكماً. ⭐ **القرار الحامل: «موصولٌ» ليست «يقلع»** — أسهلُ شيءٍ كان أن يُكتب «العامل يعمل الآن» وهو **غير صحيح**، فتُستبدَل كذبةٌ بكذبةٍ معاكسة: ما ثبت أنّ المصنع لم يعد يرفع وأنّ المعالج مُثبَتٌ حيّاً فوق PG+MinIO+Qdrant+Redis، وما لم يثبت أنّ العمليّة تُقلع في حاويةٍ مقابل Vault وخدمة تضمينٍ حقيقيَّين — والصيغةُ المعتمدة في كلّ موضعٍ لُمس هي **«موصولٌ بالكامل، لم يُقلَع بعد»**. **ونتيجتُه المباشرة: `WORKER=memory` لم يتغيّر ولا يجوز أن يتغيّر هنا** — `memory` ليس افتراضيّاً لأنّ `knowledge` كان محجوباً بل لأنّه الوحيد الذي **قِيس إقلاعه حاويّاً** ([§3.83](3.83.md))، والافتراضيّ وعدٌ لمن لم يكتب شيئاً فيجب أن يشير إلى ما خلفه دليل؛ تغييرُه **يكسبه أوّلُ إقلاعٍ ناجح** لا تعليقٌ يفترضه. تعليق `docker-compose.yml` **أُعيدت كتابتُه لا حُذف** كما تفرض الخطة، وصار يُبرِّر الافتراضيّ بدل أن يذكره. **وقاعدتان مفصولتان للوثائق**: وثائقُ الحالة الجارية أُعيدت كتابتُها إلى حقيقة اليوم؛ والسجلّاتُ المؤرَّخة والخططُ المُغلَقة **صِينَ نصُّها** وأُضيف إليه مؤشّرٌ `~~…~~ ✅ أُغلق` — إذ إعادةُ كتابة مدخلةٍ مؤرَّخة تزوّرُ السجلّ وتركُها عاريةً يُبقي الكذبة، والمؤشّرُ يفعل الاثنين (سابقة «🐛 تصحيحٌ من مراجعة الخطوة 5»). **البرهان بآليّة معيار الإنجاز نفسه** إذ العطبُ نثرٌ لا يرفع استثناءً: `git archive HEAD` ثمّ `grep` مؤتمَتٌ على الشجرتين ⇒ **24 ادّعاءَ غيابٍ قبل، 5 بعد** — والخمسةُ مفحوصةٌ واحداً واحداً ولا واحدَ منها حيّ (صيغةُ ماضٍ · نصٌّ مُصانٌ فوقه مؤشّرُه · سطرُ نطاق الخطّة · فقرتان مؤرَّختان)، والصفرُ **ليس مطلوباً** لأنّ بلوغه يعني حذف التاريخ. ⭐ **ولم يُضَف حارسٌ نصّيّ عمداً** — امتداداً لرفضِ مراجعة الخطوة 6 في `release-blockers-plan.md` حارساً يقارن نثراً بنثرٍ («يصير مصدرَ حقيقةٍ ثالثاً ينجرف، والمسرحيُّ أسوأ من لا شيء»): تُعاد صياغةُ جملةٍ فيسكت، وتُكتَب صادقةٌ فيصرخ؛ وما يستحقّ الحراسة محروسٌ سلفاً بآليّةٍ حقيقيّة — `test_deploy_worker_default.py` يقارن **قيمةً** عبر ثلاثة مصادر لا وصفاً، و`_ONLY_WORKER_THAT_BOOTS_TODAY` أُعيدت تسميتُه `_EXPECTED_WORKER_DEFAULT` **بلا تغيير قيمةٍ ولا تأكيد**. البوّابات: ruff **527** · نظيف · mypy **356** · **8/0** · pytest **2443 ناجح · 0 فاشل · 11 متخطًّى** في 217.10ث. ⚠️ **الفرقُ عن §3.100 بيئةٌ لا كود**: المجموع 2454 كما هو، والمتخطَّى الثاني عشر هناك (`lint-imports` غيرُ مرئيّ) رُئي هنا فمرّ بعد `source .venv/bin/activate` — وهذه الخطوة لم تلمس سطرَ منطقٍ أصلاً. §6: **`api/v1/routers/knowledge.py` ما يزال يقول إنّ `POST /search` يجيب 503 لغياب محوّل `EmbeddingProvider`** — كذبةٌ من الصنف نفسه لكنّها تخصّ دَيناً آخر أُغلق في [§3.77](3.77.md)، والنطاقُ مسمّىً)
- [§3.102](3.102.md) — `deferred-adapters-plan.md` الخطوة 18: **توسيع `ProviderResolver` بفضاء `image`** — 🔵 بانتظار مراجعة المستخدم (أوّلُ خطوات المسار (ب)، ملفّاتٌ فقط. المنظومة كانت **لا تعرف توجيهَ صورةٍ إطلاقاً**: الفضاءات `("llm","embedding")` وحدها، بينما `ports/image_provider.py` كاملٌ منذ المرحلة 2 و`max_image_dim` قائم — فالغيابُ فجوةٌ لا قرار. **القرار: الفضاء قبل المحوّل** (‏`external_image.py` ما يزال صفرَ بايت حتّى الخطوة 19) لأنّ الترتيب **مجّانيّ**: قاعدةُ «مزوّدٌ بلا محوّلٍ موصول ⇒ ارفض الإقلاع» (2.9) ليست واعيةً بالفضاء، وخريطةُ الصور فارغةٌ في جذرَي الإنتاج كليهما ⇒ توجيهُ صورةٍ **يُرفَض قبلُ وبعدُ**، وما تغيّر هو **دقّةُ السبب** لا النتيجة: «لا أفهمك» ← «أفهمك ولا أستطيع بعد». والرفضُ نفسه هو الصواب لا مرحلةٌ مؤقّتة — عمليّةٌ بلا محوّل صورةٍ يجب أن تموت عند الإقلاع لا أن تُجيب 500 على أوّل طلب. ⚠️ **وأهمُّ ما في الخطوة لم يكن في نصّها**: الفضاءُ الجديد كان **سيكسر عامل `knowledge`** — `_embedding_routing` تحذف `llm` وحده، فتوجيهُ صورةٍ شرعيٌّ يكتبه المشغّل كان سيعبر التضييق إلى عاملٍ يمرّر `image_providers={}` فيموت بسبب قدرةٍ لا يدّعيها أصلاً؛ انضمّ `image` إلى `llm` في `_NAMESPACES_FOREIGN_TO_THIS_WORKER` بالحجّة نفسها، مع بقاء «الاسمُ المجهول يصل إلى المحلِّل» (حذفٌ بقائمة معروفٍ لا بقائمة إبقاء)، والاختبارُ يبني **المحلِّل الحقيقيّ** مرّتين على الجدول نفسه: يُقلع بعد التضييق ويرفض بدونه. `video` بقي خارجاً — و**غيابُه صار مُختبَراً** (`video-is-still-not-a-namespace`) كي لا يلتبس المقصودُ بالمنسيّ. `image_providers` **مُلزِمةٌ لا افتراضيّة `{}`**: الافتراضيّ يجعل جذراً نسي التمرير يلوم **إعدادَ المشغّل** على خطأ توصيلٍ عندنا (حجّةُ `keyless_providers` نفسها، 2.9). و`_parse_routing` صارت تعيد `_Routes` مسمّاةً بدل ثلاثيّةٍ بالنوع نفسه، واختبارُ «الفضاءات الثلاثة لا تستعير محوّلات بعضها» يوصل الاسم `openai` بثلاثة كائناتٍ مختلفة فيحمرّ وحده عند التبديل. البرهان بآليّة العطب: `HEAD` يرفض توجيهاً **صحيحاً** بـ`unknown namespace 'image'` — لا `ImportError` ولا خطأ جمع. البوّابات: ruff **527** · نظيف · mypy **356** · **8/0** · pytest **2455 ناجح · 0 فاشل · 11 متخطًّى** = **+12 بالتطابق**. §6: فصلُ `ProviderResolver` إلى وجوهٍ ضيّقة (أكثرُ مستهلكيه يحتاج دالّةً واحدة)، و`max_image_dim`/`max_video_seconds` ما زالا سقفَين بلا نافذ — على الخطوة 19 أن تصلهما)
- [§3.103](3.103.md) — `deferred-adapters-plan.md` الخطوة 19: **محوّل `ImageProvider` (OpenAI Images) + `MediaGenerator`** — 🔵 بانتظار مراجعة المستخدم (ملفّان بصفر بايت ودرزٌ فارغ منذ المرحلة 3 صاروا مساراً عاملاً. 🐛 **وأهمُّ ما في الخطوة تصحيحٌ لقرارٍ سجّلته الخطّة نفسها**: اسمُ المزوّد `image:openai` لا `openai-image` — `ProviderRef` يقبل مزوّداً أساسيّاً أو مفتاح `<modality>:<name>` (06 §3)، فالاسمُ الموصولُ بشرطة كان **يُحلَّل ويُوصَل ويُقلِع سليماً** ثمّ يُفشِل كلَّ توليدٍ حقيقيّ بـ`credentials.provider_unknown`/422: عطبٌ يعبر البوّابات الخمس كلَّها ولا يظهر إلّا عند مستخدم؛ وصار الاسمُ مُثبَّتاً عبر `ProviderRef` الحقيقيّ لا عبر حرفٍ مكرَّر. ⚠️ **والملفُّ المولَّد يُخرَج عمداً من مسار `FileUploaded`**: ذاك الحدثُ هو ما يوقظ عامل `knowledge`، فنشرُه يُدخل كلَّ صورةٍ يولّدها وكيلٌ قاعدةَ معرفة المساحة ثمّ يستشهد الوكيلُ بما اختلقه هو — حلقةُ تلوّثِ سياقٍ لا يكشفها نظامُ استرجاعٍ من الداخل؛ ولها برهانان: بنيويٌّ (لا `EventOutbox` في المُنشئ إطلاقاً) وسلوكيٌّ (لا صفَّ `files.file.uploaded.v1` في Outbox الحيّ بينما صفُّ `media.job.generated.v1` موجود). وقراراتُ المحوّل: **PNG مُبرهَنٌ بالبايتات السحريّة لا مفترَض** (ولا `response_format` يُرسَل إطلاقاً)، والـ`url` البعيد **يُرفَض ولا يُجلَب** (حجّةُ الفيديو نفسها)، والـ400 يسمّي المقاس المشتقَّ من **طلبنا الخارج** بلا قائمةِ مقاساتٍ بيضاء تَبلى، و`extra` يُرفَض لا يُدمَج، والمهلة `media_timeout_s` (300ث) لا `llm_timeout_s` (60ث) ⇒ عميلٌ ثالثٌ منفصل. 🐛 وملاحظةُ §6‑ج رقم 7 كانت **خاطئة**: لـ`max_image_dim` نافذٌ منذ 3.12 (`RequestMedia._enforce_limits`)، فلا يُضاف مُحقِّقٌ ثانٍ ينجرف. البرهان بآليّة العطب: سطرُ `PROVIDER_ROUTING` الذي تشحنه هذه الخطوة **يوقف إقلاع الـAPI على `HEAD`** برسالة «مزوّدٌ بلا محوّلٍ موصول» — لا `ImportError` ولا خطأ جمع. البوّابات: ruff **530** · نظيف · mypy **357** · **8/0** · pytest **2484 ناجح · 0 فاشل · 21 متخطًّى** = 2455 **+39** −10، والعشرةُ تخطٍّ بيئيٌّ (`gemma3:1b` لم يعد مسحوباً محلّيّاً) تحقّق على شجرة `HEAD` أيضاً)
- [§3.104](3.104.md) — `deferred-adapters-plan.md` الخطوة 20: **`build_media_worker_from_env` + توثيق الإغلاق** — 🔵 بانتظار مراجعة المستخدم (**خاتمة الخطة — 6/6**، ملفّاتٌ فقط). الخطوة 19 بنت المحوّل وأثبتته حيّاً، **ثمّ لم يستطع أحدٌ تشغيله كعمليّة**: `build_media_worker_from_env` تبني كلّ شيءٍ حقيقيّ ثمّ **ترفع**. صارت `async` ولم تعد ترفع: **نفس** `build_vault`/`bind_minio` اللتين يبني بهما جذرُ التركيب وعاملُ المعرفة (شكلُ سرّ MinIO يُتحقّق منه في موضعٍ واحد لا ثلاثة) + `SettingsProviderResolver` على فضاء `image` وحده + `OpenAIImage` بمهلة `media_timeout_s` + `WorkerMediaGenerator` فوق `RegisterUpload`/`CompleteUpload`/`storage`. 🐛 **التضييق كان ثابتاً واحداً اسمُه `_NAMESPACES_FOREIGN_TO_THIS_WORKER`** — ولا يصحّ مع عاملين يُضيّقان **متعاكسين** (`knowledge` يُبقي `embedding` ويُسقط `image`، و`media` العكس تماماً)، والنسخُ هو ما حذّرت منه ملاحظةُ الخطّة رقم 2 نفسُها ⊙ `_routing_for(..., foreign=)` + جدولان منطوقان، واختبارٌ يؤكّد تعاكسَهما (عكسُهما سهواً **لا يفشل بصوتٍ عال**: عاملٌ يُقلع بلا توجيهٍ للنداء الوحيد الذي يقوم به). ⭐ **قاعدةُ الفشل الصادق تُسجّل ولا تُحذف** — زال موضوعُها لا فائدتُها، فبقيت منصوصةً مع جدول الدروز الثلاثة وتواريخ إغلاقها (2.10 · 15‑16 · 19‑20). ⭐ **اختبارٌ ثانٍ لأنّ الأوّل لا يرى الفرق**: المجاري و`disposables` كانت ستبدو متطابقةً لو سُلّم المصنعُ زائفاً، فيُلتقط المولّد عند موضع التوصيل ويُؤكّد أنّه `WorkerMediaGenerator` الإنتاجيّ. الدرز في `media/ports/generation.py` **يبقى** (عقد `modules-independent` يمنع المولّد من السكن داخل `app.modules.*`)، و🐛 تنبّؤُ docstring‑ه بـ«جذر التركيب» صُحّح (الموضع جذرُ تركيب **العامل**). وملاحظة §6‑ج رقم 4 أُغلقت بإذنها المكتوب (503 في موجّه `knowledge`). البرهان بآليّة العطب: المصنع **REFUSED** قبلها و**BUILT** بعدها، والعمّال الثلاثة يُبنَون من البيئة فعلاً. ⚠️ **موصولٌ ليس مُقلعاً** — لا `knowledge` ولا `media` أُقلِع حاويّاً، والافتراضيّ `WORKER=memory` باقٍ لـ**دليل** لا لحجب. كنسةُ النصّ: 4 ملفّات نشر + 9 وثائق. البوّابات: **530 · نظيف · 357 · 8/0 · 2486 ناجح · 21 متخطًّى** (2484 +3 −1). ملاحظتان جديدتان في §6‑ج: **لا سقفَ استهلاكٍ على مسار الوسائط** (أوّل عاملٍ يُنفق مالاً لكلّ حدث) · **جدولٌ بلا مدخلة `image` يُقلع بنجاح ثمّ يُفشل كلّ مهمّة**)
- [§3.105](3.105.md) — `stream-topology-plan.md` الخطوة 1: **مهلة صمتٍ تناسب القراءة الحاجزة** — 🔵 بانتظار مراجعة المستخدم (**سادّة**، تسبق بقيّة الخطّة كلّها). قياسٌ حيٌّ سابق وجد `knowledge`/`memory` يقلعان ثمّ ينهاران بعد **خمس ثوانٍ** من الهدوء: `redis.exceptions.TimeoutError` ⇐ `AppError: event consume failed`، إذ `_SOCKET_TIMEOUT_S` (2.0ث) أقصر من `CONSUMER_BLOCK_MS` (5.0ث) على قراءةٍ حاجزة — عطبٌ **رباعيّ الموضع لا ثلاثيّ**: نفس الآليّة تقتل جسر الإشعارات داخل عمليّة الـAPI **بصمتٍ تامّ** (لا `restart` يعيده، ولا سطرَ خطأٍ يُطبَع). الإصلاح فصلَ طبعَي انتظار: `_CONNECT_TIMEOUT_S` **لم يُمَسّ لأحد**، و`create_redis_client` يقبل `read_timeout_s` كلمة‑مفتاحيّة اختياريّة (افتراضيّها القديم بلا تغيير)، واشتقاقٌ واحدٌ مسمّى `blocking_read_timeout_s(block_ms)` — لا ثابتٌ ثانٍ ينحرف عند أوّل تعديل `.env`. العمّال الثلاثة يمرّرون المشتقّة؛ الـAPI صار يحمل **عميلَي** Redis: الأصليّ عند `2.0` لذاكرة التخزين وسجلّ الويب‑سوكِت، وثانٍ بالمهلة المشتقّة لجسر الإشعارات وحده (مسجَّلٌ في `disposables()` بنفس طريقة الأوّل)؛ وعميل المُرحّل (الخامس) يبقى `2.0` **باسمٍ صريح** — يستقصي بنومٍ قصير، لا يقرأ حاجزاً. ⭐ **البند 5 — رؤيةٌ لا إدارة**: نداءٌ راجعٌ على كلّ مهمّة `background=` يسجّل استثناء موتها، **مستثنياً `CancelledError` صراحةً** كي لا يصير الإيقاف النظيف ضجيجاً يُتعلَّم تجاهله؛ لا إعادة تشغيل ولا تراجعٌ أُسّيّ ولا إطفاءُ `ready` ولا أيّ تنبيه (§6‑د تبقى مفتوحة بلا استباق). البرهان الحيّ الفعليّ (Redis محلّيّ متاح، بلا حاوية): نفس القراءة الحاجزة على مجرًى هادئ **تسقط** بالعميل القديم و**تنجح** بالمشتقّة — آليّة العطب ذاتها، مُعادةُ الإنتاج لا مُستنتَجة. البوّابات: **532 · نظيف · 357 · 8/0 · 2472 ناجح · 51 متخطًّى** (2523 = 2507 +16؛ والقفزة في التخطّي بيئيّةٌ — MinIO/Qdrant/Vault/التضمين غير متاحةٍ هذه الجلسة، مُتحقَّقٌ منها بتشغيلٍ منفصل). صُحِّح صفّ `memory` في `quickstart.md` §0 («يقلع ويعمل» كان صحيحاً في نصفه). ✅ **والإثبات الحاويّ سُدِّد بإذنٍ صريحٍ في اليوم نفسه** (§5‑ج): `memory` صمد **138 ثانية** و`knowledge` **≥ 5 دقائق** بـ`RestartCount = 0` على مجارٍ هادئةٍ تماماً — و`knowledge` **لم يكن قد أُقلع حاويّاً ولا مرّةً في تاريخ المشروع**؛ وجسرُ الإشعارات قِيس **بالصورتين معاً** (صورةُ ما قبل الإصلاح حُفظت وسماً قبل البناء): المُصلَح قرأ الحدث وأقرّه بعد **6 دقائق** من الهدوء في كلتا عمليّتَي gunicorn، والقديم **`lag = 1` بلا قراءةٍ واحدة** وحاويتُه `healthy` وسجلُّه **بلا سطرِ خطأ** — و`idle` مستهلكِه **80 ثانيةً** بعد 82 ثانيةً من الإقلاع، أي مات في الثانية الثانية؛ والإيقاف النظيف ترك **صفر أسطر**. ⚠️ الوصلة الأخيرة — مقبس ويب‑سوكِت متّصلٌ فعلاً — لم تُقَس حاويّاً (تحتاج هويّة Firebase حقيقيّة)، والمقيس أنّ الجسر قرأ وعالج وأقرّ بعد الهدوء)
- [§3.106](3.106.md) — `stream-topology-plan.md` الخطوة 2: **جدول الطوبولوجيا الواحد** — 🔵 بانتظار مراجعة المستخدم (الأزواج الأربعة `(stream, group)` كانت مكتوبةً حرفيّاً في ثلاثة أماكن بلا رابطٍ بينها — `Subscription`ات العمّال الثلاثة في `bootstrap.py`، وأربعة ثوابت `STREAM`، وجدول `04-event-catalog.md` §2 — بلا اختبارٍ يقارنها. **ملفٌّ جديدٌ رابع، هو الوحيد المخصَّص ليُقرأ برمجيّاً**: `src/app/framework/events/topology.py` — `ConsumerBinding` + `STATIC_CONSUMER_TOPOLOGY` (بيانٌ نقيٌّ بلا I/O، طبقة الكِرنل). **الأسماء الأربعة حرفيّةٌ لا مشتقّة**: `.importlinter` يضع `app.framework` في أدنى طبقات عقد `layers`، فاستيرادُ `app.modules.*` لاشتقاقها خرقٌ مباشر؛ البديل حراسةٌ باختبارٍ لا استيرادٌ يشتقّ. **`tests/unit/test_stream_topology.py` جديد** (4 اختبارات): تطابق `stream` مع ثوابت `STREAM` الأربعة (مستوردةٌ من الاختبار وحده، فلا عقد مخروق) · تطابقٌ تامٌّ — لا زائدَ ولا ناقص — مع الأزواج التي تبنيها `build_knowledge_worker`/`build_media_worker`/`build_memory_worker` فوق كائنٍ زائفٍ خاملٍ واحد (الدوالّ الثلاث لا تستدعي أيّ اعتماد، فبديلٌ خاملٌ يكفي) · غياب `cg.notify.*` · **حارسُ الحارس** (سابقة 3.69): العدد أربعة بالضبط وكلّ حقلٍ غير فارغ. سَرْدُ الوحدة يشرح ثلاثةً: لماذا الأسماء حرفيّة ومن يحرسها · **لماذا `cg.notify.<host>.<pid>` غائبةٌ عمداً لا نقصاً** — أسرةُ مجموعاتٍ لكلّ عمليّةٍ تُنشأ عند إقلاع الـAPI وتُتلَف عند إغلاقه وتُكنَس عند اليُتم (`04-event-catalog.md` §2 وحاشيتها · `docs/log/3.81.md`)، فمجموعةٌ ساكنةٌ مسبقة الإنشاء خاطئةٌ بالتعريف · ومن يقرأ الجدول لاحقاً (المُرحّل، الخطوة 3). **البرهان بآليّة العطب نفسها**: تعديل حرفٍ واحدٍ في اسم مجموعةٍ أسقط اختباراً واحداً مسمّياً الزوج المنحرف بنصّه («`missing from the table`»/«`extra in the table`»)؛ وتعديل حرفٍ في اسم مجرًى أسقط **اختبارَين معاً** بالآليّة نفسها؛ وإعادة الحرف في الحالتين أعادت الأربعة إلى النجاح. البوّابات: ruff **534 ملفّاً منسَّق** (+2) · نظيف · mypy **358** (+1) · lint-imports **8/0** (410 ملفّاً · 1717 تبعيّة، `framework-kernel` من ضمن الثمانية المحفوظة) · pytest **2476 ناجح · 0 فاشل · 51 متخطًّى** (2527 = 2523 +4، مطابقٌ لعدّ `--collect-only` رقماً برقم). ⚠️ **مُخرَج pytest في هذه البيئة لا يطبع سطر الملخّص الختاميّ** — نفس ملاحظة §3.105، فالعدّ اعتمد أسطر شريط التقدّم مقارنةً بـ`--collect-only`)
- [§3.107](3.107.md) — `stream-topology-plan.md` الخطوة 3: **المُرحّل يضمن المجموعات قبل أوّل نشر** — 🔵 بانتظار مراجعة المستخدم (`build_relay_from_env` (‏`bootstrap.py`) يركّب `RedisStreamsConsumer` فوق **عميل Redis نفسه** الذي يأخذه `RedisStreamsPublisher` — لا اتّصالَ ثانٍ — ويعيد إغلاق `ensure_topology()` جديداً يمرّ على `STATIC_CONSUMER_TOPOLOGY` ([§3.106](3.106.md)) مستدعياً `ensure_group` للأزواج الأربعة؛ التوقيع صار `tuple[OutboxRelay, EnsureTopology, list[Disposable]]`. `EnsureTopology` عُرِّف **بجوار مستعمِله الوحيد في `bootstrap.py`**، لا في `framework/di/lifecycle.py` بجوار `Disposable` — ذاك انتقل هناك فقط حين احتاجه **جذرا تركيبٍ اثنان**؛ هنا جذرٌ واحد. **`outbox_relay.py::run`** يسلسل `await ensure_topology()` **قبل** `await relay.run_forever()` داخل نفس `try` القائم، فالهدم مضمونٌ حتّى لو فشلت التهيئة. **الاعتراض المتوقَّع محسومٌ بالتسمية** (§2 من الخطّة): ما يفعله منتِجٌ هنا ليس استهلاكاً بل **تهيئةَ طوبولوجيا** (`provision`)، من جنس ما يفعله `app.ops.provision` بالمخطّطات؛ والمُرحّل هو الموضع الوحيد الذي يغطّي Compose وRunPod معاً (نفس نقطة الدخول، §1‑هـ) بلا سطرٍ إضافيّ. ⚠️ **الاستثناء المسمّى صراحةً**: عميل المُرحّل يبقى `2.0` رغم أنّ ما يُركَّب فوقه اسمه «مستهلك» — `ensure_group` لا يحجز، والمُرحّل لا يقرأ حاجزاً في أيّ سطر؛ تعليقٌ صريحٌ في `bootstrap.py` يمنع «تصحيحاً» لاحقاً يعمّم المهلة المشتقّة. **تبعة توقيعٍ إلزاميّة**: موضعا تفكيكٍ في `tests/unit/test_redis_blocking_timeouts.py` صُحِّحا من ثنائيٍّ إلى ثلاثيّ **بلا لمس تأكيداتهما** — الشيء الوحيد المسموح لمسه من عمل الخطوتين 1/2. **`tests/unit/test_relay_topology_provisioning.py` جديد** (3 اختبارات، عبر `RedisStreamsConsumer` مُبدَّلٍ بجاسوسٍ و`build_relay_from_env()` حقيقيّاً — كلّ مصنعٍ كسول فلا اتّصال يُفتَح): **الترتيب** — أربعة `ensure_group` بالضبط (حارسُ الحارس) قبل أوّل `"publish"`، مقاسةٌ عبر `await outbox_relay.run()` الحقيقيّة لا عبر استدعاء الإغلاق مباشرةً؛ **الفشل يمنع الإقلاع** — `AppError` من `ensure_group` الثاني يصعد بلا ابتلاعٍ ولا `"publish"` واحدة؛ **لا اتّصال ثانٍ** — هويّة عميل الجاسوس `is` عميل `disposables` نفسه. **البرهان مُشغَّلٌ فعلاً**: حذف `await ensure_topology()` من `outbox_relay.py` أسقط الاختبارَين 1 و2 (`assert 0 == 4` / `DID NOT RAISE AppError`)؛ إعادة السطر أعادت الثلاثة إلى النجاح. البوّابات: ruff **535 ملفّاً منسَّق** (+1) · نظيف · mypy **358** (بلا تغيير) · lint-imports **8/0** · pytest **2479 ناجح · 0 فاشل · 51 متخطًّى** (2530 = 2527 +3، مطابقٌ لعدّ `--collect-only` ولعدّ أسطر شريط التقدّم بالنمط الصارم رقماً برقم). لم يُلمَس: `docker-compose.yml` (معيار القبول 8) · `redis_streams.py` (§1‑ب) · `topology.py`)
- [§3.108](3.108.md) — `stream-topology-plan.md` الخطوة 4: **الإثبات الحيّ على Redis حقيقيّ (`live_redis`)** — 🔵 بانتظار مراجعة المستخدم (`tests/integration/test_relay_ensures_groups_live.py` جديد، اختباران موسومان `live_redis`. ⚠️ **مسألة عنوان Redis قِيست لا افتُرضت**: `build_relay_from_env` يحلّ عميله عبر `load_settings()`/`REDIS_URL`، والمُقاس فعلاً `redis://redis:6379/0` (اسم مضيف Docker) بينما مِحضنة `live_redis` تُخرِج `redis://127.0.0.1:6379/0` — عنوانان مختلفان؛ فكلا الاختبارَين يبدأ بـ`monkeypatch.setenv("REDIS_URL", live_redis)` **قبل** أوّل نداءٍ لـ`build_relay_from_env()`، موثَّقاً في docstring الملفّ. **الاختبار الأوّل** (الإصلاح عبر الإغلاق الحقيقيّ): `STATIC_CONSUMER_TOPOLOGY` مُبدَّلةٌ عند `app.workers.bootstrap.STATIC_CONSUMER_TOPOLOGY` (حيث تُقرأ فعلاً، لا عند تعريفها) بزوجٍ واحدٍ جديد؛ `build_relay_from_env()` حقيقيّاً ← `await ensure_topology()` ← ثلاث `XADD` ← مستهلكٌ جديدٌ يقرأ الثلاث بحمولاتها حرفاً بحرف. **الاختبار الثاني** (العطب القديم بآليّته الحقيقيّة): نفس المجرى، لكن `XADD` ثمّ `ensure_topology()` ⇒ **صفر مدخلاتٍ بالضبط**، مع حارس حارسٍ `XLEN == 3` قبلها. **البرهان مُشغَّلٌ فعلاً**: `ensure_topology()` نُزع جسمها (`return` قبل الحلقة) في `bootstrap.py` (نسخةٌ احتياطيّةٌ محفوظة أوّلاً)، فسقط الاختباران معاً — ⚠️ **بآليّةٍ أقسى ممّا توقّعته الخطّة**: لا مجرّد «صفر مدخلاتٍ» بل `NOGROUP`/`AppError("event consume failed")` لأنّ المجموعة لم تُنشأ إطلاقاً؛ مُسجَّلٌ صراحةً بلا تلطيفٍ للنصّ. أُعيد الملفّ من النسخة الاحتياطيّة وتحقَّق `diff` صامتٌ من التطابق التامّ، وعاد الاختباران للنجاح. لا تغييرٌ دائمٌ في أيّ ملفّ إنتاجٍ. البوّابات: ruff **536 ملفّاً منسَّق** (+1) · نظيف · mypy **358** (بلا تغيير) · lint-imports **8/0** (410 ملفّاً · 1719 تبعيّة) · pytest **2471 ناجح · 0 فاشل · 61 متخطًّى** (2532 = 2530 +2، مطابقٌ لعدّ `--collect-only` ولعدّ أسطر شريط التقدّم الصارم رقماً برقم). ⚠️ **فرقٌ بيئيٌّ داخل الرقمين لا صلة له بهذه الخطوة**: عشرة اختبارات Ollama انزاحت من ناجحةٍ إلى متخطّاةٍ (`gemma3:1b` لم يعد مسحوباً محلّيّاً) — الاختباران الجديدان كلاهما **ناجحٌ فعلاً لا متخطًّى**)
- [§3.109](3.109.md) — `stream-topology-plan.md` الخطوة 5: **حذف القاعدة اليدويّة من سطح الوثائق** — 🔵 بانتظار مراجعة المستخدم (وثائقٌ وتعليقات شيفرةٍ فقط — **لا** يغيّر حالة النظام الحيّ، ولا سطرَ منطقٍ واحد تحت `src/`/`tests/`). القاعدة القديمة «العمّال قبل المُرحّل على مجرًى جديد» صارت **خاطئةً بوصفها تعليمات** بعد أن ضمنت الخطوات 2–4 المجموعات آليّاً قبل أوّل نشر (`ensure_topology()` في `outbox_relay.py`/`bootstrap.py`)؛ حُذفت من سطح الوثائق **الحيّ** واستُبدلت بذكر الضمان الجديد وموضعه، بدءاً بالمرجع المُلزِم `design/08-local-runbook.md §4` ثمّ `docker-compose.yml` (تعليقان فقط)، `quickstart.md`، `stack-commands.md` (العنوان صار «السبع» لا «الثماني»)، `deploy-linux-server.md`، `deploy-runpod.md` (قواعدها الذهبيّة حُذف منها البند الخاصّ بالترتيب وأُعيد الترقيم في كلّ ملفّ)، و`design/04-event-catalog.md §2` أُضيف إليه إشارةٌ إلى `topology.py` بوصفه التمثيل التنفيذيّ المحروس باختبار. **مسحٌ أوسع بالإنجليزيّة (`WORKERS BEFORE THE RELAY`/`boot-order`) وجد موضعاً لم تذكره الخطّة**: `deploy/runpod/bootstrap.sh:110` — عُدِّل **تعليقه فقط**؛ الأسطر التنفيذيّة الثلاث (`start worker` → `sleep 3` → `start outbox-relay`) بقيت **حرفيّاً بلا تغيير**. الحقيقة التي لا تتغيّر مُعلَنةٌ في كلّ موضع: المجموعات ما تزال تُنشأ عند `$` (‏`redis_streams.py:235`، لم يُمَسّ). **لا برهان فشلٍ سلوكيّاً هنا** — لا يوجد سطر منطقٍ لتحويره؛ البرهان السلوكيّ أُنتج فعلاً في [§3.107](3.107.md)/[§3.108](3.108.md) ولم يُعَد تشغيله. معيار القبول 9 (`grep -rn "العمّال قبل المُرحّل" docs/` لا يصيب إلّا `docs/log/`) **مستحيلٌ حرفيّاً** لأنّ عنوان الخطّة ونصّ المعيار نفسه يحملان العبارة — قِيس المسح قبل وبعد ونُقلا حرفيّاً في السجلّ، والإصابات الباقية خارج `docs/log/` كلّها نصٌّ عن عمليّة الحذف في الخطّة نفسها أو سجلّان تاريخيّان (`implementation-plan.md`/`implementation-status.md`) ممنوعان صراحةً بتحذير §5. `git diff --stat -- src/ tests/` فارغ · `git diff -- docker-compose.yml` أسطر `#` فقط · `git diff --numstat` على `INDEX.md`/`CHANGELOG.md`/`3.51.md`/`3.48.md`/`3.107.md` صفر حذفٍ. البوّابات: ruff **536** (بلا تغيير) · نظيف · mypy **358** (بلا تغيير) · lint-imports **8/0** (بلا تغيير) · pytest **2471 ناجح · 0 فاشل · 61 متخطًّى** (2532، بلا تغيير — لا اختبار أُضيف أو حُذف). ⚠️ **حادثةٌ صادقة أثناء القياس**: أوّل تشغيلةٍ كاملة أعطت فشلاً عابراً واحداً في اختبارٍ حيٍّ حسّاسٍ للتوقيت من الخطوة 1 (`test_blocking_read_timeout_live.py`)، لا صلة له بهذه الخطوة؛ نجح معزولاً فوراً ونجحت التشغيلة الكاملة التالية بصفر فشل — مُسجَّلةٌ كما وقعت لا مُخفاة)
- [§3.110](3.110.md) — `stream-topology-plan.md` الخطوة 6: **الإثبات الحاويّ ومدخلة السجلّ — خاتمة الخطّة 6/6** — 🔵 بانتظار مراجعة المستخدم (🔐 خطوةٌ مأذونةٌ صراحةً — **تغيّر حالة النظام الحيّ**، خلافاً لـ[§3.109](3.109.md): بُنيت الصورة وأُقلع المكدّس، وحُذف مفتاح `stream.memory` **وحده** ثلاث مرّات، وأُدخل صفّان اختباريّان في `platform.outbox` — **ولا سطرَ منطقٍ واحد تغيّر**، `git diff --stat -- src/ tests/ docker-compose.yml` **فارغ**). الخطوات 1–5 أصلحت الجذر وقاسته وحدةً وحيّاً داخل `pytest` ثمّ حذفت القاعدة من الوثائق — لكنّ الادّعاء المكتوب («العامل لم يعد يحتاج أن يسبق المُرحّل») لم يُقَس قطّ **بالخدمات الحاويّة التي يقرأ عنها المشغّل**. فنُفِّذ **بالضبط الترتيب المحظور قديماً**، على مجرًى جديدٍ حقّاً (`XLEN stream.memory = 0` قِيس قبل الحذف، فالحذف لا يُتلف مدخلةً بل يمحو تسجيل المجموعة). **ثلاثة قياساتٍ محوريّة، كلٌّ بنصّه الحرفيّ**: (1) بعد إقلاع المُرحّل وحده — `EXISTS = 1` · `XLEN = 0` · **`cg.memory` موجودةٌ بـ`consumers = 0`** و`docker compose ps` لا يحوي `worker` إطلاقاً ⇒ **المجموعة وُلدت من المُرحّل قبل أن ينشر، لا من العامل** — وهذا وحده انقلاب القاعدة؛ (2) بعد نشر حدثٍ واحد (`memory.item.stored.v1` حقيقيٌّ من `event_mapping.py:61`، مظروف CloudEvents على شكل `build_envelope` حقلاً بحقل، مُدخَلٌ يدويّاً بـ`psql` — وهذا يُقال صراحةً، فالمقيس مسار المُرحّل←Redis←العامل لا مسار إنتاج الحدث) — `XLEN = 1` · `published_at` مختومة · **`lag = 1` و`entries-read` فارغة** ولا مستهلكَ في الوجود؛ (3) بعد إقلاع العامل **أخيراً** — **`entries-read = 1` · `lag = 0` · `last-delivered-id` = مُعرِّف المدخلة نفسها** و`RestartCount = 0`. ⇒ **حدثٌ نُشر قبل أن يوجد العامل، وصل إليه.** ⚠️ **وما فعله العامل يُسمّى بلا تلطيف**: قرأ المدخلة، فشل **خمس مرّات** بـ`NotFoundError: memory item not found`، فأرسلها إلى `stream.memory.dlq` بسببٍ مقروء — والسبب صفُّ الاختبار اليتيم مرجعيّاً لا الطوبولوجيا؛ **إثبات وصولٍ كامل**، إذ لا يمكن رفضُ ما لم يُقرأ. ⭐ **والطور «ب» نجح كاملاً — برهانُ فشلٍ حاويٌّ لا موصوف**: على الصورة المحفوظة `aizzak/app:prefix` (‏2026‑08‑03 02:32، قبل إصلاحات 1–4)، المُرحّل القديم أقلع فمرّت 12 ثانيةً و**`XINFO GROUPS` تعود `ERR no such key`** (بينما المُصلَح أنشأ المجموعة خلال ثانية)، ثمّ نشر ⇒ `XLEN = 1` و**`XINFO GROUPS` فارغةٌ تماماً بلا `cg.memory` إطلاقاً**؛ وحين أُقلع العامل القديم **بعده** وُلدت المجموعة عند `$` فصار `last-delivered-id` = **المدخلة نفسها، مُتخطّاةً**، و**`entries-read` فارغة** و`lag = 0` مع `XLEN = 1` ⇒ **المدخلة في المجرى ولن تصل المستهلك أبداً**. وانهار العامل القديم **8 إعادات تشغيلٍ في ~60ث** بـ`AppError: event consume failed` من `redis_streams.py:338` (عطب §1‑أ‑1، تأكيدٌ جانبيٌّ لا مفاجأة) — **وبعد الثماني كلّها ظلّت `entries-read` فارغة**. **الطور «ج»**: الوسم أُعيد إلى `dev` والحاوية أُعيد إنشاؤها على صورة الرأس فأقلعت بصفر تتبّعات، ثمّ `docker compose down` **بلا `-v`** والأحجام الستّة كلّها قائمة والمكدّس مطفأٌ كما وُجد. `stream.files` (1) و`stream.knowledge` (2) **مطابقان لخطّ الأساس مجموعةً مجموعة**، و`stream.memory` عاد إلى حاله الابتدائيّ بالضبط (`XLEN 0` · `cg.memory` · `consumers 0` · `last-delivered-id 0-0`). ⚠️ **وفرقان يُبلَّغان لا يُخفيان**: `stream.media` اكتسب `cg.media` — **أثرٌ مقصودٌ للإصلاح نفسه** (§2 من الخطّة: تُنشأ الأزواج الأربعة كلّها ولو لم يُقلَع عاملها)، وقع عند **أوّل أمرٍ يفرضه التكليف** وأثرُه البيانيّ صفر (`XLEN` 0 قبل وبعد) ولا يمكن التراجع عنه بلا `XGROUP DESTROY` المحظور؛ و`stream.memory.dlq` مفتاحٌ جديدٌ بمدخلةٍ واحدة أنشأه العامل وحذفُه غير مأذون. ⚠️ **وحادثةٌ حقيقيّةٌ كشفتها الخطوة ولم تُصلحها**: `docker compose up -d --build outbox-relay` **لا يجرّ `pgbouncer`** (‏`depends_on` تحمل `redis` و`migrate` وحدهما بينما `DATABASE_URL` تشير إلى `pgbouncer:6432`) ⇒ المُرحّل يقلع في حلقة `socket.gaierror: Temporary failure in name resolution`؛ ثغرةٌ سابقةٌ لهذه الخطّة كلّها، **لم تُصلَح احتراماً لمعيار القبول 8** وسُجِّلت ملاحظةً في §6‑هـ. ومُشاهَدةٌ عارضة من الحادثة نفسها أقوى ممّا خُطِّط: المُرحّل المنهار على قاعدة البيانات **كان قد أنشأ `cg.memory` أصلاً**، لأنّ `ensure_topology()` تسبق `run_forever()` وتسبق أوّل لمسةٍ لصفّ outbox. البوّابات: ruff **536** · نظيف · mypy **358** · lint-imports **8/0** · pytest **2471 ناجح · 0 فاشل · 61 متخطًّى** (**2532**، بلا تغيير — لا اختبار أُضيف) — **مُتحقَّقٌ بثلاث طرقٍ متطابقة**: `--collect-only` (2532) · شريط التقدّم بالنمط الصارم (2471 نقطة + 61 `s`) · ⭐ **وسطر الملخّص الختاميّ طُبع فعلاً هذه المرّة** (`2471 passed, 61 skipped in 119.11s`) خلافاً لعادة هذه البيئة المسجّلة في §3.105/§3.106. لم يُمَسّ: `src/`/`tests/` · `docker-compose.yml` (فارغ حرفيّاً) · `redis_streams.py:235` (`id="$"` كما هو) · السجلّ التاريخيّ (صفر حذف) · `implementation-status.md`/`implementation-plan.md`)
- [§3.111](3.111.md) — **سدُّ ثغرة `depends_on`: `pgbouncer` صار تبعيّةً مُعلَنة لـ`worker` و`outbox-relay`** — 🔵 بانتظار مراجعة المستخدم (‏[`stream-topology-plan.md`](../stream-topology-plan.md) **§6‑هـ**، لا خطوةً من الستّ. **لا يغيّر حالة النظام الحيّ**: لا حاويةَ أُقلعت ولا صورةَ بُنيت — الإثبات كلّه `config` و`--dry-run`. الخدمتان تصلان قاعدة البيانات عبر `pgbouncer:6432` حصراً ولم تُعلناه في `depends_on`، فأمرا [`08 §4`](../design/08-local-runbook.md) نفساهما — `up -d worker` و`up -d outbox-relay` — كانا يُقلعان بلا مُجمِّع ⇒ `socket.gaierror` في حلقة، بينما `up -d` الكامل لا يُظهر شيئاً لأنّ المُجمِّع يقلع مع الجميع؛ ولهذا عاش العطب من الطور الأوّل. ⚠️ **وتصحيحٌ لِما قالته §6‑هـ**: الثقب **خدمتان لا واحدة** — `app` تُعلن المُجمِّع صراحةً لا «عرضاً»، و`worker` لا تجرّه إطلاقاً، **مقيساً** بمحاكاةٍ على الملفّ قبل الإصلاح حرفيّاً (`grep -c pgbouncer ⇒ 0`). العلاج: `pgbouncer: {condition: service_started}` منسوخةً من `app`، سطران لكلٍّ — و`service_started` لا `_healthy` لأنّ المُجمِّع لا يُعرِّف فحصَ صحّةٍ أصلاً؛ **رفعُه إلى `_healthy` تُرك عمداً** كقرارٍ مستقلٍّ يمسّ ثلاث خدمات (§7). **وشرطُ §6‑هـ على نفسها استُوفي**: مسار RunPod لا يعرف `pgbouncer` إطلاقاً — `127.0.0.1:5432` مباشرةً في `entrypoint.sh`/`bootstrap.sh` — فلا `supervisord` مسّه التغيير، بل أُلغي فارقٌ كان قائماً. البوّابات: ruff **536** · mypy **358** · lint-imports **8/0** · pytest **2532** بلا فاشل، والاختباران القارئان لـ`docker-compose.yml` شُغِّلا أوّلاً (**14 ناجحاً**). لم يُمَسّ: `src/`/`tests/` (‏`git diff` فارغ) · `deploy/` · `redis_streams.py:235`)
- [§3.112](3.112.md) — **ص‑1: خطّ الأساس لم يُعَد إنتاجه، فأُعيد قياس §1 كلّه** — أولى خطوات [`containerised-test-harness-plan-v2.md`](../containerised-test-harness-plan-v2.md) (المرحلة ص — القياس). **لا يغيّر حالة النظام الحيّ**: قياسٌ خالص، ولا سطرَ خارج `docs/`. المطلوب `2471 ناجح · 61 متخطًّى`، والمقيس **`2481 ناجح · 0 فاشل · 51 متخطًّى`** — فوجب بحكم نصّ الخطوة إعادةُ قياس §1 كلّه، وقد أُعيد: **§1‑ب و§1‑ج و§1‑د مطابقةٌ صفّاً بصفّ** (‏والمجموع 2532 ثابتٌ ⇒ لا اختبارَ أُضيف ولا حُذف)، **و§1‑أ وحدها تبدّلت**. الفارق مصدرُه واحد: `gemma3:1b` صار مسحوباً و`llama3.2:1b` اختفى — **ولم تسحبه الجلسة**، والحساب مغلق `61 − 10 − 1 + 1 = 51`. ⚠️ **وقد أثبت الفارقُ تحذيراً في الخطّة بإزالة سببه**: النصّ `no live embedding service reachable` — الذي شهدت §1‑أ أنّه لا يرد في الحزمة كلّها — **ظهر للمرّة الأولى** على `test_orchestrator_live.py:134` حين انكشفت البوّابة الثانية تحت الأولى ⇒ نشر المنفذ 8080 ([أ‑2](../containerised-test-harness-plan-v2.md)) صار شرطه الوحيد المتبقّي. 🔴 **وقياسٌ ثانٍ يُبطل شرطاً في د‑1**: `-m "live_ollama or live_embedding"` يجمع **10 لا 11** ⇒ `live_embedding` مجموعةٌ جزئيّةٌ من `live_ollama` بسبب `pytestmark` على مستوى الوحدة (`:45`) ⇒ فقرة «إضافة الوسم» تُسقَط، **و[د‑2](../containerised-test-harness-plan-v2.md) كانت سليمةً أصلاً**. وفارقٌ ثالثٌ **منهجيّ**: تخطّي `lint-imports` زال بلا تثبيتٍ جديد (‏`import-linter 2.13` مثبَّتٌ منذ 2026‑07‑10) — القياس السابق لم يكن من بيئةٍ مفعَّلة كما تُلزم §0، وتحذير «الملخّص الختاميّ لا يُطبَع» **باطلٌ** (يُطبَع؛ المُلتبس `--collect-only -q` وحده). **والعقد لم يتزحزح**: `2481 + 15 + 13 + 12 + 1 = 2522` — تحرّكت نقطة الانطلاق لا نقطة الوصول، ولوحة [ب‑3](../containerised-test-harness-plan-v2.md) تبقى حرفيّاً. البوّابات: ruff **536** · mypy **358** · lint-imports **8/0** · pytest **2481** بلا فاشل. سُجِّل ولم يُنفَّذ (§0 البند 1): ن‑ج ([أ‑6](../containerised-test-harness-plan-v2.md) مُستوفاةٌ سلفاً ⇒ تحقّقٌ لا سحب) · ن‑د · ن‑هـ
- [§3.113](3.113.md) — **ص‑2: العنقود الحاوي كامل الأدوار، و`aizzak_test` غائبة — وبها تكتمل المرحلة ص** — ثانية خطوات [`containerised-test-harness-plan-v2.md`](../containerised-test-harness-plan-v2.md) وخاتمة مرحلة القياس. **لا يغيّر حالة النظام الحيّ**: أُقلعت حاوية `postgres` وحدها ثمّ أُنزلت بـ`down`، ولا كتابةَ واحدة في القاعدة (`SELECT` فقط)، ولا سطرَ خارج `docs/`. **الأسئلة الثلاثة أُجيبت:** الأدوار الستّة **كلّها موجودة** — والصفات قِيست لا الوجود وحده، لأنّ دوراً موجوداً بصفةٍ خاطئة أسوأ من غائب (الغائب يُفشل الترحيل صراحةً، والخاطئ يمرّ ويكسر RLS صامتاً): `aizzak_owner` وحده `INHERIT`، والخمسة `NOINHERIT`، ولا `SUPERUSER`/`CREATEROLE`/`BYPASSRLS` في أيٍّ منها ⇒ مطابقٌ حرفيّاً لـ[`10-roles.sh:76‑93`](../../deploy/postgres/initdb/10-roles.sh) · `aizzak_test` **غير موجودة** كما تُوقّع · **البند 3 لم يُفعَّل** (لا `CREATE ROLE` نُفِّذ). ⇒ **حذر الخطوة من التوريث كان في محلّه والقياس برّأ الادّعاء**، وشرط [هـ‑1](../containerised-test-harness-plan-v2.md) **مُستوفًى**. **وثلاثة قياساتٍ لم تُطلَب تُسقِط مخاطرَ لاحقة:** كلمات المرور السبع من `.env` **تُصادَق 7/7** بلا طباعة سرّ (‏[أ‑5](../containerised-test-harness-plan-v2.md) كانت ستكتشف أيّ انحرافٍ في ب‑1 بعد ستّ خطوات، بصورة فشل ترحيلٍ لا فشل سرّ — والمذكرة قائمة من [§3.94](3.94.md)) · مخطّط `public` في `aizzak` مُحصَّنٌ سلفاً (`PUBLIC` = `U` بلا `C`، و`aizzak_owner` = `UC`) ⇒ **النموذج الذي على [أ‑2](../containerised-test-harness-plan-v2.md) إعادةُ إنتاجه داخل `aizzak_test` صار مقيساً لا مستنبَطاً** (المنح لكلّ قاعدةٍ على حدة، لا يُورَث) · PG **16.14** يطابق الأصليّة ⇒ لا فجوةَ إصدارٍ تفسّر فرقاً في [ج‑3](../containerised-test-harness-plan-v2.md). ⚠️ **وقياسٌ رابع نشأ عرَضاً وسُجِّل ولم يُنفَّذ** (§7 ن‑و): `docker compose port postgres 5432` ⇒ **`0.0.0.0:15432`**، والمنافذ الستّة كلّها بلا بادئة عنوان (`:153`·`:198`·`:208`·`:230‑231`·`:245`·`:305`؛ و`nginx` 80/443 مقصودةٌ عامّة فخارجها) — وهي **حرفيّاً حجّة أ‑2 نفسها** مطبَّقةً على ما هو قائم، لكنّ تعديل `docker-compose.yml` قرارٌ مغلقٌ مرفوض ⇒ ملفّ التجاوز الاختباريّ سيكون أشدّ تحصيناً من ملفّ النشر الإنتاجيّ. البوّابات: ruff **536** · mypy **358** · lint-imports **8/0** · pytest **2481 ناجح · 51 متخطًّى** — **مطابقٌ لخطّ أساس [§3.112](3.112.md) حرفيّاً**، وهو البرهان على أنّ الإقلاع والإنزال لم يتركا أثراً
- [§3.114](3.114.md) — **إغلاق ن‑ز: الخطة الأولى حُذفت بقرار المستخدم، وكُنست روابطها المعلَّقة** — ملاحظةُ [`containerised-test-harness-plan-v2.md`](../containerised-test-harness-plan-v2.md) **§7 ن‑ز**، **لا خطوةً من السبع عشرة** (الحالة تبقى **2/17** والتالية **أ‑1**). **تعديلُ نصٍّ فقط**: لا `src/` ولا `tests/` ولا `deploy/` ولا حالةٌ حيّةٌ من أيّ نوع. رُصد في [§3.113](3.113.md) أنّ `docs/containerised-test-harness-plan.md` محذوفٌ من شجرة العمل ولم تحذفه الجلسة، فتُرك بلا لمسٍ لقرار المستخدم ([§0](../containerised-test-harness-plan-v2.md#0-قاعدة-التنفيذ) البند 1) — وحسمه بثلاثٍ: حَذَف بنفسه · **التزم** · و**«لا أريد استخدامها كمرجع»**. ⇒ سقطت الاستعادة، ونُفِّذ الكنس **فوراً** لا مؤجَّلاً إلى [د‑3](../containerised-test-harness-plan-v2.md#د3--مسح-الحقيقة-الوثائقيّة). ⭐ **والمسح صحّح عدد ن‑ز نفسها**: الذكر **ستّةٌ** في خمسة ملفّات لكنّ الروابط **ثلاثةٌ لا أربعة** — الترويسة و[`3.99.md`](3.99.md) و[`CHANGELOG.md`](CHANGELOG.md) روابطُ تنكسر، بينما [`INDEX.md`](INDEX.md) يذكر الاسم في **مقطع شفرة** و[`3.113.md`](3.113.md) داخل **مخرَج `git status` منقولٍ حرفيّاً** ⇒ **لم يُلمسا**، فالقاعدة: يُكنَس ما يَعِد بوجهةٍ ويبقى ما يذكر اسماً؛ ولو عوملت الستّة معاملةً واحدة لأُتلف موضعان بلا سبب (أحدهما تزويرُ دليل). وطُبِّقت سابقة [§3.101](3.101.md) صنفين: **الترويسة أُعيدت كتابتها** — وكانت أخطرَ المواضع إذ لم تكن تذكر الأولى بل **تصفها مرجعاً حيّاً** («تَخلُف… ولا تُكمِلها») — فصارت تفتح بصندوق 🔴 وتُعلن v2 **مصدر الحقيقة الوحيد** وأنّه لم يبقَ في الأولى ما يُرجَع إليه (أ‑2 مُدمَجٌ أمراً واقعاً · المنطق السببيّ مُستبقًى في §9)؛ **والسجلّان المؤرَّخان صِين نصُّهما** وأُزيل الرابط وحده مع مؤشّرٍ مؤرَّخ، لأنّهما كانا صادقَين يوم كُتبا وإعادةُ كتابتهما تزوّر السجلّ — الترويسة **توجّه سلوكاً** والسجلّ **يشهد على ماضٍ**. **ولا نسخةَ أرشيفٍ أُبقيت داخل المستودع**: نقلُه تحت اسمٍ آخر يعيد إنتاج ما طُلب إنهاؤه — مرجعاً ثانياً قابلاً للاستشهاد — والقرار كان عن **الاستعمال** لا عن مكان التخزين (ونسخةُ المستخدم محفوظةٌ خارجه). ⚠️ ومعلومةٌ لم تكن في ن‑ز: الحذف مُلتزَمٌ في **`90ba6a3` نفسه** — التزامُ توثيق ص‑2 — فلا التزامَ منفصلاً يدلّ عليه بالاسم. **ولا `git restore` ولا التزام.** البوّابات: ruff **536** · mypy **358** · lint-imports **8/0** · pytest **2481 ناجح · 0 فاشل · 51 متخطًّى** — مطابقةٌ لـ[§3.112](3.112.md)/[§3.113](3.113.md)، وهي هنا **برهانُ عدمِ أثرٍ لا برهانُ صحّة**، شُغّلت لأنّ «تعديلُ نصٍّ فقط» يُقاس ولا يُفترَض


- [§3.115](3.115.md) — **أ‑1: `MINIO_TEST_*` تدخل `.env` و`.env.example`، وتعليقٌ كاذبٌ في `bootstrap.sh` يُصحَّح** — الخطوة **أ‑1** من [`containerised-test-harness-plan-v2.md`](../containerised-test-harness-plan-v2.md)، **أوّل خطوةٍ تكتب شيئاً** وأوّل خطوات المرحلة أ (التزويد بلا حذف). `docker-compose.yml:377‑380` كان يمرّر المتغيّرات الأربعة سلفاً و`bootstrap.sh` يقرؤها سلفاً؛ **الحلقة المفقودة طرفُ الإدخال وحده** ⇒ `false` مُستبدَلٌ وخروجٌ عند `:34` قبل تمفيح أيّ شيء. ⭐ **والقياس قبل الكتابة منع عطباً**: التزويد يمفح بـ`MINIO_TEST_SECRET_KEY` والاختبارات تقرأ **`TEST_MINIO_SECRET_KEY`** (‏`conftest.py:675`) — اسمان مقلوبان لا يربطهما شيء، ولا يتّفقان إلّا بتطابق افتراضيَّيهما ⇒ سرٌّ عشوائيّ كان **يعيد تمفيح الحساب بصمت** (`svcacct edit`) فتسقط `live_minio` كلّها على `SignatureDoesNotMatch`، وبرهانُ الخطوة نفسه هو المفجّر. أُبقيت القيمة على الافتراضيّ وسُجِّلت **ن‑ح** (التمفيح العشوائيّ يؤجَّل إلى أ‑5 حيث يتحرّك الطرفان معاً) مع تحذيرٍ في كلا الملفّين. وتعليق `bootstrap.sh:30‑33` («*the local Compose stack does through `.env`*») **لم يكن صحيحاً يوماً** — فما عمل هذا الشقّ من نسخةِ مستودعٍ قطّ، والحساب عاش في حجم `aizzak_minio-data` وحده منذ [§3.99](3.99.md) ⇒ أيّ `down -v` يأخذ الحزمة معه؛ والصياغة الجديدة **تحفظ الخطأ تاريخاً** بدل محوه. **البرهان:** `test account aizzak_test updated` — **«حُدِّث» لا «أُنشئ»** ⇒ الحساب نجا وتقاربت العمليّة عليه، وهو تأكيدٌ تجريبيٌّ أنّ سرّه الحيّ كان ما كُتب؛ و`pytest -m live_minio` ⇒ **13 ناجح** **بلا تجاوزٍ بيئيّ** (منها `test_out_of_scope_bucket_is_denied_not_leaked` ⇒ الحصر بالدلو الواحد صحيحٌ كذلك). البوّابات: ruff **536** · mypy **358** · lint-imports **8/0** · pytest **2493 ناجح · 0 فاشل · 39 متخطًّى** — **اثنتا عشرة انتقلت لا ثلاث عشرة**: `test_connection_failure_surfaces_as_common_internal` تقصد منفذاً مغلقاً عمداً فتنجح في الحالين (12+1+1=14). ورقمٌ **مشروطٌ لا خطُّ أساسٍ جديد**: العنقود أُنزل بـ`down` بلا `-v` فتعود الشجرة إلى `2481 · 51`. لم يُمَسّ: `src/` · `tests/` · `docker-compose.yml` (قرارٌ مغلق). و**ن‑ط** جديدة: `bootstrap.sh:21` يقول `(plan أ-2)` قاصداً أ‑2 **الخطةِ المحذوفة** بينما أ‑2 هنا خطوةٌ أخرى ⇒ **مُضلِّلةٌ لا معلَّقةٌ فحسب**، تُصحَّح في د‑3. **3/17**

- [§3.116](3.116.md) — **أ‑2: `docker-compose.test.yml` وسكربت قاعدة الاختبار — والصيغة التي كتبتها الخطّة لا تُقلِع** — الخطوة **أ‑2** من [`containerised-test-harness-plan-v2.md`](../containerised-test-harness-plan-v2.md)، **أوّل خطوةٍ تُنشئ ملفّاً**. ملفّان جديدان و`docker-compose.yml` **صفرُ أسطر** (قرارٌ مغلق). 🔴 **والقياس أسقط صيغة الخطّة نفسها بشقَّيها**: التركيب `/docker-entrypoint-initdb.d/testdb` يقع داخل تركيبٍ `:ro` فلا يجد runc موضعاً لنقطة التركيب وتموت الحاوية قبل بلوغ Postgres (`create mountpoint … read-only file system`) — ⚠️ و`docker compose config` **يُجيزه صامتاً**، فالعطب في زمن التشغيل وحده؛ **ولو أقلع لَما عمل**، إذ يوسّع مدخلُ الصورة `/docker-entrypoint-initdb.d/*` **مستوًى واحداً** وذراعُ `case` لغير `*.sh`/`*.sql` هي `ignoring` ⇒ الدليل يُتجاهَل، **فادّعاء أ‑3 أنّ «سكربت أ‑2 يسري على أيّ عنقودٍ جديد» لم يكن صحيحاً قطّ**. ⇒ التركيب صار `/opt/aizzak/testdb`، **لا** دليلاً نائباً فارغاً في `initdb/` (‏وهو يعمل متى وُجدت نقطته سلفاً — مُتحقَّقٌ منه — لكنّه عنصرٌ متتبَّعٌ يقرأ كدليل سكربتاتٍ بلا سكربت، عينُ ما حُذف من أجله `pgbouncer.ini`): الخروج من `initdb.d` يجعل التشغيل التلقائيّ **مستحيلاً بنيويّاً** لا محكوماً بسلوك glob وببقاء `:ro`. **قياساتٌ شكّلت السكربت:** مالك `aizzak` هو `postgres` لا `aizzak_owner` ⇒ منحُ `10-roles.sh:108` **لا يُكرَّر** (هنا هو المالك) · `nspacl` المقيس يُستنسَخ بالزوج `REVOKE`+`GRANT` · أمر أ‑3 يستدعي بـ`sh` و`/bin/sh` هو dash ⇒ `#!/bin/sh` و`set -eu` **بلا** `pipefail` · و`aizzak_test` مُثبَّتةٌ في سبعة DSN بـ`conftest.py` ⇒ الاسم مُثبَّتٌ لا متغيّر. و`CREATE DATABASE` لا تقبل `DO $$` (ليست كتلة معاملة) ⇒ `\gexec`. **البرهان:** الدمج يُبقي أحجام `postgres` **ثلاثةً لا واحداً** و`host_ip=127.0.0.1`؛ والحاوية **تقلع**، والتركيب للقراءة فقط، و`initdb.d` فيه `10-roles.sh` وحده، **و`aizzak_test` غائبة** ⇒ ملفّ التجاوز **يُحضِر ولا يزوّد**. البوّابات: ruff **536** · mypy **358** · lint-imports **8/0** · pytest **2481 · 51** — **مطابقٌ لخطّ الأساس تماماً**، وهو المقصود: خضرةٌ تُثبت **انعدام الأثر** لا الصحّة (لا سطرَ بايثون، ولا اختبارَ يقرأ `docker-compose.test.yml`). و**ن‑ي** جديدة تحمل أثر ذلك على [أ‑3](../containerised-test-harness-plan-v2.md) — أمرُها يصير `sh /opt/aizzak/testdb/…` **ولم تعد لحاقاً بحجمٍ قائمٍ بل المسار الوحيد لكلّ عنقود** — ولم تُنفَّذ، بل رُفعت إلى سطر «الخطوة التالية» في الترويسة. **4/17**

- [§3.117](3.117.md) — **أ‑3: `aizzak_test` تُزوَّد على الحجم القائم، وبالمسار الذي أصلحته ن‑ي لا بمسار الخطّة** — الخطوة **أ‑3** من [`containerised-test-harness-plan-v2.md`](../containerised-test-harness-plan-v2.md)، **أوّل خطوةٍ تغيّر حالةً حيّةً بشكلٍ دائم** (ما سبقها ملفّاتٌ أو قياس) و**لا تكتب سطراً في المستودع خارج `docs/`**: منتَجها قاعدةُ بياناتٍ داخل حجم Docker. **وأمرُها المُنفَّذ يخالف أمر الخطّة في موضعين لا واحد**: المسار `/opt/aizzak/testdb/20-test-database.sh` معروفٌ من [ن‑ي](../containerised-test-harness-plan-v2.md)، **أمّا الثاني فظهر هنا** — سطرُ **الرفع** نفسه يحتاج `-f docker-compose.test.yml`، وبدونها ترتفع الحاوية بلا التركيب فلا وجود للسكربت، ولا يقول ذلك خطأً مفهوماً بل `No such file or directory` على مسارٍ يبدو من الصورة. **حالة ما قبل** تُثبت ادّعاء الخطوة نفسه: الأدوار **ستّة** و`aizzak` فيها **4** جداول `platform` ⇒ عنقودٌ مُرحَّلٌ مأهول لا حجمٌ جديد. ⭐ **والبرهان الأقوى من `\l`/`\dn+` اللتين طلبتهما الخطّة**: `nspacl` لمخطّط `public` في القاعدة الجديدة **مطابقٌ حرفاً بحرف** لمرجع `aizzak` ⇒ `{pg_database_owner=UC/…, =U/…, aizzak_owner=UC/…}`؛ و**فرقُ `datacl` هو بعينه ما تنبّأ به قياسُ [§3.116](3.116.md)**: `aizzak_owner` يحمل `CTc` **بصفته المالك** لا بمنحٍ صريح، حيث يحمله `postgres` في `aizzak` ويُمنَح هو `Cc` سطراً منفصلاً — **فرقٌ مقيسٌ مُتوقَّعٌ سلفاً لا سهو**. ⚠️ **وصدقٌ عن المنح الخمسة**: `=Tc/…` يعني أنّ `PUBLIC` يحمل `CONNECT` افتراضاً، فمنحُ الخمسة **إعلانُ نيّةٍ لا تمكين** — وهو **عين ما يفعله `aizzak`** (يحمل `=Tc` ومعه المنح الصريحة) فالاستنساخ تامٌّ بازدواجيّته، والفائدة بقاءُ المنح لو رُفع `CONNECT` عن `PUBLIC` يوماً — تحصينٌ **لم يُنفَّذ، خارج النطاق**. **وبرهانٌ وظيفيٌّ لا كتالوجيّ** من المضيف على `15432`: `aizzak_owner` ينشئ جدولاً ويُدرج ويحذف (خروج `0` — **وهي حركة الترحيل نفسها**)، بينما `app_rw` **يتّصل** ويُمنَع بـ`ERROR: permission denied for schema public` — **عينُ الرسالة التي يوثّق `10-roles.sh:116‑124` اكتشافها بالطريقة الصعبة، مضبوطةً هنا على الدور الصحيح**؛ ولو انعكس الطرفان لكان السكربت خاطئاً واجتاز `\dn+` مع ذلك. **وإعادة التشغيل ثلاث مرّات**: خروج `0` في الثلاث و`CREATE DATABASE` تظهر في الأولى وحدها ⇒ حارس `\gexec` يعمل. 📌 **والتزويد يعيش في الحجم لا في الحاوية**: بعد `down` (بلا `-v`) وإعادة رفعٍ **بلا ملفّ التجاوز**، التركيب اختفى (`No such file or directory`) و`aizzak_test` باقيةٌ تُقرأ ⇒ الملفّ لازمٌ للتزويد وحده لا للاستعمال، والأمر **مرّةٌ واحدةٌ لكلّ حجم**، ولا يعيده إلّا `down -v` الممنوعُ أصلاً (حساب MinIO في `aizzak_minio-data`، [§3.115](3.115.md)) — والأحجام **6/6 ناجية** بعد كلّ إنزال. البوّابات: ruff **536** · mypy **358** · lint-imports **8/0** · pytest **2481 ناجح · 0 فاشل · 51 متخطًّى** — **مطابقٌ لخطّ الأساس، ولم يكن ليكون غيره**: الكتلتان **منفصلتان بالمنافذ** (الأصليّة `127.0.0.1:5432` وهي ما تقصده الحزمة بـ`conftest.py:97‑120`، والحاويّة `15432`) ⇒ **قاعدةٌ صحيحةٌ في عنقودٍ لا أحد يسأله**، و**أ‑3 لا يبرهن عليها اختبارٌ اليوم** وبوّابتها الحقيقيّة مؤجَّلةٌ إلى أوّل تشغيلٍ لأ‑4. لم يُفعَل: لا ترحيل (القاعدة **فارغة** — ذاك ب‑1) · لا `conftest.py` (أ‑4) · لا `REVOKE CONNECT … FROM PUBLIC` · و**متن أ‑3 لم يُصحَّح** فأمراه الخاطئان يبقيان كما كُتبا ([§0] البند 1) وهذه المدخلة ون‑ي هما ما يُقرأ بدلاً منهما ⇒ **ن‑ي مغلقة**. ووُثّق الأمر في [`stack-commands.md`](../stack-commands.md) صفّاً رقم **36** بشرحٍ مفصَّل. **ولم يُلتزَم شيء.**

</div>
