<div dir="rtl">

# §3.124 — ب‑3: البوّابة المطلقة مجتازة — خاتمة المرحلة ب

> عودة إلى الفهرس: [`INDEX.md`](INDEX.md) · الخطة: [`../containerised-test-harness-plan-v2.md`](../containerised-test-harness-plan-v2.md) §ب‑3 · برهان SLO: [§3.123](3.123.md)

## النتيجة

اكتملت ب‑3 واجتازت الحزمة الحاويّة البوابة المطلقة بالوسم وبالمجموع. صار التقدّم **11/17** والمرحلة ب **3/3**؛ التالية ج‑1 وحدها. لم تبدأ مرحلة الحذف، وبقي المكدّس عاملاً كي تُقاس حالة ج‑1 قبل تغييرها.

| الوسم | النتيجة | الزمن |
|---|---:|---:|
| `live_db` | **165 ناجحة · 0 متخطّاة** | 54.75s |
| `live_redis` | **53 ناجحة · 0 متخطّاة** | 35.07s |
| `live_qdrant` | **18 ناجحة · 0 متخطّاة** | 28.15s |
| `live_minio` | **14 ناجحة · 0 متخطّاة** | 13.95s |
| `live_vault` | **13 ناجحة · 0 متخطّاة** | 11.45s |
| `live_ollama` | **10 ناجحة · 0 متخطّاة** | 29.35s |
| `live_embedding` | **1 ناجح · 0 متخطًّى** | 14.21s |
| `live_stack_slo` | **2 ناجحان** | برهان ب‑2، [§3.123](3.123.md) |

لا يُستنتج أيّ عدد من المجموع الكلي: كل أمر `pytest -rs -m <marker>` شُغّل منفرداً، وتسلسل التنفيذ منع اختبارات قاعدة البيانات من التداخل.

## الحزمة الكاملة والتخطيات

من بيئة `.venv` مفعّلة وبعد تحميل `.env.test`:

```text
2522 passed, 10 skipped, 1 warning in 152.55s
```

سطر `-rs` أثبت أسباب العشر واحدةً واحدةً:

- 7 اختبارات `live_openai`: لا `TEST_OPENAI_API_KEY`.
- اختبار `live_web_search` واحد: لا `TEST_EXA_API_KEY`.
- اختبارا `live_stack_slo`: راية `RUN_P1_6_LOAD_TEST=1` غير مضبوطة في التشغيل الاعتيادي.

لا تخطّي لأي خدمة حاويّة، ولا سبب حادي عشر.

## انحراف تشغيلٍ كُشف وصُحّح

المحاولة الأولى للحزمة استدعت `.venv/bin/pytest` مباشرةً بلا `source .venv/bin/activate`، فكان `import-linter` مثبتاً داخل البيئة لكن `.venv/bin` غائباً عن `PATH`. لذلك أعطى `test_import_contracts` تخطّياً إضافياً وكانت النتيجة `2521 passed · 11 skipped`.

هذا تشغيل غير صالح بحسب §0 من الخطة، لا فشل منتج ولا اعتماداً ناقصاً: `pip show import-linter` أثبت النسخة 2.13 وملف `.venv/bin/lint-imports` موجود. أُعيد الأمر بعد تفعيل البيئة فنجح اختبار العقد وعادت النتيجة المطلوبة **2522/0/10**. حُفظت المحاولة هنا كي لا تُمحى من سجل الإثبات.

## الحالة والنطاق

- خدمات compose الإحدى عشرة الطويلة بقيت `Up`؛ السبع ذات فحص الصحة بقيت `healthy`.
- لا تعديل تحت `src/` أو `tests/` أو `deploy/` أو ملفات Compose؛ نطاق الخطوة قياسٌ وتوثيق فقط.
- لا `stage` ولا `commit`.
- لا تبدأ ج‑2 مع ج‑1؛ الخطوة التالية توقف الخدمات الأصلية وتعطّلها بلا حذف بيانات.

## البوابات الخمس

- `ruff format --check .` ⇒ **536** ملفاً.
- `ruff check .` ⇒ نظيف.
- `mypy src` ⇒ **358** ملفاً بلا مشكلة.
- `lint-imports` ⇒ **8 kept · 0 broken**.
- `pytest -rs` من بيئة مفعّلة ومع `.env.test` ⇒ **2522 ناجحاً · 0 فاشل · 10 متخطّيات** في `152.55s`، بالأسباب الثلاثة المحصورة أعلاه.

</div>
