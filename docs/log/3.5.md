<div dir="rtl">

[← الفهرس](INDEX.md) · [حالة التنفيذ](../implementation-status.md)

## 3.5 ما بُني — المرحلة 0 (حصاد المراجع من `alpha`)

خمسة ملفات مرجعية في [`docs/migration/refs/`](migration/refs/) (سلوكية/خوارزمية؛ التصميم يبقى مصدر الحقيقة، **لا أسرار منقولة**):

| # | الملف | المحتوى | وجهة التصميم |
|---|---|---|---|
| 0.1 | [`parsers.md`](migration/refs/parsers.md) | مُحلّلات PDF/Excel/OCR/JSON/نص + عقدا الإخراج + التقطيع | `06 §7` · `01 §2.7` |
| 0.2 | [`retrieval.md`](migration/refs/retrieval.md) | RRF · BM25 · MMR · النية · parent‑chunk · rerank · التلخيص + جداول الإعداد + 10 مخاطر | `06 §7` · `11` · `02 §1.5` |
| 0.3 | [`tools.md`](migration/refs/tools.md) | `web_search`(Exa) · `calculator`(safe‑AST) كمثالَي `BaseTool` | `11 §4` · `02 §3.1` |
| 0.4 | [`llm-providers.md`](migration/refs/llm-providers.md) | 4 مزوّدين · num_ctx/keep_alive · اكتشاف Ollama · **لا بثّ في alpha** · عدّ الرموز الثلاثي | `02 §1.1` · `00 DD‑13` |
| 0.5 | [`credentials-oauth-jobs.md`](migration/refs/credentials-oauth-jobs.md) | Fernet→Vault Transit · Gmail OAuth/PKCE · Redis jobs→Outbox/Streams | `06 §3,§9` · `04` |

**قرارات/مخاطر بارزة استُخرجت (تغذّي المراحل القادمة):**
- **الهجين (dense+sparse/RRF) غير مُغطّى بعقد `VectorStore` الحالي** (كثيف فقط) — قرار المرحلة 2.5/3: توسيع sparse أم dense‑only في v1 (`retrieval.md §7‑1`).
- **`parent_chunk_id` بلا عمود في `knowledge.chunks`** — قرار مخطّط payload الـQdrant قبل بناء `knowledge` (`retrieval.md §7‑2` · `parsers.md`).
- **البثّ (streaming) غير موجود في `alpha`** — عمل جديد صافٍ للمرحلتين 2.8/5.3 (`llm-providers.md §4`).
- **عتبات L2 لا تُنقَل كما هي** (العقد يقترح cosine) — إعادة معايرة تجريبية (`retrieval.md §7‑6`).
- **علّة تجديد Gmail** (التوكن المُجدَّد لا يُحفَظ) و**ثغرة CSRF** (`state==user_id`) — يُصلحان في `integrations` (`credentials-oauth-jobs.md §5`).
- **⚠️ تنبيه أمني:** مفتاح Exa صريح في `alpha/services/tools/web_search.py` — يُدوَّر في `alpha`، ولا يُنقل النمط (المفاتيح عبر `SecretsProvider`).

---

</div>
