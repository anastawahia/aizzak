<div dir="rtl">

# خطة إغلاق قاعدة «العمّال قبل المُرحّل» — ضمانُ مجموعات المستهلكين قبل أوّل نشر

> **Stream-Topology Plan** · وثيقةٌ حيّة تُحدَّث بعد كلّ خطوة · مصمَّمةٌ لتُستأنف عبر جلساتٍ منفصلة
>
> هذه الخطة **لا تعدّل طوبولوجيا `docker-compose.yml`**: يبقى `worker` خلف ملمحه، ويبقى `outbox-relay` بلا حافّة `depends_on` نحوه. تُصلح **الجذر** الذي جعل تلك الحافّة مطلوبةً أصلاً — إنشاء مجموعة المستهلكين عند `$` بعد أن يكون النشر قد بدأ — فتسقط القاعدة اليدويّة من تلقاء نفسها بدل أن تُفرَض بترتيب إقلاع.

| | |
|---|---|
| **أُنشئت** | 2026‑08‑03 |
| **عُدِّلت** | 2026‑08‑03 — **مراجعةُ ما قبل التنفيذ**: فُصلت مهلةُ الاتّصال عن مهلة القراءة (الخطوة 1، البند 1) · سُمّي عميل المُرحّل استثناءً صريحاً (قاعدة التغطية · الخطوة 3) · أُلزم الاختبارُ الحيّ ببناء عميله (الخطوة 1، اختبار 6) · أُضيف **البند 5: رؤيةُ موتِ مهامّ الخلفيّة — تسجيلٌ فقط، بلا إدارة** |
| **الأساس** | `814cb59` (‏`master`، شجرةٌ نظيفة — أُنشئت على `928bd5b`) · [`log/INDEX.md`](log/INDEX.md) حتّى §3.104 · البوّابات الخمس لم تُقَس عند الإنشاء (وثيقةُ تخطيطٍ لا تغيّر كوداً) · **§1‑أ‑1 مقيسٌ حيّاً على المكدّس العامل في 2026‑08‑03** · **§1‑أ‑2 مستنتَجٌ من الشيفرة على `814cb59` ولم يُقَس بعد — قياسه بندٌ في إثبات الخطوة 1** |
| **النطاق** | **مهلةُ صمتِ Redis في العمّال الثلاثة وفي جسر الإشعارات داخل الـAPI (§1‑أ — سادّة)** · **رؤيةُ موتِ مهامّ خلفيّة الـAPI — تسجيلٌ فقط (الخطوة 1، البند 5)** · جدولُ طوبولوجيا واحد · ضمانُ المجموعات في مُرحّل Outbox قبل أوّل `XADD` · اختبارٌ مانعٌ للانحراف · إثباتٌ حيّ · حذفُ القاعدة اليدويّة من سطح الوثائق |
| **خارج النطاق** | **تقسيم `worker` إلى ثلاث خدمات** (§6‑أ) · **حذف الملمح** (§6‑أ) · ترقية الافتراضيّ `WORKER` · `stream_prefix` الميت (§6‑ب) · **إدارةُ مهامّ الخلفيّة: إعادةُ تشغيلٍ أو تراجعٌ أُسّيّ أو إطفاءُ `ready` أو إسقاطُ العمليّة (§6‑د)** · **أيّ تنبيهٍ أو عتبةٍ أو لوحةِ مراقبةٍ مبنيّةٍ على السطر الجديد** |
| **الحالة العامّة** | ✅ **6 / 6 — مكتملة** · **أُغلق ما جاءت له هذه الخطّة كلّه**: الجذرُ أُصلح داخل عمليّة المُرحّل، وقِيس وحدةً وحيّاً على Redis حقيقيّ، وحُذفت القاعدة اليدويّة من سطح الوثائق الحيّ، **وأُثبت أخيراً حاويّاً بالترتيب الذي كانت القاعدة موجودةً لمنعه** ([§3.110](log/3.110.md)): حدثٌ نُشر على مجرًى جديدٍ **قبل أن يوجد العامل** وصل إليه (`entries-read = 1` · `lag = 0`)، وعلى الصورة المحفوظة من قبل الإصلاح **لم يصل ولن يصل** (`XLEN = 1` مع `entries-read` فارغةٍ و`lag = 0`، حتّى بعد ثمانِ إعادات تشغيل). ⇒ القاعدة لم تُحذف لأنّها أُهملت، بل لأنّ سببها زال — مقيساً في الاتّجاهين. ✅ **وثغرةٌ حقيقيّةٌ كشفتها الخطوة الأخيرة ولم تُصلحها — ثمّ سُدَّت بعدها بإذنٍ صريح** ([§3.111](log/3.111.md)): `up -d outbox-relay` (و`up -d worker`، **وهي الثانية التي لم تُسمَّ يومَها**) لا يجرّ `pgbouncer` رغم أنّ `DATABASE_URL` لكلٍّ منهما لا تعرف طريقاً غيره ⇒ صارت تبعيّةً مُعلَنة (§6‑هـ‑1) · الخطوة 1 مُغلَقةٌ شيفرةً وبوّاباتٍ وإثباتاً حيّاً **وحاويّاً** ([§3.105](log/3.105.md))، والخطوة 2 مُغلَقةٌ ببرهان فشلٍ فعليّ ([§3.106](log/3.106.md))، والخطوة 3 مُغلَقةٌ ببرهان فشلٍ فعليّ مُشغَّلٍ مرّتين ([§3.107](log/3.107.md))، والخطوة 4 مُغلَقةٌ بإثباتٍ حيٍّ على Redis حقيقيّ وبرهان فشلٍ فعليّ عبر تحوّرٍ مؤقّتٍ في `bootstrap.py` مُعادٍ حرفيّاً ([§3.108](log/3.108.md))، والخطوة 5 مُغلَقةٌ بحذف القاعدة اليدويّة من سطح الوثائق الحيّ كلّه (بما فيه موضعٌ إنجليزيٌّ لم تسمّه الخطّة، `deploy/runpod/bootstrap.sh`) بلا مسّ لسطر منطقٍ واحد ولا لطوبولوجيا `docker-compose.yml` ولا للسجلّ التاريخيّ ([§3.109](log/3.109.md)) · **معايير القبول 2 و3 و5 و7 قِيست فعلاً — ولم يُقَس أوّلها الثلاثة في هذا المشروع ولا مرّةً قبل اليوم** · **ومعيار القبول 6 قِيسَ الآن أيضاً** (الخطوة 4) · **ومعيار القبول 9 قِيس صادقاً بلا تزوير** (الخطوة 5 — الإصابات الباقية خارج `docs/log/` كلّها نصٌّ عن الحذف في الخطّة نفسها أو سجلّان تاريخيّان ممنوعان صراحةً) |
| **الخطوة التالية** | **لا خطوةَ تاليةً داخل هذه الخطّة — اكتملت الستّ.** ما بقي مفتوحاً موصوفٌ في §6 ولم تستبقه أيّ خطوةٍ بسطر: تقسيم `worker` إلى ثلاث خدمات (§6‑أ، وشرطُه — أوّلُ إقلاعٍ حاويٍّ مقيسٍ لـ`knowledge` — تحقّق في [§3.105](log/3.105.md)) · `stream_prefix` الميت (§6‑ب) · `XACK` لا يحذف (§6‑ج) · إدارةُ مهامّ خلفيّة الـAPI بعد أن فُتحت العين (§6‑د) · ~~وثغرةُ `depends_on` المكتشفةُ في الخطوة 6 (§6‑هـ)~~ ✅ **سُدَّت** في [§3.111](log/3.111.md) — ولم تحتج خطّةً خاصّةً كما ظُنّ، لأنّ شرطها الوحيد (مقارنةُ مسار RunPod) تحقّق بلا نزاع (§6‑هـ‑1). ⇒ هذه الوثيقة تصير **مرجعاً تاريخيّاً** لا خطّةً حيّة؛ وأيُّ عملٍ على البنود أعلاه يحتاج خطّته الخاصّة. |

---

## 0. قاعدة التنفيذ

منقولةٌ من [`deferred-adapters-plan.md`](deferred-adapters-plan.md) §0 — أثبتت جدواها عشرين مرّة.

**وكيلٌ فرعيٌّ واحد لكلّ خطوة، ثمّ توقّف.** لا تُطلَق خطوتان معاً ولا يُدمَج بندان في وكيل.

كلّ وكيل ملزَمٌ بـ:

1. تنفيذ **مهمّته وحدها** — ما خرج عن نطاقها يُسجَّل ملاحظةً في §6 ولا يُنفَّذ.
2. تشغيل **البوّابات الخمس** قبل إعلان الانتهاء: `ruff format --check` · `ruff check` · `mypy src` · `lint-imports` · `pytest`.
3. **برهانُ الفشل على الحالة القديمة بآليّة العطب نفسها** — لا بخطأ جمعٍ ولا `ImportError`. في هذه الخطّة العطب مقيسٌ بالسلوك: **مدخلةٌ نُشرت قبل وجود المجموعة لا تصل المستهلك أبداً**.
4. كتابة مدخلة سجلٍّ في `docs/log/` بالرقم التالي (**§3.105 فما فوق**) وربطها في [`log/INDEX.md`](log/INDEX.md) و[`log/CHANGELOG.md`](log/CHANGELOG.md).
5. تحديث صفّه في §3 وسجلّ §5 من هذه الوثيقة.
6. **`docs/log/*` سجلٌّ تاريخيٌّ لا يُمَسّ**: القاعدة القديمة تبقى مكتوبةً فيه كما كانت وقتها؛ الحذف في §4 يطال الوثائق **الحيّة** وحدها.

بعد كلّ وكيل: **المُنسِّق يراجع الفرق ويعيد تشغيل البوّابات بنفسه ويعرض النتيجة على المستخدم**، ولا يُطلق التالي إلّا بإذن.

---

## 1. ما وجده الفحص

### 1‑أ · ⛔ عطبٌ **أسبقُ من هذه الخطّة كلّها**، في **أربعة** مواضع لا ثلاثة

#### 1‑أ‑1 · العمّال الثلاثة — **مقيسٌ حيّاً**

قياسٌ فعليٌّ على المكدّس العامل (2026‑08‑03)، بعد بناء الصورة من `928bd5b` — الصورة القائمة كانت أقدم من الشيفرة بيومين فكان الإقلاع عليها بلا معنى:

| العامل | النتيجة خلال 15 ثانية |
|---|---|
| `knowledge` | ✅ **أقلع كاملاً** (‏Vault · MinIO · Qdrant · التضمين · الاشتراكان) ثمّ انهار بعد **خمس ثوانٍ** — 6 إعادات تشغيل |
| `memory` | ✅ أقلع ثمّ انهار **بالرسالة نفسها حرفيّاً** — 3 إعادات تشغيل |

الرسالة واحدة: `redis.exceptions.TimeoutError: Timeout reading from redis:6379` ⇐ `AppError: event consume failed`، من `redis_streams.py:338` عبر `engine.py:189` (‏`run_once` → `read`).

**والسبب رقمان متناقضان:**

| الرقم | الموضع | معناه |
|---|---|---|
| `_SOCKET_TIMEOUT_S = 2.0` | `cache/redis_cache.py:42` | اقطع أيّ قراءةٍ صمتت ثانيتين |
| `CONSUMER_BLOCK_MS = 5000` | `settings.py` · `.env.example` | `XREADGROUP ... BLOCK 5000`: انتظر خمس ثوانٍ |

فالقراءة الحاجزة تنتظر خمساً بينما المقبس يقطع عند ثانيتين ⇒ **كلّ دورة قراءةٍ على مجرًى هادئ انهيارٌ حتميّ**، و`restart: unless-stopped` يحوّله حلقة. والمهلة **صحيحةٌ في موضعها الأصليّ** — سَرْدها يبرّرها بميزانيّات زمن الـAPI («fail fast … sub-second») — لكنّ `create_redis_client` مصنعٌ واحدٌ يخدم الـAPI والعامل معاً، ومهنة العامل هي الانتظار.

**لماذا لم يكشفه أيّ اختبار:** كلّ اختبارٍ حيٍّ ينشر **ثمّ** يقرأ (‏`tests/integration/conftest.py:628` يستعمل المصنع نفسه)، فالقراءة تجد مدخلةً جاهزةً وتعود فوراً ولا تبلغ الثانيتين قطّ. **لا اختبارَ واحدٌ يقرأ على مجرًى هادئ** — وهي حالة النظام الغالبة في الواقع.

⚠️ **وهذا يُبطل ادّعاءً مسجَّلاً في سطح الوثائق**: «`memory` يقلع ويعمل — مقيسٌ حيّاً ([§3.83](log/3.83.md))» صحيحٌ في نصفه فقط: يقلع، ولا يصمد في الهدوء. ⇒ **لم يعمل أيّ عاملٍ في هذه المنصّة قطّ لأكثر من خمس ثوانٍ من الهدوء.** والمُرحّل غير متأثّر (يستقصي بنومٍ قصير، لا ينتظر حاجزاً) — ولهذا يعمل بلا انقطاع.

#### 1‑أ‑2 · الموضع **الرابع**: جسر الإشعارات داخل الـAPI — الآليّة نفسها، بأثرٍ أخطر

مستنتَجٌ من قراءة الشيفرة على `814cb59`، **ولم يُقَس حيّاً بعد** (قياسه بندٌ صريحٌ في إثبات الخطوة 1، ولا يُعلَن مغلقاً قبله).

الواجهة ليست منتِجاً فقط: هي تشغّل **مستهلكاً حاجزاً من الجنس نفسه** داخل عمليّتها. `build_notification_consumer` (`composition_root.py:376`) يُبنى بـ`block_ms=settings.events.consumer_block_ms` (`composition_root.py:605`) فوق **عميل Redis عينه** الذي يخدم ذاكرة التخزين المؤقّت (`composition_root.py:885`) ⇒ خمس ثوانٍ انتظارٍ فوق مقبسٍ يقطع عند ثانيتين: **الرقمان المتناقضان أنفسهما، بلا حرفٍ مختلف.**

والفرق كلّه في الأثر لا في الآليّة:

| | العامل (§1‑أ‑1) | جسر الإشعارات (§1‑أ‑2) |
|---|---|---|
| بعد الانهيار | `restart: unless-stopped` يعيده ⇒ حلقةٌ **مرئيّةٌ** في `docker ps` | لا شيء يعيده: `api/main.py:240` ينشئ مهامّ `background=` ولا يراقب أيّاً منها، و`api/main.py:477` يستدعي `run()` عارياً |
| ما يراه المشغّل | إعادات تشغيلٍ تتراكم | **لا شيء**: العمليّة حيّة، `ready` صحيحة، ولا سطرَ خطأٍ واحد |
| ما يُفقَد | معالجةٌ متأخّرة، تُستأنَف | **كلّ إشعارٍ فوريّ في هذه العمليّة، إلى الأبد** — حتّى إعادة تشغيل الخدمة |

الاستثناء يخرج من `run()` فتموت مهمّة الخلفيّة صامتةً: الـ`finally` في `_make_lifespan` لا يعود إلى المهمّة إلّا عند الإغلاق، وهناك يبتلع `BaseException` أصلاً ⇒ **حتّى نصّ الانهيار لا يُطبَع.** (‏`run()` نفسها `while True` عاريةٌ بلا التقاطٍ لأيّ استثناء — `consumers/engine.py:210` — فالخروج منها خروجٌ نهائيّ.)

⇒ **هذا الشقّ وحده — الصمت لا الموت — يعالجه البند 5 من الخطوة 1: تسجيلٌ فقط، بلا إدارة** (§6‑د لما بقي).

⚠️ وكنسُ المجموعات اليتيمة **لا يلتقط هذه الحالة**: `_sweep_stale_notify_groups` يحذف مجموعات العمليّات **الميتة** بفحص حياة الـpid ([`log/3.81.md`](log/3.81.md))، والعمليّة هنا **حيّة تماماً** — فتبقى `cg.notify.<host>.<pid>` قائمةً وقائمةُ انتظارها تنمو بلا قارئٍ إلى أن يقصّها `STREAM_MAXLEN`.

⚠️ **وهذا يُبطل نصف جملةٍ كُتبت في الخطوة 1 أوّلاً**: «سلوك الـAPI لا يتغيّر بحرف» — مستحيلٌ الآن، لأنّ الواجهة **أحد المواضع المعطوبة** لا طرفاً بريئاً. ويبقى صحيحاً ما كانت تقصده: **مسار الطلب/الاستجابة** (ذاكرة التخزين) يجب أن يحتفظ بفشله السريع. الخطوة 1 تفصّل كيف يُحفظ الاثنان معاً.

⇒ **الترتيب المنطقيّ محسوم:** لا معنى لضمان وصول الحدث إلى مستهلكٍ لا يصمد — لا في العامل ولا في الواجهة. هذه الخطوة تسبق كلّ ما في هذه الخطّة.

### 1‑ب · الجذر سطرٌ واحد، وهو **صحيحٌ ولا يُغيَّر**

```python
# src/app/infrastructure/messaging/redis_streams.py:235
await self._client.xgroup_create(stream, group, id="$", mkstream=True)
```

`$` = ابدأ من ذيل المجرى. قرارٌ مقصودٌ ومُبرَّرٌ في سَرْده: عاملٌ يقلع أوّل مرّةٍ يجب ألّا يبتلع تاريخ المجرى كلّه. **الخطّة لا تلمس هذا السطر** — لا تحويل إلى `0` ولا خيار. ما تغيّره هو **متى** يُنفَّذ: قبل أوّل `XADD` بدل بعده.

و`MKSTREAM` موجودةٌ أصلاً ⇒ إنشاء المجموعة على مجرًى **غير موجودٍ بعد** عمليّةٌ مشروعةٌ اليوم، وهذا بالضبط ما يجعل الإصلاح ممكناً بلا أيّ تغييرٍ في المحوّل.

### 1‑ج · الطوبولوجيا الساكنة أربعة أزواجٍ، مكتوبةٌ في **ثلاثة أماكن** بلا رابطٍ بينها

| المجرى | المجموعة | العامل | المصدر |
|---|---|---|---|
| `stream.files` | `cg.knowledge` | knowledge | `bootstrap.py:523‑525` |
| `stream.knowledge` | `cg.knowledge` | knowledge | `bootstrap.py:532‑534` |
| `stream.media` | `cg.media` | media | `bootstrap.py:745‑747` |
| `stream.memory` | `cg.memory` | memory | `bootstrap.py:931‑932` |

والأسماء نفسها مكرّرةٌ حرفيّاً في مكانين آخرين: أسماء المجاري في أربعة ثوابت `STREAM` (‏`modules/files/application/event_mapping.py:40` · `knowledge:54` · `media:42` · `memory:47`)، والجدول المرجعيّ في [`design/04-event-catalog.md`](design/04-event-catalog.md) §2. **لا شيء يربط الثلاثة**، فانحرافُ اسمٍ في أحدها لا يُكتشف إلّا بمجرًى صامتٍ في الإنتاج.

> ⚠️ **`cg.notify.<host>.<pid>` خارج هذا الجدول عمداً** (‏04 §2 الحاشية · [`log/3.81.md`](log/3.81.md)): أسرةُ مجموعاتٍ **لكلّ عمليّة**، تُنشأ داخل عمليّة الـAPI عند إقلاعها وتُتلَف عند إغلاقها وتُكنَس عند اليُتم. مجموعةٌ ساكنةٌ مسبقة الإنشاء لها ستكون خاطئةً بالتعريف.
>
> ⚠️ لكنّها **خارج جدول الطوبولوجيا لا خارج العطب**: مستهلكها يقع في §1‑أ‑2 بحذافيره. الخلل هناك في المهلة، لا في مكان إنشاء المجموعة.

### 1‑د · المُرحّل هو **الوحيد** الذي ينشر — وهذا ما يجعل الإصلاح ممكناً في مكانٍ واحد

`OutboxRelay.run_once` ينشر بـ`self._publisher.publish(entry.stream, entry.payload)` (‏`outbox.py:112`)، واسم المجرى يأتي من عمود `stream` في صفّ الـoutbox (`persistence/outbox.py:99`). ولا أحد غيره ينشر (‏10 §7: «لا نشر خارج الـoutbox»)، وتعليق `bootstrap.py:250` يعيد تأكيدها: «المُرحّل هو المنتج الوحيد على هذه المجاري».

⇒ نقطةٌ واحدةٌ يمرّ منها كلّ نشرٍ في المنصّة ⇒ ضمانٌ واحدٌ يسبقها يكفي لكلّ المجاري.

### 1‑هـ · مسار RunPod يُغطَّى مجّاناً

`deploy/runpod/supervisord.conf:219` يشغّل `python -m app.workers.outbox_relay` — **نفس نقطة الدخول** التي تشغّلها خدمة Compose. فإصلاحٌ داخل عمليّة المُرحّل يسري على مسارَي النشر معاً بلا سطرٍ إضافيّ في أيٍّ منهما، ولا خطر انحرافٍ من النوع الذي يحرسه `tests/unit/test_deploy_worker_default.py`.

---

## 2. القرار المعماريّ: أين تُنشأ المجموعات

| # | البديل | الحكم |
|---|---|---|
| **أ** | **داخل عمليّة المُرحّل، قبل `run_forever`** | ✅ **المختار** |
| ب | خدمة `streams-bootstrap` لمرّةٍ واحدة في Compose | ❌ |
| ج | إعادة حافّة `depends_on: worker` | ❌ |

**لماذا (أ):** موضعٌ واحدٌ يغطّي **كلّ** مسارات التشغيل — Compose وRunPod و`python -m` المجرّد — لأنّه داخل العمليّة التي لا يبدأ النشر بدونها. لا خدمةَ جديدة، ولا حافّة `depends_on` جديدة، ولا سطرَ إعدادٍ يمكن نسيانه.

**لماذا لا (ب):** يطابق عُرف الملفّ (‏`vault-bootstrap` · `minio-bootstrap` · `nginx-certs`) لكنّه يضمن الترتيب **في Compose وحده**؛ RunPod يبقى بلا حارس، فيصير الضمان صحيحاً في نصف مسارات النشر — وهو تحديداً نوع الانقسام الذي يحرسه المشروع باختبار اتّفاقٍ صريح.

**لماذا لا (ج):** الحافّة تضمن **بدء** العامل، لا أنّ `ensure_group` نُفِّذت قبل أوّل `XADD` — ترتيبُ بدءٍ لا ترتيبُ حدث؛ الفوز فيها بالتوقيت. وتُبطل معنى الملمح: المُرحّل يجرّ عاملاً إلى الوجود كلّما أُقلع.

> **الاعتراض المتوقَّع: «منتِجٌ ينشئ مجموعات مستهلكين».** صحيحٌ شكلاً، ويُحسَم بالتسمية: ما يفعله المُرحّل ليس استهلاكاً بل **تهيئةَ طوبولوجيا** (`provision`)، من الجنس نفسه الذي يفعله `app.ops.provision` بالمخطّطات قبل أن يكتب أحدٌ صفّاً. والبديل الحقيقيّ للتهيئة المسبقة ليس «نقاءً معماريّاً» بل قاعدةً يدويّةً في وثيقة.

### قرارٌ فرعيٌّ مُتَّخذ: تُنشأ الأزواج الأربعة كلّها، ولو لم يُقلَع عاملها

الأثر: مجموعةُ عاملٍ غير منشورٍ تتراكم لها متأخّرات، وعند إقلاعه أوّل مرّةٍ يستهلك ما بقي منها. **مقبولٌ ومقصود:**

- **`knowledge`**: هذا هو المطلوب حرفيّاً — لا فهرسةَ مفقودةً لملفٍّ رُفع قبل إقلاع العامل.
- **`media`**: إعادةُ مهمّةٍ قديمة محميّةٌ بسجلّ `platform.processed_events` (‏DD‑09: تكرار = تجاهُل)، وما فقد صفَّه يفشل فشلاً صريحاً إلى `stream.<m>.dlq` بسببٍ مقروء — لا تنفيذٌ صامتٌ لمهمّةٍ مهجورة.
- **مكسبٌ جانبيّ:** التأخّر يصير **قابلاً للقياس** (`XINFO GROUPS`) بدل أن يكون عدماً غير مرئيّ.

⚠️ ويبقى `STREAM_MAXLEN` سقفاً يقصّ ما لم يُقرأ: الضمان هنا «لا تُفقد مدخلةٌ **بسبب ترتيب الإقلاع**»، لا «لا تُفقد مدخلةٌ أبداً».

---

## 3. الخطوات

| # | الخطوة | الحالة |
|---|---|---|
| 1 | ~~⛔~~ **مهلة صمتٍ تناسب القراءة الحاجزة** — العمّال الثلاثة + جسر إشعارات الـAPI (‏§1‑أ) **+ رؤيةُ الموت بلا إدارته** | ✅ [§3.105](log/3.105.md) |
| 2 | جدول الطوبولوجيا الواحد + اختبارٌ مانعٌ للانحراف | ✅ [§3.106](log/3.106.md) |
| 3 | المُرحّل يضمن المجموعات قبل أوّل نشر | ✅ [§3.107](log/3.107.md) |
| 4 | الإثبات الحيّ على Redis حقيقيّ (‏`live_redis`) | ✅ [§3.108](log/3.108.md) |
| 5 | حذف القاعدة اليدويّة من سطح الوثائق | ✅ [§3.109](log/3.109.md) |
| 6 | الإثبات الحاويّ + مدخلة السجلّ | ✅ [§3.110](log/3.110.md) |

### الخطوة 1 · مهلة صمتٍ تناسب القراءة الحاجزة ⛔ **سادّة**

**الملفّات:** `src/app/infrastructure/cache/redis_cache.py` · `src/app/workers/bootstrap.py` · `src/app/framework/di/composition_root.py` · `src/app/api/main.py` (‏البند 5 وحده).

**المبدأ الحاكم:** المهلة `2.0` **صحيحةٌ لمسار الطلب/الاستجابة وخاطئةٌ لكلّ قراءةٍ حاجزة**. فالمطلوب ليس رقماً جديداً يحلّ محلّ القديم، بل **فصلُ طبعين** كانا يتقاسمان مصنعاً واحداً: مَن الانتظارُ مهنتُه يأخذ مهلةً مشتقّةً من `block_ms`، ومَن يخدم مستخدماً واقفاً يبقى على فشله السريع.

**التغيير — أربعةُ مواضعَ للمهلة (لا ثلاثة)، وبندٌ خامسٌ للرؤية:**

1. `create_redis_client` يقبل **مهلةَ القراءة وحدها** اختياريّةً بافتراضيّ `2.0` (‏`_SOCKET_TIMEOUT_S`، `redis_cache.py:42`): كلّ نداءٍ قائمٍ اليوم يبقى كما هو حرفيّاً.
   ⚠️ **والمهلتان في الملفّ اثنتان لا واحدة**: `_CONNECT_TIMEOUT_S = 2.0` (‏`redis_cache.py:43`) **لا تُمَسّ لأحد، ولا للعمّال**. من يفشل في *إنشاء* الاتّصال ليس منتظِراً على مجرًى هادئ بل يواجه Redis غائباً؛ ورفعُها يحوّل «Redis مفقود» من فشلٍ فوريٍّ مقروء إلى تجمّدٍ ستّ ثوانٍ في كلّ محاولةِ إقلاع — وهو ثمنٌ يُدفع بلا مقابلٍ البتّة. **الطبعان المفصولان هنا طبعا الانتظار، لا طبعا الاتّصال.**
2. **اشتقاقٌ واحدٌ مسمّى** بجوار المصنع يعطي المهلة من `block_ms` (‏`block_ms/1000` + هامش) — **لا ثابتَ جديد**: رقمٌ مجرّدٌ ينحرف عن `CONSUMER_BLOCK_MS` عند أوّل تعديلٍ في `.env` ويعيد العطب بصمت.
3. مسارات العمّال الثلاثة (`bootstrap.py:653` · `:842` · `:981`) تمرّر المشتقّة.
4. **الـAPI: عميلٌ ثانٍ لجسر الإشعارات وحده.** عميل `composition_root.py:885` يبقى على `2.0` لذاكرة التخزين، ويُبنى بجواره عميلٌ بالمهلة المشتقّة يُمرَّر إلى `build_notification_consumer` **دون سواه**.
5. **رؤيةُ الموت — تسجيلٌ فقط، لا إدارة.** `_make_lifespan` (‏`api/main.py:240`) يطلق مهامّ `background=` ولا يعود إليها إلّا عند الإغلاق. يُضاف نداءٌ راجعٌ عند انتهاء كلّ مهمّةٍ يسجّل الاستثناء الذي قتلها، **مع استثناء الإلغاء صراحةً** (`asyncio.CancelledError`): الإغلاق النظيف يُنهي هذه المهامّ عمداً (‏الـ`finally` نفسه، `api/main.py:246`)، فتسجيلُه خطأً يملأ السجلّ بإنذاراتٍ كاذبةٍ في كلّ إيقافٍ عاديّ — **وسطرٌ يتعلّم الجميع تجاهله أسوأ من لا سطر.**

**البند 4 قرارٌ له ثمنٌ يُذكر صراحةً:** الشيفرة اليوم تتعمّد عميلاً مشتركاً («اتّصالٌ زائدٌ بلا عزلٍ مكتسب»، تعليق `composition_root.py`). كان ذلك التبرير صحيحاً ما دام الطبعان واحداً — وقد تبيّن أنّهما ضدّان:

| البديل | الحكم |
|---|---|
| **عميلان: تخزينٌ عند `2.0` · جسرٌ عند المشتقّة** | ✅ **المختار** — ثمنه مجمّع اتّصالاتٍ ثانٍ في عمليّة الـAPI، وهو أرخص ما في الجدول |
| رفع مهلة العميل المشترك | ❌ يجعل طلبَ مستخدمٍ يتجمّد ست ثوانٍ على Redis معطوب — نقضٌ مباشر لميزانيّات 07‑nfr دون الفوز بشيء |
| تصغير `CONSUMER_BLOCK_MS` تحت الثانيتين | ❌ استقصاءٌ محمومٌ يهدر Redis، وقنبلةٌ موقوتةٌ لأوّل من يرفع القيمة — ويضرب العمّال الثلاثة معه |

**سَرْدٌ إلزاميّ ضمن التغيير:** يُحدَّث تعليق العميل المشترك في `composition_root.py`، وإلّا قُرئ لاحقاً بوصفه نهياً عن الانقسام الذي صار مطلوباً — وهو تحديداً كيف يعود عطبٌ أُصلح مرّة.

**والبند 5 حدُّه محسومٌ سلفاً: يرى ولا يُدير.** لا إعادةَ تشغيل، ولا تراجعَ أُسّيّ، ولا إطفاءَ لـ`ready`، ولا إسقاطَ للعمليّة — يبقى ذلك كلّه في §6‑د بلا سطرٍ واحدٍ يستبق قراره.

والسبب أنّ البند **ليس علاجاً ثانياً بل أداةَ القياس للعلاج الأوّل**: البنود 1‑4 تُثبَت مرّةً واحدةً بيد المشغّل في ظرفٍ مُصطنَع، وبعد تلك اللحظة يعود النجاح والفشل إلى **التطابق التامّ من الخارج** (§1‑أ‑2: خدمةٌ حيّة، `ready` صحيحة، ولا سطرَ واحد). ⇒ إصلاحٌ لا وسيلةَ للتحقّق من بقائه ليس إصلاحاً مضموناً بل رهاناً. ومن يُصلح عطباً صامتاً ثمّ يرفض ميزان الحرارة بحجّة النطاق لم يلتزم بنطاقٍ، بل أغمض عينيه.

⚠️ **ولا يُبنى نظام إنذارٍ على هذا السطر في هذه الخطّة**: لا تنبيهات، ولا عتبات، ولا لوحة. **تُفتَح العين أوّلاً، ويُقرَّر ما يستحقّ الإنذار بعد أن يُرى — لا قبله.** إنذارٌ مصمَّمٌ على تخمينِ ما سيحدث يحرس ما لم يقع ويعمى عمّا يقع.

⚠️ **قاعدة التغطية:** كلّ موضعٍ يُبنى فيه عميل Redis ثمّ يُقرأ منه قراءةً حاجزة — لا `knowledge` وحده ولا العمّال وحدهم. المعيار العمليّ: **من يمرّر `block_ms` يجب أن يمرّر المهلة المشتقّة معه**، وهي أربعة مواضع اليوم (‏`bootstrap.py` ×3 · `composition_root.py:605`). ومسار RunPod يشغّل الشيفرة نفسها بالمصنع نفسه فيُغطّى بلا سطرٍ إضافيّ (‏§1‑هـ).

⚠️ **والاستثناء يُسمّى بالاسم — عميل المُرحّل (‏`bootstrap.py:249`) يبقى على `2.0`.** هو العميل **الخامس** في المنصّة، ولا يقرأ قراءةً حاجزةً في أيّ سطر: يستقصي بنومٍ قصير، ولهذا وحده نجا من العطب (‏§1‑أ‑1). والتسمية الصريحة ليست ترفاً: **الخطوة 3 تركّب `RedisStreamsConsumer` فوق هذا العميل بالذات**، فمن يقرأ الشيفرة بعدها يرى «مستهلكاً» فيستنتج تلقائيّاً أنّه يستحقّ المهلة المشتقّة — والاستنتاج خاطئ: ذلك المستهلك يعلّق مجموعاتٍ وينصرف، ولا ينتظر مدخلةً واحدة. **المعيار يحسمها بلا اجتهاد: من لا يمرّر `block_ms` لا يأخذ المشتقّة** — والاختبار 2 يحرسها.

**الاختبارات:**

1. **وحدة:** عملاء العمّال الثلاثة **وعميل جسر الإشعارات** يُبنون بمهلةِ **قراءةٍ** أكبر من `block_ms` المضبوط.
2. **وحدة:** **عميل ذاكرة التخزين في الـAPI وعميل المُرحّل** يبقيان على `2.0` بالضبط — حارسان: الأوّل يمنع «إصلاحاً» يعالج الجسر بإبطاء كلّ طلبٍ في المنصّة، والثاني يمنع تعميماً لاحقاً على المُرحّل بدافع الاتّساق الشكليّ بعد أن يصير عليه مستهلك (‏الخطوة 3).
3. **وحدة:** `_CONNECT_TIMEOUT_S` يبقى `2.0` في المواضع **الخمسة** بلا استثناء — بما فيها العمّال: الفصل طال الانتظار وحده، لا الاتّصال.
4. **وحدة:** تعديل `CONSUMER_BLOCK_MS` يحرّك المهلة المشتقّة معه في المواضع الأربعة — حراسةُ الانحراف التي تجعل الإصلاح صامداً لا لحظيّاً.
5. **وحدة (البند 5):** مهمّةُ خلفيّةٍ تموت باستثناء ⇒ يُسجَّل سطرُ خطأٍ يحمل نصّ الاستثناء؛ ومهمّةٌ تُلغى عند الإغلاق ⇒ **لا سطرَ خطأٍ البتّة**. الشقّ الثاني هو الحارس الحقيقيّ: بدونه يصير السطر ضجيجاً في كلّ إيقافٍ عاديّ فيُتعلَّم تجاهله، وتُفقَد الرؤية التي جاء يشتريها.
6. **حيّ** (`live_redis`): قراءةٌ حاجزةٌ على مجرًى **هادئ** لمدّةٍ تتجاوز `block_ms` تعود بلا استثناء. **برهان الفشل: يسقط على `814cb59` بالانهيار نفسه المقيس في §1‑أ‑1.**
   ⚠️ **ويبني الاختبارُ عميلَه بالمهلة المشتقّة صراحةً**، لا من تركيبات الاختبارات القائمة (‏`tests/integration/conftest.py:628` يبني بالمصنع نفسه، أي بالمهلة القصيرة المعطوبة). اختبارٌ يرث العميل المعطوب **يفشل بعد الإصلاح كما يفشل قبله** ⇒ يُقرأ إصلاحٌ ناجحٌ فشلاً، أو — الأسوأ — يُلطَّف الاختبار حتّى «ينجح» فيفقد ما جاء ليقيسه. **بناءُ العميل جزءٌ ممّا يُقاس، لا تفصيلٌ جانبيّ.**

**تصحيحٌ في الوثائق ضمن الخطوة نفسها:** صفّ `memory` في [`quickstart.md`](quickstart.md) §0 يقول «✅ يقلع ويعمل» — يُصحَّح إلى ما قِيس فعلاً.

**الإثبات الحاويّ** (🔐 كان يحتاج إذناً — ✅ **أُذن به وسُدِّد في 2026‑08‑03، 03:47–03:57 UTC**، تفصيلُه في [§3.105](log/3.105.md) §5‑ج):

1. `WORKER=memory` ثمّ `WORKER=knowledge`، كلٌّ **≥ 60 ثانيةً بلا أيّ حركةٍ على مجراه**، و`RestartCount = 0`.
   ✅ **`memory`: 138 ثانية · `knowledge`: ≥ 5 دقائق · كلاهما `RestartCount = 0`** — و`knowledge` **لم يكن قد أُقلع حاويّاً ولا مرّةً قبل اليوم**.
2. **والـAPI — وهذا ما يقيس §1‑أ‑2 لأوّل مرّة:** عمليّةٌ تُترك **≥ 60 ثانيةً من الهدوء**، **ثمّ** يُنشر حدثٌ واحدٌ على `stream.knowledge` ⇒ يصل الإشعار إلى مقبسٍ متّصل، بلا إعادة تشغيلٍ للخدمة. **وقبل الإصلاح: لا يصل** — وهو الفرق بين استنتاجٍ من الشيفرة وقياسٍ حقيقيّ.
   ✅ **قِيس بالصورتين معاً** (‏`aizzak/app:prefix` محفوظةٌ قبل البناء): بعد **6 دقائق** من الهدوء قرأ الجسرُ المُصلَح الحدثَ وأقرّه (`entries-read = 1` · `pending = 0`) في كلتا عمليّتَي gunicorn؛ وعلى الصورة القديمة **`lag = 1` و`entries-read` فارغة** — لم يصل، والحاوية `healthy` وسجلُّها **بلا سطرِ خطأٍ واحد**. وسببُه مقيسٌ لا مستنتَج: `idle` للمستهلك **80 ثانيةً** بعد 82 ثانيةً من الإقلاع ⇒ مات في الثانية الثانية.
   ⚠️ **والوصلة الأخيرة لم تُقَس**: من المِحور إلى **مقبس ويب‑سوكِت متّصلٍ فعلاً** — تحتاج هويّةً حقيقيّةً من Firebase. المقيس أنّ الجسر قرأ وعالج وأقرّ بعد الهدوء، وهو الشقّ الذي كان يموت.
3. **وحارسُ الضجيج يُقاس هو الآخر (البند 5):** إيقافُ الخدمة إيقافاً نظيفاً لا يطبع سطرَ خطأٍ واحداً من النداء الراجع الجديد — وإلّا وُلد السطر ميتاً، لأنّ أوّل ما يفعله المشغّل بسطرٍ يظهر في كلّ إيقافٍ عاديّ هو أن يكفّ عن رؤيته.
   ✅ **صفر أسطر** `background_task_died` في السجلّ كلّه بعد إيقافٍ نظيفٍ لعمليّتَي gunicorn.

### الخطوة 2 · جدول الطوبولوجيا الواحد

**الملفّ الجديد:** `src/app/framework/events/topology.py` — بيانٌ نقيٌّ بلا I/O (طبقة الكِرنل، فيستورده كلٌّ من `workers/` و`ops/` بلا خرقِ عقد).

```python
@dataclass(frozen=True)
class ConsumerBinding:
    stream: str
    group: str

STATIC_CONSUMER_TOPOLOGY: tuple[ConsumerBinding, ...] = (...)   # الأزواج الأربعة، §1-ج
```

سَرْدُ الوحدة يذكر صراحةً **لماذا `cg.notify.<host>.<pid>` غائبة** وأنّ غيابها ليس نقصاً.

⚠️ **لا يجوز أن يستورد `framework/` من `modules/`** (عقود `.importlinter`)، فالأسماء الأربعة مكتوبةٌ هنا حرفيّاً **ومحروسةٌ باختبار** بدل أن تُشتقّ:

**الاختبار المانع للانحراف** (`tests/unit/test_stream_topology.py`) — على غرار `test_deploy_worker_default.py`:

1. كلّ `stream` في الجدول يساوي ثابت `STREAM` لوحدته (‏استيرادٌ من `app.modules.*.application.event_mapping`؛ اختبارٌ لا شيفرةَ إنتاج، فلا عقد مخروق).
2. مجموعة أزواج الجدول **تطابق تماماً** الأزواج التي تبنيها `build_knowledge_worker` و`build_media_worker` و`build_memory_worker` من `Subscription`ات — لا زائدَ ولا ناقص.
3. لا زوجَ في الجدول تبدأ مجموعته بـ`cg.notify`.

**برهان الفشل:** غيّر حرفاً في أحد الثوابت الأربعة ⇒ يسقط الاختبار مسمّياً الزوج المنحرف.

### الخطوة 3 · المُرحّل يضمن المجموعات قبل أوّل نشر

**التغيير:**

- `src/app/workers/bootstrap.py::build_relay_from_env` (‏السطر 223) يركّب زيادةً على ما يركّبه اليوم `RedisStreamsConsumer` فوق **عميل Redis نفسه** (لا اتّصالَ ثانٍ)، ويعيد إغلاقاً `ensure_topology()` يمرّ على `STATIC_CONSUMER_TOPOLOGY` مستدعياً `ensure_group` لكلّ زوج. التوقيع يصير:
  `tuple[OutboxRelay, EnsureTopology, list[Disposable]]`.
- `src/app/workers/outbox_relay.py::run` يسلسل: `await ensure_topology()` **قبل** `await relay.run_forever()`، داخل `try` القائم نفسه.

**لماذا هذا التقسيم:** سَرْدُ `outbox_relay.py` يعلن أنّه «يسلسل بناءً ← تشغيلاً ← هدماً» وأنّ **كلّ التركيب في `bootstrap.py`** — فالإغلاق يُبنى هناك ويُستدعى هنا، بلا استثناءٍ لأيٍّ من السَّرْدين.

⚠️ **والعميل يبقى على مهلته القصيرة `2.0` رغم أنّ ما يُركَّب فوقه اسمه «مستهلك»:** `ensure_group` نداءٌ لا يحجز، والمُرحّل لا يقرأ قراءةً حاجزةً في أيّ سطر. هذا هو الاستثناء المسمّى في قاعدة تغطية الخطوة 1، ويُذكر هنا ثانيةً لأنّ هذه الخطوة بالذات هي التي تخلق الالتباس.

**العمليّة عديمة الأثر عند التكرار أصلاً:** `ensure_group` يبتلع `BUSYGROUP` (‏`redis_streams.py:237`)، فإعادة تشغيل المُرحّل أربعُ نداءاتٍ لا أثرَ لها.

**اختبارات الوحدة** (`tests/unit/test_relay_topology_provisioning.py`):

1. مستهلكٌ زائفٌ يسجّل الترتيب ⇒ `ensure_group` للأزواج الأربعة **قبل** أوّل `publish`.
2. فشل `ensure_group` يمنع الإقلاع صراحةً (لا يبتلَع ولا يُنشر على طوبولوجيا نصف مُهيّأة).
3. عميل Redis المُمرَّر للمستهلك **هو نفسه** المُمرَّر للناشر (لا اتّصالَ إضافيّ).

**برهان الفشل:** احذف سطر `await ensure_topology()` ⇒ يسقط الاختبار 1.

### الخطوة 4 · الإثبات الحيّ

`tests/integration/test_relay_ensures_groups_live.py`، موسومٌ `live_redis` (يتخطّى نظيفاً بلا Redis):

1. مجرًى **جديدٌ تماماً** باسمٍ عشوائيّ + طوبولوجيا مُحقونةٌ بزوجٍ واحدٍ عليه.
2. `ensure_topology()` ← ثمّ `XADD` لثلاث مدخلات ← **ثمّ** ينضمّ المستهلك أوّل مرّة.
3. **التوقُّع: يقرأ الثلاث.**
4. **برهان الفشل** في الاختبار نفسه: مسارٌ ثانٍ يتخطّى `ensure_topology()` وينشر أوّلاً ثمّ ينشئ المجموعة ⇒ **صفر مدخلات** — العطب القديم بآليّته الحقيقيّة، مقيساً لا موصوفاً.

### الخطوة 5 · حذف القاعدة اليدويّة من سطح الوثائق

بعد نجاح 2–4 **فقط**. المواضع الحيّة (المصدر: مسحٌ على `العمّال قبل المُرحّل` + `boot-order`):

| الملفّ | الموضع | الإجراء |
|---|---|---|
| `docker-compose.yml` | تعليق `outbox-relay` (‏«starts AFTER the workers») + سَرْد قاعدة `$` | يُستبدَل بالضمان الجديد وموضعه |
| [`quickstart.md`](quickstart.md) | §4 التحذير · §10 القاعدة الذهبيّة **4** | تُحذف القاعدة 4 ويُعاد ترقيم ما بعدها |
| [`stack-commands.md`](stack-commands.md) | §17 · §ج · القواعد الذهبيّة **4** | المثل |
| [`design/08-local-runbook.md`](design/08-local-runbook.md) §4 | **المرجع المُلزِم** | يُعدَّل هنا **أوّلاً**، والبقيّة تتبعه |
| [`deploy-linux-server.md`](deploy-linux-server.md) · [`deploy-runpod.md`](deploy-runpod.md) | خطوات الإقلاع | المثل |
| [`design/04-event-catalog.md`](design/04-event-catalog.md) §2 | جدول الطوبولوجيا | تُضاف إشارةٌ إلى `topology.py` بوصفه التمثيل التنفيذيّ |
| `deploy/runpod/bootstrap.sh` | تعليق §7 (`⚠️ WORKERS BEFORE THE RELAY, and this is not stylistic`) | **لم يكن في الجدول أعلاه — مسحٌ أوسع بالوجه الإنجليزيّ (`WORKERS BEFORE THE RELAY`/`boot-order`) وجده لأنّ المسح العربيّ وحده لا يصيب ملفّاً إنجليزيّاً.** تعليقٌ فقط؛ الأسطر التنفيذيّة (`start worker` → `sleep 3` → `start outbox-relay`) لم تُمَسّ حرفاً واحداً — تغيير ترتيب إقلاعٍ حيّ خارج نطاق خطوةٍ توثيقيّة |

⚠️ `docs/log/*` و`implementation-status.md`/`implementation-plan.md` **سجلّاتٌ تاريخيّة**: لا تُعدَّل، ولو ذكرت القاعدة.

### الخطوة 6 · الإثبات الحاويّ ومدخلة السجلّ

🔐 **يحتاج إذناً صريحاً** — يغيّر حالة النظام الحيّ.

```bash
docker compose up -d --build outbox-relay
```

ثمّ — **بالترتيب المحظور قديماً عمداً**: المُرحّل يعمل مسبقاً، ويُقلَع العامل بعده على مجرًى جديد، ويُتحقَّق من وصول الحدث. ثمّ مدخلة [`docs/log/3.110.md`](log/3.110.md) وربطها في `INDEX.md` و`CHANGELOG.md`.

> ⚠️ **تصحيحُ إشارةٍ ضمن هذه الخطوة نفسها:** كان مكتوباً هنا «ثمّ مدخلة `docs/log/3.105.md`» — وهو **رقمٌ قديمٌ سهواً** (‏3.105 مدخلةُ الخطوة **1**، كُتبت قبل أن تُستهلَك أربعةُ أرقامٍ في الخطوات 2–5). الرقم التالي الصحيح وقت التنفيذ هو **3.110**، وقد صُحِّح أعلاه. مُسجَّلٌ في [§3.110](log/3.110.md).

✅ **سُدِّد في 2026‑08‑03، 13:07–13:16 UTC** ([§3.110](log/3.110.md)) — ثلاثة قياساتٍ محوريّة بنصوصها الحرفيّة على مجرًى جديدٍ حقّاً: **`cg.memory` موجودةٌ بـ`consumers = 0` والمُرحّل وحده يعمل ولا `worker` في الوجود** (‏`XLEN = 0`) ⇒ المجموعة وُلدت من المُرحّل قبل أن ينشر · ثمّ **`XLEN = 1` و`lag = 1` و`entries-read` فارغة** بعد النشر وقبل وجود أيّ مستهلك · ثمّ **`entries-read = 1` و`lag = 0` و`last-delivered-id` = مُعرِّف المدخلة نفسها** بعد إقلاع العامل أخيراً بـ`RestartCount = 0`. وبرهانُ الفشل الحاويّ على `aizzak/app:prefix` نجح كاملاً: المُرحّل القديم نشر و**`XINFO GROUPS` فارغةٌ بلا `cg.memory` إطلاقاً**، والعامل القديم أنشأها بعده عند `$` فبقيت **`entries-read` فارغةً و`lag = 0` مع `XLEN = 1`** — المدخلة في المجرى ولن تصل المستهلك أبداً، حتّى بعد ثمانِ إعادات تشغيلٍ للعامل (عطب §1‑أ‑1، تأكيدٌ جانبيّ).

---

## 4. معيار القبول

1. البوّابات الخمس خضراء بعد كلّ خطوة.
2. **عاملٌ على مجرًى هادئ ≥ 60 ثانيةً و`RestartCount = 0`** (الخطوة 1) — المعيار الذي لم يُقَس في هذا المشروع ولا مرّة.
3. **إشعارٌ فوريٌّ يصل بعد ≥ 60 ثانيةً من هدوء عمليّة API لم تُعَد تشغيلها** (الخطوة 1) — ولم يُقَس هو الآخر قطّ.
4. **عميل ذاكرة تخزين الـAPI وعميل المُرحّل ما زالا يفشلان عند ثانيتين**، و**مهلة الاتّصال `2.0` في المواضع الخمسة كلّها**: الإصلاح لم يُشترَ بإبطاء مسار المستخدم، ولا عُمّم على من لا ينتظر.
5. **موتُ مهمّةِ خلفيّةٍ في الـAPI يترك سطراً، وإغلاقُها النظيف لا يترك شيئاً** (الخطوة 1، البند 5) — رؤيةٌ بلا ضجيج.
6. اختبار الخطوة 4 **يسقط** على `814cb59` و**ينجح** بعد الخطوة 3.
7. اختبار الانحراف يسقط عند تغيير أيّ حرفٍ في أيٍّ من الأسماء الثمانية.
8. لا سطرَ واحدٍ تغيّر في طوبولوجيا `docker-compose.yml`: `profiles: ["workers"]` قائم، ولا حافّة `depends_on` جديدة.
9. `grep -rn "العمّال قبل المُرحّل" docs/` لا يصيب إلّا `docs/log/`.
10. **لم تلد هذه الخطّة تنبيهاً ولا عتبةً ولا لوحةَ مراقبة، ولا سطرَ إعادةِ تشغيلٍ لمهمّةِ خلفيّة**: أُضيفت الرؤية وحدها، ويُقرَّر ما يستحقّ الإنذار بعد أن يُرى.

---

## 5. سجلّ التنفيذ

| الخطوة | التاريخ | الالتزام | السجلّ | البوّابات |
|---|---|---|---|---|
| 1 | 2026‑08‑03 | `fee9dd2` (‏شجرةٌ عاملة، غير مُلتزَمة) | [§3.105](log/3.105.md) | ruff **532** نظيف · mypy **357** · lint-imports **8/0** · pytest **2472 ناجح · 0 فاشل · 51 متخطًّى** (2523) · ✅ **حاويّاً**: `memory` 138ث و`knowledge` ≥5د بـ`RestartCount = 0` · الجسر يقرأ بعد 6 دقائق هدوء (وقبل الإصلاح `lag = 1`) · صفر أسطر عند الإيقاف النظيف |
| 2 | 2026‑08‑03 | شجرةٌ عاملة، غير مُلتزَمة | [§3.106](log/3.106.md) | ruff **534** نظيف (+2) · mypy **358** (+1) · lint-imports **8/0** · pytest **2476 ناجح · 0 فاشل · 51 متخطًّى** (2527 = 2523 +4) · برهان الفشل مُشغَّلٌ فعلاً مرّتين (اسم مجموعة ثمّ اسم مجرًى)، كلٌّ يسمّي الزوج المنحرف بنصّه ويعود أخضر بعد إعادة الحرف |
| 3 | 2026‑08‑03 | شجرةٌ عاملة، غير مُلتزَمة | [§3.107](log/3.107.md) | ruff **535** نظيف (+1) · mypy **358** (بلا تغيير) · lint-imports **8/0** · pytest **2479 ناجح · 0 فاشل · 51 متخطًّى** (2530 = 2527 +3) · برهان الفشل مُشغَّلٌ فعلاً: حذف `await ensure_topology()` أسقط اختبارَي الترتيب والفشل (`assert 0 == 4` / `DID NOT RAISE AppError`)، وإعادة السطر أعادت الثلاثة للنجاح |
| 4 | 2026‑08‑03 | شجرةٌ عاملة، غير مُلتزَمة | [§3.108](log/3.108.md) | ruff **536** نظيف (+1) · mypy **358** (بلا تغيير) · lint-imports **8/0** (410 ملفّاً · 1719 تبعيّة) · pytest **2471 ناجح · 0 فاشل · 61 متخطًّى** (2532 = 2530 +2؛ الفرق 10/10 بين النجاح والتخطّي بيئيٌّ — زحزحة نموذج Ollama المسحوب محلّيّاً، لا صلة له بهذه الخطوة) · برهان الفشل مُشغَّلٌ فعلاً على Redis حيٍّ: نزع جسم `ensure_topology()` مؤقّتاً في `bootstrap.py` (نسخةٌ احتياطيّةٌ أُعيدت وتحقَّق `diff` صامتٌ منها) أسقط الاختبارَين معاً — بآليّةٍ أقسى ممّا توقّعته الخطّة (`NOGROUP`/`AppError` لا «صفر مدخلاتٍ» فحسب، لأنّ المجموعة لم تُنشأ إطلاقاً)، وإعادة الملفّ أعادت الاختبارَين للنجاح · **وأعاد المُنسِّق البوّابات الخمس وبرهانَ الفشل بنفسه** (§0): نفس الإجمال **2532** بانقسام **2472/60**، والفرقُ عن رقم الوكيل واحدٌ يتذبذب بين تشغيلتين بحسب توفّر خدمةٍ خارجيّة — الإجمال هو الثابت، وقد طابق `--collect-only` (2532) رقماً برقم |
| 5 | 2026‑08‑03 | شجرةٌ عاملة، غير مُلتزَمة | [§3.109](log/3.109.md) | ruff **536** (بلا تغيير — توثيقٌ خارج نطاق `ruff`) · mypy **358** (بلا تغيير) · lint-imports **8/0** (بلا تغيير) · pytest **2471 ناجح · 0 فاشل · 61 متخطًّى** (2532، بلا تغيير — لا اختبار أُضيف أو حُذف) · **لا برهان فشلٍ سلوكيّاً** (لا سطر منطقٍ تغيّر؛ الترخيص بنجاح 2–4 وحده، مُثبَتٌ فيهما) · معيار القبول 9 قِيس صادقاً قبل/بعد ومُسجَّلٌ حرفيّاً في [§3.109](log/3.109.md) §3 — الإصابات الباقية خارج `docs/log/` نصٌّ عن الحذف في هذه الخطّة نفسها أو سجلّان تاريخيّان ممنوعان · **وراجع المُنسِّق الفرق وأعاد البوّابات الخمس بنفسه** (§0): ruff **536** · mypy **358** · lint-imports **8/0** · pytest **2471 ناجح · 0 فاشل · 61 متخطًّى** (2532، خروج 0) — مطابقٌ لرقم الوكيل حرفاً، والإجمال 2532 ثابتٌ منذ الخطوة 4 · **ووجدت المراجعة ثلاثة مواضعَ ناجيةٍ بصياغةٍ ثالثة** لم يُصبها أيٌّ من المسحَين: «**وترتيب الإقلاع**» سبباً تشخيصيّاً في جداول الأعطال بـ`design/08-local-runbook.md:438` و`quickstart.md:288` و`deploy-linux-server.md:628` — أُصلحت وسُجِّلت في [§3.109](log/3.109.md) §8، ودرسُها أنّ **مسحَ عبارةٍ ليس مسحَ فكرة**: خضرةُ المعيار 9 وحدها كانت ستُخفيها |
| 6 | 2026‑08‑03 | شجرةٌ عاملة، غير مُلتزَمة (الرأس `65855c7`) | [§3.110](log/3.110.md) | ruff **536** (بلا تغيير — قياسٌ وتوثيقٌ خالص) · mypy **358** (بلا تغيير) · lint-imports **8/0** (بلا تغيير) · pytest **2471 ناجح · 0 فاشل · 61 متخطًّى** (**2532**، بلا تغيير — لا اختبار أُضيف أو حُذف)، مُتحقَّقٌ **بثلاث طرقٍ متطابقة**: `--collect-only` (2532) · شريط التقدّم بالنمط الصارم (2471 نقطة + 61 `s`) · **وسطر الملخّص الختاميّ طُبع فعلاً هذه المرّة** (`2471 passed, 61 skipped in 119.11s`) خلافاً لعادة هذه البيئة المسجّلة في §3.105/§3.106 · 🔐 **إثباتٌ حاويٌّ مأذونٌ صراحةً بالترتيب المحظور قديماً**: (1) المُرحّل وحده يعمل على مجرًى عمره دقيقة، `XLEN = 0` ومع ذلك **`cg.memory` موجودةٌ بـ`consumers = 0`** و`docker compose ps` بلا `worker` إطلاقاً ⇒ المجموعة من المُرحّل لا من العامل؛ (2) بعد نشر حدثٍ واحد — `XLEN = 1` · `published_at` مختومة · **`lag = 1` و`entries-read` فارغة** ولا مستهلكَ في الوجود؛ (3) بعد إقلاع العامل أخيراً — **`entries-read = 1` · `lag = 0` · `last-delivered-id` = مُعرِّف المدخلة نفسها** · `RestartCount = 0`؛ والعامل قرأ ثمّ رفض إلى `stream.memory.dlq` بـ`NotFoundError: memory item not found` بعد 5 محاولات — **إثبات وصولٍ كامل**، والسببُ صفُّ الاختبار اليتيم مرجعيّاً لا الطوبولوجيا · **وبرهانُ الفشل الحاويّ على `aizzak/app:prefix` نجح كاملاً**: المُرحّل القديم مرّت عليه 12ث و`XINFO GROUPS ⇒ ERR no such key`، ثمّ نشر ⇒ `XLEN = 1` و**`XINFO GROUPS` فارغةٌ بلا `cg.memory` إطلاقاً**؛ والعامل القديم أنشأها بعده عند `$` فصار `last-delivered-id` = المدخلة نفسها مُتخطّاةً و**`entries-read` فارغة** و`lag = 0` مع `XLEN = 1` ⇒ لن تصل أبداً، **حتّى بعد 8 إعادات تشغيلٍ** بـ`AppError: event consume failed` (‏`redis_streams.py:338` — عطب §1‑أ‑1، تأكيدٌ جانبيّ) · **الإعادة**: الوسم إلى `dev` وإعادةُ إنشاء الحاوية على صورة الرأس (صفر تتبّعات) ثمّ `docker compose down` **بلا `-v`**، والأحجام الستّة كلّها قائمة؛ `stream.files` (1) و`stream.knowledge` (2) **مطابقان لخطّ الأساس مجموعةً مجموعة**، و`stream.memory` عاد إلى حاله الابتدائيّ بالضبط · ⚠️ **فرقان مُبلَّغان**: `stream.media` اكتسب `cg.media` (أثرٌ **مقصودٌ** للقرار الفرعيّ في §2، وقع عند أوّل أمرٍ تفرضه الخطوة، أثرُه البيانيّ صفر، ولا تراجعَ عنه بلا `XGROUP DESTROY` المحظور) و`stream.memory.dlq` مفتاحٌ جديدٌ بمدخلةٍ واحدة أنشأه العامل · ⚠️ **وثغرةٌ حقيقيّةٌ كُشفت ولم تُصلَح** (معيار القبول 8): `up -d --build outbox-relay` لا يجرّ `pgbouncer` ⇒ §6‑هـ · **لم يُمَسّ**: `src/`/`tests/` · `docker-compose.yml` (‏`git diff` **فارغ حرفيّاً**) · `redis_streams.py:235` · السجلّ التاريخيّ (صفر حذف) |

---

## 6. ملاحظاتٌ خارج النطاق

### 6‑أ · ✅ تقسيم `worker` إلى ثلاث خدمات — **نُفِّذ في [§3.133](log/3.133.md)** (2026‑08‑13)

> 📌 **مُغلَق.** النصّ الأصليّ يبقى كما كُتب، والخاتمة تحته.

خدمة `worker` واحدةٌ يختار عاملَها `WORKER: ${WORKER:-memory}`، ونسخةٌ واحدةٌ ⇒ **عاملٌ واحدٌ فقط ممكنٌ في المرّة** رغم أنّ تصميمها يقول «ثلاثة مستهلكين، كلٌّ عمليّتُه». قيدٌ حقيقيٌّ يجب أن يزول قبل تشغيلٍ جادّ، **وشرطُه أوّلُ إقلاعٍ حاويٍّ مقيسٍ لـ`knowledge`** — لا هذه الخطّة. وبعد إغلاقها يصير التقسيم قراراً تشغيليّاً بحتاً: لم يعد يحمل معه ضمان ترتيبٍ يخصّ الصحّة.

**الخاتمة.** الشرط المُسمّى تحقّق في [§3.105](log/3.105.md) §5‑ج (‏`knowledge`: ≥ 5 دقائق، `RestartCount = 0`، أوّل إقلاعٍ حاويٍّ له في تاريخ المشروع)، فنُفِّذ التقسيم: `worker-memory` · `worker-knowledge` · `worker-media`، كلٌّ بقيمة `WORKER` **حرفيّة**. والأوّلان بلا بروفايل ⇒ داخل `docker compose up -d` الافتراضيّ؛ و`worker-media` وحده بقي خلف `profiles: ["workers"]` **لأنّه لم يُقلَع كعمليّةٍ ولا مرّة** — وإقلاعٌ نظيفٌ واحدٌ مقيس هو كلّ ما لزم لحذف سطرَيه. القاعدة كلّها صارت مفروضةً في `tests/unit/test_deploy_worker_default.py`، لا موصوفةً هنا فقط.

**وذلك الإقلاع تمّ في اليوم نفسه** ([§3.134](log/3.134.md)): 45 دقيقةً و46 ثانية، `RestartCount = 0`، صفر سطر خطأ، ومستهلكٌ **مسجَّل** على `cg.media` بـ`pending 0` و`idle` ≈ 5 ث. فحُذف البروفايل، **ولم يبقَ في `docker-compose.yml` مفتاح `profiles:` واحد**. البند مُغلَقٌ نهائيّاً: الطوبولوجيا التي وصفها §2 من `08` — ثلاثة مستهلكين كلٌّ في عمليّته، كلّهم بأمرٍ واحد — قائمةٌ فعلاً لا على الورق.

### 6‑ب · `stream_prefix` إعدادٌ ميت

`EventSettings.stream_prefix` (‏`settings.py:178`) مربوطٌ بـ`EVENT_STREAM_PREFIX` (‏`env_settings.py:102,163`) و**لا يقرؤه أيّ سطرٍ في `src/`**: أسماء المجاري ثوابتُ حرفيّةٌ في أربعة `event_mapping.py`. فتغييره في `.env` اليوم **لا أثر له**، ولو قُرئ لاحقاً في مكانٍ واحدٍ دون البقيّة لأنتج مُرحّلاً ينشر على مجرًى وعمّالاً يستمعون لآخر. إمّا يُوصَل في مكانٍ واحدٍ (الطوبولوجيا مشتقّةٌ منه) أو يُحذف من الثلاثة. **خارج نطاق هذه الخطّة، ويجب ألّا يُخلط بها.**

### 6‑ج · `XACK` لا يحذف

لا تلمسه هذه الخطّة. `STREAM_MAXLEN` يبقى السقف الوحيد، ومراقبة `XLEN` تبقى واجبةً — وقد تصير المتأخّرات **أوضح** بعد هذه الخطّة لا أخفى (‏§2، القرار الفرعيّ).

### 6‑د · مهامّ خلفيّة الـAPI بلا **إدارة** — ما يبقى بعد أن تُفتَح العين

`_make_lifespan` (‏`api/main.py:240`) ينشئ مهامّ `background=` ولا يراقب أيّاً منها: مهمّةٌ تموت باستثناء **(أ)** لا تترك سطراً · **(ب)** لا تُعيد نفسها · **(ج)** لا تُطفئ `ready` · **(د)** لا تُسقط العمليّة.

**البند (أ) وحده يسقط في الخطوة 1** (البند 5): صار الموت يترك سطراً. والثلاثة الباقية بحالها — والفرق أنّ العطب بعدها **مرئيٌّ غير مُدار** بدل أن يكون غير مرئيٍّ أصلاً. والخطوة 1 تزيل كذلك **السببَ** الوحيد المعروف اليوم لموت جسر الإشعارات (§1‑أ‑2)، لا كلّ سببٍ ممكن: انقطاع Redis طويل أو عطبٌ في معالجٍ ما سيقتل المهمّة كما كان — لكنّه لن يقتلها في الظلام.

⇒ **الترتيب مقصود: يُرى أوّلاً، ثمّ يُقرَّر ما يُدار.** الإصلاح البنيويّ — مراقبٌ يعيد التشغيل بتراجعٍ أُسّيّ، أو يُسقط `ready` فتراه المِجَسّات، أو يُنهي العمليّة لتتكفّل بها إعادةُ تشغيل الحاوية — **خارج نطاق هذه الخطّة ويجب ألّا يُخلط بها**، وهو المرشّح الأوّل لما بعدها. لكنّه يُبنى حينها على ما أظهره السطر الجديد فعلاً: **ما الذي يموت، وكم مرّة، ولأيّ سبب** — لا على تخمينٍ سابقٍ للرؤية. مراقبٌ يُصمَّم قبل أن يُرى ما يستحقّ المراقبة يحرس ما لم يقع ويعمى عمّا يقع.

### 6‑هـ · ✅ `outbox-relay` لا يعتمد على `pgbouncer` رغم أنّه يتّصل به — **مكتشفٌ حاويّاً في الخطوة 6 ولم يُصلَح فيها · وسُدَّ بعدها في [§3.111](log/3.111.md)**

> 📌 **اقرأ الخاتمة في آخر هذا القسم قبل أن تبني على نصّه**: الملاحظة أدناه مكتوبةٌ كما كُتبت يوم الاكتشاف — بما فيها **جملةٌ تبيَّن خطؤها**.

`depends_on` لخدمة `outbox-relay` تحمل **`redis` و`migrate` وحدهما**، بينما `DATABASE_URL` لها هي `postgresql+asyncpg://outbox_relay:...@pgbouncer:6432/...`. فأمرُ الخطوة 6 نفسه — `docker compose up -d --build outbox-relay` — يُقلع `postgres` و`migrate` و`redis` **ولا يُقلع `pgbouncer`**، فيدور المُرحّل في حلقةٍ:

```
socket.gaierror: [Errno -3] Temporary failure in name resolution
```

مقيسٌ حرفيّاً في [§3.110](log/3.110.md) §3‑ب. وهو عطبٌ **سابقٌ لهذه الخطّة كلّها** ولا صلةَ له بأيٍّ من خطواتها الستّ (‏`migrate` يتّصل بـ`postgres` مباشرةً فلا يكشفه، و`app`/`worker` لهما شبكاتُ تبعيّاتٍ أوسع تجرّ `pgbouncer` عرضاً).

**ولم يُصلَح عمداً — احتراماً لمعيار القبول 8 حرفيّاً** («لا سطرَ واحدٍ تغيّر في طوبولوجيا `docker-compose.yml`»)، ولأنّ خطوةً غايتها **قياسُ** حالٍ قائمة لا يجوز أن تغيّر تلك الحال في أثناء قياسها. العلاج المؤقّت في تلك الجلسة كان أمراً واحداً إضافيّاً (`docker compose up -d pgbouncer`)، والعلاج الدائم — إضافةُ `pgbouncer: {condition: service_started}` إلى `depends_on` — **قرارٌ في طوبولوجيا الإقلاع يحتاج خطّته الخاصّة**، إذ يمسّ ترتيب إقلاعٍ حيٍّ في مسار Compose ويجب أن يُقارَن بمسار RunPod (‏`supervisord`) قبل تثبيته.

⚠️ **ولا يُخلط بينه وبين ما أصلحته هذه الخطّة**: هذا عطبُ **إقلاعٍ صاخب** — يفشل فوراً ويطبع تتبّعاً في كلّ دورة، فيراه أوّلُ من ينظر إلى `docker compose logs`. أمّا ما أصلحته الخطّة فكان عطبَ **صمت**: حدثٌ يُنشر بنجاحٍ ولا يصل أحداً، بلا سطرٍ واحد. الأوّل مزعجٌ ومرئيّ؛ الثاني هو الخطير.

#### 6‑هـ‑1 · ✅ الخاتمة — **سُدَّ في [§3.111](log/3.111.md)** (2026‑08‑03، بإذنٍ صريح)

أُضيفت `pgbouncer: {condition: service_started}` إلى `depends_on`، منسوخةً من `app` حرفيّاً. ونصّ الملاحظة أعلاه **لم يُعَد كتابتُه** — بل يصحّحه ما يلي:

- ⚠️ **الثقب كان خدمتين لا واحدة.** قيل أعلاه إنّ «`app`/`worker` لهما شبكاتُ تبعيّاتٍ أوسع تجرّ `pgbouncer` عرضاً» — **والنصفان خاطئان**: `app` تُعلنه **صراحةً** منذ الطور الأوّل، و`worker` لا تجرّه لا صراحةً ولا عرضاً. مقيسٌ بمحاكاةٍ على الملفّ قبل الإصلاح حرفيّاً: `grep -c pgbouncer` ⇒ **0**. فالطريقان الموصى بهما في [`08 §4`](design/08-local-runbook.md) كانا معطوبين معاً.
- ✅ **والشرط الذي وضعه هذا القسم على نفسه** («قرارٌ يمسّ ترتيب إقلاعٍ حيٍّ ويجب أن يُقارَن بمسار RunPod قبل تثبيته») **استُوفي**: مسار RunPod لا يعرف `pgbouncer` إطلاقاً — `127.0.0.1:5432` مباشرةً — فلا `supervisord` مسّه شيء. ⇒ لم يكن قراراً يحتاج خطّةً كاملة، بل شرطاً واحداً تحقّق فسقط ما بُني عليه.
- 🔒 **وما بقي مفتوحاً بعده مُسمًّى**: `service_started` تعني «بدأت» لا «تقبل اتّصالاً»؛ رفعُها إلى `service_healthy` يحتاج فحصَ صحّةٍ للمُجمِّع ويمسّ ثلاث خدمات — **تُرك عمداً** قراراً مستقلّاً لا رديفاً صامتاً لسدّ ثقب.

</div>
