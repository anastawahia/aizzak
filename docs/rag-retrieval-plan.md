<div dir="rtl">

# خطّة تقوية RAG — النصف الثاني: الاسترجاع

> **RAG Retrieval Plan** · تصنيف النيّة · الاسترجاع · الفلترة والإجابة النهائية
>
> **القاعدة الحاكمة:** يُنقَل من alpha **الخوارزميات وقرارات المعايرة والتعليلات** — لا البنية. وأخصّ ما في هذا النصف أن **أرقام alpha لا تُنسَخ**: هي معايَرة لمسافة L2 (‏الأدنى أقرب)، و AIZZAK على جيب التمام (‏الأعلى أفضل) وعلى مقياس RRF — الاتّجاه ينقلب والمقياس يختلف.

| | |
|---|---|
| **أُنشئت** | 2026‑08‑19 |
| **الأساس** | [`rag-pipeline-alpha-vs-aizzak.md`](rag-pipeline-alpha-vs-aizzak.md) · [`rag-gaps-and-ports.md`](rag-gaps-and-ports.md) · [`rag-open-decisions.md`](rag-open-decisions.md) (‏٢٦/٢٦ مُجابة) |
| **النطاق** | المرحلتان **٤ · ٥** (النيّة · الاسترجاع · الفلترة والإجابة) **+ `P-04`** (حلّ اسم الملف — خوارزمية وقت-استعلام) |
| **خارج النطاق** | المراحل **١ · ٢ · ٣ · ٦** — كلّها في [`rag-indexing-plan.md`](rag-indexing-plan.md) |
| **البنود** | **٢١ بندًا**: `P-04` · `P-21`..`P-30` · `P-31`..`P-40` |
| **الحالة** | ⬜ **12/21** — قيد التنفيذ |

---

## 0. قاعدة التنفيذ

مخفَّفة بقرار **س-02 = ب**: البوّابات الخمس وجدول الحالة يبقيان؛ **لا مدخلة سجلّ في `docs/log/` ولا برهان فشل على الحالة القديمة**.

**خطوة واحدة في كلّ مرّة، ثمّ توقّف.** كلّ خطوة ملزَمة بـ:

1. تنفيذ **مهمّتها وحدها** — ما خرج عنها يُسجَّل في §7 ولا يُنفَّذ.
2. **البوّابات الخمس** قبل إعلان الانتهاء:
   `ruff format --check` · `ruff check` · `mypy src` · `lint-imports` · `pytest`
3. تحديث صفّها في §4.

---

## 1. القرارات المُقفلة

أُقرّت في [`rag-open-decisions.md`](rag-open-decisions.md) بتاريخ 2026‑08‑19 ولا تُعاد مناقشتها داخل هذه الخطّة.

| # | السؤال | القرار |
|---|---|---|
| س-01 | خطّ التقسيم | **`P-04` هنا** · التلخيص كلّه (‏`P-41`..`P-45`) في خطّة الفهرسة |
| س-16 | أين يعيش توجيه النيّة | **حالة استخدام موجِّهة داخل `knowledge/application`** — تُصنّف ثمّ تُرسل إلى `RetrieveContext` أو `RequestSummary`؛ الوكيل يستدعي بذرة واحدة ولا يستورد شيئًا |
| س-17 | ما الذي يُعدّ سؤال تلخيص | **أفعال الأمر بلا شرط + الأسماء بشرط**: «لخّص/summarize» تكفي وحدها · «ملخّص/summary» تحتاج المفعولية |
| س-18 | حين لا يُحَلّ اسم الملف | **سؤال توضيحي نصّي** يبثّه الوكيل كإجابة عادية — بلا نوع حدث جديد |
| س-19 | عقد القطعة المسترجَعة | **توسيع الطبقات الثلاث بحقول صريحة**: `file_name` · `page_number` · `section` |
| س-20 | أين يُنفَّذ MMR | **خوارزمية نقيّة في `domain/`** فوق `search_k` موسَّع — بـ`with_vectors=True` |
| س-21 | مُعيد الترتيب | **منفذ `RerankProvider` + محوّل HTTP**، **مع تفعيل وإيقاف**: يُفعَّل عند الحاجة إلى دقّة أعلى، وثمنه وقت أطول |
| س-22 | معايرة العتبات | **البوّابة على «لا نتائج» فقط، بلا عتبة رقمية** — العتبات تبقى `0.0` حتى تتوفّر مجموعة تقييم |
| س-23 | وعي الكوربوس | **الرأس دائمًا + في مسار الاحتياط أيضًا** |
| س-24 | الضبط وقت التشغيل | **`Settings` فقط** — تُمرَّر كوسائط إلى الدومين؛ لا تجاوزات لكلّ طلب |
| س-25 | تسجيل المراحل و retrieve-only | **سجلّ بنيويّ فقط** (`get_logger` بحقول المراحل) — **بلا تغيير عقد** |

**الأثر المباشر لثلاثة من هذه القرارات على حجم البنود:**

| البند | كان في `rag-gaps-and-ports.md` | صار بعد الجواب |
|---|---|---|
| `P-30` | `Settings` **+ تجاوزات لكلّ طلب** | `Settings` وحدها (س-24) |
| `P-40` | `k` قابل للضبط **لكلّ طلب** | `_TOP_K` ⇒ قيمة في `Settings` (س-24) |
| `P-39` | `context_text` و`stages` **في الاستجابة** | قدرة **داخليّة** + قياسات في السجلّ البنيويّ (س-25) |
| `P-38` | معايرة العتبات على مقياس RRF | **مؤجَّل** — يحتاج مجموعة تقييم غير موجودة (س-22) ⇒ §7 |

---

## 2. ما يحكم هذا النصف — حقائق مُتحقَّقة

| # | الحقيقة | الدليل | الأثر على الخطّة |
|---|---|---|---|
| ح‑٩ | `RetrievedChunk` أربعة حقول (`document_id, chunk_id, text, score`) و**1:1 مع `RetrievedChunkOut`** في العقد الملزِم | `knowledge/ports/retrieval.py:14` | الخطوة ١ تغيّر **عقدًا عامًّا** لا شيفرة داخلية |
| ح‑١٠ | `RetrievedChunkView` بروتوكول بأربع خصائص للقراءة في بذرة الإطار | `framework/agent_runtime/deps_ports.py:51-73` | التوسيع يمسّ **ثلاث طبقات** متزامنة + `openapi.yaml` |
| ح‑١١ | عقد `layers`: `api → agents → modules → framework` ⇒ الوكيل **يجوز** له استيراد وحدة، لكن `rag_agent` يعلن أنه لا يستورد شيئًا ويصل عبر `self.deps` | `.importlinter:13-30` · `agents/rag_agent/agent.py` | التوجيه قرار تصميم لا قرار مُدقِّق ⇒ س-16 = أ |
| ح‑١٣ | `_build_filter` يبني `must` + `MatchValue`/`MatchAny` فقط | `qdrant_store.py:180` | تقييد النطاق بملفّ يعمل **على جانب Qdrant** بلا مضاعِف جلب FAISS |
| ح‑١٦ | `_CITATION_KEYS` بلا `file_name` | `application/indexing.py:53` | **الخطوة ١ محجوزة على خطوة الفهرسة ١١** (§3.1) |
| ح‑١٧ | `filter_relevant(min_score=0.0, relative_floor=0.0, jaccard_threshold=0.95)` — العتبتان معطَّلتان، و Jaccard يعمل على مخرَج `tokenize` لا `.split()` | `domain/relevance.py:35,51-73` | `0.95` **ثابت مستقلّ عن المقياس** ⇒ ينجو من س-22؛ العتبتان تبقيان `0.0` |
| ح‑١٨ | `domain/intent.py` مبنيّ ومختبَر لكنّه **لا يُستورَد من أيّ مسار حيّ** | `grep` على `classify_intent` | البند `P-21` **وصلٌ** لا بناء |

---

## 3. التصميم

### 3.1 توسيع عقد القطعة — ثلاث طبقات متزامنة (‏`P-18` `P-31` `P-32` + س-19)

```
knowledge/ports/retrieval.py       RetrievedChunk      + file_name, page_number, section
framework/agent_runtime/           RetrievedChunkView  + ثلاث خصائص للقراءة
  deps_ports.py                                          (خصائص لا تعليقات — الحاملات frozen)
api  ·  03-api-spec.md §2  ·       RetrievedChunkOut   + ثلاثة حقول + اختبار مطابقة العقد
  openapi.yaml
```

⚠️ **محجوزة على خطّة الفهرسة.** الحقول تُقرأ من حمولة Qdrant، وهي لا تحملها اليوم (ح‑١٦). **الخطوة ١ هنا لا تبدأ قبل الخطوة ١١ في [`rag-indexing-plan.md`](rag-indexing-plan.md) وقبل إعادة البناء في §5 هناك.** بدون ذلك تُشحَن ثلاثة حقول قيمتها `None` دائمًا.

**لماذا حقول صريحة لا `metadata: Mapping`:** المشروع على `mypy --strict`، والواجهة يجب ألّا تخمّن المفاتيح. الثمن المُعلَن: كلّ مفتاح استشهاد جديد لاحقًا يحتاج تعديل عقد.

### 3.2 تسمية المصدر — مصدر واحد للحقيقة (‏`P-31` `P-32`)

```
[maintenance.pdf p.12 | section: المسؤوليات]
<نصّ القطعة>
```

**وحدة تنسيق واحدة** يتقاسمها مسار التوليف ومسار `context_text` الداخليّ (‏`P-39`) — لا صيغتان تنحرفان.

**التحذير المنقول من alpha حرفيًّا:** التسميات تُضاف **عند العرض لا عند الفهرسة**. حقن الميتاداتا في نصّ العقدة يلوّث التضمين و IDF معًا: كلّ عقد الملفّ تتقاسم الرموز نفسها فتنهار IDF، ويُضاف انحياز ثابت للمتّجهات.

**الاستشهادات (‏`P-32`):** `{document_id, file_name, page, chunk_id}` بدل UUID عارٍ. حدث الوكيل النهائيّ يحمل الشكل الجديد.

### 3.3 بوّابة الثقة والاحتياط الأمين (‏`P-28` `P-33` + س-22)

**أخطر فجوة في الملفّ كلّه:** اليوم `if chunks:` وإلّا يمضي المسار بـ`SYSTEM_PROMPT` وحده — أي أن **النموذج يجيب من معرفته الخاصّة وكأنها من مستندات المستخدم**.

```
نتائج الاسترجاع
├─ صفر قطع  ──────────────► احتياط أمين: «لا أملك معلومات كافية»
│                            + رأس وعي الكوربوس (§3.5) في المسار نفسه
└─ قطع موجودة ────────────► التوليف الطبيعيّ
```

**البوّابة الرقمية مؤجَّلة بقرار س-22 = أ:** لا عتبة على «أفضل درجة كثيفة» ولا على السَّبارس. `min_score` و`relative_floor` يبقيان `0.0`.

**`P-28` يبقى داخل النطاق رغم ذلك** — إشارتا الثقة (أفضل درجة كثيفة وأفضل درجة سَبارس) تُعادان مع النتيجة **لأن السجلّ البنيويّ يحتاجهما** (‏`P-29`)، وهما المُدخَل الجاهز لأيّ معايرة لاحقة. ⚠️ **اتّجاه المقياس منقلب:** alpha يستعمل `best_dense_distance` = **أدنى** مسافة L2؛ AIZZAK على جيب التمام حيث **الأعلى أفضل**. الأرقام لا تُنسَخ والمقارنات تنقلب.

### 3.4 توجيه النيّة (‏`P-21` `P-22` + س-16 س-17)

```
agents/rag_agent  ──► self.deps.knowledge.answer(question)      ← بذرة واحدة، بلا استيراد
                                     │
knowledge/application/routing.py ────┤ classify_intent(question)   ← دالّة دومين نقيّة
                                     ├─► SUMMARIZE_DOC ──► RequestSummary   (موجود أصلًا)
                                     └─► CONTENT       ──► RetrieveContext
```

**لماذا هنا:** المساران يعيشان في **وحدة واحدة** بعد استثناء `METADATA`. فالتوجيه شأن الوحدة لا شأن الوكيل؛ الوكيل يبقى رقيقًا ولا يكسر عرفه المُعلَن (ح‑١١)، والتوجيه يصير متاحًا لكلّ مستهلك للوحدة.

**ما يسقط من `domain/intent.py`:** `Intent.METADATA` · `_METADATA_PATTERNS` · `_TOPICAL_GUARD_PATTERNS` (وُجد **فقط** ليحمي من METADATA خاطئ — بلا METADATA لا وظيفة له).

**معايرة `SUMMARIZE_DOC` (‏`P-22` + س-17 = ب):** بمسارين فقط صارت **دقّة المصنّف كلّها دقّة `SUMMARIZE_DOC`** — كلّ إيجابية كاذبة تعطب سؤال محتوى مشروعًا.

| النمط | الشرط | المثال الحاسم |
|---|---|---|
| **فعل أمر** (‏«لخّص» · `summarize`) | **بلا شرط** | «لخّص لي هذا» ⇒ تلخيص ✅ |
| **اسم** (‏«ملخّص» · `summary`) | **يتطلّب المفعولية**: اسم مستند مفعولًا به (`_DOC_NOUN`) | «هل يوجد **ملخّص** تنفيذي في التقرير؟» ⇒ محتوى ✅ |

**تبقى المطابقة الجزئية** للأنماط العربية: اللغة اشتقاقية، و«لخص» داخل «يلخّص/ملخصًا/التلخيص». هذا صحيح ومقصود ولا يُستبدَل بحدود كلمات.
**يتوسّع `_DOC_NOUN`** إلى `ملف|مستند|كتاب|كتب|مرجع|مراجع` **مع الحفاظ على شرط المفعولية**، حتى تبقى «ما هو الحدّ الأقصى في ملف السياسات؟» محتوًى.

### 3.5 حلّ اسم الملف والسؤال التوضيحي (‏`P-04` + س-18)

```
اسم في السؤال ──► تطابق تامّ
              ──► ضبابيّ: Jaccard + difflib + تجريد «ال» التعريف
              ──► دلاليّ
              └─► تعادل داخل _BAND = 0.10  ──► لا يخمّن: يعيد المرشّحين
```

**التعليل المنقول:** alpha **لا يخمّن** عند التعادل. و«أعلى مرشّح دائمًا» أسوأ فشل ممكن هنا: **يلخّص الملفّ الخطأ بثقة**.

**السلوك في AIZZAK (س-18 = أ):** الوكيل يبثّ سؤالًا توضيحيًّا نصّيًّا — «أيّ ملفّ تقصد؟ أ / ب / ج» — **كإجابة عادية**، بلا نوع حدث جديد وبلا تغيير عقد البثّ.

**البنية:** وحدة دومين نقيّة فوق `ListDocuments` — تُنقَل **الخوارزمية** لا تخزين JSON الذي يستعمله alpha.

### 3.6 وعي الكوربوس (‏`P-36` + س-23)

**إلزاميّ لا اختياريّ** بعد استثناء `METADATA`: «كم ملفًا لديك؟» ستذهب إلى `CONTENT`، ولن تجد قطعًا، فتُطلِق بوّابة الاحتياط. والفرق بين احتياط مفيد واحتياط جافّ هو **هل يعرف النموذج ما لديه**.

**القرار س-23 = ج:** الرأس **دائمًا** — في المسار الطبيعيّ **وفي مسار الاحتياط**. سقف عرض **٥٠ اسمًا** ثمّ «و N ملفًا آخر».

**الثمن المُعلَن:** ٥٠ اسمًا ≈ ٥٠٠ رمز في **كلّ** طلب. مقبول لأن البديل جواب «لا أملك معلومات» على مستخدم لديه الملفّ فعلًا.

### 3.7 توسيع الأب وميزانية السياق (‏`P-26` `P-34` `P-35`)

```
الدمج ──► احتفظ بـ 3×k ──► filter_relevant ──► استبدل بنصّ الأب ──► ميزانية السياق ──► k النهائيّ
          (P-26)            (قائم، 0.95)        (P-34, dedup بالأب)   (P-35)
```

⚠️ **`P-34` محجوز على خطّة الفهرسة الخطوة ٦** (‏`knowledge.parent_chunks`). وهو **حرج** لا تحسين: بعد استثناء نافذة الجمل لم تبقَ **أيّ** آليّة أخرى لتوسيع المطابقة إلى سياقها — النموذج يرى ٥١٢ كلمة معزولة.

**لماذا 3×k (`P-26`):** الاحتفاظ بثلاثة أضعاف بعد الدمج يعطي التوسيعَ بالأب تنوّعًا كافيًا لملء العدد النهائيّ بأقسام **فريدة** — بدونه قد ينهار k قطعة إلى أبوين اثنين.

**ميزانية مزدوجة (`P-35`):** `max_context_chars` سقف صلب و`max_context_tokens` تقديريّ، **ويُؤخَذ الأصغر**. اقتراح البداية: `12000` حرفًا و`3000` رمز.

**قرار مرفوض يُنقَل كملاحظة تصميم:** `LongContextReorder` — يضع الأعلى درجة في آخر السياق، وهذا **يضرّ** النماذج الصغيرة (‏≤7B) التي تنتبه لبداية السياق. alpha رفضه صراحةً. الترتيب هنا: تنازليّ ثمّ قصّ، والأكثر صلة في `[#1]`.

### 3.8 العتبات: الآليّة تُشحَن والأرقام لا (‏`P-27` + س-22)

قرار س-22 = أ يمنع **الأرقام غير المعايَرة**، لا الآليّة. لذلك:

| ما يُشحَن | القيمة الافتراضية | لماذا |
|---|---|---|
| `min_dense_score` · `min_bm25_score` (‏`P-27`) | **`0.0`** = معطَّلة | المقبض موجود، والرقم ينتظر مجموعة تقييم |
| `min_score` · `relative_floor` (‏`domain/relevance.py`) | **`0.0`** كما هي | نفسه |
| سقف عدد مرشّحي السَّبارس (‏`P-27`) | **قيمة فعليّة** | سقف **عدد** لا **درجة** — لا علاقة له بالمعايرة |
| Jaccard dedup | **`0.95`** كما هي | ثابت alpha الوحيد **المستقلّ عن المقياس** (ح‑١٧) — ينجو من انقلاب الاتّجاه |
| `λ` في MMR | **`0.7`** | مقايضة **تنوّع**، لا بوّابة «هل هذا جيّد كفاية» — خارج نطاق س-22 |

### 3.9 MMR (‏`P-23` + س-20)

`λ·sim(q,d) − (1−λ)·max sim(d,dⱼ)` — خوارزمية **نقيّة في `domain/`** إلى جانب `fusion.py` و`relevance.py`، قابلة للاختبار بلا شبكة.

**الثمن المُعلَن:** تحتاج متّجهات المرشّحين ⇒ `with_vectors=True` على استعلام Qdrant ⇒ نقل متّجهات كاملة لكلّ مرشّح عبر الشبكة. عرض حزمة ملموس، مقبول بالقرار س-20.

**المشكلة التي تحلّها:** خمس قطع من الفقرة نفسها نتيجة مشروعة اليوم.

### 3.10 مُعيد الترتيب (‏`P-24` + س-21)

**منفذ `RerankProvider` + محوّل HTTP** — يتّسق حرفيًّا مع نموذج المزوّدين القائم (تضمين · LLM). **لا أوزان داخل صورة العامل**: قاعدة معمارية قائمة، وكسرها يضخّم الصورة.

**التفعيل والإيقاف (طلب صريح في س-21):** `Settings.rerank_enabled` — **مطفأ افتراضيًّا** كما في alpha. يُفعَّل عند الحاجة إلى دقّة أعلى، وثمنه **وقت أطول** في كلّ طلب.

**حارس منقول:** ألّا يجوّع المُعيد `final_top_n` — إن أعاد أقلّ من المطلوب يُكمَّل من ترتيب RRF ولا يُقصَّر الجواب.

**النطاق:** أوّل ١٠–٢٠ مرشّحًا بعد الدمج، لا الكوربوس.

### 3.11 السجلّ والقياس (‏`P-29` `P-39` + س-25)

**بلا تغيير عقد** (س-25 = أ). كلّ ما يلي يذهب إلى `get_logger` بحقول بنيوية:

- عدّ ودرجات كلّ مرحلة، و**لقطة قبل** أن يدهس RRF الدرجات الخامّ.
- وسم `retrieval_origin` لكلّ مرشّح: `dense` / `bm25` / `both`.
- قياسات `total_ms` · `llm_ms` · `context_nodes` · `fallback`.

**`P-39` يصير قدرة داخليّة:** `context_text` جاهز يُبنى بوحدة التنسيق نفسها (§3.2) ويُستعمَل داخل الوحدة — **ولا يظهر في `openapi.yaml`**.

---

## 4. الخطوات

| # | الخطوة | البنود | يمسّ | الحالة |
|---|---|---|---|---|
| **أ — العقد والإجابة** (أكبر مكسب مستخدم) ||||
| ١ | ⚠️ **محجوزة على الفهرسة ١١**: توسيع الطبقات الثلاث بـ`file_name` · `page_number` · `section` + `openapi.yaml` + اختبار مطابقة العقد | `P-18` | `ports/retrieval.py` · `agent_runtime/deps_ports.py` · `api/v1` · `docs/design/03` | ✅ |
| ٢ | وحدة تنسيق واحدة: تسمية المصدر `[file p.N \| section: S]` فوق نصّ القطعة — تُضاف **عند العرض** | `P-31` | `agents/rag_agent` · وحدة تنسيق مشتركة | ✅ |
| ٣ | استشهادات مفهومة `{document_id, file_name, page, chunk_id}` بدل UUID عارٍ | `P-32` | `agents/rag_agent/agent.py` · عقد الاستجابة | ✅ |
| ٤ | إعادة إشارتَي الثقة (أفضل كثيفة · أفضل سَبارس) — ⚠️ الاتّجاه منقلب عن alpha | `P-28` | `application/retrieval.py` | ✅ |
| ٥ | **بوّابة الثقة + الاحتياط الأمين** على «لا نتائج» فقط، بلا عتبة رقمية | `P-33` | `agents/rag_agent/agent.py` | ✅ |
| ٦ | وعي الكوربوس في الرأس: أسماء الملفّات بسقف ٥٠ — **دائمًا وفي مسار الاحتياط** | `P-36` | `agents/rag_agent` · `ListDocuments` | ✅ |
| ٧ | تعليمات الرأس: اجمع من **كلّ** الأقسام · أدرج **كلّ** عنصر إن كانت الإجابة قائمة · استشهد بالملفّ والقسم · لا تسرد تفكيرك | `P-37` | `SYSTEM_PROMPT` | ✅ |
| ٨ | الاحتفاظ بـ 3×k بعد الدمج | `P-26` | `application/retrieval.py` | ✅ |
| ٩ | ⚠️ **محجوزة على الفهرسة ٦**: التوسيع بقطعة الأب عند التسليم + dedup **بالأب** + `max_parent_chunk_chars` | `P-34` | `application/retrieval.py` · مستودع الأب | ✅ |
| ١٠ | ميزانية سياق مزدوجة: `max_context_chars` و`max_context_tokens`، ويُؤخَذ الأصغر | `P-35` | وحدة بناء السياق | ✅ |
| **ب — التوجيه** ||||
| ١١ | تقليص `Intent` إلى مسارين (حذف `METADATA` وأنماطه والحارس الموضوعيّ) + **حالة استخدام موجِّهة** في `knowledge/application` + بذرة واحدة للوكيل | `P-21` | `domain/intent.py` · `application/routing.py` جديد · `agent_runtime` | ✅ |
| ١٢ | معايرة `SUMMARIZE_DOC`: فعل الأمر بلا شرط · الاسم بشرط المفعولية · توسيع `_DOC_NOUN` · إبقاء المطابقة الجزئية | `P-22` | `domain/intent.py` | ✅ |
| ١٣ | حلّ اسم الملفّ: تامّ ← ضبابيّ (Jaccard + `difflib` + تجريد «ال») ← دلاليّ · `_BAND=0.10` يعيد مرشّحين ولا يخمّن | `P-04` | وحدة دومين جديدة فوق `ListDocuments` | ⬜ |
| ١٤ | السؤال التوضيحي النصّي عند التعادل — إجابة عادية بلا نوع حدث جديد | `P-04` `P-21` | `agents/rag_agent/agent.py` | ⬜ |
| ١٥ | تقييد النطاق بملفّ من الاستعلام — **صارم**: ملفّ مسمّى بلا نتائج يُجيب بأمانة بدل السحب من ملفّ آخر | `P-25` | `application/retrieval.py` | ⬜ |
| **ج — الضبط والقياس** ||||
| ١٦ | عتبات مطلقة لكلّ ساق (**افتراضها `0.0` = معطَّلة**) + سقف **عدد** مرشّحي السَّبارس | `P-27` | `application/retrieval.py` | ⬜ |
| ١٧ | السجلّ البنيويّ: عدّ ودرجات كلّ مرحلة · لقطة **قبل** RRF · وسم `retrieval_origin` · قياسات `total_ms`/`llm_ms`/`fallback` | `P-29` | `application/retrieval.py` · `observability` | ⬜ |
| ١٨ | `_W_DENSE` · `_W_BM25` · `_RRF_K` · `_SEARCH_OVERFETCH` · `_TOP_K` ⇒ `Settings` تُمرَّر كوسائط — **بلا `os.getenv` في الدومين وبلا تجاوزات لكلّ طلب** | `P-30` `P-40` | `Settings` · `application/retrieval.py` | ⬜ |
| ١٩ | `context_text` جاهز كقدرة **داخليّة** بوحدة التنسيق نفسها — بلا ظهور في `openapi.yaml` | `P-39` | `application/retrieval.py` | ⬜ |
| **د — الترتيب المتقدّم** ||||
| ٢٠ | MMR: خوارزمية نقيّة في `domain/` فوق `search_k` موسَّع · `λ=0.7` · `with_vectors=True` | `P-23` | `domain/` جديد · `application/retrieval.py` · محوّل Qdrant | ⬜ |
| ٢١ | منفذ `RerankProvider` + محوّل HTTP + `Settings.rerank_enabled` (مطفأ افتراضيًّا) + حارس ألّا يجوّع `final_top_n` | `P-24` | `framework/ports` · `infrastructure` · `Settings` | ⬜ |

**الترتيب المُلزِم:** ١ قبل ٢ و٣ · ٤ قبل ٥ · ٥ قبل ٦ (الاحتياط قبل ما يُحقَن فيه) · ٨ قبل ٩ · ١١ قبل ١٢ و١٤ · ١٣ قبل ١٤ و١٥ · ١٨ قبل ٢٠ (‏`λ` و`search_k` من `Settings`). ما عدا ذلك قابل لإعادة الترتيب.

---

## 5. التبعية على خطّة الفهرسة

خطوتان هنا **لا تبدآن** قبل خطوتهما هناك، وثالثة تفقد قيمتها بلا إعادة البناء:

| الخطوة هنا | تنتظر | لماذا |
|---|---|---|
| ١ (توسيع العقد) | الفهرسة **١١** (‏`_CITATION_KEYS`) **و§5 هناك** | الحقول تُقرأ من حمولة Qdrant؛ بدونها ثلاثة حقول قيمتها `None` دائمًا (ح‑١٦ · ح‑١٣) |
| ٩ (التوسيع بالأب) | الفهرسة **٦** (‏`knowledge.parent_chunks`) | لا جدول ⇒ لا أب ⇒ لا توسيع |
| ٢ و٣ (التسمية والاستشهاد) | الخطوة ١ هنا | تبنيان فوق الحقول الجديدة |

**ما لا ينتظر:** ب و ج و د كلّها (‏١١..٢١) مستقلّة عن خطّة الفهرسة تمامًا، وتصلح للتنفيذ بالتوازي معها.

---

## 6. المخاطر

| # | الخطر | التخفيف |
|---|---|---|
| ١ | شحن الخطوة ١ قبل الفهرسة ١١ ⇒ حقول `None` في عقد عامّ | §5 — قيد مُلزِم، والاختبار يطالب بقيمة فعليّة لا بحضور الحقل |
| ٢ | **الهلوسة الصامتة تبقى مفتوحة جزئيًّا** | الخطوة ٥ تغلق الحالة الصريحة (صفر قطع) فقط. «قطع ضعيفة» تبقى مفتوحة بقرار س-22 حتى تتوفّر مجموعة تقييم — ⚠️ **خطر معلوم ومقبول** |
| ٣ | نسخ أرقام alpha على مقياس مختلف | مكتوب في ثلاثة مواضع (‏§الرأس · §3.3 · §3.8): L2 ⇒ الأدنى أقرب · جيب التمام ⇒ الأعلى أفضل. لا رقم يُنسَخ |
| ٤ | إيجابية كاذبة في `SUMMARIZE_DOC` تعطب سؤال محتوى | الخطوة ١٢: شرط المفعولية على الأسماء دون أفعال الأمر (س-17) |
| ٥ | `with_vectors=True` يثقّل الشبكة | مقبول بالقرار س-20؛ والنطاق `search_k` الموسَّع لا الكوربوس |
| ٦ | مُعيد الترتيب يبطّئ كلّ طلب | مطفأ افتراضيًّا (س-21)؛ التفعيل قرار نشر واعٍ بثمنه |
| ٧ | الوكيل يكسب استيرادات فيخالف عرفه المُعلَن | س-16 = أ: التوجيه في `knowledge/application`، والوكيل يستدعي بذرة واحدة. يحرسه `lint-imports` جزئيًّا والعرف كاملًا |

---

## 7. خارج النطاق — يُسجَّل ولا يُنفَّذ

- **`P-38` معايرة العتبات على مقياس RRF** — القرار س-22 = أ. يحتاج **مستندات حقيقية وأسئلة بإجابات مرجعية** (‏١٥–٣٠ سؤالًا) وهي غير موجودة ولا تُختلَق. ⚠️ **البند الوحيد الذي ينتظر مُدخَلًا من المستخدم لا قرارًا.** الآليّة كلّها مشحونة والمقابض على `0.0` (§3.8)، فتفعيلها لاحقًا سطر إعداد لا خطوة.
- **تجاوزات الضبط لكلّ طلب** ونقطة إدارة تكتب الإعدادات حيًّا — س-24 = أ. الثاني **حالة عالمية قابلة للكتابة** تخالف روح المشروع وتحتاج RBAC وتدقيقًا وعزل مستأجرين.
- **`stages` و`context_text` في العقد العامّ** — س-25 = أ. المراحل تكشف بنية الفهرس وتحتاج ضبط صلاحية.
- **حدث `clarification` مُهيكَل** في بثّ الوكيل — س-18 = أ اختار النصّ العاديّ. الحدث المُهيكَل يمسّ عقد البثّ ويحتاج واجهة تعرفه.
- **مسار `METADATA`** وكلّ ما يتبعه: `router._metadata_flow` · مطابقة العدد العربي · `_METADATA_LIST_MAX` · `_TOPICAL_GUARD_PATTERNS` — استثناء مُقفل.
- **`LongContextReorder`** — قرار رفض موثَّق في alpha نفسه، يُنقَل كملاحظة تصميم (§3.7) لا كشيفرة.
- **طبقة LLM ثانية في تصنيف النيّة** — مرفوضة تقنيًّا: في alpha تقرأ `Settings.llm` العالميّ (خلل موثَّق).
- **تفعيل مُعيد الترتيب لكلّ طلب** (لا لكلّ نشرة): لم يشمله أيّ جواب — س-21 طلب «إمكانية التفعيل والإيقاف» وس-24 حصر الضبط في `Settings`. المُنفَّذ هو مفتاح نشرة؛ المفتاح لكلّ طلب يحتاج قرارًا جديدًا.

**سُجِّل في الخطوة ١١ (‏`P-21`) ولم يُنفَّذ:**

- **‏`ConflictError` من محادثة**: طلب تلخيص مكرَّر على المفتاح نفسه (‏بناء جارٍ أصلًا) يخرج من `RequestSummary` كـ**409** ويصعد كما هو إلى المستدعي بدل جملة ودّية «الملخّص قيد الإعداد بالفعل». الترجمة قرار **عرض** يخصّ الجهة التي تعرض، لا حالة الاستخدام الموجِّهة — ولا يُخفى الخطأ داخل الموجِّه.
- **تسليم الملخّص إلى المحادثة نفسها**: مسار `SUMMARIZE_DOC` **يُدرِج مهمّة** ويردّ إيصالًا؛ الملخّص نفسه يبنيه العامل ويُقرأ عبر مسارات الملخّص (‏`GET`). إيصاله إلى الخيط الذي طلبه يحتاج مسار إشعار/حدثًا — وهو تغيير عقد بثّ يخرج عن هذه الخطوة (‏وعن س-18 التي حصرت الجديد في نصّ عاديّ).
- **`OVERVIEW` و`AUTO` كافتراضَي التلخيص المُوجَّه**: لم يفصل فيهما أيّ سؤال مُجاب. المُنفَّذ `OVERVIEW` **حارسَ كلفة** (المسار يُدخَل بإيجابية كاذبة من التعبير النمطيّ — §6 خطر ٤ — و`FULL` تعني map-reduce على كامل المستند)، و`AUTO` لأنها عضو حقيقيّ يعني «بلغة المستند». تغييرهما قرار جديد، لا تعديل شيفرة.
- **هدف التلخيص من نصّ السؤال**: الخطوة ١١ تلخّص فقط حين يحدّد المستدعي **مستندًا واحدًا** بتثبيته؛ وما عداه يسقط إلى `CONTENT` مع `intent` معلَنة بصدق. حلّ الاسم (‏١٣ · `P-04`) والسؤال التوضيحي (‏١٤) هما ما يغلقان الباقي — ولا تخمين قبلهما.

**سُجِّل في الخطوة ١٢ (‏`P-22`) ولم يُنفَّذ:**

- **أسماء مستندات خارج جذور `_DOC_NOUN` الستّة** (‏«تقرير» · «وثيقة» · «مذكّرة» · «عرض»): القائمة المُقفلة في §3.4 هي جذور alpha الستّة وحدها، فـ«ملخّص التقرير» تبقى **محتوًى**. توسيع القائمة قرار معايرة جديد لا تعديل شيفرة — ومسار الأمر غير متأثّر أصلًا («لخّص التقرير» تلخيص لأن الفعل بلا شرط).
- **قياس دقّة المصنّف على أسئلة حقيقية**: المعايرة اليوم مبرهَنة بأمثلة §3.4 الحاسمة الثلاثة وباختبارات وحدة، **لا بقياس**. القياس يحتاج المُدخَل نفسه الذي ينتظره `P-38` (مستندات حقيقية وأسئلة بإجابات مرجعية) وهو غير موجود.

</div>
