<div dir="rtl">

# تدقيق أمانة النقل — alpha ← aizzak

> **RAG Port Fidelity Audit** · فحص مستقلّ لخطّتي الفهرسة والاسترجاع مقابل شيفرة alpha نفسها
>
> **طريقة الفحص:** قراءة شيفرة `Ubuntu-24.04\home\alpha\rag` (‏٥١ ملفًّا · ‎11483‎ سطرًا) مقابل `src/app/modules/knowledge` (‏٤٥ ملفًّا · ‎15351‎ سطرًا)، دالّةً بدالّة وثابتًا بثابت. لا اعتماد على ادّعاءات الخطّتين ولا على المراجعتين — كلّ سطر أدناه مُتحقَّق منه باستشهاد من الطرفين.

| | |
|---|---|
| **أُنشئت** | 2026‑08‑24 |
| **المقارَن** | `rag-indexing-plan.md` (‏٢٣ بندًا) · `rag-retrieval-plan.md` (‏٢١ بندًا) · ‏٤٥ بندًا مُغطّاة بالكامل، لا بند ضائعًا في الحساب |
| **الحكم العامّ** | ✅ **الخوارزميات مَنقولة بأمانة عالية** — تطابق حرفيّ في كلّ دالّة نقيّة فُحصت · ⚠️ **الأرقام والبنية المحيطة بها هي موضع الخلل**: ستّة مواضع نُقل فيها الثابت وتُرك السياق الذي كان يجعله صحيحًا |
| **المفتوح** | **‏١٩ نقطة** — ٦ منها لم تُسجَّل في أيّ ملفّ حتّى الآن (§3)، و٣ **كانت مُصلَحة ثمّ تراجعت** (§4) · ~~و§5 من خطّة الفهرسة **لم تُنفَّذ** فتُبطِل عمل 2026‑08‑24 كلّه~~ ✅ **§5 نُفِّذت 2026‑08‑25** (§4‑ب) |
| **الأخطر** | ~~**§3‑د — بترٌ صامت عند ‎128‎ رمزًا داخل خدمة التضمين**~~ ✅ **أُغلق 2026‑08‑25.** كان مُثبَتًا بالقياس (‏٧٩٪ من القطعة كاملة الحجم لا يدخل المتّجه)، وأُغلق بالقياس نفسه: `cos(354 كلمة, أوّل 100 كلمة)` كان **‏1.000000** (‏متّجهان متطابقان) وصار **‏0.927171**. الخدمة تقرأ حتّى ‎512‎ · المعامل `2.4` ⇒ نافذة ‎192‎ كلمة · والصورتان أُعيد بناؤهما وأُعيدت الفهرسة (§4‑ب) |
| **تصحيح ‎2026‑08‑24‎** | أب الصفحة (`_widen_to_parents`) يبدّل دلالة `default_k`، و`max_context_tokens = 3000` يقصر أب الصفحة على **أبٍ واحد** ⇒ §3‑ب انتقل من موجة أ إلى شرطٍ سابق للموجة ب (§3‑ب · §4‑أ · §5) · 🟡 **نُفِّذ: ‎6000‎** ⇒ أبوان (‏لا ثلاثة — انظر §3‑ب) |
| **تصحيح ‎2026‑08‑25‎** | إعادة البناء (§5) نُفِّذت، وكشفت ثلاثة: (١) خطوة §5‑٣ لم تكن مُنجَزة كما تقول الخطّة — `PIPELINE_VERSION` رُفِع ٣ ← ٤ · (٢) الإجراء اليدويّ في §5 تجاوزه `POST /knowledge/reindex`، وحذفُ التجميعة صار الخيار الأسوأ · (٣) **إعادة البناء الثانية تفشل** على `fk_reindex_item_doc` ما لم يُصلَح `purge` (§4‑ب) |

---

## 1. ما نُقل بأمانة — تطابق مُتحقَّق

يُسجَّل حتّى لا يُعاد فحصه. كلّ صفّ قُورن بالشيفرة عند الطرفين.

| الموضع | alpha | aizzak | الحكم |
|---|---|---|---|
| **RRF** | `reranking.py::reciprocal_rank_fusion` — `w · 1/(rrf_k + rank + 1)` | `domain/fusion.py` — الصيغة نفسها حرفًا بحرف · `rrf_k=60` · `0.5/0.5` | ✅ تطابق تامّ + حتميّة أعلى (‏قاموس مرتَّب بالإدراج بدل `set`) |
| **مُرمِّز BM25** | `bm25_tokenizer.py` — ‏٩٨ كلمة وقف عربية · ٦٠ إنجليزية · تطبيع الألف/الهمزة/التشكيل/التاء المربوطة/الياء · حدّ الرمز ‎>1‎ | `domain/tokenization.py` | ✅ **‏٩٨/٩٨ و ٦٠/٦٠ — تطابق عدديّ تامّ**. الاشتقاق (`BM25_STEMMING`) مُسقَط، وهو مطفأ أصلًا في alpha |
| **فلتر الصلة** | `relevance_filter.py` — أرضيّة مطلقة ← أرضيّة نسبيّة ← Jaccard ‎0.95‎ | `domain/relevance.py` | ✅ + تحسين: Jaccard على مخرَج المُرمِّز لا `.split()` |
| **MMR** | `λ·sim − (1−λ)·max sim` (‏عبر llama-index) | `domain/mmr.py` — الصيغة نفسها + تسوية تزايدية | ✅ الصيغة · ⚠️ حدّ الصلة والموضع مختلفان (§2‑ح · §2‑ط) |
| **استبدال الأب** | `parent_chunk_postprocessor.py` — استبدال + dedup بالأب + سقف | `_widen_to_parents` | ✅ + حارس `is_complete` (‏تحسين حقيقيّ) · ⚠️ صدق البتر ضاع (§3‑و) |
| **انقسام الطول** | `node_builder.py:655` — `max(int((tok/1.3)*0.9), 32)` + تداخل ‎10٪‎ | `domain/chunking.py` — `_TOKENS_PER_WORD=`**`2.4`** · `_MAX_WORDS_SAFETY_MARGIN=0.9` · `SPLIT_OVERLAP_RATIO=0.1` | ✅ تطابق تامّ في **الصيغة** · ✅ المُدخَل والمُخرَج كلاهما أُصلِح (§3‑د): الخدمة تقرأ حتّى ‎512‎، والمعامل رُفع إلى `2.4` بعد قياسٍ موسّع (‏العربيّ المُشكَّل ‎2.18‎ لا ‎1.69‎) — **خروجٌ متعمَّد عن رقم alpha، لا انحراف** · ~~يبقى إعادة البناء~~ نُفِّذت: أطول قطعة ‎354 ← 192‎ كلمة (§4‑ب) |
| **التقسيم الدلالي** | `buffer=1` · `breakpoint=95` | `semantic_boundaries(buffer=1, breakpoint_percentile=95)` | ✅ + `_percentile` ينقل سلوك `numpy` بلا `numpy` |
| **تفجير الجداول** | `table_processor.py` — `_NOISE_HEADERS = {"no.","no","row","#","index","id"}` · `TABLE_PARENT_MAX_ROWS=20` | `domain/tables.py` | ✅ المجموعة نفسها حرفيًّا · ⚠️ تنظيف الخلية ناقص (§3‑هـ) |
| **حلّ اسم الملفّ** | `file_resolver.py` — تامّ ← `max(0.6·احتواء + 0.4·difflib, jaccard)` ← دلاليّ · `_BAND=0.10` · لا يخمّن عند التعادل | `domain/file_resolution.py` | ✅ تطابق تامّ بما فيه العتبات وقاعدة عدم التخمين |
| **ثوابت الفهرسة** | `MIN_NODE_CHARS=15` · `MIN_BLOCK_CHARS=25` · `TABLE_MIN_COLUMNS=2` · `STREAM_MIN_ACCURACY=60` · تداخل الجداول `0.5` · `max_parent_chunk_chars=4000` | كلّها بالقيم نفسها | ✅ الأرقام · ⚠️ عتبة كامِلوت فقدت سياقها (§3‑أ) |
| **`LongContextReorder`** | مرفوض بتعليل في alpha | مُسجَّل ملاحظةَ تصميم لا شيفرة | ✅ نُقل القرار لا الشيفرة — وهو المطلوب |
| **مُعيد الترتيب** | مطفأ افتراضيًّا · حارس ألّا يجوّع `final_top_n` | `rerank_enabled=False` · `_apply_rerank` دالّة نقيّة تُبرهِن `len(out)==len(in)` | ✅ |

**الخلاصة:** القاعدة الحاكمة المُعلَنة في رأس الخطّتين («يُنقَل الخوارزميات والتعليلات لا البنية») **نُفِّذت بأمانة على مستوى الخوارزمية**. لا دالّة نقيّة واحدة فُحصت وظهر فيها انحراف.

---

## 2. انحرافات مُعلَنة ومُعلَّلة — لا مأخذ عليها

| # | الانحراف | التعليل المُوثَّق | الحكم |
|---|---|---|---|
| أ | Qdrant/Postgres بدل FAISS/`pickle.gz` | بنية لا خوارزمية | ✅ مقصود |
| ب | لا مسار `METADATA` | استثناء مُقفل + رأس الكوربوس يعوّض | ✅ مع تحفّظ §4‑٩ |
| ج | لا تقطيع هرميّ ولا نافذة جمل | استثناءان مُقفلان | ✅ مقصود |
| د | اتّجاه المقياس منقلب (‏L2 ← جيب التمام) | مكتوب في ثلاثة مواضع؛ لا رقم يُنسَخ | ✅ منضبط |
| هـ | `min_bm25_score = 0.0` بدل `0.1` | مقياس مختلف كلّيًّا (‏TF·IDF لا `rank_bm25`) | ✅ صحيح |
| و | مُعيد ترتيب عبر منفذ HTTP بنموذج آخر | «لا أوزان داخل صورة العامل» — قاعدة معمارية قائمة | ✅ |
| ز | المُعيد **قبل** توسيع الأب (‏alpha بعده) | مُعلَّل في `_rerank` بأربع نقاط: المرحلة بادئة-حسّاسة، والمُعيد يقرأ نصّ الورقة وهو ما يريده المُرمِّز المتقاطع | ✅ تعليل سليم |
| ح | MMR **بعد** الدمج (‏alpha داخل الرِّجل الكثيفة وحدها) | التنويع يجب أن يرى الرِّجلين لا واحدة | ✅ أفضل من alpha |
| ط | حدّ الصلة في MMR = درجة RRF لا جيب تمام الاستعلام | «نصف الترتيب من رجل لا يراها التضمين» | ✅ صحيح مبدئيًّا — ⚠️ لكنّه يُحدِث مشكلة λ (§4‑٢) |

---

## 3. ستّة مواضع نُقل فيها الرقم وتُرك سياقه — **لم تُسجَّل في أيّ ملفّ**

هذا هو المكسب الأصليّ لهذا التدقيق. كلّ بند أدناه غير مذكور في الخطّتين ولا في المراجعتين ولا في أيّ تشخيصٍ سابق.

### أ — عتبة كامِلوت `60`: نُقلت كرقم، لا كدور 🔴

alpha لا يشغّل `stream` وحدها. `pdf_table_extractor.py:553-600`:

```
lattice أوّلًا  ──► accuracy ≥ LATTICE_MIN_ACCURACY = 80  ──► يُعتمَد
              └─► وإلّا: stream ──► ويُحتفَظ بالنكهة الأعلى دقّةً بين الاثنتين
```

فـ`STREAM_MIN_ACCURACY = 60` عند alpha **أرضيةُ نكهةٍ ثانية داخل مُنافسة**، لا بوّابةً وحيدة. وفي aizzak صارت البوّابة الوحيدة بعد إسقاط `lattice` (‏قرار س-04 الترخيصيّ).

**الأثر المُقاس** (‏على الكوربوس الحيّ): ‏٥٤ جدولًا في ٤٤ صفحة · ‏٣٧٪ من القطع تحمل `Column_N` · ‏١٢٪ تبدأ بترويسة الشركة الجارية.

⚠️ **الخطر ٢ في §6 من خطّة الفهرسة قدّر الأثر معكوسًا:** «الخسارة **جدول ضائع** لا **جدول كاذب**». الواقع المقيس عكسه تمامًا. و`pdf_tables.py:10-16` **يذكر** سقوط المنافسة، لكنّ الخطّة لم تستنتج منها الخطر الصحيح ولم تُلزم ببديل. البديل — حارس الجدول الكاذب (‏د‑٢، وهو س-30 في §4‑هـ) — **ما يزال غير مبنيّ**.

### ب — `max_context_tokens = 3000` بينما alpha عند `6000` 🟡 **نُفِّذ**

| | alpha | aizzak |
|---|---|---|
| `max_context_chars` | 12000 | 12000 |
| `max_context_tokens` | **6000** | ~~3000~~ ⇒ **6000** ✅ |
| `chars_per_token` | 4.0 ثابتًا | ‏2.0 عربيّ · 4.0 لاتينيّ |
| **الميزانية الفعليّة** | `min(12000, 6000×4=24000)` = **‏12000 دائمًا** | `min(12000, 3000×2=6000)` = **‏6000 عربيًّا** |

عند alpha **السقف الرمزيّ لا يعضّ أبدًا** — الحرفيّ هو الملزِم في كلّ لغة. وفي aizzak صار الرمزيّ يعضّ عند نصف ذلك بالعربية.

⚠️ الخطّة تسمّي `3000` «اقتراح البداية» — وهو **ليس رقم alpha**. والأدهى أنّ العلاج الأوّل الذي تقترحه §3.7 («رفع `max_context_tokens` إلى ‎~6000‎») **هو رقم alpha حرفيًّا**. أي أنّ المسألة موقوفة خلف «تحتاج مجموعة تقييم» بينما المصدر المنقول عنه يُجيبها أصلًا.

**الأثر الآجل — وهو الأخطر، ولم يكن مرئيًّا قبل قياس أب الصفحة:**

`fit_to_context_budget` يجري **قبل** `budgeted[:k]` (‏`retrieval.py:832` ثمّ `:847`)، ويُبقي **أطول بادئة تتّسع** ويقف عند أوّل مرشّح يتجاوز أيّ سقف. وبعد §5 يصير كلّ مرشّح **أبًا مرصوصًا حتّى ‎4000‎ حرف** (`_TEXT_PARENT_MAX_CHARS`):

```
الميزانية العربية اليوم = min(12000, 3000 رمز × 2) = 6000 حرف
أب واحد (4000) يمرّ · والثاني يبلغ 8000  ⇒  يتوقّف
⇒ budgeted = 1   ثمّ budgeted[:5] لا يفعل شيئًا
```

⇒ **بعد إعادة البناء، و`max_context_tokens` على `3000`، تُبنى الإجابة من صفحةٍ واحدة.**

⚠️ **لذلك يخرج هذا البند من موجة أ ويصير شرطًا سابقًا للموجة ب** — لا لاحقًا لها. إن نُفِّذت §5 وهذا الرقم على حاله، فأب الصفحة — وهو أغلى ما في الموجة — يظهر مقصورًا على أب واحد.

---

#### ✅ **نُفِّذ — ‏`6000`، والرقم مُشتَقّ لا مُقترَح**

الرقم لم يُؤخَذ من alpha تقليدًا. `estimate_tokens` يحسب الحرف العربيّ بـ`2.0`، فسياقٌ ممتلئ حتّى `max_context_chars` بعربيّة خالصة يُقدَّر بـ`12000 / 2.0 = 6000` رمزًا بالضبط. فأيّ قيمة **دون** ‎6000‎ تجعل السقف الرمزيّ يعضّ قبل الحرفيّ — **على العربية وحدها**، فينقسم السلوك بالخطّ ويكفّ السقف القابل للتدقيق عن أن يكون النافذ. و‎6000‎ هي **أصغر** قيمة لا تفعل ذلك:

```
estimate_tokens("ا" × 12000) = 6000
  عند 5999 → السقف الرمزيّ يعضّ أوّلًا ✓ (أي: يقصّ العربية)
  عند 6000 → لا يعضّ أبدًا           ✓
```

⇒ **اشتقاقان مستقلّان يلتقيان على الرقم نفسه**: أصغر قيمة تُبقي السقف الحرفيّ هو النافذ، وهي رقم alpha حرفيًّا (‏الذي بلغه من طريق أخرى: معدّله المسطّح `4.0` وضع سقفه الرمزيّ خارج المنال أصلًا).

| الموضع | التغيير |
|---|---|
| [`settings.py`](../src/app/framework/settings/settings.py) `Limits.max_context_tokens` | `3_000` ← **`6_000`** + الاشتقاق في التعليق |
| [`retrieval.py`](../src/app/modules/knowledge/application/retrieval.py) `RetrievalTuning.max_context_tokens` | `3_000` ← **`6_000`** (‏مرآة الأوّل — رقمٌ واحد بإملاءين) |
| `test_the_token_ceiling_cannot_bite_before_the_character_ceiling` | **جديد** — يثبّت الخاصيّة لا الرقم: `estimate_tokens("ا"×max_context_chars) ≤ max_context_tokens`، وأنّها أصغر قيمة تُحقّقها |
| `test_context_budget_module_reads_no_environment_and_hardcodes_no_budget` | الحارس `{12_000, 3_000}` ← `{12_000, 6_000}` |

**البرهان:** بإعادة الرقم إلى `3000` يسقط اختبار الخاصيّة (`assert 6000 <= 3000`)؛ وبتغيير **أحد** الموضعين دون الآخر يسقط `test_retrieval_tuning_defaults_mirror_settings_field_for_field` (`6000 != 3000`). البوّابات الخمس خضراء: **‏٣٣٩٤ ناجحًا · ٤٠٤ متخطًّى**.

⚠️ **تصحيح: «ثلاثة آباء» أعلاه خطأ — الناتج الفعليّ ‎٢‎.** الحساب `12000 ÷ 4000 = 3` يُغفل أنّ الميزانية تقيس **النصّ المعروض** (‏وسم المصدر `[file p.N | section: S]` معه — §3.2)، لا نصّ الأب وحده. بالقياس على `fit_to_context_budget` نفسها بآباءَ حقيقيّين:

```
أبٌ معروضٌ واحد = 4055 حرفًا (4000 + وسم)
  3000 رمزًا: عربيّ → أبٌ واحد   · لاتينيّ → أبوان   ← ينقسم السلوك بالخطّ
  6000 رمزًا: عربيّ → أبوان      · لاتينيّ → أبوان   ← السقف الحرفيّ هو النافذ في اللغتين
الثالث يبلغ 12165 > 12000  ⇒  يُقصّ بفارق 165 حرفًا
```

فالغرض المُعلَن تحقّق — **انتهى انقسام السلوك بالخطّ، وعادت الإجابة العربية من صفحةٍ إلى صفحتين** — لكنّ الثالثة يمنعها سقفٌ آخر، وهو **خلل مستقلّ يُسجَّل هنا أوّل مرّة**:

⚠️ **`max_context_chars = 12000` يتّسع لأبَوين لا لثلاثة.** وتعليق `settings.py` نفسه كان يَعِد بأنّه «‏comfortably above three widened parents» — والقياس يكذّبه بـ‎١٦٥‎ حرفًا. الرقم `12000` هو `3 × 4000` بلا مخصَّصٍ لوسمٍ يضعه هذا المسار على **كلّ** قطعة. صُحِّح التعليق؛ أمّا رفع الرقم فقرارٌ منفصل عن هذا البند (‏`12000` يطابق alpha تمامًا، فليس انحرافًا في النقل) ولم يُتَّخذ.

⚠️ **وسقف التوليد فُحص حيًّا ولا يمنع الرفع.** `OllamaLlmProvider._NUM_CTX = 8192` أصغر من `Limits.max_input_tokens = 32000`، وما بينهما يُبتر **صامتًا داخل Ollama** — أُسرة العطب نفسها التي أغلقها البندان ١ و٢. بالقياس على `gemma3:1b` المنشور (‏`prompt_eval_count` الفعليّ، لا تقديرًا):

| | رموز |
|---|---|
| `SYSTEM_PROMPT` وحده | ‏١٩٩ |
| + ترويسة وعي المجموعة | ‏٢٥٤ |
| + سياق ‎12000‎ حرف عربيّ (‏الأسوأ) | **‏٥٠٨٦ من ‎8192‎** ⇒ ‏٣١٠٦ تبقى للإجابة |

و`estimate_tokens` يُبالغ على العربية الخالصة بنحو ‎٢٢٪‎ (‏٥٣٣١ تقديرًا مقابل ‎٤٣٧٦‎ فعليًّا) — أي في الاتّجاه الذي توثّق أنّها تخطئ فيه: تُنفق أقلّ من المسموح، لا أكثر.

⚠️ **ونُشِر هذا البند 2026‑08‑25 مع §5، ولم يُقَس أثره — ولا يمكن أن يُقاس على هذا الكوربوس.** المحتوى المفهرَس **صفر حرف عربيّ** (‏مقيسًا على السياق المُسلَّم: ‏‎4.00‎ حرفًا/رمز بالضبط)، وعند اللاتينيّ الخالص يكون ‎3000‎ رمز × ‎4.0‎ = ‎12000‎ حرف = السقف الحرفيّ نفسه. أي أنّ ‎3000‎ و‎6000‎ **لا يُفرَّق بينهما إلّا على العربيّ**، وهو بالضبط ما تقوله الحجّة التي بُني عليها الرقم — فغياب الفرق هنا تأكيدٌ لها لا نقضٌ. أمّا ما **تغيّر** فعلًا بعد §5 فهو أنّ الميزانية صارت تعضّ أصلًا: `widened ← budgeted` انتقل من ‏10 ← 10 إلى **‏10 ← 6** على نفس السؤال (§4‑ب).


### ج — الرِّجل المتفرّقة TF·IDF لا Okapi BM25 🟠

`qdrant_store.py:375` يضبط `Modifier.IDF` — فيصل **IDF** وحده. ولا مقابل في aizzak لِـ:

| ثابت alpha | وظيفته | المقابل في aizzak |
|---|---|---|
| `BM25_K1 = 1.2` | تشبّع TF (‏تكرار ٦ مرّات لا يساوي ٦ أضعاف) | **لا شيء** — TF خطّيّ |
| `BM25_B = 0.75` | تطبيع الطول (‏قطعة أطول لا تفوز لطولها) | **لا شيء** |

والدليل في سجلّ الاسترجاع المقيس: `sparse_scores: [7.800722, 7.800722, 3.900361, …]` — ‏7.800722 = 2 × 3.900361 **بالضبط**. مضاعفة صحيحة تامّة = TF خطّيّ بلا تشبّع.

⚠️ §7 من خطّة الفهرسة تستثني **`rank_bm25`** (المكتبة) بتعليل تزايديّة الفهرسة — وهو تعليل سليم. لكنّها **لا تذكر أنّ نموذج التسجيل يسقط معها**، وخطّة الاسترجاع (‏وهي مالكة التسجيل) لا تذكره أصلًا. و`sparse.py:7` يقولها في الشيفرة، ولا خطّة تحملها.

**الأثر الآجل، وهو الأخطر:** السبب ٨ (‏§4‑ج) يقول إنّ BM25 ستستعيد معناها بمجرّد أن تكبر القطعة. **بلا `b` سيحدث العكس جزئيًّا:** حين تكبر القطع تبدأ الأطول بالفوز لطولها لا لصلتها، وهو تشويه ثانٍ لم يُتوقَّع.

### د — بترٌ صامت داخل المُضمِّن — **مُثبَت بالقياس على الصورة المنشورة** 🔴

§3.5 من خطّة الفهرسة تنصّ حرفيًّا: «‏افتراض **512**، **يُتحقَّق منه مقابل النموذج المنشور عند التنفيذ**». **لم يُتحقَّق.** والنموذج **واحد في المشروعين**: `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`.

**القياس — مُشغَّل داخل `aizzak/embedding:dev` نفسها، لا استنتاجًا:**

```
max_seq_length = 128
AR: words=16 tokens=27 ratio=1.69
EN: words=22 tokens=25 ratio=1.14
```

ويؤكّده ملفّ النموذج المُخبَّأ (‏اللقطة `e8f8c211…`): `sentence_bert_config.json` ⇒ `"max_seq_length": 128` · و`config.json` ⇒ `"max_position_embeddings": 512` (‏السقف المعماريّ الحقيقيّ).

| | alpha | aizzak |
|---|---|---|
| الإعداد | `EMB_MAX_LEN = 1024` | `embedding_max_input_tokens = 512` |
| هل يُرفَع سقف النموذج؟ | ✅ `tokenizer_kwargs={"truncation": True, "max_length": 1024, …}` (`core/embeddings.py:132-140`) — **يتجاوز الـ128 عمدًا** | ❌ `services/embedding/app.py:106` — `SentenceTransformer(model_name, device=device)` **ولا سطر بعده يمسّ `max_seq_length`** |
| **ما يُضمَّن فعلًا** | حتّى السقف المعماريّ ‎512‎ | **‎128‎ رمزًا — لا أكثر** |
| نافذة الكلمات المشتقّة | ‏٧٠٨ كلمة | ‏٣٥٤ كلمة |

**الحساب الكامل للانحراف عند aizzak:**

```
max_words = int((512 / 1.3) × 0.9) = 354 كلمة
‏354 كلمة عربية × 1.69 = ‎~598‎ رمزًا  ⇐  يُضمَّن منها ‎128‎ فقط
‏⇒ ‏٧٩٪ من القطعة كاملة الحجم لا يدخل المتّجه إطلاقًا
```

وهذا **حرفيًّا** العطب الذي وُجد `P-16` لمنعه («البتر عند مزوّد HTTP **صامت** — القطعة تُضمَّن ناقصة ولا أحد يعلم»)، مُعادًا إنتاجه طبقةً أدنى: لا في المزوّد الخارجيّ بل في خدمة التضمين الداخلية.

⚠️ **وهو عطبٌ حيٌّ اليوم لا كامنًا.** حدّ الـ128 رمزًا يعني **‎~76‎ كلمة عربية** أو **‎~112‎ كلمة إنجليزية**. مسار PDF الحاليّ ينجو صدفةً (‏القطعة ‎١١٧‎ حرفًا بالسبب ١)، لكنّ **DOCX · النصّ الصِرف · JSON · Excel** تُنتج نوافذ تبلغ ٣٥٤ كلمة كاملة ⇒ **تُبتَر عند ١٢٨ رمزًا اليوم، في الإنتاج**. أي أنّ السبب ١ كان **يُخفي** هذا البند، وعلاجُه هو ما سيكشفه على كلّ المسارات.

**وطبقة ثالثة — معامل `1.3` نفسه مقيس الآن ومُخطئ للعربية:**

⚠️ **والقياسُ الأوّل أعلاه (‏`AR 1.69` · `EN 1.14`) عيّنتُه ١٦ كلمة عربية و٢٢ إنجليزية — أُعيد على نطاق حقيقيّ فتبدّل الرقم.** التالي مقيسٌ بمُرمِّز اللقطة المنشورة نفسها على ‎37,652‎ كلمة عربية و‎50,078‎ إنجليزية من نثر المشروع، **مُقطَّعةً إلى نوافذ كما يفعل `chunk_segments` تمامًا**، بعدّ الرموز التي تبلغ النموذج فعلًا (‏الرموز الخاصّة داخلة):

| النصّ | المتوسّط | أسوأ نافذة |
|---|---|---|
| إنجليزيّ تقنيّ | **1.44** | 1.86 |
| عربيّ بلا تشكيل | **1.77** | 2.07 |
| عربيّ مُشكَّل | **2.18** | **2.53** ← الحالة الحاكمة |

**والتشكيلُ هو ما يُغلي العربية:** الحركات لا تدخل مدخلةَ المعجم مع الكلمة المجرّدة، فالكلمة المُشكَّلة تُكلّف قطعًا فرعيّةً زائدة. ووثائق هذا المشروع نفسها مُشكَّلة.

و§3.5 تقول «العربية أكثر رموزًا… **لذلك المعامل `1.3` مُبقًى ولا يُخفَّض**». الاتّجاه صحيح لكنّه ناقص **مرّتين**: عدمُ الخفض لا يكفي، و‎1.7‎ التي اقترحها هذا التقرير نفسه أوّلًا **لا تكفي هي الأخرى** — عندها (‏نافذة ٢٧١ كلمة) تتجاوز ‎512‎ رمزًا **‏137 من ‎138‎** نافذة عربية مُشكَّلة، و‎8‎ من ‎138‎ بلا تشكيل.

**العلاج — شقّان، ولا يكفي أحدهما:**

1. ✅ **في الخدمة — نُفِّذ.** `_load_model` يضبط `model.max_seq_length` على `512` (‏السقف المعماريّ المُتحقَّق منه، لا رقمًا مخترعًا). كان بدونه يبقى `embedding_max_input_tokens` إعدادًا لا أثر له.
2. ✅ **في المعامل — نُفِّذ.** `_TOKENS_PER_WORD` رُفع من `1.3` إلى **`2.4`** — لا إلى ‎1.7‎: القياس الموسّع أعلاه أسقطها ⇒ نافذة `(512 / 2.4) × 0.9 = ‎192‎ كلمة` بدل ٣٥٤، وتداخل ‎19‎ بدل ٣٥. وعند أسوأ نسبة مقيسة (‏2.53) تبلغ النافذة ‎485‎ رمزًا من ‎512‎ — داخل السقف بفسحة ‎27‎ رمزًا.

**تفصيل الشقّ الأوّل (‏الخدمة):**

| الموضع | ما تغيّر |
|---|---|
| `services/embedding/app.py` | `_DEFAULT_MAX_SEQ_LEN = 512` · `_load_model(model_name, max_seq_length)` يُطبّقه **بعد** `.half()` على الجسم الذي سيُخدَم فعلًا · `EMB_MAX_SEQ_LEN` مسربًا على مستوى الخدمة إلى جانب `EMBEDDING_MODEL`/`EMBEDDING_DIM`/`EMB_BATCH` |
| `GET /health` | يُذيع `max_seq_length` النافذ. **هذا نصف الإصلاح الحقيقيّ:** خلاف اسم النموذج — الذي يحرسه ‎400‎ صريح — لا حارسَ لهذا الرقم، ولا وسيلةَ لرؤيته من خارج الحاوية. وغيابُه هو تحديدًا ما أبقى الـ‎128‎ غير مرئيّة حتّى قِيست داخل الحاوية |
| `docker-compose.yml` | `EMB_MAX_SEQ_LEN: "512"` مثبَّتًا صراحةً، ومعه تحذير أنّ تغييره يُبطِل كلّ متّجه مفهرَس |
| `tests/unit/test_embedding_service_app.py` | ثلاثة اختبارات جديدة — **مُتحقَّق أنّها تسقط على القيمة ‎128‎ وتنجح على ‎512‎**، فليست تثبيتًا للسلوك القائم |
| `tests/integration/test_embedding_service_live.py` | جديد. البرهان الفعليّ الذي لا يقدر عليه اختبار وحدة: `tokens` المُعادة تتجاوز ‎128‎ (‏`tokenize` تقصّ عند `max_seq_length`، فالعدد نفسه دليل)، ونصّان يشتركان في ‎٢٠٠‎ كلمة ويختلفان بعدها **لا يعودان متّجهًا واحدًا** — وهو ما كان يفعله البتر بالضبط |

**تفصيل الشقّ الثاني (‏المعامل):**

| الموضع | ما تغيّر |
|---|---|
| `domain/chunking.py` | `_TOKENS_PER_WORD` ‏`1.3` ← **`2.4`**، ومعه جدول القياس كاملًا في التعليق: **الصيغة** لا تزال صيغة alpha المنقولة حرفيًّا، و**الرقم** لم يعد رقمها. ومع الرقم تحذيرٌ صريح أنّ «إعادة `1.3` باسم الأمانة للنقل» تُعيد فتح العطب نفسه — فهذا أوّل خروجٍ **متعمَّد** على alpha في النقل، ولا بدّ أن يُميَّز من الانحراف غير المقصود |
| التعليق نفسه | صُحّح فيه خطأ وصفٍ قديم: كان يقول إنّ المعامل «كلمات لكلّ رمز»، وهو رموزٌ لكلّ كلمة — كما يقول اسمه وكما تستعمله القسمة |
| `max_words_for_token_limit` | لا تغيير في الشيفرة · التوثيق يُصرّح أنّ ‎512‎ تُعطي ‎192‎ كلمة لا ٣٥٤ |
| `settings.py` | إشارةٌ مقابلة تربط `embedding_max_input_tokens` بـ`EMB_MAX_SEQ_LEN` في الخدمة — نصفا السقف الواحد، ولا شيء يحرس تطابقهما |
| `tests/unit/test_knowledge_pipeline.py` | الأرقام المثبَّتة حُدِّثت (‏‎192‎/‎19‎ بدل ٣٥٤/٣٥) · واختبارٌ جديد يثبّت **الخاصيّة** لا الرقم: `‎192‎ × 2.53 ≤ 512` — يسقط فورًا إن خُفِّض المعامل، وهو ما تفشل به الأرقام المثبَّتة وحدها في التعبير عنه |

⚠️ **الإنجليزية تدفع الثمن:** بمعامل عربيّ واحد للّغتين تستهلك النافذة الإنجليزية ‎277‎ رمزًا من ‎512‎ وسطيًّا — أي أنّ ‎٤٦٪‎ من الميزانية تبقى دون استعمال. البديل — معامل لكلّ لغة — مرفوض لسببين مذكورين في التعليق: الميزانية تُحسم **مرّة واحدة** في `IndexDocument.__init__` قبل قراءة أيّ نصّ فلا لغةَ يُفرَّع عليها، ورَبْطُ حجم النافذة بالمحتوى يجعل حدود القطع — و`seq` معها — رهينةَ استدلالٍ على الكتابة.

⚠️ **وعدد القطع يكاد يتضاعف** (‏النافذة ٣٥٤ ← ١٩٢). فبـ`default_k = 5` يهبط النصّ المُسترجَع بالنسبة نفسها ⇒ **هذا يشدّ §3‑ب (`max_context_tokens`) والموجة أ (`default_k`) إلى الشحن مع هذه الموجة، لا بعدها.** ولا يُعالَج الأمر بترك المتّجهات خاطئة: متّجهٌ لنصفِ قطعةٍ لا يُصلحه أيّ `k`.

⚠️ **البتر لم يُلغَ بالشقّ الأوّل وحده، بل انتقل من ‎128‎ إلى ‎512‎ — والشقّ الثاني هو ما أنزله تحت السقف فعلًا.** ‏٧٩٪ ← ‎~١٤٪‎ ← صفرٌ مقيسًا على أسوأ نافذة.

⚠️ ~~**يستلزم إعادة فهرسة**~~ ✅ **نُفِّذت 2026‑08‑25** (§4‑ب). المتّجهات القائمة كانت كلّها مبتورة ولا تُصلَح إلّا بإعادة حسابها، فأُعيد بناء الصورتين وأُعيدت الفهرسة. `GET /health` يُذيع الآن `"max_seq_length":512`، والقياس على الخدمة الحيّة بنفس النصّ: `cos(354 كلمة, أوّل 100 كلمة)` **‏1.000000 ← 0.927171** — أي أنّ المتّجهين كانا متطابقين حرفيًّا، فصارا يختلفان.

### هـ — `row_to_sentence` فقد تنظيفين من alpha 🟡

```python
# alpha table_processor.py:46-48
clean_val = str(val).replace("\n", " ").strip()
if not clean_val or clean_val.lower() in ("none", "null"):  continue
```
```python
# aizzak domain/tables.py::_cell_text
return str(value).strip()          # لا طيّ للأسطر · لا إسقاط none/null
```

`"nan"` مُسقَط (`pdf_tables.py:456`)، والاثنان الآخران لا. وخلايا كامِلوت `stream` تحمل `\n` داخليًّا بكثرة (الخلايا متعدّدة الأسطر)، فتدخل الأسطر إلى نصّ الجملة المُضمَّنة.

### و — صدق البتر مكسورٌ في نصف الاسترجاع 🟡

alpha يختم القصّ بعلامة: `parent[:max] + "\n…[parent chunk truncated]…"`.
aizzak يقصّ صامتًا: `replace(candidate, text=parent.text[:max_parent_chunk_chars])`.

و§3.10 من خطّة الفهرسة ترفع «**صدق البتر** — مكسب AIZZAK لا يُكسَر» قاعدةً. هي مُلتزَمة في الفهرسة (‏`truncated` في الجداول وOCR والتلخيص) ومكسورة هنا وحدها.

---

## 4. النقاط المفتوحة — ١٩ نقطة

### أ · ثلاث نقاط **كانت مُصلَحة ثمّ تراجعت** — فهي أعطاب حيّة اليوم

| # | البند | حالة `HEAD` | الإيداع المُصلِح | الإيداع المُتراجِع |
|---|---|---|---|---|
| ١ | `default_k = 5` رقمٌ من عصر الصفحة (السبب ٢) | **‏5 حيًّا** — ‏١٣ قطعة تجتاز كلّ البوّابات ثمّ يرمي `budgeted[:k]` ثمانيًا · ⚠️ **قياسٌ من عالمٍ بلا آباء نثريّة** (أدناه) | `524ec8f` (‏← 20) | `0461119` |
| ٢ | `mmr_lambda = 0.7` يقودها التنويع (السبب ٦) | **‏0.7 حيًّا** — نافذة λ المقيسة `[0.817, 0.917]` والقيمة خارجها | `c7235ba` (‏← 0.87) | `60f3838` |
| ٣ | **`files.FileDeleted` لا يبلغ أيّ مُستقبِل في `knowledge`** | ✅ **مُصلَح** — تسلسل متزامن: `framework/di/file_deletion.py` | — | `b2ff5fb` (‏أزال **التوثيق** نفسه) |

~~⚠️ **البند ٣ هو الأخطر لأنّه فقد توثيقه:** حذف المستخدم لملفّ **لا يفعل شيئًا بفهرسه** — المستند يبقى `indexed`، وقطعه ونقاطه تبقى قابلة للبحث، والإجابة قد تستشهد بملفّ حذفه المستخدم. وهو **كيف صُنع الازدواج** في السبب ٧ أصلًا. التراجع `b2ff5fb` أزال المدخلة التي كانت تسجّله، فلا أثر له اليوم في أيّ ملفّ.~~

✅ **البند ٣ نُفِّذ.** `DELETE /api/v1/files/{id}` يمرّ الآن عبر `DeleteFileService` (‏`framework/di/file_deletion.py`): وسمُ الملفّ أوّلًا ثمّ `PurgeFileKnowledge` — نقاط Qdrant قبل صفوف Postgres، وكلّ مستندات الملفّ لا أحدثها (فتذهب نسخة إعادة الفهرسة مع أصلها، وهو **جذر السبب ٧**). الاختيار: **تسلسل متزامن عند جذر التركيب** على غرار `space_deletion.py`، لا ترقيةُ الحدث إلى `stream.files` — العامل لم يعد مشترَكًا هناك، والنشر غير المتزامن يترك نافذةً يبقى فيها المحذوف قابلًا للاستشهاد.

> **`purge_file` طريقة مستودعٍ مستقلّة، لا حلقةً على `purge`.** الحلقة كانت الأصغر والأخطأ: `fk_reindex_item_doc` بلا `ON DELETE`، فحذفُ ملفّ أُعيدت فهرسته كان ليصير `23503`. و`test_file_cascade_live.py` يزرع ذلك الصفّ عمدًا.

> **إن كان التراجع مقصودًا** (‏تأجيل موجة أ حتّى تُنفَّذ إعادة الفهرسة) فهو قرار متماسك: أرقام `k` و`λ` تتغيّر دلالتها بعد أب الصفحة. **لكنّ التوثيق لا يُتراجَع مع الشيفرة** — وس-29 صار أضيق ممّا يجب: السؤال الأسبق ليس «ماذا نفعل ببصمة مكرّرة» بل «ماذا يفعل حذف الملفّ بفهرسه»، والجواب اليوم: لا شيء.

> **⚠️ وتقييدٌ على البند ١ يغيّر دلالته:** القياس `budgeted_count: 13 → context_nodes: 5` جرى على مستندات **‏`parent_id = NULL` على كلّ النثر** — ‏مثبَتةً بالقياس على كلّ الـ١٧ قطعة من قسم `3.6`، والقطع الخمس حملت ‎٧٢٥‎ حرفًا إجمالًا أي **‏١٤٥ حرفًا للقطعة**، وهي أوراق لا آباء (‏الأب ‎4000‎). فبعد §5:
>
> * **عَرَض «يرمي ٨» يذوب** — `_widen_to_parents` **يُسقط المكرّرات بالأب**، فـ`retain_k = 15` ورقةً من ٤ صفحات تصير ٤ مدخلات، ثمّ تقصّها الميزانية قبل `[:k]`.
> * **لكنّ `k` لا يذوب كلّه** — فهو يقود عرض القمع أيضًا: `search_k = k×6` · `mmr_pool_k = k×6` · `retain_k = k×3`. والإسقاط بالأب **يستهلك فائض الـ3x بلا تعويض** (‏الإسقاط بعد قصّ `retain_k`)، فحجّة توسيع `k` **تقوى** بعد إعادة البناء لا تضعف.
> * ⇒ **التراجع `0461119` كان أصوب ممّا يبدو:** `20` عُوير على العالم غير المُعاد بناؤه. والرقم الصحيح يُقاس عندئذٍ على `widened_count` **بعد** الإسقاط، لا على `budgeted_count` قبله.

### ب · ~~إعادة البناء (§5) لم تُنفَّذ~~ ✅ **نُفِّذت 2026‑08‑25**

~~`PIPELINE_VERSION` رُفِع ٢ ← ٣ مع `_attach_text_parents`. وحتّى تُنفَّذ §5 فكلّ مستند مفهرَس اليوم يبقى `parent_id = NULL` على النثر ⇒ `P-34` ما يزال يعمل على الجداول وحدها ⇒ **السببان ١ و٥ مُصلَحان في الشيفرة وغير مُصلَحين في المنتج**.~~

⚠️ **التشخيص كان أقوى من الواقع في نصفه.** «كلّ مستند مفهرَس اليوم» ليست دقيقة: من مستندات الكوربوس الثلاثة كان **واحد** قد فُهرِس بعد `40b1542` فحمل آباء النثر فعلًا (‏850 من 852 قطعة). فـ`P-34` لم يكن ميتًا في المنتج بل **حيًّا على ثلث الكوربوس**، وهذا بالضبط ما جعل قياس الاسترجاع الحيّ مُضلِّلًا: السؤال الواحد كان يعود بصفحةٍ كاملة (‏‎3045‎ حرفًا) من النسخة المفهرَسة بعد `P-34` وبكِسرةٍ من **‎25‎ حرفًا** من النسخة المفهرَسة قبله — نفس الملفّ، نفس السؤال، والفارق **تاريخُ الفهرسة وحده**.

#### ما نُفِّذ فعلًا، وكيف

**الإجراء اليدويّ في §5 من خطّة الفهرسة تجاوزه الزمن.** الخطّة تقول «لا أداة إعادة فهرسة جديدة… الإجراء يدويّ» لأنّها كُتبت والبيئة فارغة (2026‑08‑16). لكنّ `POST /knowledge/reindex` (‏`BE-RAG-007`) بُني منذئذٍ، و`ReindexDocuments` يؤدّي خطوات §5 **١ و٢ و٤** بنفسه وبترتيبها الصحيح وتحت `INV-K3`/`INV-K4`: يحذف نقاط المستند أوّلًا، ثمّ صفوفه، ثمّ يسكّ مستندًا `pending` جديدًا على نفس `file_id` ويُلحِق `DocumentRegistered` ليقرأ العاملُ الملفَّ من MinIO كالمعتاد.

⚠️ **وخطوة §5‑١ كما هي مكتوبة صارت الخيار الأسوأ.** «حذف تجميعة `kn-<workspace_id>`» يُسقط معها فهارس الحمولة الثلاثة (`workspace_id` · `document_id` · `space`) التي يُوفّرها `app.ops.payload_indexes`. وحذف النقاط مكافئ لها هنا تمامًا — قِيس: ‏‎1724‎ نقطة في Qdrant مقابل ‎1724‎ صفًّا في `chunks`، فلا نقاط يتيمة تنجو من الحذف النقطيّ — وأسلم منها لأنّه يُبقي التجميعة وفهارسها.

**⚠️ وسقطت خطوة §5‑٣ من الحساب.** الخطّة تقول إنّ `pipeline_version` «رُفِع إلى ٣ مع الخطوة ٢٢ فالمطلوب اليوم ١ و٢ و٤ لا أكثر» — وهذه جملة كُتبت قبل بندَي الموجة ب الأوّلين. بعدها نزل تغييران يُغيّران شكل المخزَّن ولم يرفع أيٌّ منهما الثابت: `EMB_MAX_SEQ_LEN` ‏128 ← 512 (يُغيّر كلّ متّجه)، و`_TOKENS_PER_WORD` ‏1.3 ← 2.4 (يُغيّر حدّ القطعة: ‏354 ← 192 كلمة). وبدون الرفع تكذب البصمة في اتّجاهين: `IndexFile` يُجيب «محدَّث» عن مستند بناه المقطِّع القديم (‏§6 خطر ٤ حرفيًّا)، وإعادةُ البناء **تَسِم مُخرَجها بـ‏`3`** — الرقم نفسه الذي تحمله صفوف أنتجها معامل ‎1.3‎، فيصير العمود الوحيد الموجود للتمييز بينهما شاهدًا على تطابقهما. ⇒ **رُفِع ٣ ← ٤ قبل إعادة البناء.**

#### النتيجة المقيسة

| | قبل | بعد |
|---|---|---|
| القطع | ‏1724 | **‏1731** |
| `parent_chunks` | ‏88 | **‏131** |
| قطعة تحمل أبًا | ‏1246 (‏72٪) | **‏1726 (‏99.7٪)** |
| نقاط Qdrant | ‏1724 | **‏1731** — تساوي صفوف `chunks` بالضبط |
| فهارس الحمولة | ٣ | **٣** (‏نجت) |
| `pipeline_version` | ‏2 · 2 · 3 | **4 · 4 · 4** |
| أطول قطعة | **‏354 كلمة** | **‏192** = `int(512/2.4*0.9)` |
| قطع فوق ‎192‎ كلمة | ٧ | **٠** |
| التلخيصات | ٤ | **٠** — أتلفتها §5‑٢ كما تنصّ · نُسخت احتياطيًّا |

**والبتر الصامت زال، مقيسًا على الخدمة الحيّة** بنفس النصّ قبل وبعد:

| | قبل (‏128) | بعد (‏512) |
|---|---|---|
| `cos(354 كلمة, أوّل 100 كلمة)` | **‏1.000000** | **‏0.927171** |
| `cos(354 كلمة, أوّل 60 كلمة)` | ‏0.999592 | ‏0.854360 |

`1.000000` تعني متّجهين متطابقين: كلّ ما بعد الكلمة ‎~72‎ لم يكن يصل إلى المتّجه أصلًا.

**وعلى الاسترجاع** (‏نفس السؤالين · `default_k = 5`):

| | قبل | بعد |
|---|---|---|
| س١ — حروف مُسلَّمة | ‏4286 | **‏8908** |
| س٢ — حروف مُسلَّمة | ‏3678 | **‏9381** |
| س٢ — أصغر قطعتين | **‏25 و‎73‎ حرفًا** | ‏1650 و‎1465‎ (‏أبوا صفحة) |
| س١ — `widened ← budgeted` | ‏10 ← 10 (‏لا تعضّ) | **‏10 ← 6** |

آخر سطر هو التحوّل البنيويّ: **ميزانية السياق صارت القيد الفعليّ** بعدما صار كلّ مرشّح أبًا مرصوصًا — وهو ما تنبّأت به §3‑ب.

#### ثلاثة تحفّظات تُسجَّل مع النتيجة

1. ⚠️ **أثر §3‑ب غير مرئيّ في هذا القياس، ولا يجوز نسبته إليه.** الكوربوس الحيّ **صفر حرف عربيّ** (‏مقيسًا: ‏4.00 حرفًا/رمز بالضبط)، وعندها ‎3000‎ رمز × ‎4.0‎ = ‎12000‎ حرف = السقف الحرفيّ نفسه. أي أنّ ‎3000‎ و‎6000‎ **لا يُفرَّق بينهما على نصّ لاتينيّ**؛ أثر البند ٣ يظهر على العربيّ وحده.
2. ⚠️ **الملفّ المكرّر صار أغلى لا أرخص.** `criteria.pdf` مرفوع مرّتين ببصمة واحدة (‏السبب ٧ · س-29). قبل إعادة البناء كان يُضاعف المرشّحات (‏كلّ درجة كثيفة/متفرّقة تظهر مرّتين بالضبط)؛ وبعدها صار يُضاعف **الأبناء المرصوصين**: س٢ تعود بالصفحة ‎41‎ مرّتين، ‎3045‎ حرفًا لكلٍّ ⇒ **‏6090 من ‎12000‎ حرفًا — ‏51٪ من الميزانية على نصٍّ واحد**.
3. ⚠️ **إعادة البناء الثانية ستفشل اليوم.** `DocumentRepository.purge` يحذف `summaries` و`chunks` والمستند، ولا يحذف `reindex_job_items` — بينما `purge_file`/`purge_space` يحذفانها. و`fk_reindex_item_doc` بلا `ON DELETE`. فأيّ إعادة فهرسة **ثانية** لمستند أنتجته إعادةٌ أولى تصطدم بـ:

   ```
   ERROR: update or delete on table "documents" violates foreign key
          constraint "fk_reindex_item_doc" on table "reindex_job_items"
   ```

   مُثبَتٌ بمعاملة أُلغيت (`ROLLBACK`). ولم يظهر قبل اليوم لسبب بنيويّ: كلّ اختبارات الوحدة تعمل على مستودع وهميّ، والقيد قيدُ قاعدة بيانات — فلا يراه إلّا اختبار حيّ، و`tests/integration/test_knowledge_repository_rls.py` يغطّي §7 «‏purge + reindex jobs» ولا يُجرّب هذه الحالة. ⇒ **لم يُصلَح هنا** (‏خارج نطاق البند ٤)، ويجب أن يُصلَح **قبل** البندين ٥ و٦.

### ج · الأسباب التسعة — الحالة اليوم

الترقيم ١..٩ المستعمَل في هذا الملفّ كلّه معرَّفٌ هنا، وهذا الجدول مرجعه الوحيد.

| # | السبب | الحالة |
|---|---|---|
| ١ | انهيار حبيبة القطعة ‎١٦×‎ (`P-10`) | ✅ **مُصلَح في الشيفرة وفي المنتج** — §5 نُفِّذت 2026‑08‑25: ‏99.7٪ من القطع تحمل أبًا (‏كانت ‎72٪‎)، وكِسَر الـ‎25‎ حرفًا في الاسترجاع صارت أبناء صفحة (§4‑ب) |
| ٢ | `default_k = 5` رقمٌ من عصر الصفحة (`P-40`) | ⬜ **أُصلِح ثمّ تراجَع** — ‏5 حيًّا (§4‑أ‑١) · س-28 |
| ٣ | ميزانية السياق معطَّلة عمليًّا (`P-35`) | 🟡 **جزئيًّا** — `max_context_tokens` ← ‎6000‎ (§3‑ب) أنهى انقسام السلوك بالخطّ · وبعد إعادة البناء **صارت الميزانية تعضّ فعلًا** (‏س١: ‏10 ← 6 بعد أن كانت 10 ← 10) · ويبقى `max_context_chars = 12000` الذي يتّسع لأبَوين لا لثلاثة |
| ٤ | كامِلوت يشرّح النثر ويلوّثه (`P-06` `P-07` `P-13`) — د‑١..د‑٤ | ⬜ **أربعتها مفتوحة** · تستلزم إعادة فهرسة · الجذر في §3‑أ أعلاه |
| ٥ | الأب لا يغطّي النصّ — س-27 (`P-34`) | ✅ **مُصلَح** — `_attach_text_parents` بلغ المنتج بإعادة البناء: ‏`parent_chunks` ‏88 ← 131، والقطع بلا أب خمسٌ فقط وكلّها تذييل صفحة (§4‑ب) |
| ٦ | معايرة MMR مقلوبة فعليًّا (`P-23`) | ⬜ **أُصلِح ثمّ تراجَع** — ‏0.7 حيًّا (§4‑أ‑٢) · س-31 |
| ٧ | الملفّ مفهرَس مرّتين (`P-03`) | 🟠 **اشتدّ لا خفّ** — جذره الأوّل مُسدّ (§4‑أ‑٣) لكنّ س-29 حيّ في الكوربوس: `criteria.pdf` مرفوع مرّتين ببصمة واحدة، وبعد إعادة البناء صار يُنفق **‏6090 من ‎12000‎ حرفًا (‏51٪)** على صفحة واحدة مكرّرة (§4‑ب) |
| ٨ | BM25 انهار إلى علم ثنائيّ (`P-25` `P-27`) · و`max_sparse_candidates = 20` ضيّق | ⬜ مفتوح · ويتفاعل مع §3‑ج (‏غياب `b`) |
| ٩ | التعليمات أثقل من الدليل (`P-36` `P-37`) | ⬜ تابعٌ لـ١+٢ |

### د · بقايا مراجعة فرع الاسترجاع §12.3

| # | البند | تحقّقي |
|---|---|---|
| ب‑١ | `ready_names` بلا اختبار حيّ على شرط دورة الحياة | ✅ **ما يزال** — لا اختبار تكامل يمسّه؛ اسم ملفّ محذوف قد يُعرَض |
| ب‑٢ | رأس الكوربوس يمشي المستودع كلّه للعدّ | ✅ **ما يزال** — `DocumentRepository` بلا `count` |
| ب‑٣ | الاسم الفارغ غير مثبَّت على طرف `files` | ⬜ مفتوح |

### هـ · قرارات مفتوحة

| # | السؤال | يحكم |
|---|---|---|
| س-28 | `default_k` الصحيح | أُجيب `20` ثمّ **تراجع** ⇒ مفتوح فعليًّا |
| س-29 | البصمة المكرّرة — ~~ويجب أن يتّسع لحذف الملفّ~~ **حذف الملفّ أُجيب** (§4‑أ‑٣) | ٧ |
| س-30 | حارس الجدول الكاذب: أيّ إشارة وأيّ عتبة | ٤ + §3‑أ |
| س-31 | λ تابعةً للنيّة | ٦ |
| ~~**جديد**~~ | ~~هل تُرفَع `max_context_tokens` إلى `6000` كما عند alpha~~ ✅ **حُسم: نعم — والرقم مُشتَقّ، لا مُقتبَس** | §3‑ب |
| ~~جديد~~ | ~~هل يُضبَط `max_seq_length` في خدمة التضمين~~ ✅ **أُجيب: نعم، `512`** — وهو سقف النموذج المعماريّ لا رقم مُختار. ونُفِّذ | §3‑د |

### و · `P-38` — البند الوحيد الذي ينتظر مُدخَلًا من المستخدم

مجموعة تقييم (‏١٥–٣٠ سؤالًا بإجابات مرجعية). وهي تحجز س-22، وتحجز معها معايرة الأسباب ٣ و٦ و٨. ⚠️ **لكنّ §3‑ب و§3‑د أعلاه ليسا معايرة**: الأوّل استعادةُ رقم alpha، والثاني **عطبٌ مُقاس** (‏‎128‎ رمزًا مُثبَتة · والمعامل مقيسٌ الآن ‎2.18‎ للعربيّ المُشكَّل) لا مقايضةَ جودة — كلاهما قابل للحسم اليوم بلا مجموعة تقييم.

---

## 5. الترتيب المقترح

| الموجة | العمل | إعادة فهرسة | ملاحظة |
|---|---|---|---|
| **٠ — بلا كلفة** | ~~إعادة توثيق فجوة `FileDeleted`~~ **نُفِّذت الفجوة نفسها** (§4‑أ‑٣) | ❌ | ~~التحقّق من `max_seq_length`~~ **نُفِّذ: ‎128‎ مُثبَتة** (§3‑د) |
| **أ — أرقام** | إعادة تقرير `default_k` و`mmr_lambda` **بعد** إعادة الفهرسة، مقيسَين على `widened_count` بعد الإسقاط بالأب | ❌ | **تُنفَّذ بعد ب**: الإسقاط بالأب يغيّر ما يعدّه `k`، والميزانية تعضّ قبله (§4‑أ) |
| **ب — الفهرسة** | ~~**`max_seq_length`**~~ ✅ · ~~**معامل الرموز (§3‑د)**~~ ✅ (`2.4`) · ~~**`max_context_tokens` ← 6000 (§3‑ب)**~~ ✅ · ~~**§5 إعادة بناء نظيف**~~ ✅ **نُفِّذت 2026‑08‑25** (‏و`PIPELINE_VERSION` ٣ ← ٤ معها) · **التالي:** حارس الجدول الكاذب (د‑٢) + د‑١/د‑٣/د‑٤ · `_cell_text` (§3‑هـ) · صدق البتر (§3‑و) | ⚠️ **مرّتين، لا مرّة** | ⚠️ **ترتيب هذا الصفّ كان مقلوبًا.** §5 من خطّة الفهرسة تقول «نفّذ أ و ب كاملتين ثمّ أعد البناء **مرّة واحدة**»، وخطرها ٥ يقول «تُجمَّع مرّة واحدة **بعد** ب» — بينما هذا الصفّ وضع §5 **قبل** بندين يُغيّران شكل المخزَّن (‏د‑١..د‑٤ في `pdf_tables.py`، و`_cell_text` الذي يُغذّي `row_to_sentence` ⇒ نصّ كلّ قطعة جدوليّة، و**‏792 من ‎1731‎ قطعة جدوليّة**). فهما يستلزمان دورةً ثانية حتمًا؛ وصدق البتر (§3‑و) وحده استرجاعيّ فلا يستلزم شيئًا.<br>ونُفِّذت §5 الآن رغم ذلك بحساب صريح: الكوربوس ثلاثة ملفّات فالدورة دقائق، ومقابلها أنّ أربعة أيّام من الإصلاح كانت **شيفرةً ميتة في المنتج**، وأنّ موجة أ لا تُقاس أصلًا إلّا على فهرس مُعاد بناؤه. ⚠️ **وقبل الدورة الثانية يجب إصلاح `purge`** (§4‑ب، التحفّظ ٣) وإلّا فشلت. |
| **ج — قرارات** | س-29 (‏حذف الملفّ خرج منه: نُفِّذ) · س-30 · س-31 · `k1`/`b` (§3‑ج) · ب‑١..ب‑٣ | — | |

**ترتيب التنفيذ الفعليّ: ~~٠~~ ← ب ← أ ← ج** (‏٠ نُفِّذت). التسمية أبجدية بحسب الكلفة، لا بحسب الزمن: موجة أ أرخص لكنّها معايرةٌ على أرضية تتحرّك تحتها ما لم تسبقها ب.

**قياس القبول:** إعادة اختبار خطّ الأساس — نفس السؤال ونفس النموذج، ومقارنة الجواب بالمرجع (ب). يبقى أرخص من مجموعة تقييم كاملة ويكفي لحسم «هل عاد القسم؟».

</div>
