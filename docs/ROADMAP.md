<div dir="rtl">

# خارطة طريق المشروع — منصة ذكاء اصطناعي متعددة الوكلاء (Backend)

> **Project Roadmap · v1.0**
>
> مسار تنفيذ متسلسل من التصميم إلى القبول النهائي. الترتيب يحترم **قواعد الاعتماد** (`ARC‑09`):
> نبني من الداخل (Domain) نحو الخارج (Infrastructure / API).

| | |
|---|---|
| **المصدر** | [`Requirements-v1.md`](Requirements-v1.md) · [`architecture.md`](architecture.md) · [`design/`](design/) |
| **الحالة العامة** | ✅ **12/12 مرحلة مكتملة · القبول النهائي مُجتاز — `AC‑01…16` كلّها مُحقَّقة** ([`acceptance-report.md`](acceptance-report.md) · §3.78) |
| **العمل النشط بعده** | 🚧 **رفع حاجبات النشر** — [`release-blockers-plan.md`](release-blockers-plan.md) (القبول قاس المواصفة؛ هذا يقيس **القابليّة للتشغيل**) |
| **آخر تحديث** | 2026‑07‑27 (§3.85 — مزامنةُ هذه الخارطة مع الواقع بعد §3.79…§3.85) |

---

> **هذه الخارطة تقول: ما الذي أُنجز وما الذي بقي — لا كيف أُنجز.** التفصيل خطوةً بخطوة يُحال بالروابط ولا يُستنسَخ هنا:
> [`implementation-plan.md`](implementation-plan.md) (الخطة) · [`implementation-status.md`](implementation-status.md) (حالة التسليم) · [`log/INDEX.md`](log/INDEX.md) (سجلّ كلّ خطوة).
> **مصدر الحقيقة للحالة الحيّة هو تلك الوثائق**؛ فإن اختلفت هذه الخارطة عنها فهي البائدة.
>
> وترقيمُ مراحل خطّة التنفيذ يختلف عن ترقيم هذه الخارطة (‏م1 الأساس ⇒ 5 هنا · م2 المحوّلات ⇒ 6 · م3 الوحدات ⇒ 7 · م4 الإطار ⇒ 8 · م5 الأحداث ⇒ 9 · م6 الـAPI ⇒ 10 · م7 النشر ⇒ 11 · م8 القبول ⇒ 12).

---

## مفتاح الحالة

| الرمز | المعنى |
|-------|--------|
| ✅ | **مكتملة** |
| ✅ + حاشية | مكتملةٌ لنطاق `v1`، وبجوارها دَينٌ **مُعلَنٌ صراحةً** خارج نطاق `AC` |
| 🔵 | قيد التنفيذ |
| ⬜ | لم تبدأ |

> **عن مربّعات كلّ مرحلة أدناه:** هي قائمة **نطاقها الأصليّ** كما كُتبت قبل التنفيذ، لا مربّعات حيّة تُؤشَّر واحداً واحداً؛ الحالة المُعتمَدة هي رمز عنوان المرحلة والجدول أعلاه.

---

## نظرة عامة على المراحل

| # | المرحلة | الحالة |
|---|---------|--------|
| 1 | كتابة المتطلبات | ✅ مكتملة |
| 2 | بناء الهيكل المعماري (HLD) | ✅ مكتملة |
| 3 | مخططات LLD وكتابة العقود | ✅ مكتملة |
| 4 | هيكل مشروع فارغ (Skeleton) | ✅ مكتملة |
| 5 | الأساس والركائز المشتركة (Foundation) | ✅ مكتملة |
| 6 | المنافذ والمحوّلات التحتية (Ports & Adapters) | ✅ مكتملة لنطاق `v1` (‏`EmbeddingProvider` أُغلق §3.77) — **حاشية:** محوّلات Gemini·Claude·OpenRouter لم تُبنَ (محجوبةٌ بالمفاتيح، خارج نطاق `AC`) |
| 7 | وحدات الأعمال العشر (Business Modules) | ✅ مكتملة |
| 8 | الإطار والوكلاء (Framework & Agents) | ✅ مكتملة |
| 9 | المعالجة الخلفية والبثّ (Events & Streaming) | ✅ مكتملة |
| 10 | طبقة الـAPI | ✅ مكتملة |
| 11 | النشر والتشغيل (Deployment) | ✅ مكتملة (7.1 · 7.2 · 7.3) — **حاشية:** المُرحّل وعامل `memory` مُثبَتان حيّاً؛ وعاملا `knowledge` و`media` **صارا موصولَين بالكامل** ([§3.100](log/3.100.md) · [§3.104](log/3.104.md)) ولم يُقلَعا بعد. **العمّال الثلاثة كلّهم يُبنَون من البيئة، ولا محوّلَ ناقصاً يحجب أحدهم** |
| 12 | القبول النهائي (Definition of Done) | ✅ مكتملة (§3.78 — `AC‑01…16` كلّها مُحقَّقة · [`acceptance-report.md`](acceptance-report.md)) |
| — | **ما بعد القبول** — تصفية الدَّين ✅ · رفع حاجبات النشر 🚧 | [انظر القسم الأخير](#ما-بعد-القبول--مسارٌ-تلا-اجتياز-ac0116) |

---

## المرحلة 1: كتابة المتطلبات ✅

**المخرَج:** [`docs/Requirements-v1.md`](Requirements-v1.md)

- وثيقة متطلبات كاملة (وظيفية `FR` · غير وظيفية `NFR` · أمنية `SEC` · بيانات `DAT` · أحداث `EVT` · تشغيل `OPS`).
- قيود معمارية إلزامية محسومة مسبقاً (`ARC‑01…15`).
- حزمة تقنيات إلزامية + معايير قبول (`AC‑01…16`).
- **نقاط القرار المفتوحة `OQ‑01…06` محسومة** (توقيع العميل 2026‑07‑10).

---

## المرحلة 2: بناء الهيكل المعماري (HLD) ✅

**المخرَج:** [`docs/architecture.md`](architecture.md)

- مخططات C4 (سياق النظام L1 · الحاويات L2).
- البنية الطبقية الخمسية + وحدات الأعمال العشر.
- سجل القرارات المعمارية **26 ADR** (`D‑01…D‑26`).
- معمارية الأمن · العمارة المدفوعة بالأحداث · معمارية النشر.

---

## المرحلة 3: مخططات LLD وكتابة العقود ✅

**المخرَج:** [`docs/design/`](design/)

- نموذج البيانات + هجرات لكل وحدة ([`01-data-model.md`](design/01-data-model.md)).
- عقود المنافذ المحايدة للمزوّد ([`02-port-contracts.md`](design/02-port-contracts.md)).
- مواصفة الـAPI (OpenAPI) ([`03-api-spec.md`](design/03-api-spec.md) · [`openapi.yaml`](design/openapi.yaml)).
- كتالوج الأحداث + JSON Schemas ([`04-event-catalog.md`](design/04-event-catalog.md) · [`events/`](design/events/)).
- RBAC + الإعداد/الأسرار · نماذج النطاق · NFR/SLO · دليل التشغيل · استراتيجية الاختبار · معايير الكود.
- دليلا تأليف الوكلاء والوحدات.

---

## المرحلة 4: هيكل مشروع فارغ (Skeleton) ✅

**المخرَج:** هيكل مجلدات المشروع مطابقاً للطبقات الخمس.

- شجرة المجلدات: `framework · api · agents · modules · infrastructure`.
- ملفات `__init__` وحدود الوحدات الأولية.
- تهيئة أدوات المطوّر (`pyproject` · `ruff` · `mypy` · `pytest`).

---

## المرحلة 5: الأساس والركائز المشتركة (Foundation) ✅

> تُبنى أولاً لأن كل وحدة تعتمد عليها. **ابدأ بـ import-linter و CI قبل أي كود وحدة.**

- [ ] **Settings Service** مركزي + عقد `Settings` (Pydantic v2) — `ARC‑12`.
- [ ] **Composition Root** + Manual DI — `ARC‑11`.
- [ ] إعداد **import-linter** في CI مبكراً — `ARC‑10` · `D‑17`.
- [ ] الأعمدة القياسية + مولّد **UUIDv7** (في التطبيق) — `DAT‑04/05`.
- [ ] Base classes للـ Domain / Repository Pattern — `DAT‑08`.
- [ ] schema بنيوي `platform` (`outbox` · `processed_events` · `stream_offsets`).
- [ ] تفعيل **Ruff · mypy --strict · pytest + pytest-asyncio + testcontainers**.

**معيار الإنجاز:** `AC‑01` (import-linter يمرّ) · `AC‑09` (جودة الكود).

---

## المرحلة 6: المنافذ والمحوّلات التحتية (Ports & Adapters) ✅ **لنطاق `v1`**

> تعريف المنافذ في **Framework** وتنفيذ محوّلاتها في **Infrastructure**.
>
> **ما بقي مفتوحاً (دَينٌ مُعلَن، خارج نطاق `AC`):** محوّلات المزوّدين السحابيّة الثلاثة (Gemini · Claude · OpenRouter) لم تُبنَ — محجوبةٌ بالمفاتيح، والمنفذ نفسه مُنفَّذٌ ومُثبَتٌ بمحوّلَي Ollama وOpenAI. و`EmbeddingProvider` (2.10) **أُغلق** بنموذجٍ محلّيٍّ خلف خدمة تضمينٍ مركزيّة ([§3.77](log/3.77.md))، وبُرهن حيّاً داخل الحاويات في [§3.83](log/3.83.md).

- [ ] المنافذ المحايدة للمزوّد: `LLMProvider · EmbeddingProvider · VectorStore · StorageProvider · CacheProvider` — `FR‑70…73` · `POR‑01`.
- [ ] منافذ البنية التحتية: `SecretsProvider · AuthProvider · ConnectorProvider / MCPClient`.
- [ ] محوّلات: Postgres (SQLAlchemy 2.0 async) · Redis · MinIO · **Qdrant** · **Vault (Transit)** · Firebase.
- [ ] محوّلات المزوّدين الأصلية: OpenAI · Gemini · Claude · Ollama · OpenRouter — `D‑15`.
- [ ] `ProviderResolver` من الإعداد لا من الكود — `FR‑73` · `D‑16`.
- [ ] ضبط **PgBouncer + asyncpg** (`statement_cache_size=0`) — `OPS‑02`.

**معيار الإنجاز:** `AC‑10` (الأسرار عبر Vault).

---

## المرحلة 7: وحدات الأعمال العشر (Business Modules) ✅

> كل وحدة كاملة (Domain → Application → Ports → Adapters + هجرة Alembic + RLS). الترتيب حسب الاعتماد.

**المجموعة أ — التمكين والأمان:**
- [ ] `workspace` (المستأجر / حدّ العزل) — `FR‑01…03`.
- [ ] `access` (RBAC) — `SEC‑02` · `D‑24`.
- [ ] `credentials` (مفاتيح LLM · Vault Transit) — `FR‑75`.

**المجموعة ب — التفاعل والمحتوى:**
- [ ] `conversations` · `memory` — `FR‑80/81`.
- [ ] `files` (مشتركة · MinIO) — `FR‑60…62`.

**المجموعة ج — المعرفة والتوليد:**
- [ ] `knowledge` (RAG · فهرسة + استرجاع).
- [ ] `media` (صور / فيديو).

**المجموعة د — التكامل والقياس:**
- [ ] `integrations` (موصّلات · OAuth · MCP بعيد · تجديد كسول) — `FR‑120…124`.
- [ ] `usage` (منافذ واردة · كائن قرار · التقاط إدمپوتنسي) — `FR‑130…134`.

**معيار الإنجاز:** `AC‑03` (عزل الوحدات) · `AC‑05` (عزل المستأجر) · `AC‑14` (توسّع الوحدات) · `AC‑15` · `AC‑16`.

---

## المرحلة 8: الإطار والوكلاء (Framework & Agents) ✅

- [ ] **PluginLoader + AgentRegistry** (اكتشاف آلي عبر importlib) — `FR‑31` · `D‑13`.
- [ ] **BaseAgent** + دورة الحياة الخمسية (Created→…→Disposed) — `FR‑15`.
- [ ] الوكيل **عديم الحالة** يُنشأ لكل طلب ويُتلَف — `FR‑10/11`.
- [ ] **Tool System** + `ToolRegistry` + الكتالوج الديناميكي لكل Workspace — `FR‑50…52`.
- [ ] **WorkflowEngine + WorkflowRegistry** (قابل للتوسعة) — `FR‑40/41`.
- [ ] الوكلاء الخمسة المُسبقون: RAG · Data Analysis · Image · Video · File Editing — `FR‑20`.

**معيار الإنجاز:** `AC‑04` (إثبات Plugin) · `AC‑06` (دورة الحياة) · `AC‑12` (Workflow).

---

## المرحلة 9: المعالجة الخلفية والبثّ (Events & Streaming) ✅

- [ ] **Transactional Outbox + Relay** → Redis Streams → Workers (Consumer Groups) — `EVT‑10…13` · `D‑18/20`.
- [ ] **Idempotency** الفعّالة (`processed_events` داخل نفس المعاملة) — `EVT‑05`.
- [ ] **DLQ** بعد عتبة الفشل `N` — `NFR‑04`.
- [ ] **WebSocket** تفاعلي + **Streaming Responses** (SSE للردّ الواحد) — `FR‑90/91` · `D‑10`.

**معيار الإنجاز:** `AC‑08` (عقد الأحداث + Idempotency) · `AC‑11` (البثّ).

---

## المرحلة 10: طبقة الـAPI ✅

- [x] Routers تحت `/api/v1` (بلا منطق أعمال — تفويض إلى Agents/Application) — `FR‑100`.
- [x] نموذج أخطاء **RFC 9457 Problem Details** — `API‑01`.
- [x] ترقيم بالمؤشّر (cursor) · تسمية `snake_case` · غلاف المجموعات — `API‑02…05`.
- [x] تحقق **Firebase JWT محلي** + **RBAC guards** — `SEC‑01` · `D‑25`.

**معيار الإنجاز:** `AC‑07` (مطابقة عقد الـAPI) — **مُختبَر آليّاً** ضد [`openapi.yaml`](design/openapi.yaml) في الاتجاهين (العمليات · الترقيم · الغلاف · الإصدار · مخطّط الأمان).

---

## المرحلة 11: النشر والتشغيل (Deployment) ✅

- [x] **Docker Compose** — `D‑26`. (§3.75 أنشأه بالتوازي؛ ثم نقلت [خطة الحزام الحاوي](containerised-test-harness-plan-v2.md) الاختبارات إليه وأزالت خدمات البنية الأصليّة، مع بقاء Ollama أصلياً داخل WSL)
- [x] طوبولوجيا: Nginx → Gunicorn+Uvicorn (نسخ عديمة الحالة) → خدمات البيانات — `OPS‑01…05`. (§3.75 — ترقية WS **مُثبَتةٌ بجلسةٍ حقيقيّة**: 101 ثمّ إغلاق 1008 بروتوكوليّ · `OPS‑02` مُتحقَّقٌ منه عبر pgbouncer الحاوي)
- [x] بذر قاعدة البيانات ومنحُها خطوةً تشغيليّة — `01 §6`. (§3.75 — `app/ops/provision.py`: السلاسل الإحدى عشرة ثمّ المنح، مصدرُ حقيقةٍ واحد)
- [x] عمّال ومُرحّل Outbox عمليات مستقلة قابلة للتوسّع الأفقي. — المُرحّل وعامل `memory` **يعملان** (‏2.10 §3.77 فكّ التضمين، و§3.83 أثبت الجولة حيّاً داخل الحاويات)؛ الافتراضيّ في المسارَين صار `memory` صراحةً ([§3.82](log/3.82.md)). **حاشية:** عامل `knowledge` **لم يعد محجوباً** — `WorkerDocumentContentResolver` بُني في الخطوتين 15‑16 من [`deferred-adapters-plan.md`](deferred-adapters-plan.md) ([§3.98](log/3.98.md) · [§3.100](log/3.100.md))، والمصنع لا يرفع؛ **لكنّه لم يُقلَع كعمليّةٍ بعد**. وعامل `media` **لم يعد محجوباً** بدوره — `WorkerMediaGenerator` بُني في الخطوة 19 ووُصل في الخطوة 20 من الخطّة نفسها ([§3.103](log/3.103.md) · [§3.104](log/3.104.md))، ومصنعه لا يرفع؛ ولم يُقلَع كعمليّةٍ بعد أيضاً. **فالعمّال الثلاثة يُبنَون من البيئة**، والافتراضيّ يبقى `memory` لسببٍ واحد: هو الوحيد الذي **قِيس** إقلاعه حيّاً
- [x] AppRole لـVault (`D‑22`) + TLS — 7.3 ✅ (§3.76). و**Vault صار دائماً** (‏`storage "file"` بدل وضع `dev` الذاكريّ) في [§3.80](log/3.80.md) — مفتاح Transit ينجو من إعادة التشغيل
- [x] دليل التشغيل المحلي — [`08-local-runbook.md`](design/08-local-runbook.md) (§3.76 — كُتب كاملاً، ويقول صراحةً إنّ `alembic upgrade head` **ليس** أمراً صالحاً هنا: إحدى عشرة سلسلةً مستقلّة يُرمِّز تسلسلَها `app/ops/provision.py`). ودليلٌ عمليٌّ مختصر للمشغّل: [`quickstart.md`](quickstart.md)

---

## المرحلة 12: القبول النهائي (Definition of Done) ✅ **مكتملة (§3.78 · 2026‑07‑25)**

> يُقبل التسليم فقط عند تحقّق **كل** معايير `AC‑01…16`. **✅ 16/16 مُحقَّقة** — التقرير الكامل بالأدلّة المسمّاة في [`acceptance-report.md`](acceptance-report.md).

- [x] `AC‑01/02` مطابقة الطبقات ونقاء النطاق — import‑linter **8/0** · mypy‑strict نظيف.
- [x] `AC‑03/05` عزل الوحدات وعزل المستأجر (إثبات حيّ) — لا FK عابر schema + **121 اختبار RLS حيّ**.
- [x] `AC‑04` إثبات Plugin حيّ (إضافة/حذف وكيل دون تعديل النواة) — `test_agent_runtime` + `test_orchestrator_live`.
- [x] `AC‑06…08` دورة الحياة · API · الأحداث — `test_agent_lifecycle` · `test_api_conventions` · `test_event_envelope`+`processed_events_live`.
- [x] `AC‑09/10` جودة الكود والأسرار — البوابات الخمس خضراء · Vault Transit.
- [x] `AC‑11/12` البثّ و Workflow — SSE 13 + WS 16 · `test_workflow_orchestration`.
- [x] `AC‑13` حسم `OQ‑01…06` مطبّق — مُوقَّع 2026‑07‑10.
- [x] `AC‑14…16` توسّع الوحدات · `integrations` · `usage` — دليلان + `connect_stores_cipher_ref_only` + إدمبوتنسي `usage`.
- [x] تسليم **مخرجات المورّد** الثلاثة عشر — `OPS‑10` (‏**13/13** — `acceptance-report.md §5`).

---

## ما بعد القبول — مسارٌ تلا اجتياز `AC‑01…16`

> القبول قاس **مطابقة المواصفة**. وما تلاه يقيس سؤالاً آخر: **هل المنصّة قابلةٌ للتشغيل فعلاً؟** — تشخيصه في [`pre-release-review.md`](pre-release-review.md).

| المسار | الحالة | أين يُقرأ |
|---|---|---|
| **تصفية الدَّين المُسمّى** (ستّة بنودٍ حملها التسليم) | ✅ مُصفّاة | [§3.79](log/3.79.md) |
| **رفع حاجبات النشر** — 6 خطوات، وكيلٌ واحدٌ لكلٍّ ثمّ مراجعة | 🚧 **حاجبات P0 الأربعة كلّها مرفوعة، والمكدّس حيٌّ** | [`release-blockers-plan.md`](release-blockers-plan.md) — **§2 لوحتها هي الحالة الحيّة** ([§3.80](log/3.80.md)…[§3.85](log/3.85.md)) |

**ما بقي مفتوحاً بعد ذلك — مُسمًّى لا مطويّ:**

- ~~**`DocumentContentResolver`** (يحجب عامل `knowledge`)~~ — **✅ أُغلق** في الخطوتين 15‑16 من [`deferred-adapters-plan.md`](deferred-adapters-plan.md) ([§3.98](log/3.98.md) · [§3.100](log/3.100.md)): المحوّل مبنيٌّ والمصنع لا يرفع. يبقى **أوّل إقلاعٍ حيٍّ للعامل** عملاً تشغيليّاً بإذنٍ منفصل.
- ~~**`MediaGenerator`** (يحجب عامل `media`)~~ — **✅ أُغلق** في الخطوات 18‑20 من الخطة نفسها ([§3.102](log/3.102.md) · [§3.103](log/3.103.md) · [§3.104](log/3.104.md)): فضاء `image` في المحلِّل، ومحوّل `OpenAIImage`، و`WorkerMediaGenerator` موصولاً في `build_media_worker_from_env`. يبقى **أوّل إقلاعٍ حيٍّ للعامل** عملاً تشغيليّاً بإذنٍ منفصل — كما هو حال `knowledge`.
- **توليد الفيديو** — لم يُغلَق ولا يدّعي أحدٌ إغلاقه: يحتاج مزوّداً يُختار ومسارَ جلبٍ لـ`remote_url` ([`deferred-adapters-plan.md`](deferred-adapters-plan.md) §6‑ب). و`MediaKind.VIDEO` اليوم يفشل فشلاً **مصنَّفاً** (`media.unsupported_kind`) لا صامتاً.
- **2.8‑ب‑2** — محوّلات المزوّدين السحابيّة (Gemini · Claude · OpenRouter)، محجوبةٌ بالمفاتيح.
- **تسعة مخاطر `P1`** لا تحجب النشر لكنّها أعمال تشغيلٍ تالية (قياسات وتتبّع وإنذار · تصريف DLQ · سياسات احتفاظ · اختبار حِمل · حدّ معدّلٍ وCORS على الحافّة · تدوير مفتاح Transit) — الجرد في [`release-blockers-plan.md` §5](release-blockers-plan.md).

---

<div align="center">

**خارطة طريق منصة الوكلاء الذكية · v1.0**

`12 / 12 مرحلة مكتملة · القبول مُجتاز · ومسار رفع حاجبات النشر يُتابَع في وثيقته الحيّة`

</div>

</div>
