<div dir="rtl">

# سجلُّ تنفيذ خطّة فجوات وكيل RAG — الحالة والملاحظات

> **RAG Agent Gap Remediation — Progress Log** · الملفُّ المرافق لـ[خطّة التنفيذ](rag-agent-scenarios-implementation-plan.md)
>
> **السؤال الذي يجيب عنه هذا الملفّ:** أين وصل التنفيذ، وما الذي اختلف فيه الواقعُ عن الخطّة، ولماذا — وما الذي بقي مفتوحًا لمن يفتح الموجة التالية.

| | |
|---|---|
| **أُنشئ** | 2026‑08‑29 |
| **آخر تحديث** | 2026‑08‑29 — بعد دمج الموجة ١ |
| **الفرع** | `rag-agent-scenarios` |
| **الخطّة** | [`rag-agent-scenarios-implementation-plan.md`](rag-agent-scenarios-implementation-plan.md) — ١٤ فجوة · ٦ موجات · ١١ بندًا يُنفَّذ |
| **الدراسة** | [`rag-agent-scenarios-review.md`](rag-agent-scenarios-review.md) — ٣٤ سيناريو |

---

## ٠. لماذا ملفّان؟ — القرارُ الذي أنشأ هذا الملفّ

**خطّةٌ نظيفة، وسجلٌّ يتّسخ.** ملفُّ الخطّة يجيب عن سؤالٍ واحد: *ما الذي يُكتب، وأين، ولماذا هكذا؟* — وهو سؤالٌ لا تتغيّر إجابتُه بمرور التنفيذ. أمّا *أين وصلنا* و*ما الذي فاجأنا* فسؤالان يتغيّران كلَّ يوم، وحشرُهما في الخطّة يُنتج ملفًّا لا يُقرأ: كلُّ فقرة تصميمٍ محاطةٌ بأربع فقرات حالة، والقارئ الذي جاء يسأل «كيف صُمِّم ب‑٤أ؟» يقرأ تاريخَ ثلاث محاولات قبل أن يصل.

فالتقسيمُ على هذا النحو:

| | ملفُّ الخطّة | هذا الملفّ |
|---|---|---|
| **يحمل** | التصميم · الحجج · القيود · الاختبارات المطلوبة | الحالة · ما نُفِّذ فعلًا · الفروق عن الخطّة وسببُها · الإشارات الجديدة · ما بقي مفتوحًا |
| **يتغيّر** | فقط حين يتغيّر قرارُ تصميم | مع كلّ موجة |
| **وسمُ الحالة** | **وسمٌ واحدٌ بجوار كلّ بند، لا أكثر** — والتفصيلُ هنا | الجدولُ الكامل في §١ |

⇒ إن وجدتَ في ملفّ الخطّة شرحَ حالةٍ أطولَ من وسم، فهو خطأٌ يُنقل إلى هنا.

**الأوسمة:** ✅ مُنفَّذ ومدموج · 🚧 قيد التنفيذ · 📋 لم يبدأ · ⏸️ مؤجَّل بقرارٍ مكتوب.

---

## ١. لوحةُ الحالة

| البند | الفجوة | الموجة | الحالة | الملفّات المُلامَسة |
|---|---|---|---|---|
| **ب‑١** حارسُ الردّ الفارغ | ف‑٤ | ١ | ✅ | `agent.py` · `test_rag_agent.py` |
| **ب‑٢** حراسةُ نداء أسماء الملفّات | ف‑١٠ | ١ | ✅ | نفسُهما |
| **ب‑٣** الفرعُ الخامس (تلخيصٌ بلا هدف) | ف‑٥ | ١ | ✅ | نفسُهما |
| **ب‑٤أ** التعارضُ جملةً لا خطأً | ف‑٧ (نصف) | ١ | ✅ | نفسُهما |
| **ب‑٥** سجلُّ القياس على مسار الخطأ | ف‑١٤ | ١ | ✅ | نفسُهما |
| **ب‑٦** الفضاءُ المجهول يُضاء | ف‑٩ | ١ | ✅ | نفسُهما |
| **ب‑٧أ** الاسمُ على مسار الحلّ | ف‑٢ | ٢ | 📋 | — |
| **ب‑٧ب** الاسمُ على مسار التثبيت | ف‑٢ | ٢ | 📋 | — |
| **ب‑٧ج** الاسمُ في رسالة التسليم | ف‑٢ | ٢ | 📋 | — |
| **ب‑٤ب** التعارضُ مصنَّفًا | ف‑٧ (تمام) | ٣ | 📋 | — |
| **ب‑٨** الملخّصُ المخزَّن يُقرأ | ف‑٣ | ٤ | 📋 | — |
| **ب‑٩** النيّةُ المعلَّقة على الخيط | ف‑١أ | ٥ | 📋 | — |
| **ب‑١٠** الإجابةُ الجزئيّة تُحفَظ | ف‑٨ | ٦ | 📋 | — |
| **ب‑١١** درجةُ الثقة تدخل القياس | ف‑٦ | ٦ | 📋 | — |
| **ب‑١٢** تمييزُ المصدر الأوّل | ف‑١٢ | ٦ | 📋 | — |
| ف‑١١ المرفقات · ف‑١٣ تخزينُ الأسماء · ف‑١ب نافذةُ السياق | — | — | ⏸️ | §٨ من الخطّة |

**المُنجَز: ٦ فجوات من ١٤ · ١١ فجوة تبقى منها ٥ مؤجَّلة بقرار.**

---

## ٢. الموجة ١ — ما نُفِّذ (2026‑08‑29)

**ملفّان اثنان، كما وعدت الخطّة:** [`src/app/agents/rag_agent/agent.py`](../src/app/agents/rag_agent/agent.py) · [`tests/unit/test_rag_agent.py`](../tests/unit/test_rag_agent.py). صفرُ تغييراتٍ في العقود، صفرُ هجرات، صفرُ توسعاتٍ للسيم، وصفرُ استيراداتٍ جديدةٍ تخرق `agents-independent`.

### ٢.١ البنود الستّة

| البند | ما صار عليه السلوك |
|---|---|
| **ب‑١** | ردٌّ فارغٌ أو مسافاتٌ بيضاء ⇒ جملةُ بوّابة الثقة نفسُها + ترويسةُ الفضاء نفسُها، واستشهاداتٌ **فارغة**، ومسارٌ خاصّ `empty_completion` بـ`llm_ms` **مقيس** |
| **ب‑٢** | فشلُ `list_document_names` ⇒ تحذيرٌ في السجلّ وترويسةٌ غائبة، **والدورُ يكتمل**. و`answer` ما زال عاريًا عمدًا (س‑٢٨) |
| **ب‑٣** | نيّةُ تلخيصٍ بلا وظيفةٍ ولا مرشّحين ⇒ **سؤالٌ** «أيّ ملفّ تريد تلخيصه؟» + قائمةُ الفضاء، بلا نداء نموذج، ومسار `summary_target_unknown` |
| **ب‑٤أ** | `ConflictError` من `answer` ⇒ جملةٌ محايدة، ومسار `summary_conflict`. وأيُّ استثناءٍ آخر يُسقط الدور كما كان |
| **ب‑٥** | كلُّ دورٍ فاشلٍ يكتب `rag_agent.answer` بمسار `error` و`error_type` = **صنفُ** الاستثناء، ثمّ **يُعيد الرفع** |
| **ب‑٦** | سيمُ معرفةٍ مربوطٌ بلا فضاءٍ معروف ⇒ `rag_agent.unscoped_turn` تحذيريًّا، مرّةً واحدة، بمُعرِّف المحادثة وحدَه |

### ٢.٢ الإشارات الجديدة في القياس — لمن يبني اللوحات

| الإشارة | النوع | متى تُبعث |
|---|---|---|
| `rag_agent.answer` · `path="empty_completion"` | INFO | النموذج نُودي وبثّ فراغًا. **الوحيدُ** الذي يجمع «لا إجابة» مع `llm_ms` رقميّ |
| `rag_agent.answer` · `path="summary_target_unknown"` | INFO | تلخيصٌ بلا هدفٍ محسوم — عدّادُ نجاح ب‑٣ |
| `rag_agent.answer` · `path="summary_conflict"` | INFO | بناءٌ مرفوض. **يقيس الحالتين معًا** حتّى تصل ب‑٤ب فتفصلهما |
| `rag_agent.answer` · `path="error"` + `error_type` | INFO | دورٌ فشل. نسبةُ الفشل صارت تُقرأ من وجود سطرٍ لا من غيابه |
| `rag_agent.corpus_header_unavailable` | WARNING | سيمُ الملفّات تعثّر والدورُ نجا — عطبُ تبعيّةٍ يستحقّ لوحة |
| `rag_agent.unscoped_turn` | WARNING | **عدّادُ ق‑أ.** المعيار المكتوب: أكثر من مرّةٍ في الألف دورٍ خلال أسبوعين ⇒ يُرفَض الدورُ بدل التحذير |

**و`error_type` أُضيف إلى كلّ سجلّ** (قيمتُه `null` خارج مسار الخطأ) لا إلى سجلّ الخطأ وحدَه — على قاعدة `llm_ms` نفسِها: حقلٌ حاضرٌ دائمًا وفارغٌ أحيانًا يُقرأ، وحقلٌ يظهر أحيانًا لا يُقرأ.

### ٢.٣ الاختبارات

**٢٧ اختبارًا جديدًا** (الخطّة قدّرت ~٢٠) · المجموع في الملفّ **٩١** · كلُّها خضراء.

| المجموعة | العدد | يشمل الحارسَ الحاضن |
|---|---|---|
| ب‑١ · الردّ الفارغ | ٥ | `test_an_empty_completion_never_reaches_the_orchestrators_json_fallback` — يُنادي `_turn_content` نفسَها |
| ب‑٢ · نداءُ الأسماء | ٤ | `test_a_failing_retrieval_still_fails_the_turn` |
| ب‑٣ · الفرعُ الخامس | ٦ | `test_a_content_intent_with_zero_chunks_still_apologises` · `test_the_agents_intent_literal_matches_the_modules_enum` |
| ب‑٤أ · التعارض | ٤ | `test_a_non_conflict_failure_still_fails_the_turn` |
| ب‑٥ · سجلُّ الخطأ | ٥ | `test_an_abandoned_stream_logs_no_record` |
| ب‑٦ · الفضاءُ المجهول | ٣ | `test_a_turn_without_a_knowledge_seam_does_not_warn` |

### ٢.٤ البوّابات

```bash
ruff format --check . && ruff check . && mypy src && lint-imports && pytest
```

| البوّابة | النتيجة |
|---|---|
| `ruff format --check .` | ✅ 680 ملفًّا |
| `ruff check .` | ✅ |
| `mypy src` | ✅ 431 ملفًّا |
| `lint-imports` | ✅ ٨ عقودٍ محفوظة — `agents-independent` منها |
| `pytest` | ✅ لهذه الموجة · ⚠️ **٣ إخفاقاتٍ سابقةٍ للتغيير** (§٤) |

---

## ٣. الفروقُ عن الخطّة — وسببُ كلٍّ منها

> كلُّ سطرٍ هنا موضعٌ خالف فيه التنفيذُ نصَّ الخطّة. لا شيءَ منها يغيّر قرارًا تصميميًّا، لكنّ من يقرأ الخطّة وحدَها سيتوقّع شيفرةً بشكلٍ آخر.

### ف‑أ · `run` انقسمت ثلاثًا: `run` / `_answer` / `_plan`

**الخطّة قالت:** «`try/except` حول جسد `run`».
**الواقع:** ذلك يجعل `run` بـ**١٨ فرعًا و٧٨ عبارة**، و`ruff` يرفضها: `PLR0912` (١٢ فرعًا حدًّا) و`PLR0915` (٥٠ عبارة). و**لا `noqa` واحدةً لهاتين في `src` كلّه** — فالمستودع يحترمهما بلا استثناء، والاستثناءُ الأوّل ليس شيئًا يُفتَح لتوفير إعادة هيكلة.

**فالانقسام:**

| الدالّة | ما تفعل |
|---|---|
| `run` | الإطار وحدَه: ساعةُ الدور، و`try/except Exception` الذي يسجّل ثمّ يعيد الرفع (ب‑٥) |
| `_plan` | **تقرّر** بماذا يُجاب: `_FixedReply` (نصٌّ ثابت) أو `_Synthesis` (مطالبةٌ للنموذج). لا تبثّ حدثًا واحدًا |
| `_answer` | **تبثّ**: مخرجٌ واحدٌ لكلّ `_FixedReply`، والبثُّ الحيُّ للتركيب |

**وهذا كسبٌ لا خسارة:** مخارجُ الوكيل الخمسةُ التي لا تنادي النموذج (الإيصال · التوضيح · هدفُ التلخيص المجهول · التعارض · الاعتذار) صارت تشترك في **موضعِ بثٍّ واحد**، فـق‑٤ (`token` ثمّ `final`، ولا ثالث) صارت **بنيويّة** بدل أن تكون مكرّرةً خمس مرّات. وبندٌ سادسٌ يُضاف لاحقًا هو `_FixedReply` جديدة، بلا موضعِ بثٍّ سادسٍ ينحرف.

**وحاملان جديدان:** `_FixedReply(text, path, fallback)` و`_Synthesis(binding, query, messages, chunks, corpus_header)` — كلاهما `frozen=True, slots=True` على عادة النواة.

### ف‑ب · `_TurnRecord` — الحاملُ الوحيدُ المتغيّر في هذا الوكيل

`run` صارت **فوق** الدالّة التي تعرف `retrieval_attempted`، فالسجلُّ الذي تكتبه عند الفشل يحتاج قراءةَ ما بلغته `_plan` قبل أن تتعثّر. `_TurnRecord(started, retrieval_attempted=False)` هو ذلك الجسر: `@dataclass(slots=True)` غيرُ مجمَّد، لدورٍ واحدٍ على مكدسٍ واحد، ولا يُشارَك. و`False` قيمتُه الابتدائيّة **معناها الصادق**: دورٌ فشل قبل أن يسأل لم يحاول استرجاعًا فعلًا.

### ف‑ج · `context_nodes` على مسار الردّ الفارغ = `len(chunks)` لا صفر

الخطّة لم تنصّ عليه. النموذج **أُعطي** تلك المقاطع فعلًا، فتصفيرُ العدّاد يخفي أنّ الفشل وقع **رغم وجود سياق** — وهو بالضبط ما يميّز ب‑١ عن بوّابة الثقة. و`fallback` يبقى `False` لأنّ ذلك العلَم يقيس **اشتعال البوّابة على صفر مقاطع**، لا «لم يُنادَ نموذج».

### ف‑د · جلبُ الترويسة صار دالّةً: `_fetch_corpus_header`

الخطّة خيّرت بين نقل الكتلة أو استدعائها داخل الفرع الخامس. الدالّةُ تفعل الاثنين: موضعُ نداءٍ واحدٌ في `_plan` (قبل الفرع الخامس مباشرة)، وحارسُ ب‑٢ داخلها مع حجّته كاملةً في التوثيق. والفرعان اللذان **يجب ألّا يريا ترويسة** (الإيصال والتوضيح) يعودان قبل هذا السطر، فلم يتغيّر لهما شيء — واختبارُهما القائم (`name_limit_calls == []`) ما زال يُثبته.

### ف‑هـ · الأرقامُ الهنديّة ممنوعةٌ في شيفرة `src`

`ruff` يرفض `٠١٢…٩` في التعليقات والتوثيق (`RUF002`/`RUF003`: «رقمٌ ملتبس»)، ولا استثناءَ لـ`src` في `pyproject.toml`. فالتعليقاتُ العربيّة في الشيفرة تكتب **`ب-3` · `ف-5` · `ق-1`** بأرقامٍ لاتينيّة — وهي عادةُ الملفّ أصلًا (`س-32`، `ح-11`). **الوثائقُ وحدَها** تحتفظ بالأرقام الهنديّة.

⇒ **هذا يسري على كلّ موجةٍ قادمة.** لا تنسخ وسمَ بندٍ من الخطّة إلى تعليقٍ في الشيفرة كما هو.

### ف‑و · اختبارٌ سابعٌ وعشرون خارج قائمة الخطّة

`test_a_blank_query_is_measured_too` — الخطّة سمّت أربعة اختباراتٍ لـب‑٥ ولم يغطِّ أيٌّ منها **القرار الرابع في البند نفسِه**: «`retrieval_attempted` قد لا يكون مُعرَّفًا إن وقع العطبُ باكرًا». سؤالٌ فارغ يقع قبل أيّ استرجاع، فيُثبت الاختبار أنّ السجلَّ يُكتب و`retrieval_attempted` فيه `False` **صدقًا** لا سهوًا.

### ف‑ز · صياغةُ الجملتين الجديدتين

| البند | العربيّة | الإنجليزيّة | ملاحظة |
|---|---|---|---|
| **ب‑٣** | «أيّ ملفّ تريد تلخيصه؟» | "Which file would you like me to summarise?" | **مختلفةٌ عمدًا** عن `_CLARIFY_FILE_AR` («أيّ ملفّ تقصد؟»): هناك قائمةُ مرشّحين رفضت الوحدةُ الاختيار بينهم، وهنا لا مرشّح أصلًا. جملتان متطابقتان تُخفيان حالتين مختلفتين |
| **ب‑٤أ** | «تعذّر بدءُ تلخيص هذا الملفّ الآن. إن كان تلخيصٌ له قيد الإعداد فسيصلك في هذه المحادثة عند اكتماله.» | "I couldn't start a summary of that file just now. If one is already being prepared, it will reach you in this conversation when it is ready." | صياغةُ الخطّة حرفيًّا، مع «في هذه المحادثة» لتطابق جملةَ الإيصال القائمة |

### ف‑ح · ⚠️ أرقامُ الأسطر في §١ و§٣ من الخطّة صارت قديمة

`agent.py` نما من **٧١٦ سطرًا إلى ١١٤٠**، فكلُّ مرساةٍ رقميّةٍ إليه في الخطّة تشير الآن إلى موضعٍ آخر:

| المرساة في الخطّة | كانت | صارت |
|---|---|---|
| `agent.py:102` — استيرادُ `framework.errors` (ق‑١) | 102 | **123** |
| `agent.py:293` — «الوكيل لا يملك رقم ضبط» (ق‑٢) | 293 | **290** (`_PATH_SYNTHESIS`) |
| `agent.py:484` — `_log_answer` (ق‑٥) | 484 | **864** |
| `agent.py:279` — `space_id` من الحزمة (ق‑٧) | 279 | **566** |
| مراسي §٣ كلُّها (`399` · `412` · `437` · `463` · `475` · `547` · `613` · `626`) | — | **منسوخة**، ووصفُها للشيفرة **قبل** الموجة ١ |

**ولم تُصحَّح في الخطّة عمدًا.** مراسي §٣ تصف **الشيفرةَ التي وُصف التغييرُ عليها**، وتحديثُها إلى ما بعد التغيير
يمحو الحجّة نفسَها («هذا السطرُ بلا حراسة» صار محروسًا). أمّا §١ فمراسيها دليلٌ قائم، وتصحيحُها تحريرٌ في الخطّة
يتجاوز وسمَ الحالة الذي اتُّفق عليه. ⇒ **الشيفرةُ الحاليّة هي المرجع**، والخطّةُ تُقرأ لحجّتها لا لأرقامها.

---

## ٤. ⚠️ إخفاقاتٌ قائمةٌ في المستودع — **سابقةٌ لهذه الموجة**

`pytest` من الجذر يُظهر ثلاثة إخفاقات، **مُتحقَّقٌ من سبقها للتغيير** (شُغِّلت على الشيفرة قبل التعديل فأخفقت كما هي):

| الاختبار | السبب الظاهر |
|---|---|
| `test_edge_hardening.py::test_cors_decision_is_documented_in_the_design_and_requirements_docs` | `FileNotFoundError` — وثيقةٌ يقرؤها الاختبار غيرُ موجودة |
| `test_evaluation_set.py::test_the_questions_are_the_owners_quiz_verbatim` | ملفُّ الاختبار المرجعيّ مفقود |
| `test_evaluation_set.py::test_both_quiz_files_ask_the_same_questions` | نفسُه |

لا صلةَ لأيٍّ منها بطبقة الوكلاء. **مُسجَّلةٌ هنا ولم تُصلَح**: إصلاحُها خارج نطاق هذه الخطّة، وابتلاعُها بصمتٍ يجعل البوّابةَ الخامسة بلا معنًى في الموجات القادمة.

---

## ٥. المفتوحُ بعد الموجة ١

| # | البند | ما يلزم |
|---|---|---|
| **م‑١** | **ق‑أ صارت قابلةً للقياس** | `rag_agent.unscoped_turn` تبثّ الآن. المعيارُ المكتوب: أكثرُ من مرّةٍ في الألف دورٍ خلال أسبوعين ⇒ يتحوّل التحذيرُ رفضًا. **يبدأ العدُّ من أوّل نشر** |
| **م‑٢** | **`summary_conflict` تقيس معنيين** | العدّادُ اليومَ يخلط «بناءٌ نشط» بـ«مستندٌ غير مفهرس» — وهو مقصودٌ حتّى تصل ب‑٤ب (الموجة ٣). لا تُبنَ عليه لوحةٌ تدّعي التمييز |
| **م‑٣** | **الجملةُ المحايدة مؤقّتة** | نصُّ ب‑٤أ يُستبدَل كاملًا في ب‑٤ب. من يترجم أو ينسخ الجملة إلى واجهةٍ يعرف أنّها ستتغيّر |
| **م‑٤** | **الموجة ٢ لا تنتظر شيئًا** | الموجة ١ مدموجة، والموجتان ٢ و٦ متوازيتان (ق‑و). لا اعتماديّةَ متبقّية |
| **م‑٥** | **لم تُحدَّث وثيقةٌ من الأربع** | `02-port-contracts.md` · `01-data-model.md` · `06-domain-models.md` تُحدَّث مع الموجات ٢–٥ — والموجة ١ لم تمسّ سيمًا ولا نموذجَ بيانات، فلا شيءَ عليها منها |

---

## ٦. سجلُّ التحديثات

| التاريخ | الموجة | ما جرى |
|---|---|---|
| 2026‑08‑29 | ١ | ب‑١ … ب‑٦ مُنفَّذة · ٢٧ اختبارًا · البوّابات الخمس خضراء · ٦ فجواتٍ من ١٤ مغلقة |

</div>
