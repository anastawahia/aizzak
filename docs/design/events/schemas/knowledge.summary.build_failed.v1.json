{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.platform/knowledge.summary.build_failed.v1.json",
  "title": "knowledge.summary.build_failed.v1 data",
  "type": "object",
  "required": ["job_id", "document_id", "reason"],
  "properties": {
    "job_id": { "type": "string", "format": "uuid" },
    "document_id": { "type": "string", "format": "uuid" },
    "reason": { "type": "string" },
    "conversation_id": {
      "type": "string",
      "format": "uuid",
      "description": "the thread this build was asked for inside; ABSENT when asked for outside one, and absent on a cancellation and on ب-9's release, neither of which knows the thread (ب-11أ) — an added optional field, so the type stays v1"
    }
  },
  "additionalProperties": false
}
