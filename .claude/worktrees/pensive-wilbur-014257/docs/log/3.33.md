<div dir="rtl">

[← الفهرس](INDEX.md) · [حالة التنفيذ](../implementation-status.md)

## 3.33 ما بُني — المرحلة 4.6‑ب: وكيلا Data Analysis + File Editing (+ `read_text_file`) — ✅

> **البند:** الوكيلان النصّيان الآخران (`FR‑20.2` تحليل بيانات · `FR‑20.5` تحرير ملفات). المصدر: `11 §1‑9`. **v1 نصّيّ عبر LLM لا تنفيذ كود** — الشكل الثقيل لتحليل البيانات يتبع الوحدة المحجوزة `sandbox` (خارج v1، `Req §12.1`).

**قرارات الخطوة 0:**
1. **الوكيلان يقرآن ملف مساحة العمل** عبر بذرتين: `deps.files` (بروتوكول DIP `FilesAccess` يُحقّقه `FilesQuery` بنيوياً — بيانات وصفية + `storage_key`) + `deps.storage` (`StorageProvider` منفذ **إطاري** ⇒ بلا مرآة DIP، يجلب البايتات). نموّ `AgentDependencies` بحقلَين إضافيَّين.
2. **مساعد إطاري مشترك `read_text_file`** ([`framework/agent_runtime/file_reading.py`](../src/app/framework/agent_runtime/file_reading.py)): المنطق حسّاس‑الصحّة (حرّاس) ومتطابق بين الوكيلَين، والوكلاء إضافات مستقلّة لا تستورد بعضها ⇒ يعيش مرّة في الإطار (شقيق المُنفِّذ، إطاري‑نقيّ: `FilesAccess`+`StorageProvider` فقط ⇒ الكِرنل 8/0). خطوتان (وصفية→بايتات) بحرّاس مُعمَّاة على أخطاء `AppError` الموحّدة: unwired⇒500 · لا ملف `ready`⇒404 · غير نصّي⇒415 · أكبر من الحدّ⇒413 (يُفحَص على البيانات الوصفية **قبل الجلب** ⇒ لا يُسحب كائن ضخم للذاكرة) · فكّ ترميز متساهل (`errors="replace"`).
3. **v1 لا يكتب:** File Editing يُنتج النصّ المحرَّر كـ`final` ولا يحفظه (الكتابة عبر منفذ ملفات + `storage.put` = أثر جانبي مؤجَّل لـ4.7)؛ يبقى الوكيل للقراءة فقط. كلاهما يبثّ (لا `complete`) ⇒ `ResolvedLLM` يبقى حاملاً نقيّاً.

**ما بُني (4.6‑ب):** [`deps_ports.py`](../src/app/framework/agent_runtime/deps_ports.py) (+`FilesAccess`/`FileReadView`) · [`file_reading.py`](../src/app/framework/agent_runtime/file_reading.py) (جديد) · نموّ `AgentDependencies` (`files`+`storage`) + تصدير · [`agents/data_analysis_agent/`](../src/app/agents/data_analysis_agent/agent.py) + [`file_editing_agent/`](../src/app/agents/file_editing_agent/agent.py) (`manifest`+`agent`+`prompts`؛ كانت stubs صفرية) · [`tests/unit/test_content_agents.py`](../tests/unit/test_content_agents.py) (**17** hermetic: المساعد + الوكيلان + اكتشاف) · تحديث اختبار الشجرة الحقيقية (3 مُسجّلة/2 معزولة).

**البوابات + تصليب (4.6‑ب):** ruff ✅ · `mypy src` **292** (+3: file_reading + الوكيلان) · `lint-imports` **8/0** (`framework-kernel` KEPT — DIP + منفذ إطاري؛ `agents-*` KEPT) · `pytest -m "not integration"` **1333 ناجح + 8 متخطّى** (+17). **بطارية طفرات: أوّلاً 4/5 — M5 «كسر نوع حدث `final` في FE» نجا ⇒ ثغرة تغطية حقيقية** (اختبار FE لم يؤكّد `type=="final"`) ⇒ أُضيف التوكيد ⇒ **5/5 بعد التصليب** (سابقة 4.4 «الاختبار الذي لا يمكن أن يسقط»). **⚠️ درس عدّة راسخ:** طفرة M3 تذبذبت (KILLED ثم SURVIVED) بسبب **`.pyc` بائت على `\\wsl.localhost` (دقّة mtime ثانية واحدة)** حين تُعاد كتابة الملف مرّتين في الثانية نفسها ⇒ **مسح `__pycache__` بين الطفرات** أعطى 5/5 نظيفة؛ **نتيجة KILLED موثوقة دائماً؛ SURVIVED وحدها مشبوهة** (⇒ بطاريتا 4.5/4.6‑أ السابقتان صحيحتان). **الشجرة الحقيقية:** `loaded==("data_analysis_agent","file_editing_agent","rag_agent")` + {`image_agent`,`video_agent`} معزولان (يكتمل القلب إلى 5/0 في 4.6‑ج).

---

</div>
