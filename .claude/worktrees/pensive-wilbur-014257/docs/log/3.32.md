<div dir="rtl">

[← الفهرس](INDEX.md) · [حالة التنفيذ](../implementation-status.md)

## 3.32 ما بُني — المرحلة 4.6‑أ: بذرة DIP للـ`deps` + وكيل RAG (القالب) — ✅ (4.6 على 3 خطوات)

> **البند:** 4.6 = الوكلاء الخمسة (`FR‑20` · `11`). المصدر الملزِم: `11 §1‑9` (تشريح المجلد + عقد `BaseAgent` + الاختبار بمنافذ مزيّفة) · `02 §3.2` · مُحمِّل الإضافات [`plugin_loader.py`](../src/app/framework/agent_runtime/plugin_loader.py). **قُسِّمت لثلاث خطوات** (نمط المستخدم: خطوة‑خطوة + توثيق + وقوف): **4.6‑أ** بذرة DIP + وكيل RAG (القالب) · **4.6‑ب** Data Analysis + File Editing · **4.6‑ج** Image + Video + قلب اختبار الشجرة الحقيقية. هذه الفقرة = 4.6‑أ.

**قرار عالي الأثر (بوّابة المستخدم):** الوكلاء يستهلكون منافذ **وحدات** واردة (knowledge/media/files) عبر `deps`، لكن `AgentDependencies` في `framework/` و`framework-kernel` يمنع `framework→modules`. عُرضت ثلاثة حلول (بروتوكولات DIP إطارية · `object` فضفاض + cast · استثناء جراحي لـimport‑linter) واختار المستخدم **«بروتوكولات إطارية (DIP)»**: الإطار يعلن تجريدات محايدة، والوحدات تُحقّقها **بنيوياً**، والربط يُفحَص نوعياً عند موقع الربط في 4.7 (تغيّر شكل منفذ وحدة يُحمِّر ذلك السطر) — يبقي الكِرنل 8/0 مع أمان أنواع كامل.

**ما بُني (4.6‑أ):**
- [`framework/agent_runtime/deps_ports.py`](../src/app/framework/agent_runtime/deps_ports.py) (جديد) — بذرة DIP: `ResolvedLLM{provider,model,api_key}` (ما يحلّه `ProviderResolver` مُعبّأً لطلب — حامل مجمَّد بلا سلوك) + `RetrievedChunkView(Protocol)` + `KnowledgeAccess(Protocol).retrieve(ctx,query,k)->Sequence[RetrievedChunkView]` (يُحقّقه `KnowledgeRetrieval` بنيوياً). إطار/stdlib فقط.
- [`base_agent.py`](../src/app/framework/agent_runtime/base_agent.py) — نمّت `AgentDependencies` بحقلَيها **الأولين المتّجهين للوحدات**: `llm: ResolvedLLM|None` + `knowledge: KnowledgeAccess|None` (اختياريان ⇒ كل `AgentDependencies()` قائمة صالحة؛ frozen+slots مختوم) + تصدير من [`__init__.py`](../src/app/framework/agent_runtime/__init__.py).
- **وكيل RAG (القالب):** [`agents/rag_agent/{manifest,agent}.py`](../src/app/agents/rag_agent/agent.py) + [`prompts/__init__.py`](../src/app/agents/rag_agent/prompts/__init__.py) — `METADATA` (key=`rag_agent`، snake_case=اسم المجلد) + `RagAgent(BaseAgent)` (`metadata=METADATA`؛ `run` مولّد لا‑متزامن: يسترجع عبر `deps.knowledge` [متدهور اختيارياً إن None] ⇒ يبني الرسائل ⇒ يبثّ من `deps.llm.provider.stream` أحداثَ `token` ⇒ `final` بالنص+الاستشهادات). حارسان: llm معدوم ⇒ 500 (سابقة web_search)، query فارغ/غير‑نصّي ⇒ 422. **يستورد إطاراً فقط** (لا وحدة/بنية تحتية) بفضل DIP.

**البوابات + تصليب (4.6‑أ):** ruff format/check ✅ · `mypy src` **289** (+2: deps_ports+agent؛ 0 مشاكل) · `lint-imports` **8/0** (`framework-kernel` KEPT — الإطار لا يستورد وحدة؛ `agents-independent`/`agents-no-api-no-infra` KEPT) · `pytest -m "not integration"` **1316 ناجح + 8 متخطّى** (+11 hermetic في [`test_rag_agent.py`](../tests/unit/test_rag_agent.py) + تعديل اختبار الشجرة الحقيقية). **بطارية طفرات 5/5 قتلات** (حارس llm‑معدوم · الاستشهادات · حقن السياق في موجّه النظام · حارس query · حدث `final`؛ استعادة sha256 مُتحقَّقة). **اختبار الشجرة الحقيقية انقلب جزئياً:** `report.loaded==("rag_agent",)` + 4 stubs تبقى معزولة (يكتمل القلب في 4.6‑ج). **يُسلَّم لـ4.7:** ملء `deps.llm` من `ProviderResolver` + `deps.knowledge` من `KnowledgeRetrieval` (الفحص البنيوي عند سطر الربط).

---

</div>
