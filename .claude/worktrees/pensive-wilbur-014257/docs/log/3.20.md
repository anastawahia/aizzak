<div dir="rtl">

[← الفهرس](INDEX.md) · [حالة التنفيذ](../implementation-status.md)

## 3.20 ما بُني — المرحلة 2.5: محوّل Qdrant (`VectorStore`/`HybridVectorStore`) — ✅ مكتملة (على خطوات مُبوَّبة)

> **جدول التقدّم (يُحدَّث بعد كل خطوة — توجيه المستخدم):**
>
> | الخطوة | الحالة | الخلاصة |
> |---|---|---|
> | 0 — فحص وعقد | ✅ 2026‑07‑16 | التفاصيل أدناه |
> | 1 — 📐 تصميم (architect) | ✅ 2026‑07‑16 | التفاصيل أدناه — أبرزه: توصيل الجذر **الآن** (خلاف تأجيل 2.4) + مُترجمان نقيّان يُختبَران وحدةً |
> | 2 — 🛠️ المحوّل `qdrant_store.py` + عدّة `live_qdrant` + الاختبارات | ✅ 2026‑07‑16 | البوابات الخمس خضراء · **16/16 حيّة (لا تخطٍّ — Qdrant حيّ فعلًا)** + **6/6 وحدة** · junit **758** (+22) — التفاصيل أدناه |
> | 3 — ✅ verifier (البوابات الخمس + مطابقة العقود) + إغلاق | ✅ 2026‑07‑16 | **PASS ‏8/8** + ملاحظة غير حاجبة واحدة **أُصلحت فورًا** · الإغلاق: ruff **331** · mypy **278** · **8/0** · junit **759** (758+1) · **تكامل حيّ 151/151** (111 PG + 12 Redis + 12 MinIO + **16 Qdrant**) |

**الخطوة 3 (verifier — وكيل مستقل، ثم تصليب فوري من المُنسِّق):** بوابة مستقلة أعادت البوابات الخمس بنفسها (لا نقلًا عن المنفّذ) وطابقت التواقيع حرفيًا ضد المنفذ (بما فيها `*` الحاصرة في `ensure_hybrid_collection` وحدها) وتحقّقت من: المطابقة البنيوية عبر إسناد `composition_root` لحقل `HybridVectorStore` تحت mypy strict · تدرّج استثناءات المكتبة بالاستبطان (`ResponseHandlingException ⊂ ApiException` — يفسّر نجاح اختبار المنفذ الميت) · `ValidationError` الإطارية تفلت من الالتقاط الموحَّد كما هو مقصود · ابتلاع 409 محصور في `ensure_*` فقط · **صفر تسريب مجموعات** بعد تشغيلين كاملين (`/collections` فارغ) · خطّ الأثر mtime: **6 ملفات بالضبط** والمنفذ/`.importlinter`/`__init__.py` بلا مساس — **PASS ‏8/8**. **الملاحظة غير الحاجبة** (قراءة نقدية + تجربة مباشرة): قائمة `flt` غير متجانسة العناصر (نحو `[1, "two", 3.0]`) تجعل `MatchAny` يرمي **`pydantic ValidationError` أجنبيًا خامًا** من داخل `search` — ثقب في سياسة R6 وإن تعذّر بلوغه اليوم (لا مستهلك للمسار قبل م4). **التصليب الفوري (المُنسِّق — سابقة تصليب `NULLIF` §3.14):** حارس تجانس في `_build_filter` (كل العناصر نوع قياسي واحد `str|int|bool` — مرآة `AnyVariants = List[StrictInt]|List[StrictStr]|List[StrictBool]` بما فيها رفض خلط bool/int على دلالة Strict) يرمي `ValidationError` الإطارية + اختبار وحدة U7 (ثلاث قوائم فاسدة) + تحديث docstring — ثم **إعادة البوابات الخمس كاملة: خضراء** (ruff ‏331 ✓ · mypy ‏278 ✓ · ‏8/0 ✓ · **758 ناجحًا + 1 متخطًّى قديم = junit 759**، منها الحيّة الـ16 ضمن التشغيلة). **ملاحظتان أسلوبيتان بلا أثر** (توثيقيتان): `str(p.id)`/`str(i)` زائدتان لأن `Uuid = str` أصلًا — أُبقيتا عمدًا (توثيق النية عند حدّ Any).

**الخطوة 2 (التنفيذ — وكيل implementer مستقل):** [`infrastructure/vector/qdrant_store.py`](../src/app/infrastructure/vector/qdrant_store.py) (301 سطرًا): مصنع + `QdrantVectorStore` (6 طرق تُرضي البروتوكولين بنيويًا) + المساعِدات النقية + `_translate` — كل ما أقرّه التصميم حرفيًا · توصيل الجذر في [`framework/di/composition_root.py`](../src/app/framework/di/composition_root.py) (حقلا `qdrant_client`/`vector_store: HybridVectorStore` في `from_env` — بناء دون‑اتصال) · مثبّتات conftest أربعة (`live_qdrant` مجسّ 6333 + `TEST_QDRANT_URL` · `qdrant_client` بإغلاق ذاتي · `qdrant_store` · `qdrant_collection` فريدة `aizzak-test-<uuid7>` تُحذف بعد كل اختبار) · وسم `live_qdrant` في pyproject · **لم يُمَسّ:** المنفذ، `.importlinter`، `vector/__init__.py` (مؤكَّد بـmtime). **البوابات:** ruff format ✓ (331 ملفًا) · ruff check ✓ · mypy **278** ✓ · lint-imports **8/0** ✓ · pytest ‏**757 ناجحًا + 1 متخطًّى** (القديم المعروف — PATH لا كود) = **junit 758 (+22)** · الحيّة المستهدفة **16/16 passed** في 11.1ث (لا skip — الخادم حيّ) · الوحدة **6/6**. **أفخاخ qdrant-client 1.18 المُكتشفة والمحلولة** (كلها بفحص تفاعلي مباشر ضد الحزمة والخادم الحيّ قبل الكتابة): (أ) لا وجود لوحدة `qdrant_client.exceptions` — المسارات الفعلية `http.exceptions.{ApiException, UnexpectedResponse}` و`common.client_exceptions.QdrantException`، ورفض الاتصال `ResponseHandlingException ⊂ ApiException` ⇒ التقاط موحَّد `(ApiException, QdrantException)` مُثبَت؛ (ب) لا وجود لـ`models.DEFAULT_VECTOR_NAME` — الثابت الحقيقي `""` في وحدة خاصة ⇒ ثابت محلي `_DEFAULT_VECTOR_NAME = ""` مُعلَّق الهشاشة وحارسه الاختباران 13/14؛ (ج) `MatchValue` يقبل `bool|int|str` فقط (استبطان توقيع) — يؤكّد قرار رفض float في `_build_filter`؛ (د) سلوكيات خادم مؤكَّدة حيًّا: `collection_exists→False` بلا استثناء · إنشاء مكرَّر → `UnexpectedResponse(409)` (يُبتلَع كسباق حميد عبر ثابت `_HTTP_CONFLICT` أملاه ruff PLR2004) · `delete_collection` لغائبة → `False` بلا رمي (يجعل تفكيك المثبّتة آمنًا للاختبار 9) · `query_points` لمفقودة → `UnexpectedResponse(404)` → `common.internal`. **انحراف عن التصميم: لا شيء سلوكيًا** (إضافتان ميكانيكيتان: الثابت المُسمّى + إكمال `collection_name=` حيث اختزله التصميم).

**الخطوة 1 (تصميم architect — سلسلة 📐 كاملة، وكيل مستقل):** صنف واحد `QdrantVectorStore(client)` يُرضي **كلا** البروتوكولين بنيويًا (يُمرَّر لـ`memory` كـ`VectorStore` الأضيق ولـ`knowledge` كـ`HybridVectorStore` الأوسع — Interface Segregation دون صنفين) + مصنع `create_qdrant_client(settings)` للجذر فقط. **القرارات المقرَّة:** REST لا gRPC (توحيد شجرة الاستثناءات المُترجَمة؛ gRPC رافعة أداء مؤجَّلة) · `timeout=5s` (int واحد؛ فوق p99=800ms بفسحة لمسار الفهرسة) · `check_compatibility=False` (بناء دون‑اتصال ⇒ `from_env` لا يفشل إن غاب Qdrant — سابقة `Redis.from_url` الكسولة) · `ensure_*` idempotent بـ«موجود⇒لا شيء» + ابتلاع سباق 409، **بلا تحقّق dim/distance** (خارج العقد) · الهجين = `vectors_config` مفردة (الكثيف تحت الاسم الداخلي `""`) + `sparse_vectors_config={"text": SparseVectorParams(**modifier=Modifier.IDF**)}` — جوهر IDF المُرجأ خادميًا · upsert بنقطة بوجهين `{"": dense, "text": sparse}`، ‏`id=str(uuid)`، ‏`wait=True`، دفعة واحدة (المستدعي يُقسّم بـ128) · كلا البحثين على **`query_points`** الحديثة (`using=None` كثيف / `using="text"` متفرّق) · `flt`: كل مفتاح → `FieldCondition` ضمن `must` (قياسي→`MatchValue`، قائمة→`MatchAny`، **غير ذلك→`ValidationError` لا إسقاط صامت — حارس DD‑04**) · الأخطاء: `(ApiException, QdrantException)` → `common.internal` الكل 500 (مجموعة مفقودة عند search عطبٌ تشغيلي لا `NotFoundError` — عكس دلالة `NoSuchKey` في 2.4) · **التوصيل الآن** في `composition_root` (حقلا `qdrant_client` + `vector_store: HybridVectorStore`؛ لا أسرار بعكس علّة تأجيل MinIO؛ الجذر يبقى عند «مقعد tenant_session») · مُترجمان نقيّان مستوى‑وحدة `_to_distance`/`_build_filter` لاختبار وحدة (يقفلان: distance مجهول يرمي، وشرط flt غير قابل للترجمة يرمي). **خطة الاختبار:** 16 حيًّا (منها العزل الأمني الحاكم بالساقين الكثيف **والمتفرّق** + نقطة‑بوجهين payload واحد وحذف واحد يمسح الساقين + إثبات `Modifier.IDF` عبر `get_collection`) + 6 وحدة. **مخاطر مرصودة:** سطح qdrant-client كله `Any` تحت mypy strict (لا `type: ignore` زائد؛ تحويلات صريحة عند بناء `VectorHit`؛ صحّة الأشكال يثبتها الحيّ وحده) · حرفيّة الاسم الفارغ `""` هشّة تُعلَّق بثابت · عتبات alpha (L2) لا تُنقَل — المحوّل خالٍ من أي عتبة.

**نتائج الخطوة 0:** خادم Qdrant **1.18.2** يعمل محلياً لأول مرة (حاوية Docker `qdrant/qdrant:latest` أُنشئت اليوم — **أول مكوّن بنية تحتية بالحاوية**؛ REST ‏6333 + gRPC ‏6334، ‏`/healthz` ‏200، تخزين دائم `~/qdrant/storage`، ‏`restart: unless-stopped`، **بلا مفتاح API** — مقبول للتطوير المحلي) · الـstub ‏`infrastructure/vector/qdrant_store.py` **صفري** (+ `__init__.py` صفري) · التبعية `qdrant-client>=1.12` مُعلَنة سلفاً (**1.18.0 في venv** — يوفّر `AsyncQdrantClient` أصلي‑اللاتزامن، خلافاً لـminio‑py المتزامن) وتجاوز mypy ‏`qdrant_client.*` موجود سلفاً · `QdrantSettings` موجودة سلفاً في `framework/settings/settings.py` (حقل `url` فقط، الافتراضي `http://qdrant:6333`) · **العقد مزدوج المستوى:** `02 §1.5` يعرّف `VectorStore` الأساسي (4 طرق، `flt` يحمل `workspace_id` دائماً — DD‑04)، والمنفذ الفعلي [`framework/ports/vector_store.py`](../src/app/framework/ports/vector_store.py) يضيف `HybridVectorStore` (3.k3): **كثيف افتراضي غير مُسمّى + متفرّق مُسمّى `"text"`** على نفس النقطة (وجهان لنقطة واحدة، payload واحد وحذف واحد)، و**IDF مُرجأ**: قيم `SparseVector` تردّدات خام والمحوّل هو من يطبّق مُعدِّل IDF الخادمي عند الفهرسة/الاستعلام (`refs/retrieval.md §4.2/§6‑7`) · **النمط البيتي المرجعي** (2.3/2.4): مصنع `create_*_client(settings)` للجذر فقط + صنف محوّل رقيق بمطابقة Protocol بنيوية + ترجمة أخطاء R6 (`common.internal`؛ لا استثناء سائق يتسرّب) + مهلات fail‑fast ثوابت وحدة · **عدّة الاختبار:** وسم `live_qdrant` جديد يُضاف لـpyproject + مثبّتات conftest (مجسّ TCP ‏6333 → تخطٍّ آمن + `TEST_QDRANT_URL` للتجاوز) على سابقة `live_redis`/`live_minio`؛ كل اختبار بمجموعة (collection) فريدة `aizzak-test-<uuid7>` تُحذف بعده · **البوابات الخمس** (ci.yml): ruff format → ruff check → mypy src → lint-imports → pytest ‏`-m "not integration"`.

*(الخطوات 1–3 تُوثَّق هنا تباعاً.)*

---

</div>
