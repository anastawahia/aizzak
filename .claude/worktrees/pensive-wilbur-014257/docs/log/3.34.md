<div dir="rtl">

[← الفهرس](INDEX.md) · [حالة التنفيذ](../implementation-status.md)

## 3.34 ما بُني — المرحلة 4.6‑ج: وكيلا Image + Video (حدثيان) + قلب AC‑04 — ✅ (**اكتملت المرحلة 4.6**)

> **البند:** خاتمة 4.6 — وكيلا توليد الوسائط (`FR‑20.3` صورة · `FR‑20.4` فيديو). **حدثيان (D‑04):** يطلبان توليداً ثقيلاً ولا يبثّان token؛ النتيجة تُسلَّم لاحقاً عبر WebSocket (م5).

**قرارات الخطوة 0:**
1. **بذرة DIP `MediaRequesting`** (`deps_ports`): `request(ctx, *, agent_key, kind, prompt, params) -> RequestedMediaView{job_id,status,kind}`. هذا **منفذ media الوارد المؤجَّل من §3.12** — يظهر الآن مع مستهلكه؛ الربط الفعلي فوق `RequestMedia`+الـoutbox = 4.7. نموّ `deps.media`.
2. **قاعدة مشتركة `MediaRequestAgent`** في [`app/agents/_shared.py`](../src/app/agents/_shared.py) (وحدة `_`‑مسبوقة يتجاهلها المُحمِّل): الوكيلان متطابقان إلا في `KIND` ⇒ DRY بلا استيراد متبادل (`agents-independent` سليم — سلف مشترك ليس استيراداً عبر‑إضافة؛ كل إضافة تُعرِّف صنفها الملموس الوحيد الذي يسجّله المُحمِّل بفحص `__module__`). `run` يطلب وظيفة واحدة ⇒ يبعث `final` وحيداً (job_id/status/kind)؛ لا token. حارسا prompt‑فارغ⇒422 وmedia‑معدوم⇒500.
3. **AC‑04 (جانب الإضافة) مُثبَت حيّاً:** مسح `app.agents` الحقيقي يسجّل الخمسة كلها، صفر عزل صفر رمي بلا تعديل نواة (جانب الحذف مُثبَت سلفاً على أشجار اصطناعية).

**ما بُني (4.6‑ج):** [`deps_ports.py`](../src/app/framework/agent_runtime/deps_ports.py) (+`MediaRequesting`/`RequestedMediaView`) · نموّ `deps.media` + تصدير · جديد [`agents/_shared.py`](../src/app/agents/_shared.py) (`MediaRequestAgent`) · مملوء [`agents/image_agent/`](../src/app/agents/image_agent/agent.py) + [`video_agent/`](../src/app/agents/video_agent/agent.py) (كانت stubs صفرية) · [`tests/unit/test_media_agents.py`](../tests/unit/test_media_agents.py) (**12** hermetic + إثبات AC‑04) · **قلب اختبار الشجرة الحقيقية إلى الخمسة كلها مُسجّلة / 0 عزل**.

**البوابات + تصليب (4.6‑ج):** ruff ✅ · `mypy src` **293** (+1: `_shared`) · `lint-imports` **8/0** (`agents-independent` KEPT رغم السلف المشترك؛ `framework-kernel` KEPT — DIP) · `pytest -m "not integration"` **1345 ناجح + 8 متخطّى** (+12؛ قوّيتُ اختبار prompt لـmissing/blank/non‑str قبل الطفرات). **بطارية طفرات 6/6 قتلات** (media‑معدوم · kind خاطئ · agent_key خاطئ · حدث `final` · افتراضي `params` · حارس prompt) بمسح `__pycache__` (درس عدّة 4.6‑ب). **اكتملت المرحلة 4.6:** الوكلاء الخمسة أحياء (`FR‑20`)؛ **يُسلَّم لـ4.7:** ربط `deps` بالجذر (`ProviderResolver`→`ResolvedLLM` · `KnowledgeRetrieval`→`knowledge` · `FilesQuery`+`StorageProvider`→`files`/`storage` · منفذ media وارد فوق `RequestMedia`+outbox→`media`) + فرض/التقاط `usage` + البثّ (SSE/WS) + بناء `SequentialWorkflowEngine` لكل طلب.

---

</div>
